import type { FastifyPluginCallback } from 'fastify';

// Plan 01-11 implements the session listing/revocation routes here.
const sessionsRoutes: FastifyPluginCallback = (_fastify, _opts, done) => {
  // Intentionally empty until Plan 01-11.
  done();
};

export default sessionsRoutes;
