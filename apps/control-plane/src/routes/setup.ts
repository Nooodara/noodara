import type { FastifyPluginCallback } from 'fastify';

// Plan 01-12 implements the setup-token bootstrap routes here.
const setupRoutes: FastifyPluginCallback = (_fastify, _opts, done) => {
  // Intentionally empty until Plan 01-12.
  done();
};

export default setupRoutes;
