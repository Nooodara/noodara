import type { FastifyPluginCallback } from 'fastify';

// Plan 01-10 implements the auth routes (Better Auth mount + login/logout) here.
const authRoutes: FastifyPluginCallback = (_fastify, _opts, done) => {
  // Intentionally empty until Plan 01-10.
  done();
};

export default authRoutes;
