---
phase: quick-260920-ly9
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ServerSheet.test.tsx
  - apps/web/src/components/ServerSheet.tsx
  - apps/web/src/lib/error-copy.ts
  - apps/web/src/lib/server-form.ts
  - tests/e2e/server-sheet.spec.ts
autonomous: true

must_haves:
  truths:
    - "A server-side VALIDATION_FAILED response with an issue at /sshUser renders a visible error message under the SSH user field and marks it aria-invalid=true — never silence"
    - "Every server-sheet-owned KNOWN_FORM_FIELD_PATHS key (name, host, sshPort, sshUser, credential) is proven, by a single automated guard, to render its error when the backend targets it — so this exact class of drift (a field silently dropped from the wiring) cannot recur unnoticed"
    - "The real E2E create flow proves the same /sshUser issue end to end in a browser, including that the request actually sent to POST /api/servers carries the sshUser value the user typed"
  artifacts:
    - path: "apps/web/src/components/ServerSheet.test.tsx"
      provides: "component-level RED/GREEN proof for the sshUser field error plus the cross-field guard"
    - path: "apps/web/src/components/ServerSheet.tsx"
      provides: "SSH user Field wired with error/invalid exactly like its Name/Host/SSH port siblings"
      contains: "fieldErrors.sshUser"
    - path: "apps/web/src/lib/server-form.ts"
      provides: "ServerFormErrors carries an optional sshUser key so the wiring above type-checks"
      contains: "sshUser?"
    - path: "apps/web/src/lib/error-copy.ts"
      provides: "KNOWN_FORM_FIELD_PATHS exported (read-only) so the guard test can drive off the real whitelist instead of a hand-duplicated list"
      contains: "export const KNOWN_FORM_FIELD_PATHS"
  key_links:
    - from: "apps/web/src/components/ServerSheet.tsx"
      to: "apps/web/src/lib/error-copy.ts normalizeFieldPath('/sshUser') -> 'sshUser'"
      via: "handleApiFailure's fieldErrorsFromIssues(result.issues) -> setFieldErrors"
      pattern: "fieldErrors\\.sshUser"
---

<objective>
Fix the confirmed-live regression recorded in `.planning/STATE.md` (05-37 finding) and
`.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` gap 5: `ServerSheet.tsx`'s SSH user `Field`
(around lines 303-316) renders with no `error`/`invalid` wiring, while `error-copy.ts`'s
`normalizeFieldPath` already maps a backend `/sshUser` VALIDATION_FAILED issue into
`fieldErrors.sshUser`, and `handleApiFailure` suppresses the generic "Check the highlighted
fields" toast the instant any field maps — so a real `sshUser` rejection currently produces zero
visible feedback of any kind. This is `05-REVIEW.md` WR-B-07's warned "latent second bug", worse
than the original gap 5 it followed.

Purpose: close this drift the same way its three siblings (Name, Host, SSH port) already work,
and add a guard so the same class of "field silently dropped from the wiring" defect cannot
recur unnoticed for any of the five fields this sheet owns.

Output: a component test proving the fix (`ServerSheet.test.tsx`, new), an extended E2E case
(`tests/e2e/server-sheet.spec.ts`), the one-line `ServerFormErrors`/`error-copy.ts` type/export
additions the fix needs to compile, and the SSH user `Field` itself wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-tdd/SKILL.md
@.claude/skills/noodara-ux-apple/SKILL.md
@apps/web/src/components/ServerSheet.tsx
@apps/web/src/lib/error-copy.ts
@apps/web/src/lib/server-form.ts
@apps/web/src/components/TrustFingerprintDialog.test.tsx
@apps/web/src/components/CredentialFields.test.tsx
@tests/e2e/server-sheet.spec.ts

<interfaces>
From `apps/web/src/lib/error-copy.ts` (current, module-private — Task 1 exports it):
```
const KNOWN_FORM_FIELD_PATHS: ReadonlySet<string> = new Set([
  'token', 'email', 'password', 'name', 'host', 'sshPort', 'sshUser', 'credential',
]);
export function fieldErrorsFromIssues(issues: readonly ApiIssue[]): Record<string, string>;
export function normalizeFieldPath(path: string): string | null;
```
Of these eight keys, `token`/`email`/`password` belong to setup/login; the server sheet owns
exactly five: `name`, `host`, `sshPort`, `sshUser`, `credential`.

From `apps/web/src/lib/server-form.ts` (current — Task 2 adds `sshUser?: string`):
```
export interface ServerFormErrors {
  readonly name?: string;
  readonly host?: string;
  readonly sshPort?: string;
  readonly credential?: string;
}
```

From `apps/web/src/lib/api-client.ts` (unchanged, for test mocking):
```
export interface ApiIssue { readonly path: string; readonly message: string; }
export interface ApiFailure {
  readonly ok: false;
  readonly code: ApiErrorCode;
  readonly message: string;
  readonly issues?: readonly ApiIssue[];
  readonly retryAfterSeconds?: number;
  readonly unauthorized: boolean;
}
export function apiSend<T>(method, path, body?): Promise<ApiResult<T>>;
```

`ServerSheet.tsx`'s three already-wired siblings (the pattern the fix must mirror exactly, one
Field each):
```
<Field label="Name" {...(fieldErrors.name === undefined ? {} : { error: fieldErrors.name })}>
  {(controlProps) => (
    <Input {...controlProps} ... invalid={fieldErrors.name !== undefined} ... />
  )}
</Field>
```
The SSH user `Field` (current, unwired):
```
<Field label="SSH user">
  {(controlProps) => (
    <Input {...controlProps} mono placeholder="root" disabled={submitting}
      value={formState.sshUser}
      onChange={(event) => { setFormState((prev) => ({ ...prev, sshUser: event.target.value })); }}
    />
  )}
</Field>
```

Test-mocking pattern proven in `TrustFingerprintDialog.test.tsx` / `apps/web/src/app/(shell)/servers/page.test.tsx`:
```
vi.mock('../lib/api-client', async (importOriginal) => ({
  ...(await importOriginal<typeof import('../lib/api-client')>()),
  apiSend: (...args: unknown[]) => apiSendMock(...args),
}));
vi.mock('../lib/require-session', () => ({ requireSession: () => undefined }));
vi.mock('next/navigation', () => ({
  useRouter: () => ({ push: vi.fn(), replace: vi.fn(), refresh: vi.fn() }),
}));
```
`renderUi`, `screen`, `userEvent`, `within` come from `@noodara/ui/testing` (same import every
sibling component test uses).
</interfaces>
</context>

<executor_constraints>
- FORBIDDEN: `git stash`, `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, and any path outside `noodara/code` (the git root is the parent monorepo `/Users/xch4rt/work/myself`).
- FORBIDDEN: `git push`, `git remote add`, or anything that publishes the repository.
- Conventional Commits, English, subject <= 72 chars. Do NOT add a `Co-Authored-By` trailer or any
  Claude attribution line — CLAUDE.md SS7 explicitly forbids it for this repo, and that project
  rule takes precedence over any generic attribution default.
- No new dependencies. UI copy stays English. No new visual treatment — the SSH user field must
  use the exact same `Field`/`Input` error affordance (`role="alert"` message, `aria-invalid`,
  `border-status-error`) its three siblings already use; do not invent a second pattern.
</executor_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing component test, E2E case and cross-field guard</name>
  <files>apps/web/src/components/ServerSheet.test.tsx, tests/e2e/server-sheet.spec.ts, apps/web/src/lib/error-copy.ts</files>
  <behavior>
    - New file `apps/web/src/components/ServerSheet.test.tsx`, following the mocking pattern in
      `<interfaces>` above (mock `apiSend`, `requireSession`, and `next/navigation`'s
      `useRouter`). Add a `fillMinimumValidForm()` helper (types Name, Host, switches the
      credential `SegmentedControl` to "Password" via
      `within(screen.getByTestId('server-sheet-credential-type')).getByRole('radio', { name: 'Password' })`,
      types a password) and a `submit()` helper that clicks
      `screen.getByTestId('server-sheet-save-connect')`. Render with
      `<ServerSheet open mode="create" server={null} onOpenChange={vi.fn()} onSaved={vi.fn()} />`.
      - Test 1: `apiSendMock` resolves `{ ok: false, code: 'VALIDATION_FAILED', message: '...',
        issues: [{ path: '/sshUser', message: 'sshUser must not contain whitespace.' }],
        unauthorized: false }`. After `fillMinimumValidForm()` + `submit()`, assert
        `await screen.findByText('sshUser must not contain whitespace.')` is visible AND
        `screen.getByLabelText('SSH user')` has `aria-invalid="true"`. This is the RED case: both
        assertions currently fail (no message renders, `aria-invalid` stays absent).
      - Test 2 (guard, `it.each`): a local `const SERVER_SHEET_OWNED_FIELDS = ['name', 'host',
        'sshPort', 'sshUser', 'credential'] as const;` (the five server-sheet-owned keys among
        `KNOWN_FORM_FIELD_PATHS` — `token`/`email`/`password` belong to setup/login, not this
        sheet). For each `field`: first assert `KNOWN_FORM_FIELD_PATHS.has(field)` is `true` (so
        this guard is driven off the real whitelist, not a second hand-copied list, per the task
        instruction to export the whitelist minimally rather than re-declare it) — this needs
        `error-copy.ts`'s `const KNOWN_FORM_FIELD_PATHS` changed to `export const
        KNOWN_FORM_FIELD_PATHS` (the only change to that file in this task; do not touch its
        contents or the whitelist's members). Then mock `apiSend` to fail with a single issue at
        `` `/${field}` `` and message `` `${field} guard message` ``, render+fill+submit as above,
        and assert `await screen.findByText(`${field} guard message`)` is visible. Only the
        `sshUser` case is expected to fail before Task 2's fix; the other four already pass today
        (they exercise, not newly fix, their existing wiring) — that is expected and correct.
    - Extend `tests/e2e/server-sheet.spec.ts` with one new `@sheet` test, modeled on the existing
      "a real-shaped server-rejected VALIDATION_FAILED issue" test in that file but targeting
      `/sshUser`: fill Name/Host/a typed SSH user value/Password credential, register a
      `page.on('request', ...)` collector for `POST` requests whose URL includes `/api/servers`
      BEFORE calling `page.route('**/api/servers', ...)` to stub a 400
      `{ error: 'VALIDATION_FAILED', issues: [{ path: '/sshUser', message: 'sshUser must not
      contain whitespace.' }] }`, click `server-sheet-save-connect`, then assert:
      `page.getByLabel('SSH user')` has `aria-invalid="true"`, the message text is visible, the
      sheet stays open, `server-sheet-toast` has count 0 (no orphan banner) — AND, since this
      stubs a mutation, assert the collected request itself: exactly one request, `method() ===
      'POST'`, `url()` contains `/api/servers`, and `postDataJSON()`'s `sshUser` field equals the
      exact string typed into the SSH user input (this is the "assert the request a stubbed
      mutation sent" lesson from plan 05-30's own case in this file).
  </behavior>
  <action>
    Write the two test files exactly as described above and the one-line export change in
    `error-copy.ts`. Do NOT touch `ServerSheet.tsx` or `server-form.ts` in this task — the point
    is to see them fail for the right reason first. Run
    `pnpm vitest run apps/web/src/components/ServerSheet.test.tsx` and confirm: the plain
    `/sshUser` test fails (missing text / `aria-invalid` not `"true"`), the guard's `sshUser` case
    fails identically, and the guard's other four cases (`name`, `host`, `sshPort`, `credential`)
    pass. If a local Docker-backed stack is available, also run
    `pnpm exec playwright test tests/e2e/server-sheet.spec.ts -g sshUser` and confirm the new case
    fails on the visibility/aria-invalid assertions (the request-shape assertions may already pass
    — that is fine, the message/aria-invalid assertions are what must fail here). Stage and commit
    with message `test(quick-260920-ly9): add failing coverage for the unwired sshUser field error`
    — no attribution trailer.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/src/components/ServerSheet.test.tsx 2>&1 | grep -E "sshUser|failed" | head -20</automated>
  </verify>
  <done>ServerSheet.test.tsx and the error-copy.ts export exist; the sshUser-targeted assertions fail with the expected reason (no visible message, aria-invalid absent); the E2E spec has the new /sshUser case; RED commit created with no attribution trailer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wire the SSH user Field</name>
  <files>apps/web/src/lib/server-form.ts, apps/web/src/components/ServerSheet.tsx</files>
  <behavior>
    - `ServerFormErrors` (server-form.ts) gains `readonly sshUser?: string;` alongside `name` /
      `host` / `sshPort` / `credential`. Do NOT add a corresponding rule to `validateServerForm` —
      SSH user has no client-side validation requirement (05-UI-SPEC.md SS2.4 places no format
      constraint on it); this key exists purely so a server-side `/sshUser` issue can be routed
      and rendered, exactly like the pre-existing four keys already are.
    - `ServerSheet.tsx`'s SSH user `Field` gets the identical two additions its Name/Host/SSH port
      siblings already carry: `{...(fieldErrors.sshUser === undefined ? {} : { error:
      fieldErrors.sshUser })}` on the `Field`, and `invalid={fieldErrors.sshUser !== undefined}`
      on the nested `Input`. No other prop, no new component, no new class — same affordance.
  </behavior>
  <action>
    Make exactly the two edits above. After the edit, Task 1's tests must go green with no other
    changes. Run `pnpm vitest run apps/web/src/components/ServerSheet.test.tsx` and confirm every
    case passes (the `sshUser` plain case and every `it.each` guard case). If a Docker-backed
    stack is available, run
    `pnpm exec playwright test tests/e2e/server-sheet.spec.ts -g sshUser` and confirm it passes.
    Stage and commit with message
    `fix(quick-260920-ly9): render the server-side sshUser field error`
    — no attribution trailer.
  </action>
  <verify>
    <automated>pnpm vitest run apps/web/src/components/ServerSheet.test.tsx</automated>
  </verify>
  <done>fieldErrors.sshUser compiles and renders identically to its three siblings; all of Task 1's new tests pass; GREEN commit created with no attribution trailer.</done>
</task>

<task type="auto">
  <name>Task 3: Full verification sweep</name>
  <files>(none — verification only; fix forward in scope if a gate below surfaces a regression)</files>
  <action>
    Run, in order, and require every one green: `pnpm lint`, `pnpm typecheck`, `pnpm test`,
    `pnpm check:ui-safety`, then `pnpm exec playwright test tests/e2e/server-sheet.spec.ts` twice
    in a row (back-to-back, not `--repeat-each` — two separate invocations) to rule out flakiness
    in the new E2E case per this repo's zero-known-flaky bar (CLAUDE.md SS2.2). If any gate fails
    for a reason connected to this plan's changes, fix it minimally within the files already
    listed in this plan's frontmatter `files_modified` and re-run the failing gate; commit any
    such fix separately as `fix(quick-260920-ly9): <what and why>` with no attribution trailer. If
    a failure is unrelated to this plan's changes (pre-existing, matches a known STATE.md
    Blockers/Concerns entry), do not fix it — note it instead.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm check:ui-safety</automated>
  </verify>
  <done>lint, typecheck, unit tests and the UI-safety canary are all green; two consecutive standalone runs of tests/e2e/server-sheet.spec.ts both pass with no flake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control-plane -> apps/web | Server-supplied `VALIDATION_FAILED` `issues[].message` strings cross into the browser and are rendered as inline form-field text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260920-ly9-01 | Information Disclosure | `ServerSheet.tsx` rendering `fieldErrors.sshUser` verbatim | accept | Identical, already-accepted risk shape as the three sibling fields this plan mirrors: `issue.message` is structured Zod/AJV validation text (never a stack trace, DB error, or secret value), and this fix changes only which `Field` renders an already-existing, already-trusted string — no new data source, no new trust boundary. |
| T-quick-260920-ly9-SC | Tampering | package installs | not applicable | This plan adds zero dependencies; no install task exists to gate. |
</threat_model>

<verification>
- `pnpm vitest run apps/web/src/components/ServerSheet.test.tsx` — all cases pass, including the five-field `it.each` guard.
- `pnpm test` — full unit suite green, count up from the prior baseline by the new test cases added here.
- `pnpm typecheck` — `ServerFormErrors.sshUser` and `fieldErrors.sshUser` compile cleanly.
- `pnpm exec playwright test tests/e2e/server-sheet.spec.ts` (run twice) — the new `/sshUser` case passes both times, including its request-shape assertion.
- `pnpm check:ui-safety` — passes (no new visual treatment introduced).
</verification>

<success_criteria>
- A `/sshUser` `VALIDATION_FAILED` issue renders a visible message and `aria-invalid="true"` under the SSH user input, in both a component test and a real-browser E2E flow.
- The guard test proves all five server-sheet-owned `KNOWN_FORM_FIELD_PATHS` keys render their error, not just `sshUser`.
- No visual, dependency, or scope drift beyond the two-line fix plus its tests.
</success_criteria>

<output>
Create `.planning/quick/260920-ly9-render-server-side-sshuser-field-error-i/260920-ly9-SUMMARY.md` when done
</output>
