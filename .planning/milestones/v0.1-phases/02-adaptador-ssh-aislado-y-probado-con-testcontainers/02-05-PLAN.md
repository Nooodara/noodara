---
phase: 02-adaptador-ssh-aislado-y-probado-con-testcontainers
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - packages/domain/src/discovery/os-release.ts
  - packages/domain/src/discovery/os-release.test.ts
  - packages/domain/src/discovery/system.ts
  - packages/domain/src/discovery/system.test.ts
  - packages/domain/src/discovery/access.ts
  - packages/domain/src/discovery/access.test.ts
  - packages/domain/src/discovery/resources.ts
  - packages/domain/src/discovery/resources.test.ts
  - packages/domain/src/discovery/docker-version.ts
  - packages/domain/src/discovery/docker-version.test.ts
  - packages/domain/src/discovery/index.ts
  - packages/domain/src/server/connection-result.ts
  - packages/domain/src/server/connection-result.test.ts
  - docs/domain/server-state-transitions.md
autonomous: true
requirements: [DISC-01, DISC-04]
user_setup: []

must_haves:
  truths:
    - "Every discovery command's output is turned into structured data by a pure function in packages/domain, tested against the real Ubuntu 22.04 and 24.04 output captured in plan 02-04 — never against sample text written by hand"
    - "An OS that is not Ubuntu 22.04 or 24.04 is recognised as unsupported without the parser throwing, and the rest of the parsed facts are still returned (DISC-04)"
    - "Docker detection distinguishes not-installed from installed-with-unreachable-daemon from unparseable-output, and never collapses a JSON parse failure into docker_installed = false (D-12)"
    - "statusForErrorCode('UNSUPPORTED_OS') returns CONNECTED, so an unsupported OS is a warning recorded in last_error_code and not an ERROR state (D-11)"
    - "docs/domain/server-state-transitions.md's connection-result mapping table matches the code after the D-11 change, and a test asserts every one of the seven codes has an explicit expected status"
    - "packages/domain stays pure and stays at or above 95% statement and 95% branch coverage with the new discovery module included"
  artifacts:
    - path: "packages/domain/src/discovery/os-release.ts"
      provides: "parseOsRelease: distro, version, supported-OS determination (DISC-01, DISC-04)"
      exports: ["parseOsRelease"]
    - path: "packages/domain/src/discovery/resources.ts"
      provides: "parseMeminfo and parseDiskUsage (RAM and disk in MB)"
      exports: ["parseMeminfo", "parseDiskUsage"]
    - path: "packages/domain/src/discovery/docker-version.ts"
      provides: "parseDockerVersion and parseComposeVersion with an explicit not-installed / unreachable / unparseable distinction"
      exports: ["parseDockerVersion", "parseComposeVersion"]
    - path: "packages/domain/src/server/connection-result.ts"
      provides: "D-11's UNSUPPORTED_OS -> CONNECTED mapping"
      contains: "UNSUPPORTED_OS"
  key_links:
    - from: "packages/domain/src/discovery/os-release.test.ts"
      to: "packages/domain/src/discovery/fixtures/ubuntu-22.04/os_release.txt"
      via: "readFileSync of the plan 02-04 capture"
      pattern: "fixtures/ubuntu-"
    - from: "docs/domain/server-state-transitions.md"
      to: "packages/domain/src/server/connection-result.ts"
      via: "versioned mirror of the mapping table (D-16)"
      pattern: "UNSUPPORTED_OS"
---

<objective>
Turn every captured command output into structured facts with pure, fixture-tested functions, and flip `UNSUPPORTED_OS` from a failure into a warning.

Purpose: DISC-01 lists ten facts the server detail view must show, and the roadmap's §6.4 unit-test list names "metric normalization" and "connection result mapping" as core-domain coverage. These are exactly the transforms that belong in `packages/domain`, where the 95%/95% gate already lives and where there is no I/O to mock. D-11 is the one behavioural change this phase makes to phase 1's domain code, and it has to land before `runDiscovery` can treat an unsupported OS as a warning.
Output: seven pure parsers with real-fixture tests, the D-11 mapping change, and the updated committed mirror of the transition table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-RESEARCH.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-04-SUMMARY.md
@docs/adr/0004-ssh-adapter-empirical-contracts.md
@docs/domain/server-state-transitions.md
</context>

<interfaces>
<!-- From plan 02-01: packages/domain/src/discovery/types.ts -->
```typescript
export const DISCOVERY_CHECK_IDS = ['hostname','os_release','arch','cpu','memory','disk','uptime','docker_version','docker_compose_version','sudo','docker_group'] as const;
export const DISCOVERY_CHECK_STATUSES = ['pass','fail','skipped','not_applicable'] as const;
export interface DiscoveryCheck { id: DiscoveryCheckId; status: DiscoveryCheckStatus; detail: string; durationMs: number }
export interface DiscoveryFacts { hostname, osDistribution, osVersion, arch, cpuCores, ramMb, diskTotalMb, diskUsedMb, uptimeSeconds, dockerInstalled, dockerVersion, dockerComposeVersion }  // all nullable
export interface DiscoverySnapshot { facts: DiscoveryFacts; checks: readonly DiscoveryCheck[]; warnings: readonly ServerErrorCode[] }
export const SUPPORTED_UBUNTU_VERSIONS = ['22.04','24.04'] as const;
```

<!-- Existing, to be modified: packages/domain/src/server/connection-result.ts -->
```typescript
const ERROR_CODE_STATUS = {
  AUTH_FAILED: 'ERROR', COMMAND_TIMEOUT: 'ERROR', HOST_KEY_CHANGED: 'ERROR', UNSUPPORTED_OS: 'ERROR',
  HOST_UNRESOLVED: 'UNREACHABLE', CONNECT_TIMEOUT: 'UNREACHABLE', CONNECTION_LOST: 'UNREACHABLE',
} satisfies Record<ServerErrorCode, ServerStatus>;
export function statusForErrorCode(code: ServerErrorCode): ServerStatus;
export function applyConnectionResult(state: ServerConnectionState, result: ConnectionResult, now: Date): ServerConnectionState;
```
`applyConnectionResult` refuses any result unless `state.status === 'CONNECTING'`, and routes every status change through `transition()` from `server-state.ts` — it never assigns a status literal. Both properties must survive this plan.

<!-- Existing validator pattern to mirror for parser return shapes: packages/domain/src/validators/network.ts -->
```typescript
export type ValidationResult<T> = { ok: true; value: T } | { ok: false; reason: string };
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OS, system and access parsers against the real captures</name>
  <files>packages/domain/src/discovery/os-release.ts, packages/domain/src/discovery/os-release.test.ts, packages/domain/src/discovery/system.ts, packages/domain/src/discovery/system.test.ts, packages/domain/src/discovery/access.ts, packages/domain/src/discovery/access.test.ts, packages/domain/src/discovery/index.ts</files>
  <behavior>
    - `parseOsRelease` on the captured Ubuntu 22.04 fixture returns distribution `Ubuntu`, version `22.04`, `supported: true`; on the 24.04 fixture, version `24.04`, `supported: true`
    - `parseOsRelease` on a Debian 12 input returns distribution `Debian`, version `12`, `supported: false` — and still returns the distribution and version, never throwing (DISC-04)
    - `parseOsRelease` on an Ubuntu 20.04 input returns `supported: false` with distribution and version populated (an unsupported *version* of a supported distro)
    - `parseOsRelease` handles quoted and unquoted values, `KEY=value` lines with `=` in the value, comment lines, blank lines, and CRLF line endings without misparsing
    - `parseOsRelease` on empty input or input with no `ID`/`VERSION_ID` returns a failure result naming what was missing, not a throw and not a silently-empty success
    - `parseArch` on the captured `uname -m` output returns the trimmed architecture string; on empty input it fails
    - `parseCpuCores` on the captured `nproc` output returns a positive integer; on `0`, a negative number, a float or non-numeric text it fails
    - `parseUptimeSeconds` on the captured `/proc/uptime` output returns the floor of the first field as an integer; on a single-field or malformed input it fails
    - `parseHostname` trims trailing newline and rejects empty output
    - `parseSudoCheck` maps an exit code of 0 to pass and any non-zero to fail, carrying the (already-redacted) stderr as the detail
    - `parseDockerGroupMembership` on the captured `deployer` `id -nG` output reports membership true; on the captured `restricted` output reports false; a group named `dockerx` or `docker-compose` does not count as membership (whole-token match only)
  </behavior>
  <read_first>
    - packages/domain/src/discovery/types.ts (plan 02-01 — DiscoveryFacts field names and SUPPORTED_UBUNTU_VERSIONS)
    - packages/domain/src/discovery/fixtures/README.md and every fixture file under packages/domain/src/discovery/fixtures/ (plan 02-04 — the real captures these tests read)
    - packages/domain/src/validators/network.ts (the ValidationResult<T> shape and the explicit-branching style: no catch-all regex, one branch per rule)
    - packages/domain/src/validators/network.test.ts (the table-driven test style, and how branch coverage is kept at 95% without dead branches)
    - packages/domain/src/purity.test.ts (node:fs is banned in implementation files and allowed only in *.test.ts — fixtures are read from the test files)
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (Claude's Discretion: parsers live in packages/domain, tested with real 22.04/24.04 fixtures; D-11 and D-13)
    - docs/roadmap-v0.1-v0.5.md (section 6.1 Discovery — the ten facts; section 6.2 — the two supported versions)
  </read_first>
  <action>
    Write each test file first, reading the plan 02-04 fixtures with `readFileSync(new URL('./fixtures/ubuntu-22.04/os_release.txt', import.meta.url), 'utf8')`, and watch them fail. Never inline a fixture's contents into a test as a string literal: the whole point of plan 02-04 was to stop tests asserting against hand-written samples. Hand-written inputs are allowed only for cases no fixture can supply (Debian, Ubuntu 20.04, malformed input) and each such case gets a comment saying it is synthetic and why.

    `os-release.ts` exports `parseOsRelease(stdout: string): ValidationResult<{ distribution: string; version: string; supported: boolean }>`. Parse `/etc/os-release` as a key-value file: split on newlines, skip blanks and `#` comments, split each line on the first `=` only, strip one layer of surrounding single or double quotes. Derive `distribution` from `NAME` if present and fall back to `ID` (the captures will show which is friendlier); derive `version` from `VERSION_ID`. `supported` is `ID === 'ubuntu' && SUPPORTED_UBUNTU_VERSIONS.includes(VERSION_ID)` — a case-insensitive `ID` comparison, since `ID` is lowercase by convention but not by guarantee. Reuse `SUPPORTED_UBUNTU_VERSIONS` from `./types.js`; do not restate the version list.

    `system.ts` exports `parseHostname`, `parseArch`, `parseCpuCores`, `parseUptimeSeconds`. Keep them in one module: they are four one-line transforms and four separate files would be noise. Each returns `ValidationResult<...>`.

    `access.ts` exports `parseSudoCheck({ exitCode, stderr })` and `parseDockerGroupMembership(stdout: string)`. Group membership is a whole-token comparison over whitespace-split output, not `stdout.includes('docker')` — the fixture-backed `dockerx` case is there to make that failure mode impossible to ship.

    Extend `packages/domain/src/discovery/index.ts` to re-export the three new modules alongside `./types.js`.

    Keep every function pure: no `Date`, no `process`, no `node:fs`, no clock read. `purity.test.ts` enforces the import ban; the coverage gate enforces the branch discipline.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project packages packages/domain/src/discovery --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --project packages packages/domain/src/discovery` exits 0
    - `pnpm test --coverage` exits 0 with the `packages/domain/**` 95% statement and 95% branch thresholds met
    - `grep -c 'fixtures/ubuntu-22.04\|fixtures/ubuntu-24.04' packages/domain/src/discovery/os-release.test.ts` returns at least 2
    - `grep -c "VERSION_ID" packages/domain/src/discovery/os-release.ts` returns at least 1 and `grep -c "'22.04'" packages/domain/src/discovery/os-release.ts` returns 0 (the version list comes from types.ts)
    - A test asserts a Debian input yields `supported: false` with distribution and version still populated
    - A test asserts `parseDockerGroupMembership('deployer sudo dockerx')` reports false
    - `grep -c "includes('docker')" packages/domain/src/discovery/access.ts` returns 0
    - `pnpm exec vitest run --project packages packages/domain/src/purity.test.ts` exits 0
    - `pnpm lint && pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>Hostname, OS, architecture, CPU, uptime and both SERV-08 access checks are parsed by pure functions proven against real Ubuntu output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resource and Docker parsers, with docker detection split three ways</name>
  <files>packages/domain/src/discovery/resources.ts, packages/domain/src/discovery/resources.test.ts, packages/domain/src/discovery/docker-version.ts, packages/domain/src/discovery/docker-version.test.ts, packages/domain/src/discovery/index.ts</files>
  <behavior>
    - `parseMeminfo` on the captured `/proc/meminfo` for both versions returns `ramMb` as `MemTotal` kB converted to whole MB, rounding consistently and documented which way
    - `parseMeminfo` fails when `MemTotal` is absent, non-numeric, or expressed in a unit other than `kB`
    - `parseDiskUsage` on the captured `df -P -k /` output returns `{ totalMb, usedMb }` from the 1K-block total and used columns, skipping the header line and tolerating a device name long enough to wrap (`-P` guarantees one line per filesystem, which is exactly why the template uses it — state that in a comment)
    - `parseDiskUsage` fails on header-only output, on a row with too few columns, and on non-numeric columns
    - `parseDockerVersion` returns a discriminated result with at least these cases: `not_installed` (the measured command-not-found exit code from ADR 0004), `installed` with a client version and an optional server version, `daemon_unreachable` with the client version present and no server, and `unparseable` carrying a short reason
    - `parseDockerVersion` NEVER returns `not_installed` because JSON parsing failed — a JSON failure is `unparseable` (D-12, Pitfall 2)
    - `parseDockerVersion` on the plan 02-04 capture from the CLI-present-no-daemon image returns `daemon_unreachable` with the captured client version
    - `parseDockerVersion` on the plan 02-04 capture from the plain image (exit 127, no stdout) returns `not_installed`
    - `parseComposeVersion` on the captured `docker compose version --short` output returns the version string; on a command-not-found exit code returns a null version without failing; on unexpected multi-line output returns `unparseable`
  </behavior>
  <read_first>
    - packages/domain/src/discovery/fixtures/README.md and the docker_version / docker_compose_version fixtures plus their .meta.json exit codes (plan 02-04)
    - docs/adr/0004-ssh-adapter-empirical-contracts.md (section 2 — the measured exit codes and stdout/stderr split; the note about which docker JSON shape is captured versus derived)
    - packages/domain/src/discovery/types.ts (DiscoveryFacts: ramMb, diskTotalMb, diskUsedMb, dockerInstalled, dockerVersion, dockerComposeVersion)
    - packages/domain/src/discovery/os-release.ts (Task 1 — the ValidationResult convention to stay consistent with)
    - .planning/research/PITFALLS.md (Pitfall 6 — fragile Docker detection, with the Coolify issues as evidence)
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (D-12 — docker absent is a warning, version null, status CONNECTED; never free-text parsing)
  </read_first>
  <action>
    Write both test files first and watch them fail.

    `resources.ts` exports `parseMeminfo(stdout)` and `parseDiskUsage(stdout)`. For `parseMeminfo`, locate the `MemTotal:` line, require the `kB` unit token explicitly rather than assuming it, and convert with a single documented rounding rule (`Math.round` on kB/1024 is the suggested choice; whichever is picked, the comment must say so and a test must pin an exact expected value from the real fixture). For `parseDiskUsage`, parse the second line of `df -P -k /` by whitespace-splitting into six columns and taking the 1K-blocks and Used columns; convert both to MB with the same rounding rule.

    `docker-version.ts` exports `parseDockerVersion({ stdout, stderr, exitCode })` and `parseComposeVersion({ stdout, stderr, exitCode })`, both returning a discriminated union — not a boolean, and not a nullable string. The union's `unparseable` case is the whole point: D-12 says a missing Docker is a warning, and Pitfall 6 says the failure mode is treating "I could not understand the output" as "it is not installed". Those must be different values that the caller in plan 02-09 maps to different `DiscoveryCheck` details.

    Key the `not_installed` branch on the exit code recorded in ADR 0004, not on a stderr substring match: an exit code is a stable contract and a localised shell error message is not. If ADR 0004 recorded that stderr matching is unavoidable for some case, follow the ADR and cite it in a comment.

    Derive `dockerInstalled` for `DiscoveryFacts` from the union in plan 02-09, not here — this module reports what it observed, the orchestrator decides what to store.

    Extend `packages/domain/src/discovery/index.ts` with the two new modules.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project packages packages/domain/src/discovery --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --project packages packages/domain/src/discovery` exits 0
    - `pnpm test --coverage` exits 0 with the `packages/domain/**` 95%/95% thresholds met
    - `grep -c 'unparseable' packages/domain/src/discovery/docker-version.ts` returns at least 1
    - `grep -c 'daemon_unreachable' packages/domain/src/discovery/docker-version.ts` returns at least 1
    - A test feeds valid-exit-code-but-invalid-JSON stdout and asserts the result is `unparseable`, explicitly asserting it is not `not_installed`
    - A test asserts the real CLI-present-no-daemon capture yields `daemon_unreachable` with a non-null client version
    - A test asserts the real plain-image capture (exit code from the `.meta.json`) yields `not_installed`
    - `grep -c 'docker --version\|docker -v' packages/domain/src/discovery/docker-version.ts` returns 0 (no free-text banner parsing path exists)
    - A test pins an exact `ramMb` value computed from the real 24.04 `MemTotal` capture
    - `grep -c '\-P' packages/domain/src/discovery/resources.ts` returns at least 1 (the comment explaining why the template uses `df -P`)
    - `pnpm lint && pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>RAM, disk and Docker are parsed from structured real output, and "unparseable" can never masquerade as "not installed".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-11 — UNSUPPORTED_OS becomes a warning, not an error state</name>
  <files>packages/domain/src/server/connection-result.ts, packages/domain/src/server/connection-result.test.ts, docs/domain/server-state-transitions.md</files>
  <behavior>
    - `statusForErrorCode('UNSUPPORTED_OS')` returns `CONNECTED`; the other six codes keep their phase-1 landing statuses exactly
    - The mapping test is a table over every entry in `SERVER_ERROR_CODES` with an explicit expected status per code, so a widened union or a changed row without a test update fails the suite
    - `applyConnectionResult` with `{ ok: false, errorCode: 'UNSUPPORTED_OS' }` from `CONNECTING` yields `status: CONNECTED` and `lastErrorCode: 'UNSUPPORTED_OS'` — the warning is recorded for the detail view while the server is usable
    - An `UNSUPPORTED_OS` result does not set `lastSeenAt` and does not populate `hostFingerprint`: those belong to the `{ ok: true }` branch, and conflating them would let a failure path fake a successful connection
    - `applyConnectionResult` still refuses any result when the source status is not `CONNECTING`, and still routes through `transition()` rather than assigning a status literal
    - `docs/domain/server-state-transitions.md`'s connection-result mapping table shows `UNSUPPORTED_OS -> CONNECTED` with a note that it is a warning code
  </behavior>
  <read_first>
    - packages/domain/src/server/connection-result.ts (the whole file: ERROR_CODE_STATUS, statusForErrorCode, applyConnectionResult and its CONNECTING guard)
    - packages/domain/src/server/connection-result.test.ts (the existing table-driven test over SERVER_ERROR_CODES)
    - packages/domain/src/server/server-state.ts (transition(), the reason-gated edges, InvalidTransitionError)
    - docs/domain/server-state-transitions.md (the committed mirror; D-16 makes the skill table the source of truth and this file the committed record — update the mapping table and note the change)
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (D-11 in full, including the note that v0.2 will block deploy on servers carrying this warning)
    - .planning/REQUIREMENTS.md (DISC-04 — unsupported OS reported with a clear warning without blocking the rest of the collected information)
  </read_first>
  <action>
    Change the test first: move the `UNSUPPORTED_OS` row of the mapping table's expectation from `ERROR` to `CONNECTED` and watch the suite go red, then change `ERROR_CODE_STATUS`.

    `CONNECTING -> CONNECTED` is already an allowed, non-reason-gated edge in `server-state.ts`, so `transition()` needs no change and the transition table stays untouched. Verify that by running `server-state.test.ts` unchanged — if that file needs an edit, stop and reconsider, because D-16 makes the transition table a deliberate, separately-governed artifact and this phase has no mandate to widen it.

    Rewrite the doc comment above `ERROR_CODE_STATUS` so it no longer claims `UNSUPPORTED_OS` means "something is wrong with platform support". The accurate statement is that `UNSUPPORTED_OS` is a warning code: the connection and the discovery both succeeded, the platform is simply outside the supported matrix, and the code is carried in `last_error_code` for display (D-11) while v0.2 will use it to block deploys.

    Add a test asserting the `UNSUPPORTED_OS` branch leaves `lastSeenAt` and `hostFingerprint` untouched. This is not busywork: `UNSUPPORTED_OS` is now the only error code that lands on `CONNECTED`, which makes it the one code where a future refactor could plausibly start treating a failure result as a success result.

    Update the mapping table in `docs/domain/server-state-transitions.md` and add a one-line note under it recording that phase 2's D-11 changed this row and why. Do not restructure the rest of the document.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project packages packages/domain/src/server --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --project packages packages/domain/src/server` exits 0
    - `pnpm test --coverage` exits 0 with the `packages/domain/**` 95%/95% thresholds met
    - `grep -A1 'UNSUPPORTED_OS' packages/domain/src/server/connection-result.ts | grep -c "CONNECTED"` returns at least 1
    - `grep -v '^\s*//\|^\s*\*' packages/domain/src/server/connection-result.ts | grep -c "UNSUPPORTED_OS: 'ERROR'"` returns 0
    - `grep 'UNSUPPORTED_OS' docs/domain/server-state-transitions.md | grep -c 'CONNECTED'` returns at least 1
    - `grep -c 'D-11' docs/domain/server-state-transitions.md` returns at least 1
    - `git diff --stat packages/domain/src/server/server-state.ts` shows no change to that file
    - A test asserts an `UNSUPPORTED_OS` result from `CONNECTING` gives `status: 'CONNECTED'` and `lastErrorCode: 'UNSUPPORTED_OS'`
    - A test asserts an `UNSUPPORTED_OS` result leaves `lastSeenAt` null and does not set `hostFingerprint`
    - A test asserts an `UNSUPPORTED_OS` result from `PENDING` still throws `InvalidTransitionError`
    - `pnpm lint && pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>An unsupported OS leaves the server connected and usable with a recorded warning, the mapping is asserted for all seven codes, and the committed mirror matches the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Remote command stdout → control-plane data model | Every parser input is text produced on a machine Noodara does not control, and lands in fields the UI displays and v0.2 will make deploy decisions from |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-17 | Tampering (of trust in reported state) | `parseDockerVersion` | mitigate | A three-way discriminated union (`not_installed` / `installed` / `daemon_unreachable`) plus an explicit `unparseable` case keyed on the ADR-0004 exit code; a test asserts invalid JSON never yields `not_installed` |
| T-2-18 | Tampering | `parseDockerGroupMembership` | mitigate | Whole-token match over whitespace-split groups, with a `dockerx` negative test — a substring match would report a privilege the user does not have |
| T-2-19 | Denial of Service | Parser fed hostile or malformed output | mitigate | Every parser returns a `ValidationResult` and never throws; no unbounded regex backtracking (explicit line/column splitting, no catch-all patterns); the 95% branch gate forces the malformed branches to be exercised |
| T-2-20 | Tampering | `UNSUPPORTED_OS` now landing on `CONNECTED` | mitigate | Dedicated tests assert the failure branch still does not set `lastSeenAt` or `hostFingerprint`, and that `applyConnectionResult` keeps its `CONNECTING`-only guard and its `transition()` routing |
</threat_model>

<verification>
- `pnpm test --coverage` exits 0 with `packages/domain/**` at or above 95% statements and 95% branches
- `pnpm exec vitest run --project packages packages/domain/src/purity.test.ts` exits 0 (no I/O import entered the new module)
- `pnpm lint && pnpm typecheck && pnpm boundaries` exit 0
- Every parser assertion that can read a real 22.04/24.04 capture does
</verification>

<success_criteria>
All ten DISC-01 facts can be produced from real command output by pure, fixture-tested functions, and an unsupported OS is a warning on a connected server rather than an error.
</success_criteria>

<output>
Create `.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-05-SUMMARY.md` when done
</output>
</content>
