---
phase: 05-ui-web
plan: 46
type: gate
---

# Phase 05 Plan 46: Gap Closure Round 2 — Closing Gate

This document trusts no `SUMMARY.md`. Every claim below is either a command's own output (cited by
log file and, where the reporter supports it, line number) or a `file:line` quote from source read
directly in this session. Where evidence could not be found, the verdict is written `UNVERIFIED`,
per this plan's own governing rule.

**Tree under test:** HEAD `7f56cdb` on `main`, unchanged throughout this gate (`git status --short .`
scoped to `noodara/code` showed no source drift before or after any command; the only new path is
`.planning/phases/05-ui-web/gate-logs/`, this plan's own artifact).

**NOODARA_API_ORIGIN:** the entire sequence below (except one supplementary single-file rerun used
only to obtain a citation, see Section 2 item 5) ran in a shell where `NOODARA_API_ORIGIN` was NOT
exported, per this task's own governing instruction — this is F1's own acceptance evidence, not an
oversight.

## Section 1 — Full cross-suite gate (11 commands, run once each on the final tree)

| # | Command | Exit | Duration | Counts | Log file | Delta vs. baseline |
|---|---|---|---|---|---|---|
| 1 | `pnpm lint` | 0 | 0.57s wall (turbo: 59ms, FULL TURBO) | 9/9 tasks cached, green | `gate-logs/lint.log` | Same as baseline (9/9) |
| 2 | `pnpm typecheck` | 0 | 1.69s wall (turbo: 18ms, FULL TURBO) | 8/8 turbo tasks cached, green; the two extra `tsc -p tests/integration/ssh/tsconfig.json --noEmit` and `tsc -p tests/e2e/tsconfig.json --noEmit` invocations chained by the script produced zero output and the overall `&&` chain exited 0, i.e. both passed silently | `gate-logs/typecheck.log` | Same as baseline (8/8) |
| 3 | `pnpm boundaries` | 0 | 0.53s wall | "Checked 617 files in 6 packages, no issues found" | `gate-logs/boundaries.log` | +4 files vs. baseline's 613 (round added source files; no issues either time) |
| 4 | `pnpm check:ui-safety` | 0 | 0.47s wall | 9/9 gates OK | `gate-logs/check-ui-safety.log` | Same as baseline (9/9) |
| 5 | `node scripts/check-package-provenance.mjs` | 0 | 29.3s wall | 52/52 locked direct dependencies verified, first attempt, no flake observed (O7 concern did not reproduce — no rerun needed) | `gate-logs/provenance.log` | Same as baseline (52/52) |
| 6 | `pnpm test` (unit) | 0 | 7.17s (vitest-reported) | 119 files, 1525/1525 passed | `gate-logs/test-unit.log` | +3 files / +28 tests vs. baseline's 116 files / 1497 tests — counts went up, as the round added tests |
| 7 | `pnpm build` (NOODARA_API_ORIGIN unexported) | **1** | 2.02s wall | `@noodara/web:build` fails: `Error: NOODARA_API_ORIGIN is required (the control plane origin apps/web proxies /api/* to, e.g. http://localhost:3100 in dev) -- set it before running next dev/build.` at `next.config.compiled.js:24:15` | `gate-logs/build.log` | **By design, not a regression** — F1's production rule is deliberately unchanged; see Section 3 |
| 8 | `pnpm test:boot` (NOODARA_API_ORIGIN unexported) | 0 | 55.18s wall (vitest: 54.62s) | 1 file, 7/7 passed | `gate-logs/test-boot.log` | Proves 05-42's harness-only default (`buildWorkspace()`) works: the *spawned* build succeeds without the var being exported in the parent shell, while command 7 above (a bare `pnpm build`, not harness-spawned) still fails |
| 9 | `pnpm test:integration` (Docker) | 0 (after 1 retry) | see below | see below | `gate-logs/test-integration.log` (+ `gate-logs/test-integration.attempt1-environmental-failure.log`) | See Section 1a |
| 10 | `pnpm test:e2e` (Docker) | **1** | 84.95s wall (1.4m reported) | 93 tests total, 92 passed, **1 failed** | `gate-logs/test-e2e.log` | Same total count as baseline (93); 1 real regression, see Section 1b |
| 11 | `pnpm security:scan-leaks` (Docker) | 0 | 32.07s wall | vitest portion: 3 files / 3 tests passed; Playwright `@canary` portion: 1/1 passed | `gate-logs/security-scan-leaks.log` | Green |

### Section 1a — `pnpm test:integration`: one environmental failure, one clean rerun (both logs kept)

**Attempt 1 (`gate-logs/test-integration.attempt1-environmental-failure.log`): exit 1, 2702.37s
(~45min).** `57 failed | 2 passed | 1 skipped (60)` test files, `469 failed | 54 passed | 1 skipped
(524)` tests. Root cause, read directly from the log: partway through the run, the pino logs show a
cascade of `"sse broadcaster subscriber redis error"` (`AggregateError`) culminating in `"sse
broadcaster failed to start within the boot window"` and `ECONNREFUSED` to a dynamic Testcontainers
port (`::1:60101` / `127.0.0.1:60101`, log lines ~10905-11045) — a Redis Testcontainer became
unreachable mid-run. From that point, `assertNoStrayTestContainers` (`tests/integration/helpers/
ssh.ts:202`) started failing with `expected [] to have length 0 but got 2` (log line ~16578-16581):
two containers from the disrupted test were never torn down, and every subsequent file's own
`afterEach` hit the same non-zero stray-container assertion, cascading the failure across nearly the
entire alphabetically-later file list (confirmed: the first failing file is
`tests/integration/activity/auth-events.test.ts`, essentially the start of the suite, and failures
continue through the end). This is the documented "Docker hiccup … cascades into hundreds of
failures" signature this plan's own dispatch instructions describe.

**Evidence this was environmental, not a regression:** (1) immediately after the run, `docker ps -a
--filter "label=noodara.test=true"` returned zero containers — the stray pair from mid-run
eventually tore down / were reaped, nothing was left to manually clean; (2) `docker system df`
showed a resource-constrained Docker Desktop VM (7.65 GiB total memory limit) with 353 images (85 GB,
53 GB reclaimable) and multiple **other, unrelated** projects' containers concurrently `Up` on this
shared machine (`test-ui-1`, `test-api-1`, `test-db-1` all up 24h; `nuestracasa-neon-proxy` up 8
days) — real resource contention outside this repository's control; (3) the failure signature
(`ECONNREFUSED` to a container that had been reachable moments before, not a bind-time "address
already in use") is consistent with a container dying under load, not a code defect; (4) the retry
(below) passed clean with zero source changes in between.

**Retry (`gate-logs/test-integration.log`): exit 0, 2142.47s (~35.7min).** Before rerunning: confirmed
zero stray `noodara.test=true` containers and ports 3000/3100 free. `Test Files 59 passed | 1
skipped (60)`, `Tests 523 passed | 1 skipped (524)`. The 1 skipped file/test is
`tests/integration/ssh/stress-connections.test.ts`'s `describe.skipIf(!STRESS_ENABLED)` block — a
pre-existing, `NOODARA_STRESS`-env-gated, opt-in-only suite documented in its own file header as
"deliberately excluded from the PR-blocking path… belongs to the v0.1 release gate and a future
nightly run" — not something this round touched or skipped. No other skip anywhere (confirmed:
`grep -rn "\.skip(\|\.only(\|test\.fixme\|it\.todo\|describe\.skip"` across `tests/`, `apps/*/src`,
`packages/*/src` returns exactly this one hit).

**Baseline comparison:** the plan's own baseline (487 passed / 0 failed / 1 skipped) is explicitly
named stale by the plan itself (not re-run in the prior verification session). The clean retry's
523 passed / 1 skipped is **up** by 36 tests from that stale figure, consistent with "this round
added tests, so counts must go up."

**Supplementary citation run:** `gate-logs/trust-fingerprint-binding-citation.log` — a standalone,
`--reporter=verbose` rerun of the single already-passing file
`tests/integration/servers/trust-fingerprint-binding.test.ts`, run *after* the full clean retry
above, solely because the full suite's default (non-verbose) reporter never prints a per-test
"passed" line for a file that did not fail — only failures and the aggregate summary are shown. This
is not a re-run to force a pass (the file already passed, twice, as part of both integration runs);
it exists only to produce a citable line for Section 2 item 5. Result: exit 0, `Test Files 1 passed
(1)`, `Tests 11 passed (11)`, 77.16s. Zero stray containers before or after.

### Section 1b — `pnpm test:e2e`: 1 real (non-flaky, non-environmental) failure

`92 passed (1.4m)`, `1 failed`:

```
1) [chromium] › tests/e2e/host-key.spec.ts:429:1 › @hostkey UF-01 regression: editing the host
   while ERROR/HOST_KEY_CHANGED clears the pending fingerprint, so the trust affordance disappears
   and the trusted fingerprint stays unchanged
   Error: expect(received).toBe(expected)
   Expected: "ssh-ed25519 SHA256:0YT++OJbqfolUdiU6O1+suTXfkYNzkxDX0Nkb3F8I9U"
   Received: null
   at tests/e2e/host-key.spec.ts:504:43
```

**Classification: a real, deterministic consequence of this round's own intentional GR-02 fix — not
a regression bug, not a flake, not environmental.** This test (added by plan 05-19, commit
`1dddd11`, predates round 2 and is not in 05-40's `files_modified`) edits only `host` while the
server is `ERROR`/`HOST_KEY_CHANGED`, then asserts `afterEditBody.hostFingerprint === originalFingerprint`
(line 504) — i.e. it encodes the *pre*-GR-02 expectation that a host-identity edit clears only
`pendingFingerprint`, leaving the trusted `hostFingerprint` untouched. Plan 05-40 (this round)
deliberately changed that: `edit-server.ts`'s `hostIdentityChanged` block (confirmed by direct read,
line 264-266: `if (hostIdentityChanged) { statusPatch = { ...statusPatch, hostFingerprint: null,
hostFingerprintCapturedAt: null }; }`) now clears `hostFingerprint` unconditionally whenever `host`
or `sshPort` changes, from **any** status — this is 05-40's own documented `must_haves.truths`:
"Re-pointing a server at a different host or port clears the old host's trusted fingerprint from ANY
status, so the next connect is a clean TOFU first capture instead of a guaranteed spurious
HOST_KEY_CHANGED." That is exactly what this E2E test does (edits `host` to
`renamed-....example.test`) and exactly the behavior GR-02 intentionally changed. The test's own
code comment ("The trusted fingerprint is untouched — only the *pending* one was ever at risk…")
states the pre-fix mental model 05-40 superseded on purpose.

**Not classified as a documented flake:** `deferred-items.md`'s two named flakes are both in
`tests/e2e/servers-list.spec.ts` (the `row.hover()` timeout and the keyboard-activation SSE case) —
an unrelated file and an unrelated, bimodal-timeout failure shape. This failure is a deterministic
`toBe` value mismatch with a fully-explained root cause from source reading, not a timing race, so
no `deferred-items.md` citation applies and none is claimed.

**Not fixed here, per this plan's own hard rule.** `tests/e2e/host-key.spec.ts` was not edited.
Flagged as a residual for a follow-up plan: update this test's final two assertions to expect
`hostFingerprint === null` (matching GR-02's now-intended behavior) instead of the pre-fix value,
with its own RED/GREEN cycle. This is a real, if narrow, process gap in 05-40's own scope (it audited
and added new integration coverage for GR-02 but did not audit pre-existing E2E coverage for
assertions the fix would invalidate).

### "Nothing was gamed" assertions

- No timeout value was changed anywhere in this session (no `playwright.config.ts`,
  `vitest.integration.config.ts`, or per-test `test.setTimeout` edit).
- No test was newly skipped. The one skip in Section 1a (`stress-connections.test.ts`) is a
  pre-existing, `NOODARA_STRESS`-env-gated opt-in suite, unrelated to and unmodified by this
  session or this round.
- No test was re-run to convert a failure into a pass. `pnpm test:integration` was rerun exactly
  once, per this plan's own explicit instruction for a diagnosed-environmental failure, with both
  raw logs kept on disk. The `trust-fingerprint-binding.test.ts` supplementary run was of an
  **already-passing** file, solely for report-citation purposes (see Section 1a). The one E2E
  failure (`host-key.spec.ts`) was **not** rerun — it is reported as a real, explained failure.
- Nothing was fixed. `git status --short .` (scoped to `noodara/code`) before and after every
  command in this section showed no change to any source, config or test file — only new files
  under `gate-logs/` and (after this commit) this document itself.

### Git state

- HEAD before this gate: `7f56cdb1fa8e1ada2d7c18665962d26a7d460907` (`main`).
- HEAD after Section 1's eleven commands: unchanged, `7f56cdb1fa8e1ada2d7c18665962d26a7d460907`.
- `git status --short .` (scoped to `noodara/code`) after all eleven commands: only
  `.planning/phases/05-ui-web/gate-logs/` (untracked, this plan's own artifact).
- `git diff a8c03c5..HEAD -- package.json pnpm-lock.yaml` (the whole round's package/lockfile
  drift): empty. No plan in this round installed anything.
- Secret scan of every file under `gate-logs/` before committing (T-5G-46-04): grepped for
  `NOODARA_MASTER_KEY=`, `BETTER_AUTH_SECRET=`, `DATABASE_URL=postgres`, `REDIS_URL=redis`,
  `sk-[A-Za-z0-9]{10,}`, `postgres://user:pass@`-shaped strings, PEM private-key headers,
  `ghp_`, `AKIA`, `Authorization: Bearer <token>`, and the literal canary strings used elsewhere in
  this phase (`CANARY`, the fixture admin password). Zero hits in any of the 13 log files. The
  `keyFingerprint` values pino logs during boot (e.g. `"Back up NOODARA_MASTER_KEY; credentials are
  unrecoverable without it"`) are short hex fingerprints of the key, never the key material itself —
  by design (`logger.ts`'s own redaction).
- All 13 log files are well under the 5MB trim threshold (largest is
  `test-integration.attempt1-environmental-failure.log` at ~1.7MB); none required trimming.

## Section 2 — Gap 6 verdict, re-derived from current source (six `missing:` items)

Every quote below was read directly in this session (file + line), not cited from any
`SUMMARY.md` or prior audit.

| # | `missing:` item (05-VERIFICATION.md) | Verdict | First-hand evidence | Residual |
|---|---|---|---|---|
| 1 | `trustFingerprint` refuses promotion unless `row.lastErrorCode === 'HOST_KEY_CHANGED'` | **CLOSED** | `grep -n "lastErrorCode" apps/control-plane/src/services/trust-fingerprint.ts` → `51:`, `104: if (row.lastErrorCode !== 'HOST_KEY_CHANGED') {`, `125:`, `127:`, `148: eq(servers.lastErrorCode, 'HOST_KEY_CHANGED'),`. Guard (`trust-fingerprint.ts:104-110`): refuses with `SERVER_NOT_TRUSTABLE` before `transition()`/the UPDATE. Conditional-UPDATE `WHERE` predicate (`:144-150`) repeats `eq(servers.lastErrorCode, 'HOST_KEY_CHANGED')` as defence-in-depth alongside the row's `SELECT ... FOR UPDATE` lock (`:66-70`, same transaction) — the guard cannot be bypassed by a value that changes between the check and the UPDATE while the lock holds. | None found |
| 2 | `applyConnectionResult`'s `ok:true` branch clears `pendingFingerprint`; the failure branch clears it for every non-`HOST_KEY_CHANGED` code | **CLOSED** | `packages/domain/src/server/connection-result.ts`: success branch (`:77-86`) — `pendingFingerprint: null,` at line 83, unconditional. Failure branch (`:88-97`) — ternary at `:92-95`: `pendingFingerprint: result.errorCode === 'HOST_KEY_CHANGED' ? (result.observedFingerprint ?? state.pendingFingerprint) : null`. Unit coverage confirmed present and specific to this exact invariant: `connection-result.test.ts:98` "clears a previously parked pendingFingerprint on a successful reconnect", `:116` "...on a successful first-capture (TOFU) connect too", `:159` "HOST_KEY_CHANGED with no observedFingerprint keeps the older parked value" (else-branch), `:171` a parameterized case sweeping every other error code and asserting `pendingFingerprint` is cleared — closing the exact coverage gap `05-REVIEW.md`'s original GR-01 finding named ("`connection-result.test.ts` never exercises a non-null `pendingFingerprint` going into the success branch"). | None found |
| 3 | `edit-server.ts`'s identity-changing block clears `hostFingerprint`/`hostFingerprintCapturedAt` unconditionally (not only inside `CONNECTED`) — GR-02 | **CLOSED** | `edit-server.ts:177`: `const hostIdentityChanged = host !== row.host \|\| sshPort !== row.sshPort;` — **`sshUser` is explicitly excluded** from this predicate. Composition at `:264-266`: `if (hostIdentityChanged) { statusPatch = { ...statusPatch, hostFingerprint: null, hostFingerprintCapturedAt: null }; }` — confirmed to sit **after and outside** the `if (row.status === 'CONNECTED')` block, which opens at `:235` and closes at `:255`; `hostIdentityChanged`'s own `if` at `:264` is composed unconditionally on top, so it fires for `ERROR`/`UNREACHABLE`/`DISCONNECTED`/`PENDING` as well as `CONNECTED`. Regression-tested: `tests/integration/services/edit-server.test.ts`'s "CONNECTED + ssh user change only transitions to DISCONNECTED and preserves the fingerprint (D-14 access)" and 05-40's own "ERROR + sshUser-only change preserves hostFingerprint/hostFingerprintCapturedAt". **`sshUser` exclusion is deliberate and correct**, not a defect: an SSH-user-only change does not change which machine the row points at, so clearing the trusted key there would silently reopen TOFU for an already-trusted host — this is D-14's own `access` classification, independently confirmed. | None found in the fix itself. See Section 1b for a **downstream** residual: a pre-existing E2E test (`tests/e2e/host-key.spec.ts:429`) was never updated to match this now-intentional behavior and fails as a direct, explained consequence. |
| 4 | `connect-and-discover.ts`'s `pendingFingerprintSeenAt` derivation nulls the timestamp whenever the value it describes is nulled | **CLOSED** | `connect-and-discover.ts:368-375`: `const pendingFingerprintSeenAt = nextState.pendingFingerprint === null ? null : (!outcome.ok && outcome.errorCode === 'HOST_KEY_CHANGED' && outcome.observedFingerprint !== undefined ? deps.now() : row.pendingFingerprintSeenAt);` — the outer ternary's condition is exactly "the domain just cleared the value", and its consequent is an unconditional `null`; only a fresh `HOST_KEY_CHANGED` observation sets a new timestamp, everything else (an untouched still-parked value) keeps the row's own. | None found |
| 5 | Integration test: HOST_KEY_CHANGED parks F → a second connect fails AUTH_FAILED → `POST /trust-fingerprint` with F must be 409, not 200 | **CLOSED** | Test file: `tests/integration/servers/trust-fingerprint-binding.test.ts:269`, title `'returns 409 SERVER_NOT_TRUSTABLE and promotes nothing when a later AUTH_FAILED supersedes the parked HOST_KEY_CHANGED (gap 6 / GR-01 bypass, case 1)'`. Cited passing line: `gate-logs/trust-fingerprint-binding-citation.log:703` — `✓ tests/integration/servers/trust-fingerprint-binding.test.ts > POST /api/servers/:id/trust-fingerprint binds to the fingerprint the admin actually saw (gap 6, T-5G-27) > returns 409 SERVER_NOT_TRUSTABLE and promotes nothing when a later AUTH_FAILED supersedes the parked HOST_KEY_CHANGED (gap 6 / GR-01 bypass, case 1) 6278ms` (file summary at `:1210-1211`: `Test Files 1 passed (1)`, `Tests 11 passed (11)`). This file's real HTTP traffic (11 distinct `POST .../trust-fingerprint` calls) is also independently visible in the full-suite retry log (`gate-logs/test-integration.log`, e.g. lines 761-988), confirming the same file ran and produced zero failures as part of that 523/524-passing run. The file also covers two further bypass attempts beyond the plan's literal minimum: case 2 (`COMMAND_TIMEOUT` supersedes, `:298`) and case 3 (a later successful reconnect supersedes, `:323`), plus a response-body leak check (`:350`) and a non-`ERROR`-status 409 check (`:380`). | None found |
| 6 | `.planning/todos/pending/2026-09-19-trust-fingerprint-toctou.md`'s disposition, reconciled against items 1-5 | **CLOSED** | The file now lives at `.planning/todos/completed/2026-09-19-trust-fingerprint-toctou.md` (confirmed: `find .planning/todos -iname "*trust-fingerprint-toctou*"` returns exactly this one path, nothing under `pending/`). Its "Closed 2026-09-20" section's four bullets were independently re-derived by this gate (items 1-3 above, plus the same test citation as item 5) and **match** what this session found first-hand — same grep outputs, same line numbers, same test file/title. No discrepancy between the todo's closing evidence and this session's own derivation. | None found |

**Overall gap 6 verdict: CLOSED.** Constructing each of the six bypass paths the plan's hard rules
require on paper, against current source:

1. *Stale pending after success* — `connection-result.ts:83` nulls it unconditionally. No bypass.
2. *Stale pending after a non-host-key failure* — `connection-result.ts:92-95` nulls it for every
   code except `HOST_KEY_CHANGED`. No bypass.
3. *Status back on ERROR for another reason* — `trust-fingerprint.ts:104` refuses unless
   `lastErrorCode === 'HOST_KEY_CHANGED'`, and the conditional UPDATE (`:148`) repeats the same
   check. No bypass.
4. *TOCTOU between read and UPDATE* — the row is `SELECT ... FOR UPDATE`-locked at `:66-70` inside
   the same transaction as both the guard and the UPDATE; Postgres row-lock semantics make
   `lastErrorCode` immutable-under-this-transaction between the two. No bypass.
5. *Re-point via host/port edit from every status* — `edit-server.ts:264-266`'s `hostIdentityChanged`
   clear is unconditional on status. No bypass (and GR-02, the specific defect this closes, is
   independently confirmed fixed — item 3 above).
6. *`sshUser`-only edit* — `hostIdentityChanged` (`:177`) deliberately excludes `sshUser`, so
   `hostFingerprint` correctly survives a user-only edit (this is intended TOFU-preserving behavior,
   not a gap).

No path was found that still allows promoting a stale fingerprint.

## Section 3 — Residuals and orchestrator findings (O1-O7)

### Named residuals (05-VERIFICATION.md `gaps_remaining:`)

| # | Residual | Status | First-hand evidence |
|---|---|---|---|
| 1 | GR-03 — swallowed recovery-call failure inside `connectAndDiscover`'s catch was unlogged | **PARTIAL** | Production wiring is genuinely fixed: `connect-and-discover.ts:477-485` now calls `logRecoveryFailure(deps, row.id, recoveryErr)` in the `.catch(...)` (confirmed by read); `apps/app.ts` passes `app.log` and `worker.ts` passes its module-level `logger` (both confirmed by grep) — the real production entrypoints are wired correctly, unit-tested (`log-recovery-failure.test.ts`, 6/6 behaviours) and integration-tested end-to-end with a real pino logger (`tests/integration/servers/connect-wedge.test.ts`'s "recovery-failure logging" block, per 05-41-SUMMARY.md's own RED/GREEN evidence, cross-checked against the unit test file directly). **Why only PARTIAL:** `ServerServicesDeps.logger` is declared **optional** (`server-service-deps.ts:58`, `readonly logger?: ServiceLogger`), and `logRecoveryFailure`'s own body (`log-recovery-failure.ts:39`) is `deps.logger?.warn(...)` — an absent logger silently no-ops, by design. Confirmed by grep across every `resolveServerServicesDeps({...})` call site (`apps/control-plane/src/app.ts`, `apps/control-plane/src/worker.ts`, `tests/integration/services/helpers/service-fixture.ts`, `tests/integration/helpers/worker-fixture.ts`, `tests/integration/routes/api-e2e.test.ts`, `tests/integration/routes/events-sse.test.ts`, `tests/integration/activity/canary-http.test.ts`) that **only `app.ts` and `worker.ts` pass `logger`** — the shared integration test fixture (`tests/integration/services/helpers/service-fixture.ts:118`) and every other test call site never do. A future service-construction site (or a future test relying on the shared fixture to exercise this exact catch block) can silently omit `logger` and GR-03's log line disappears with no test ever failing — matching O2 exactly (see below). |
| 2 | GR-05 — `check-package-provenance.mjs`'s `execFileSync` calls had no per-call timeout | **CLOSED** | `scripts/check-package-provenance.mjs:254` (`pnpm list` call) and `:301` (`npm view` call) both now carry `timeout: 30_000`, matching the original WR-C-12 fix text exactly. Gate command 5 (Section 1) ran both code paths live: 52/52 coverage, no timeout hit, 29.3s total. |
| 3 | F1 — `pnpm build`/`pnpm test:boot` fail locally without `NOODARA_API_ORIGIN` exported | **PARTIAL, by design — matches 05-42's own stated scope** | Gate commands 7 and 8 (Section 1) are this residual's own acceptance evidence, reproduced live in this session, not merely re-cited: a bare `pnpm build` (command 7) still fails (`exit 1`, `NOODARA_API_ORIGIN is required...`) — the production rule is deliberately unchanged (`next.config.ts`, `proxy.ts`, `api/events/route.ts` all still fail-fast, per `apps/control-plane/src/env.ts`'s INST-06 no-default policy, confirmed untouched by this round: none of those three files appear in `git diff a8c03c5..HEAD --name-only`). `pnpm test:boot` (command 8) now passes unexported (`7/7`), because `buildWorkspace()` (`tests/integration/helpers/boot-process.ts`) supplies a test-only default — confirmed via the harness's own doc comment and the literal env-spread line, matching 05-42-PLAN.md's Task 2 spec. **Why PARTIAL, not CLOSED:** this was never meant to close the "bare shell" case — 05-42-PLAN.md itself states "Do NOT add a default for NOODARA_API_ORIGIN in apps/web/next.config.ts... The production requirement stays explicit" and lists asserting the bare-`pnpm build`-still-fails case as an acceptance criterion. A developer running a bare `pnpm build` on a fresh clone still needs `.env.example`'s documented export — this is accepted, disclosed local-DX friction, not a bug. |
| 4 | WR-A-04 — no `hooks.logMethod` interceptor guarding a future bare-Error `logger.error(err)` call | **CLOSED** | `apps/control-plane/src/logger.ts:61-74`: a `hooks.logMethod` interceptor is now registered on every `createLogger()` instance — global, not per-call-site. It rewrites any bare `Error` first argument into `{ err: <error> }` plus a message that is never derived from the error's own text (a fixed `NO_MESSAGE_FALLBACK` literal unless the caller supplied a string second argument), confirmed by direct read (`:62-73`). This is a stronger fix than the original review anticipated (which only asked for a single call-site fix at `server.ts`) — it closes the class of bug for every current and future `logger.error(someError)`/`logger.warn(someError)` call in the codebase, not just the one found site. |
| 5 | Contract seam — no single test proves the real backend's `VALIDATION_FAILED` body (over real HTTP) is consumed by the real frontend in the same test | **CLOSED, with one disclosed, honest, in-scope limitation** | `tests/integration/routes/validation-issue-contract.test.ts` (added whole by 05-44, commit `4d3af03`, 204 lines) feeds a real `app.inject()` HTTP response's `issues` array directly into the real `fieldErrorsFromIssues` in the same test, for a top-level field, a nested credential-block field, and an unknown strict-rejected path — no stub, no hand-written array on either end, confirmed by reading the file's own header comment and test bodies. **Disclosed limitation, stated by the test file's own header, not found independently by this gate:** service-level `VALIDATION_FAILED` responses (validator rejections inside a service, e.g. `validateSshUser`) carry no `issues` array at all — only the schema-level `app.setErrorHandler` branch does — so the specific `/sshUser` whitespace message `tests/e2e/server-sheet.spec.ts` stubs remains genuinely unreachable through this real-HTTP seam, "a necessity, not a shortcut" per the test's own comment. This matches the gap-5 residual's original, narrower scope (the schema-level shape), not a new gap. |
| 6 | Contrast residual A — light-mode `--accent` as link text on `--canvas` (4.31:1) / `--surface-3` (4.12:1), both failing AA | **CLOSED** | `packages/ui/tokens.css:36` (`--accent-text: #0066cc;`, light) / `:177` (`--accent-text: #2997ff;`, dark, unchanged value). `docs/contrast-decision-05.md` §7 "as-shipped" table (taken directly from `auditTokens()`'s own output, not hand-computed): light `--canvas` 5.11, `--surface-1` 5.56, `--surface-2` 5.33, `--surface-3` 4.89 — all ≥4.5. `grep -rn "text-accent\b" apps/web/src packages/ui/src` after 05-45's Task 3 (per the decision doc, independently re-derivable): no real call site remains, only two comments naming the historical pattern. `packages/ui/src/contrast.test.ts` gate: 36/36 green. |
| 7 | Contrast residual B — `Button.tsx`'s destructive-filled variant, white text on `--status-error` (3.54:1 light / 3.40:1 dark), failing AA | **CLOSED, with one disclosed non-blocking trade-off (O3)** | `packages/ui/tokens.css:58` / `:189`: `--status-error-fill: #d70015;` (same value both themes, per the user's D5 decision). `docs/contrast-decision-05.md`: `--on-accent` on `--status-error-fill` measures 5.38:1 in **both** themes (≥4.5, PASS) — the only role this token is used for (`bg-status-error-fill` in `Button.tsx`'s `DESTRUCTIVE_FILLED_CLASSES`, confirmed no `text-`/`border-`/`outline-status-error-fill` call site exists). **O3, independently confirmed exact:** the same document's own §"Roles extra medidos" table (informative, not gated by `contrast.test.ts`, since no opacity-composition helper exists in `contrast.ts`) records the fill's **non-text edge/border** contrast against dark surfaces as materially *lower* than the value it replaced — `#d70015` vs. dark `--surface-1` = 3.12, vs. `--surface-2` = 2.84, against the old `#ff453a`'s 4.94 / 4.49 — both independently re-derived by this gate to the same 2-decimal figures the document itself records. This is real and disclosed, not found only by the orchestrator. It is informational only (not a `contrast.test.ts` gate failure, since the fill is never rendered as a border/outline anywhere in the tree) and is exactly what belongs in the human real-display item (item 2 of Section 4). |

### Orchestrator findings O1-O7

| # | Finding | Status | Evidence |
|---|---|---|---|
| O1 | 05-42 has no separate RED commit; other round-2 plans do | **CONFIRMED TRUE** | `git log --oneline a8c03c5..7f56cdb`: 05-42's two behaviour commits are `dce7e7f fix(05-42): add per-call timeouts to provenance gate subprocess calls` and `756fa99 fix(05-42): buildWorkspace supplies a test-only NOODARA_API_ORIGIN default`, each with no preceding `test(05-42): ...` commit. Every other round-2 plan with a behaviour change has one: 05-38 (`ab92f34` test → `53d7799` fix; `5671dc0` test → `c252f46` fix), 05-39 (`b93ab6c` test → `a23cbc3` fix), 05-40 (`2a5c98a` test → `89beb7f` feat), 05-41 (`add88f0` test → `fa2ed4f` feat; `238ad4e` test → `06da279` feat), 05-43 (`c453177` test → `2281702` fix), 05-45 (`0e3190b` test → `2f82ce0`/`cd90c23`). 05-44 is test-only (adds a proving test, no production change needed), consistent with its own nature. |
| O2 | 05-41 made `ServerServicesDeps.logger` optional; app.ts/worker.ts supply it, the shared integration fixture does not — a future omission would silently drop GR-03's log line with nothing failing | **CONFIRMED TRUE** | See residual #1 above — identical finding, independently re-derived via grep across every `resolveServerServicesDeps` call site. Judged **PARTIAL**, not CLOSED, for exactly this reason (production correctness: closed; future-regression protection: open). |
| O3 | `--status-error-fill`'s non-text edge contrast on dark surfaces (3.12 / 2.84) is lower than the old `#ff453a` (4.94 / 4.49); label/hover contrast (5.38, hover 5.01-5.08) is fine; recorded in `docs/contrast-decision-05.md`, belongs in the human real-display item | **CONFIRMED TRUE, exact figures** | See residual #7 above. `docs/contrast-decision-05.md`'s own "Roles extra medidos" table records `edge #d70015 vs dark surface-1 = 3.12`, `vs dark surface-2 = 2.84`, `edge actual #ff453a vs dark surface-1 = 4.94`, `vs dark surface-2 = 4.49` — all four independently re-derived to the same 2-decimal values by this gate. The hover figures (5.01 light / 5.08 dark ×2) and the 5.38 label figure are also present and match. This is a real, disclosed, user-decided (D5) trade-off, not a hidden defect — carried into Section 4 item 2. |
| O4 | Commit `a23cbc3`'s body mentions the unversioned instructions file by name; not an attribution trailer, note only | **CONFIRMED TRUE** | `git show -s --format="%B" a23cbc3` → body reads "...closing the boundary half of gap 6 / GR-01 (**CLAUDE.md** 2.3: backend-enforced, never UI-only)..." — a bare-name reference, not a trailer or an "AI assisted" style attribution. Independently confirmed **unversioned**: `git ls-files CLAUDE.md` (this repo) returns nothing, and `git check-ignore -v CLAUDE.md` → `noodara/code/.gitignore:3:CLAUDE.md CLAUDE.md` — the file is explicitly excluded under `.gitignore`'s own `# AI assistant config (not versioned)` header (line 1), alongside `.claude/` (line 2). So "unversioned" is accurate for this repository, not a mischaracterization. Note-only, as O4 says: this is a project-history fact, not a rule violation this gate enforces (this plan's own hard rule 3 — no `CLAUDE.md` mentions in commit messages — governs *this* plan's own commits, not a retroactive judgment on round 2's). |
| O5 | 05-41's first executor stalled (no progress 600s) with RED committed and GREEN staged; a continuation agent verified the staged diff and finished; nothing lost | **CONSISTENT WITH SOURCE, not independently re-derivable from git alone** | `05-41-SUMMARY.md`'s own "Continuation note" describes exactly this sequence, and it is internally consistent with the git log's shape (`add88f0` RED exists alone before `fa2ed4f`, which is a single GREEN commit for a full task). This process claim cannot be independently confirmed or refuted from source/tests alone (there is no artifact that proves an agent "stalled" vs. was simply spawned fresh) — recorded as **process-consistent**, not verified as an independent fact the way the six gap-6 items are. |
| O6 | `pnpm lint`/`pnpm typecheck` do not cover most of `tests/**` and `scripts/**` | **CONFIRMED TRUE** | Every package's `lint` script is `eslint --config ../../packages/config/eslint.config.js .` run with that **package's own directory** as cwd (`apps/control-plane`, `apps/web`, `packages/domain`, `packages/ssh`, `packages/ui`, `packages/config` — confirmed by reading each `package.json`); no root-level `eslint.config.js` exists (`find . -maxdepth 1 -iname "eslint.config*"` → empty), so `tests/**` and `scripts/**` (both at repo root, outside every package directory) are never linted by any turbo task. `pnpm typecheck`'s two extra chained `tsc -p` invocations cover only `tests/integration/ssh/tsconfig.json` and `tests/e2e/tsconfig.json` (`find tests -iname "tsconfig*.json"` → exactly these two) — every other `tests/integration/**` subdirectory (`activity`, `auth`, `queue`, `routes`, `servers`, `services`, `events`, `db`, `helpers`, ...) and all of `tests/unit/` and `scripts/*.mjs` have zero type-checking or lint coverage from either gate command. Independently corroborated by 05-41-SUMMARY.md's own "Issues Encountered" section, which records the identical finding from inside that plan's own session. **Listed here as a residual for backlog, not fixed by this gate** (per this gate's own no-fix rule). |
| O7 | One provenance run in 05-42 came back 50/52 due to registry flakiness; the rerun was 52/52 | **DID NOT REPRODUCE — clean on first try, no rerun needed** | Gate command 5 (Section 1): `node scripts/check-package-provenance.mjs` → `Coverage: 52/52 locked direct dependencies verified` on the **first** attempt in this session, exit 0, 29.3s. No flake observed; both outputs were not needed since there was only one (clean) run. |

## Requirements reconciliation

| Requirement | `REQUIREMENTS.md` status | This gate's evidence | Verdict |
|---|---|---|---|
| SERV-04 | Complete | No defect found against this requirement in any command or source read this session | Supports `Complete` |
| DETL-01 | Complete | Not independently re-walked end-to-end this session (out of this gate's own Section 2 scope, which is gap-6-specific); no contradicting evidence found in any gate command (unit/integration/E2E all pass on this surface) | Supports `Complete` |
| DETL-02 | Complete | Same as DETL-01 | Supports `Complete` |
| ACT-02 | Complete | No defect found; `pnpm test` (1525/1525) and `pnpm test:integration` (523/524, only the unrelated pre-existing stress skip) both green on this surface | Supports `Complete` |
| SET-01 | Complete | No defect found | Supports `Complete` |
| UI-01 | Complete | Both previously-disclosed AA-contrast residuals (Section 3, residuals 6-7) are now CLOSED by 05-45, confirmed by direct token/doc read and a green `contrast.test.ts` gate (36/36, per `docs/contrast-decision-05.md`'s own citation, not independently re-run as a standalone command by this gate but covered by `pnpm test`'s 1525/1525 which includes `contrast.test.ts`) | Supports `Complete`, now more strongly than before this round (the two named residuals are gone; O3's edge-contrast note is informational only, not a `contrast.test.ts` failure) |
| UI-02 | Complete | The contract-seam residual (Section 3, residual 5) is now CLOSED with one disclosed, in-scope, narrow limitation (service-level `VALIDATION_FAILED` bodies still lack an `issues` array by design, unrelated to this requirement's own text) | Supports `Complete` |
| DISC-02 | Complete | No defect found | Supports `Complete` |
| QA-04 | Pending | `git remote -v` (this session): empty output, exit 0. No GitHub Actions run — CI or nightly — has ever executed for this repository (confirmed via `docs/ci-readiness.md`, read in full this session, and the empty remote). Section 1's eleven gate commands are all **local** evidence; none constitutes "a real, single, scheduled execution" per the roadmap's own wording, cited in `docs/ci-readiness.md`. | **Must remain Pending.** Not closable from this sandbox. |
| QA-05 | Pending | Same reasoning as QA-04, plus: `pnpm security:scan-leaks` (gate command 11) passed locally (4/4 across both its vitest and Playwright portions) — necessary but not sufficient; the real CI `security` job (with its own runner, `permissions:`, and now-fixed `playwright install` step per `docs/ci-readiness.md`) has never executed for real. | **Must remain Pending.** Not closable from this sandbox. |

`git remote -v` re-run in this session: empty output, exit 0 — reconfirmed, not merely cited from
a prior report.

`.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`: none edited by this
plan (`git status --short .` throughout this session never showed any of the three as modified;
only `.planning/phases/05-ui-web/gate-logs/` and this document itself changed).

## Section 4 — Human verification (Task 3 checkpoint)

**Checkpoint reached:** 2026-09-21, per Task 3's `human-check` gate. The user was shown the seven
`how-to-verify` items verbatim, in order (05-46-PLAN.md lines 182-193), and exercised what they could
against a local dev stack the orchestrator brought up for them (web `:3000`, API `:3100`, a
disposable `linuxserver/openssh-server` container at `127.0.0.1:2222` whose host key the orchestrator
rotated by recreating it). The user sent screenshots of each step they tried.

**User's verbatim answer (Spanish):**

> "aprueba la ronda, y arregla el CR-01 y WR-01 por favor"

**Reading of that answer:** the round is **APPROVED**. The user additionally asked that CR-01 and
WR-01 (both named in `05-REVIEW.md`, see Section 5 below) be fixed. That fix is explicitly OUT OF
SCOPE for this plan — the orchestrator will run it as a separate `/gsd-quick` task immediately after
this plan closes. Approval of the round is not the same as saying the phase is complete: see the
closing read at the end of this section and Section 5's verdict.

The user did not enumerate which of the seven items they personally checked. What follows is what the
orchestrator directly observed in a live session with the user. Per this plan's own hard rule, nothing
below is upgraded from "not confirmed" to "passed" — an automated result, however strong, is never
substituted for a human statement.

### Item-by-item

| # | Item (05-46-PLAN.md wording, abbreviated) | Status | Evidence |
|---|---|---|---|
| 1 | Trust-new-fingerprint end to end on a real host-key change, plus the AUTH_FAILED-refusal negative case | **PARTIALLY VERIFIED BY THE USER** | See "Item 1 in detail" below |
| 2 | New contrast tokens on a real display, both themes | **NOT CONFIRMED** | All screenshots sent were dark mode; the user made no statement about link colour or the destructive-confirm button |
| 3 | A real CI run | **NOT DONE** | No git remote (`git remote -v` empty, reconfirmed in Section 1's git-state block); QA-04/QA-05 stay Pending |
| 4 | Live SSE walkthrough, not through a buffering tunnel | **NOT CONFIRMED** | Session was on plain `localhost` (no tunnel — the buffering-tunnel caveat itself no longer applies); the add → connect → discovery → detail path visibly worked in front of the user during item 1's walkthrough, but the user never stated whether updates appeared without a manual reload |
| 5 | Elevation (Sheet/Dialog/RowMenu) on a real display, screen reader, sub-1280px touch, reduced motion | **NOT CONFIRMED** | No statement from the user on any of these four sub-checks |
| 6 | Theme toggle on reload | **NOT CONFIRMED** | No statement from the user |
| 7 | Revoked session in a second tab | **NOT CONFIRMED** | No statement from the user |

### Item 1 in detail (the only item substantively exercised)

- **1a — add + connect:** Connected, first-trust notice with fingerprint and verification command
  shown, discovery 3 of 6 passed / 3 warnings, Alpine shown amber as outside the supported matrix,
  Docker-missing amber, no error banner. **PASS** (per the user's screenshot).
- **1b — after the host key was rotated:** status `Error`, `HOST_KEY_CHANGED` banner with `Trusted` =
  the old fingerprint and `Observed` = `SHA256:D1mD++HijMeRqaqSx5K7AN0lPTYZ0aaVZc1xKz9TbqY`, which is
  byte-identical to the fingerprint the orchestrator independently read inside the container with
  `ssh-keygen -lf`. Trust button present, no toolbar Retry/Connect. **PASS**.
- **1c — trust:** the user confirmed the dialog, a Connect button appeared, Connect returned the
  server to `Connected` with the new fingerprint. **PASS**. The user did not say whether the red
  banner stayed visible between trust and Connect — that is exactly WR-01's own symptom (Section 5).
  Recorded here as: **not observed by the user; confirmed independently in code** (`05-REVIEW.md`'s
  WR-01, unchanged by this plan).
- **1d — negative case (Trust refused after an unrelated AUTH_FAILED):** **NOT performed by hand.**
  It cannot be driven from the UI as currently built — no reconnect control exists in the
  host-key-changed state, and since 05-38 the domain clears the parked fingerprint before a Trust
  click is even possible. Covered only by
  `tests/integration/servers/trust-fingerprint-binding.test.ts` (11/11, re-run by the orchestrator as
  part of Section 1's gate, see `gate-logs/trust-fingerprint-binding-citation.log`). Recorded as:
  **not verified by a human; test-covered only.**
- **1e — CR-01 reproduction:** with a pending fingerprint parked, the user edited `Host` from
  `127.0.0.1` to `localhost`. Result on screen: status `Error`, the `HOST_KEY_CHANGED` banner still
  shown with "Trusted: not available" and "Observed: not available", no Trust button, **no
  Connect/Retry control anywhere** — the server is stuck from the UI. **CONFIRMED BY THE USER**,
  verbatim: "sigue en rojo, y aparece en not available sin el boton de trust sin boton de connect."
  This is a direct, hands-on human reproduction of `05-REVIEW.md`'s CR-01 finding (Section 5). The
  security half behaved correctly in the same reproduction: both fingerprints cleared, nothing
  promotable — the defect is a UI dead end, not a trust-boundary bypass.

### UX observations (backlog for a future UI redesign, not gaps of this round)

Drawn by the orchestrator from the user's screenshots during the item-1 walkthrough, not requested by
the user and not part of this round's scope:

- In the `Error` state, the first-trust notice and the red `HOST_KEY_CHANGED` banner both show a
  fingerprint panel at the same time.
- After a manual trust, the first-trust notice still reads "trusted this server's host key on first
  connection," which is no longer accurate once a second trust event has happened.
- "Trust new fingerprint" renders as plain text, giving low visual affordance to the most
  security-sensitive action on the page.

### Closing read of Section 4

The round is **APPROVED by the user**. Of the seven human-only items, only item 1 received any
hands-on exercise, and even that is partial: the negative AUTH_FAILED-refusal case remains test-only
(1d), and WR-01's banner-persistence-after-trust was not observed live (1c) — though it is
independently confirmed in code (Section 5). Items 2, 5, 6, 7 are not confirmed at all; item 3 was not
done (no remote exists, per policy); item 4 is not confirmed. Nothing above is recorded as passed
beyond what the user actually stated or visibly demonstrated on screen.

## Section 5 — Post-checkpoint events

Everything in this section happened after the Task 3 checkpoint was answered, as the orchestrator
acted on the user's approval and, separately, verified what the user's screenshots (item 1e) had just
surfaced. None of this reopens Sections 1-4 above; it is recorded here as an addendum because it
happened after this plan's own execution had otherwise finished, and because it bears directly on
whether the round can be called complete.

1. **E2E: the gate's one failure was a stale test, not a regression — fixed test-only.**
   `tests/e2e/host-key.spec.ts:429` (Section 1b) was verified first-hand by the orchestrator to be a
   stale assertion, not a product regression: only
   `expect(hostFingerprint).toBe(originalFingerprint)` failed, pinning exactly the pre-fix behavior
   05-40's GR-02 fix deliberately replaced. The planner had left that spec out of 05-40's
   `files_modified`. Fixed test-only, commit `23d8486`
   (`test(05-46): align UF-01 E2E with GR-02 trusted-fingerprint clear`). Full E2E rerun: 93/93
   (log: `gate-logs/test-e2e.rerun-after-uf01-test-fix.log`). `pnpm typecheck` green after it.

2. **Integration attempt 1's 469/524-failure diagnosis, independently corroborated.** Already recorded
   in full in Section 1a above: attempt 1 hit a Redis Testcontainer that went unreachable mid-run
   (`ECONNREFUSED` to a dynamic port), which cascaded through `assertNoStrayTestContainers` across 57
   files. The orchestrator read the raw log independently rather than trusting a paraphrase; attempt 2
   on the same, unmodified tree was clean (523 passed / 0 failed / 1 pre-existing skip). The
   environmental diagnosis is accepted on that evidence — no source change was made between the two
   attempts. No new action taken post-checkpoint beyond this confirmation; see Section 1a for the full
   root-cause chain and the log citations.

3. **Code review of the round-2 delta.** Commit `ffb41a9`
   (`.planning/phases/05-ui-web/05-REVIEW.md`; round 1's review archived as
   `05-REVIEW-gap-closure-round1.md`): 1 critical, 3 warnings, 1 info. GR-01, GR-02, GR-03, GR-05 are
   resolved; GR-04 (SSE idle-peer budget) is untouched by this round (`apps/control-plane/src/routes/
   events.ts` has zero diff, per the review's own citation). **CR-01** (a UI dead end after an
   identity edit while in `ERROR`/`HOST_KEY_CHANGED`: `editServer` leaves `status`/`lastErrorCode`
   untouched, `derivePrimaryAction` returns `null` for that exact combination, and "Re-run discovery"
   is disabled outside `CONNECTED`) is confirmed in code by the orchestrator **and** reproduced by the
   user by hand (item 1e above). It predates round 2 (it is round 1's own WR-A-02 residual, newly
   reachable once GR-01/GR-02 landed), fails closed (no trust-boundary bypass), and is UX-blocking, not
   a security defect. **WR-01** (`trustFingerprint`'s successful promote never clears `lastErrorCode`,
   so `deriveDetailState` — which checks `lastErrorCode` before `status` — keeps rendering the
   `HOST_KEY_CHANGED` banner after a correct trust, until the next connect attempt) is confirmed in
   code; the user did not observe it live but asked for it to be fixed anyway. **Both CR-01 and WR-01
   are OPEN at the time of this SUMMARY**, and fixing them is the reason this round's closure cannot be
   read as "phase complete" — the user's own instruction sends both to a follow-up `/gsd-quick` task,
   not to this plan.

4. **Local-run friction found while bringing the stack up for the user (backlog, docs/onboarding, not
   a gap of this round).** Neither app loads `.env` on its own; `NOODARA_API_ORIGIN` was missing from
   the user's `.env`; the API's default `PORT` (3000) collides with the web dev server, so `PORT=3100`
   is required; over plain `http://localhost` the session cookie is dropped unless
   `NOODARA_COOKIE_INSECURE=true`, and the UI gives no hint when this happens (login returns 200, the
   page silently bounces back to `/login`). The default `POSTGRES_PORT` 5433 also collided with an
   unrelated container already running on the user's machine.

### Overall verdict for this closing gate

- Gap 6: **CLOSED**, re-derived from current source (Section 2), no path found that promotes a stale
  or wrong-host fingerprint.
- GR-03: **PARTIAL** (production wiring correct; optional `logger` leaves no regression guard —
  Section 3, residual 1 / O2).
- F1: **PARTIAL, by design** — matches 05-42's own stated scope, not a defect (Section 3, residual 3).
- CR-01 and WR-01: **OPEN**, confirmed in code and (CR-01) reproduced by the user; fix scheduled as a
  separate `/gsd-quick` task at the user's explicit request, not part of this plan.
- Human items: 1 partially verified; 2, 5, 6, 7 not confirmed; 3 not done; 4 not confirmed.
- The round is **APPROVED by the user**. **The phase is NOT complete** while CR-01 and WR-01 remain
  open and five of seven human items remain unconfirmed.
