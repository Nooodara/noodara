---
phase: 05-ui-web
plan: 22
type: execute
wave: 4
depends_on: [05-06]
files_modified:
  - packages/ui/src/tone.ts
  - packages/ui/src/tone.test.ts
  - packages/ui/src/cn.ts
  - packages/ui/src/cn.test.ts
  - packages/ui/src/Button.tsx
  - packages/ui/src/Button.test.tsx
  - packages/ui/src/StatusPill.tsx
  - packages/ui/src/StatusPill.test.tsx
  - packages/ui/src/index.ts
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Button renders all four skill variants, and every variant has a component test asserting its rendered class/attribute contract — no variant ships without automated behavioural coverage"
    - "Button's loading state keeps its label text, sets disabled and sets aria-busy, and never renders a spinner (skill §5) — asserted in a component test, not by grep"
    - "StatusPill renders a dot plus its status word for all six ServerStatus values, so state is never colour-only — asserted by reading the rendered text for each of the six values"
    - "serverStatusTone is declared satisfies Record<ServerStatus, Tone> so a new domain status is a compile error, and every mapping is unit-tested"
    - "No component in packages/ui contains a hex colour, an rgb() literal or a px font-size — only var(--token) or a Tailwind class backed by one"
  artifacts:
    - path: "packages/ui/src/tone.ts"
      provides: "pure ServerStatus/DiscoveryCheckStatus to semantic-token mapping with its status word"
      exports: ["serverStatusTone", "discoveryCheckTone", "STATUS_WORDS"]
    - path: "packages/ui/src/Button.test.tsx"
      provides: "the RED gate for every Button variant and state"
      contains: "aria-busy"
    - path: "packages/ui/src/StatusPill.test.tsx"
      provides: "the RED gate proving the status word is in the DOM for all six statuses"
      contains: "STATUS_WORDS"
    - path: "packages/ui/src/index.ts"
      provides: "the barrel, now exporting the first components"
      exports: ["Button", "StatusPill", "cn", "serverStatusTone", "discoveryCheckTone", "STATUS_WORDS"]
  key_links:
    - from: "packages/ui/src/StatusPill.tsx"
      to: "packages/ui/src/tone.ts"
      via: "serverStatusTone and STATUS_WORDS"
      pattern: "serverStatusTone"
    - from: "packages/ui/src/Button.test.tsx"
      to: "packages/ui/src/testing/render.tsx"
      via: "renderUi and userEvent from the harness built in Plan 05-06"
      pattern: "renderUi"
---

<objective>
Build the first two components of the design system — `Button` and `StatusPill` — plus the two pure helpers they rest on, every one of them test-first.

Purpose: these two components set the pattern every later component follows: token-only styling, state never conveyed by colour alone, and a colocated component test written before the component (ADR-0005, CLAUDE.md §2.1). `Button` in particular has four variants and four states, all of which need real behavioural coverage rather than a source grep.
Output: `tone`, `cn`, `Button` and `StatusPill`, each with a colocated test seen to fail first, exported from the barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-web/05-UI-SPEC.md
@.planning/phases/05-ui-web/05-CONTEXT.md
@.planning/phases/05-ui-web/05-06-SUMMARY.md
@.claude/skills/noodara-ux-apple/SKILL.md
@.claude/skills/noodara-tdd/SKILL.md
@docs/adr/0005-ui-package-and-component-testing.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tone and cn — the two pure helpers</name>
  <files>packages/ui/src/tone.ts, packages/ui/src/tone.test.ts, packages/ui/src/cn.ts, packages/ui/src/cn.test.ts, packages/ui/src/index.ts</files>
  <read_first>
    - packages/domain/src/server/server-state.ts (the six `ServerStatus` values — import the type, never re-type the union)
    - packages/domain/src/discovery/types.ts (`DiscoveryCheckStatus`, needed by the tone map used later by Plan 05-18)
    - .claude/skills/noodara-ux-apple/SKILL.md (§2.1's four semantic status colours and their `-soft` forms)
    - .planning/phases/05-ui-web/05-UI-SPEC.md (§8 Accessibility's "state never colour-only" rule — the reason `STATUS_WORDS` exists at all)
    - .planning/phases/05-ui-web/05-CONTEXT.md (D-08 — the warning reclassification is applied by the caller in Plan 05-18, not here)
  </read_first>
  <behavior>
    - `serverStatusTone('CONNECTED')` is `'ok'`; `'CONNECTING'` is `'warn'`; `'ERROR'` and `'UNREACHABLE'` are `'error'`; `'PENDING'` and `'DISCONNECTED'` are `'idle'` — asserted for all six values by iterating the imported status list, not a local array.
    - `discoveryCheckTone` maps `pass` → ok, `fail` → error, `skipped` and `not_applicable` → idle, asserted for all four values.
    - `STATUS_WORDS` provides a non-empty human word for every `ServerStatus` and for every tone, asserted exhaustively, so no caller can render colour alone.
    - `cn('a', false && 'b', undefined, 'c')` is `'a c'`; it drops `false`, `null` and `undefined`, trims, and collapses duplicate whitespace.
    - `cn()` with no arguments returns an empty string.
  </behavior>
  <action>
    **RED first:** write `tone.test.ts` and `cn.test.ts` and confirm both fail because the modules do not exist. Record the failure in the SUMMARY.

    Create `packages/ui/src/tone.ts` exporting a `Tone` union (`'ok' | 'warn' | 'error' | 'idle'`), `serverStatusTone(status: ServerStatus): Tone` backed by a frozen object literal declared `as const satisfies Record<ServerStatus, Tone>`, `discoveryCheckTone` for the four `DiscoveryCheckStatus` values, and `STATUS_WORDS` giving the visible word for each `ServerStatus` and each tone. Import `ServerStatus` from `@noodara/domain/server` and `DiscoveryCheckStatus` from `@noodara/domain/discovery` — never re-declare either union.

    Create `packages/ui/src/cn.ts` with a ~10-line `cn(...parts: (string | false | null | undefined)[]): string` helper. This is the deliberate hand-rolled alternative to adding `clsx` (05-RESEARCH.md's Supporting table leaves the choice open); state that in a one-line comment.

    Append `cn`, `discoveryCheckTone`, `serverStatusTone`, `STATUS_WORDS` and the `Tone` type to `src/index.ts`, alphabetically.
  </action>
  <verify>
    <automated>pnpm test packages/ui/src/tone.test.ts packages/ui/src/cn.test.ts && pnpm build && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test packages/ui/src/tone.test.ts packages/ui/src/cn.test.ts` exits 0 with all five behaviour groups asserted
    - Behavior: the completeness assertions iterate the status/check-status lists imported from `@noodara/domain`, not a locally declared array (asserted)
    - `packages/ui/src/tone.ts` contains `satisfies Record<ServerStatus` and imports from `@noodara/domain/server`
    - `pnpm build && pnpm typecheck` exit 0
    - The SUMMARY records the RED failure for both test files
  </acceptance_criteria>
  <done>Status-to-tone mapping and class composition are tested pure functions that cannot drift from the domain package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button — four variants, four states, all asserted</name>
  <files>packages/ui/src/Button.tsx, packages/ui/src/Button.test.tsx, packages/ui/src/index.ts</files>
  <read_first>
    - .claude/skills/noodara-ux-apple/SKILL.md (§4.1 buttons: four variants, 32px height, `scale(0.97)` active, focus outline; §5's ban on full-surface spinners)
    - .planning/phases/05-ui-web/05-UI-SPEC.md (Component Inventory row for `Button`; the Color section's accent reserved-for list)
    - packages/ui/src/testing/render.tsx (`renderUi`, `userEvent`, `screen` — the only permitted test entry point)
    - packages/ui/src/cn.ts and packages/ui/tokens.css (the class helper and the only source of colour/size values)
    - packages/config/tsconfig.base.json (`exactOptionalPropertyTypes: true` — an optional prop must be omitted, never passed as `undefined`, which changes how default props are written)
  </read_first>
  <behavior>
    - Each of the four variants (`primary`, `secondary`, `ghost`, `destructive`) renders a `<button>` reachable by `getByRole('button', { name })` and carries a distinguishing `data-variant` attribute equal to that variant — so every variant, including `secondary`, has automated coverage.
    - The default variant when none is passed is `primary`, asserted via `data-variant`.
    - `loading` keeps the label text exactly as passed (assert the accessible name is unchanged), sets `aria-busy="true"`, and sets `disabled`.
    - While `loading`, `userEvent.click` does not invoke `onClick` — asserted with a spy, which is the behavioural proof that `disabled` is really applied rather than just styled.
    - `disabled` alone also blocks `onClick` and does not set `aria-busy`.
    - A `destructive` button without `filled` carries `data-filled="false"`; with `filled` it carries `data-filled="true"` — the skill restricts the filled form to confirm dialogs, so the distinction must be observable.
    - An enabled button invokes `onClick` exactly once per `userEvent.click`.
    - No rendered output contains an element matching `[class*="animate-spin"]` and no `role="progressbar"` exists in the loading state (the ban on spinners is asserted, not assumed).
  </behavior>
  <action>
    **RED first:** write `packages/ui/src/Button.test.tsx` covering all eight behaviours using `renderUi` and `userEvent` from `packages/ui/src/testing/render.tsx`. Run `pnpm test --project dom` and confirm it fails because `Button.tsx` does not exist. Record the failure in the SUMMARY.

    Then create `packages/ui/src/Button.tsx`: a forwardRef-free function component accepting `variant` (`primary` | `secondary` | `ghost` | `destructive`), `loading`, `filled`, plus the native button props. Primary fills `--accent` with `--on-accent` text; `destructive` is ghost-styled by default and only filled `--status-error` when `filled` is set. Height 32px, radius `--r-sm`, active transform `scale(0.97)`, focus `outline: 2px solid var(--accent)` with `outline-offset: 2px`, disabled reduced opacity with no pointer events. `loading` keeps the label text unchanged and sets `disabled` plus `aria-busy` — never a spinner. Emit `data-variant` and `data-filled` attributes so both the component tests and Playwright can assert state without depending on colour. Every class name must resolve to a token — no hex, no `rgb()`, no literal `font-size` in px.

    Note for the executor: `scale(0.97)`, the focus outline and the reduced opacity are CSS-only effects that jsdom does not compute. They are verified by Playwright and by the `noodara-ux-review` audit in Plan 05-21, per ADR-0005 — do not write a component test that asserts a computed style, and do not drop the properties because a unit test cannot see them.

    Append `Button` to `src/index.ts` alphabetically.
  </action>
  <verify>
    <automated>pnpm test packages/ui/src/Button.test.tsx && pnpm build && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test packages/ui/src/Button.test.tsx` exits 0 with all eight behaviours asserted, including one assertion per variant
    - Behavior: clicking a `loading` Button does not invoke `onClick` (asserted with a spy)
    - Behavior: the `loading` render's accessible name equals the non-loading render's accessible name (asserted)
    - `packages/ui/src/Button.tsx` contains `aria-busy`, `scale(0.97)` and `data-variant`
    - `grep -rcE "#[0-9a-fA-F]{3,8}|rgb\(" packages/ui/src/Button.tsx` is 0
    - `pnpm build && pnpm lint && pnpm typecheck` exit 0
    - The SUMMARY records the RED failure message
  </acceptance_criteria>
  <done>Every Button variant and state has a behavioural test, and the spinner ban is machine-checked rather than assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StatusPill — never colour-only, for all six statuses</name>
  <files>packages/ui/src/StatusPill.tsx, packages/ui/src/StatusPill.test.tsx, packages/ui/src/index.ts</files>
  <read_first>
    - .claude/skills/noodara-ux-apple/SKILL.md (§4.2 status pill: 6px dot plus label, `-soft` background, pill radius, pulse for the in-progress state; §2.4 motion and `prefers-reduced-motion`)
    - .planning/phases/05-ui-web/05-UI-SPEC.md (Component Inventory row for `StatusPill`; §8 Accessibility's "state never colour-only" rule; §9's `data-testid` convention)
    - packages/ui/src/tone.ts (created in Task 1 — `serverStatusTone` and `STATUS_WORDS`, the only permitted source of the pill's tone and word)
    - packages/domain/src/server/server-state.ts (the six `ServerStatus` values)
    - packages/ui/src/testing/render.tsx
  </read_first>
  <behavior>
    - For each of the six `ServerStatus` values — iterated from the imported status list, not a local array — the rendered pill's text content contains the corresponding `STATUS_WORDS` entry. This is the one assertion that makes "state is never colour-only" true rather than aspirational.
    - Each rendered pill carries `data-testid="status-pill"` and a `data-status` attribute equal to the raw status, so Playwright can assert state without depending on colour.
    - Each rendered pill carries a `data-tone` attribute equal to `serverStatusTone(status)`, so the tone mapping is observable from the DOM.
    - The dot element is present and is marked `aria-hidden` — the word, not the dot, carries the meaning for assistive technology.
    - `CONNECTING` renders with `data-pulsing="true"` and every other status with `data-pulsing="false"`, so the in-progress treatment is observable without computing an animation.
    - The pill exposes no `role="status"` or `aria-live` of its own — announcements are owned by the discovery section's live region in Plan 05-18, and two live regions would double-announce.
  </behavior>
  <action>
    **RED first:** write `packages/ui/src/StatusPill.test.tsx` covering all six behaviours, driving the per-status assertions with `describe.each` / a loop over the imported status list. Run `pnpm test --project dom` and confirm it fails. Record the failure in the SUMMARY.

    Then create `packages/ui/src/StatusPill.tsx`: takes a `ServerStatus`, renders a 6px `aria-hidden` dot plus the word from `STATUS_WORDS` inside a `--r-pill` chip using the tone's `-soft` background and full-saturation text token, with `--text-caption` sizing. Emit `data-testid="status-pill"`, `data-status`, `data-tone` and `data-pulsing`. Gate the CONNECTING pulse behind `@media (prefers-reduced-motion: reduce)` so it degrades to a static dot. Every class name must resolve to a token — no hex, no `rgb()`, no literal `font-size` in px.

    Append `StatusPill` to `src/index.ts` alphabetically.
  </action>
  <verify>
    <automated>pnpm test packages/ui/src/StatusPill.test.tsx && pnpm build && pnpm lint && pnpm typecheck && test $(grep -rcE "#[0-9a-fA-F]{3,8}|rgb\(" packages/ui/src --include="*.tsx" --include="*.ts" | awk -F: '{s+=$2} END {print s+0}') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test packages/ui/src/StatusPill.test.tsx` exits 0 with six assertions driven by the six imported `ServerStatus` values
    - Behavior: for every status, the rendered text contains the `STATUS_WORDS` entry (asserted in a loop over the imported list, so a seventh domain status fails the test)
    - `packages/ui/src/StatusPill.tsx` contains `STATUS_WORDS`, `data-status`, `data-tone` and `prefers-reduced-motion`
    - `grep -cE "aria-live|role=\"status\"" packages/ui/src/StatusPill.tsx` is 0
    - `grep -rcE "#[0-9a-fA-F]{3,8}|rgb\(" packages/ui/src` totals 0
    - `packages/ui/src/index.ts` exports `Button`, `StatusPill`, `cn`, `serverStatusTone`, `discoveryCheckTone` and `STATUS_WORDS`
    - `pnpm build && pnpm lint && pnpm typecheck` exit 0
  </acceptance_criteria>
  <done>The status pill proves, for all six statuses, that the status word is in the DOM — colour is never the only signal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design system → every future screen | A component shortcut here becomes a systemic accessibility pattern everywhere |
| caller-supplied label strings → rendered DOM | Button labels and status words are the first strings this package renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-23 | Information Disclosure | components rendering untrusted strings | mitigate | No `dangerouslySetInnerHTML` in either component; React's escaping is the control and Plan 05-21's grep gate keeps it |
| T-5-25 | Accessibility failure | StatusPill light-mode contrast | accept | Reproduced from the locked skill (Plan 05-06's `tokens.css` comment) and escalated to the user at Plan 05-21's checkpoint; mitigated in the meantime by the status *word* always being present, which the component test asserts for all six statuses |
| T-5-27 | Spoofing | a disabled/loading Button that is styled but not actually disabled | mitigate | The component test clicks a `loading` Button with a spy and asserts the handler never fires — a styling-only implementation fails the test |
| T-5-SC | Tampering | npm installs | mitigate | No new packages: react, react-dom and the test stack were installed and provenance-checked in Plan 05-06 |
</threat_model>

<verification>
- `pnpm test packages/ui/src` green (both the `packages` and `dom` projects)
- `pnpm build && pnpm lint && pnpm typecheck && pnpm boundaries` green
- Zero hex or `rgb(` literals anywhere in `packages/ui/src`
- The SUMMARY records a RED observation for each of the four test files

</verification>

<success_criteria>
- All four Button variants and all six StatusPill statuses have automated behavioural coverage
- State is never conveyed by colour alone, and that is proven by a test rather than by review
- Both components are token-only and both pure helpers are tested against the domain package's own unions
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-22-SUMMARY.md` when done, including the RED observations. Commit with a Conventional Commit subject in English, ≤72 chars, and no attribution trailers.
</output>
