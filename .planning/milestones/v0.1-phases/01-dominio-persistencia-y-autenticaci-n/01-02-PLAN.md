---
phase: 01-dominio-persistencia-y-autenticacion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .nvmrc
  - .gitignore
  - .prettierignore
  - packages/config/package.json
  - packages/config/tsconfig.base.json
  - packages/config/eslint.config.js
  - packages/config/prettier.config.js
  - vitest.config.ts
  - vitest.integration.config.ts
  - tests/unit/harness.test.ts
  - packages/domain/package.json
  - packages/domain/tsconfig.json
  - packages/domain/src/index.ts
  - packages/domain/src/server/index.ts
  - packages/domain/src/server/server-state.ts
  - packages/domain/src/server/connection-result.ts
  - packages/domain/src/security/index.ts
  - packages/domain/src/security/secret-value.ts
  - packages/domain/src/security/redactor.ts
  - packages/domain/src/security/envelope.ts
  - packages/domain/src/security/setup-token.ts
  - packages/domain/src/security/login-backoff.ts
  - packages/domain/src/validators/index.ts
  - packages/domain/src/validators/network.ts
  - packages/domain/src/validators/identity.ts
  - packages/domain/src/validators/password.ts
  - packages/domain/src/activity/index.ts
  - packages/domain/src/activity/activity-event.ts
  - packages/domain/src/purity.test.ts
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "`pnpm install`, `pnpm lint`, `pnpm typecheck` and `pnpm test` all exit 0 on the fresh monorepo, making CLAUDE.md section 3.2's target commands real"
    - "A test in packages/domain that drops below 95% statement or branch coverage fails the run (QA-02 gate exists before any domain code is written)"
    - "packages/domain cannot import an app or an I/O package — the boundary is machine-enforced, not a convention (RESEARCH Pitfall 4)"
    - "Every domain module that later plans in this phase will implement already exists as an exported stub with a stable import path, so wave-3 plans never edit a shared barrel (per CONTEXT.md Integration Points: stable index exports for packages/ssh, services and routes to consume)"
    - "A hardcoded string fallback for an env var (`process.env.X ?? \"literal\"`) fails lint — RESEARCH Pitfall 1 / PITFALLS.md #1 is blocked at the toolchain level"
  artifacts:
    - path: "turbo.json"
      provides: "Task graph plus boundaries rules isolating packages/domain"
      contains: "boundaries"
    - path: "vitest.config.ts"
      provides: "Vitest projects config with v8 coverage and the QA-02 threshold"
      contains: "packages/domain"
    - path: "packages/domain/src/index.ts"
      provides: "Stable barrel re-exporting server, security, validators and activity areas"
      contains: "export *"
    - path: "packages/config/eslint.config.js"
      provides: "Shared flat ESLint config including the env-fallback ban"
      contains: "no-restricted-syntax"
  key_links:
    - from: "vitest.config.ts"
      to: "packages/domain/**"
      via: "coverage.thresholds glob entry"
      pattern: "statements"
    - from: "packages/domain/package.json"
      to: "turbo.json boundaries"
      via: "package tag"
      pattern: "tags"
---

<objective>
Turn an empty repo into a working pnpm + Turborepo monorepo whose commands match CLAUDE.md section 3.2 exactly, with the QA-02 coverage gate and the domain purity boundary in place *before* any behavior is written.

Purpose: everything else in this phase depends on `pnpm test` being a real, fast, failing-when-it-should feedback loop. This plan also front-loads the module skeleton of `packages/domain` so the four wave-3 plans can run in parallel without ever touching the same barrel file.
Output: root workspace config, `packages/config` (tsconfig/eslint/prettier), Vitest unit + integration configs, and a fully stubbed `packages/domain`.
</objective>

<execution_note>
This plan touches 31 files, but almost all of them are one-line stubs or small config files: the `packages/domain` module skeleton is intentionally inert (each file exports a placeholder that the wave-3 plans replace), and the workspace/tsconfig/eslint/prettier/vitest files are configuration, not logic.

Create them in a single pass with the content specified in the tasks below. Do not read a stub back after writing it, and do not open the config files individually to "confirm" them — the task-level `<verify>` commands are the confirmation. Reading 31 files back would consume the context budget this plan needs for the coverage gate and the domain-purity boundary.
</execution_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-CONTEXT.md
@.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace, shared config package, Turborepo boundaries</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, .nvmrc, .gitignore, .prettierignore, packages/config/package.json, packages/config/tsconfig.base.json, packages/config/eslint.config.js, packages/config/prettier.config.js</files>
  <read_first>
    - CLAUDE.md (section 3.1 repo structure, section 3.2 command table — these command names are the contract)
    - .gitignore (current content; `CLAUDE.md` and `.claude/` are deliberately ignored and must stay ignored)
    - .planning/research/STACK.md (sections "Development Tools" and "Version Compatibility" — TypeScript 6.0.3, typescript-eslint 10.x, ESLint 9.x flat config, pnpm 10.x, Turborepo 2.x, Node 22 LTS)
    - .planning/research/PITFALLS.md (Pitfall 1, hardcoded secret fallback)
  </read_first>
  <action>
    Create the root workspace. Root `package.json`: `"name": "noodara"`, `"private": true`, `"type": "module"`, `"engines": { "node": ">=22.12.0" }`, and `"packageManager"` pinned to an exact pnpm 10.x string — obtain it with `npm view pnpm@10 version` and pin that exact value (do not invent a version, do not use a range). Scripts, named exactly as CLAUDE.md section 3.2 requires: `dev` (`turbo run dev`), `build`, `lint` (`turbo run lint`), `typecheck` (`turbo run typecheck`), `test` (`vitest run`), `test:integration` (`vitest run --config vitest.integration.config.ts`), `test:e2e` (placeholder that exits 0 with a message until phase 5), `db:migrate` (`pnpm --filter @noodara/control-plane db:migrate` — the filter target is created in Plan 03/07; until then this script may fail, which is expected and documented in the SUMMARY).

    `pnpm-workspace.yaml`: `packages:` entries `apps/*` and `packages/*`.

    `turbo.json`: schema-versioned, with tasks `build` (dependsOn `^build`, outputs `dist/**`), `lint`, `typecheck` (dependsOn `^build`), `test`, `test:integration`. Enable Turborepo boundaries: tag `packages/domain` as `pure-domain` (via the `turbo.tags` field in that package's `package.json`, added in Task 3) and add a `boundaries.tags` rule denying `pure-domain` any dependency on workspace packages or apps. Add root script `boundaries` running `turbo boundaries`.

    `.nvmrc`: `22`.

    Append to `.gitignore`: `node_modules/`, `dist/`, `.turbo/`, `coverage/`, `.env`, `.env.*` with a `!.env.example` negation. Do not remove the existing `CLAUDE.md` / `.claude/` entries.

    `packages/config` is a private workspace package named `@noodara/config` exporting three files:
    - `tsconfig.base.json`: `"strict": true`, `"noUncheckedIndexedAccess": true`, `"exactOptionalPropertyTypes": true`, `"noImplicitOverride": true`, `"module": "nodenext"`, `"moduleResolution": "nodenext"`, `"target": "es2023"`, `"verbatimModuleSyntax": true`, `"declaration": true`, `"isolatedModules": true`.
    - `eslint.config.js`: ESLint 9 flat config composing `typescript-eslint`'s `strictTypeChecked` and `stylisticTypeChecked` presets with type-aware parsing. Add a `no-restricted-syntax` rule that rejects any `LogicalExpression` (`??`, `||`) whose left side is a `process.env.*` member expression and whose right side is a `Literal` — message: `Hardcoded env fallbacks are forbidden (PITFALLS.md #1). Validate in src/env.ts instead.` Also add `no-restricted-imports` forbidding deep imports into `packages/domain/src`.
    - `prettier.config.js`: single quotes, trailing commas, 100 print width, semicolons.

    Install exactly: `typescript@6.0.3`, `eslint@9`, `typescript-eslint@10`, `prettier`, `turbo@2` at the root as devDependencies. Do not install `typescript@7` (typescript-eslint peer range excludes it — STACK.md "What NOT to Use").
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false && pnpm lint && pnpm typecheck && pnpm exec turbo boundaries</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and creates `pnpm-lock.yaml`
    - `node -e "const p=require('./package.json');process.exit(/^pnpm@10\.\d+\.\d+$/.test(p.packageManager)?0:1)"` exits 0
    - `node -e "const p=require('./package.json');const s=p.scripts;process.exit(['dev','test','test:integration','lint','typecheck','db:migrate'].every(k=>k in s)?0:1)"` exits 0
    - `pnpm lint` exits 0 and `pnpm typecheck` exits 0
    - `pnpm exec turbo boundaries` exits 0
    - `grep -v '^#' packages/config/eslint.config.js | grep -c "no-restricted-syntax"` returns at least 1
    - Writing a temporary file containing `const k = process.env.NOODARA_MASTER_KEY ?? 'dev-default';` inside `packages/config` and running `pnpm lint` exits non-zero; the temporary file is deleted before the task completes
    - `grep -c 'CLAUDE.md' .gitignore` returns at least 1 (the deliberate ignore is preserved)
    - `node -e "process.exit(require('./package.json').devDependencies.typescript==='6.0.3'?0:1)"` exits 0
  </acceptance_criteria>
  <done>`pnpm install`, `pnpm lint`, `pnpm typecheck` and `turbo boundaries` all pass on an otherwise empty monorepo, and an env fallback literal is a lint error.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest harness with the QA-02 coverage gate and the integration project</name>
  <files>vitest.config.ts, vitest.integration.config.ts, tests/unit/harness.test.ts, package.json</files>
  <read_first>
    - package.json (created in Task 1 — the `test` / `test:integration` scripts must line up with these config filenames)
    - .planning/phases/01-dominio-persistencia-y-autenticaci-n/01-RESEARCH.md (sections "## Validation Architecture" and "## State of the Art" — Vitest uses `projects`, not the deprecated `workspace` key, and global coverage thresholds merge into one CoverageMap so per-package enforcement must use glob-pattern thresholds)
    - .claude/skills/noodara-tdd/SKILL.md (section 7, coverage table)
  </read_first>
  <action>
    Install `vitest@5.0.0` and `@vitest/coverage-v8` (matching major) at the root as devDependencies.

    `vitest.config.ts`: use the `projects` key (NOT `workspace` — deprecated since Vitest 3.2). Define one project per workspace package discovered under `packages/*` and `apps/*`, with `include` limited to `src/**/*.test.ts` so integration tests never run in the unit pass, and `exclude` covering `tests/integration/**`. Set `test.coverage.provider` to `'v8'`, `reporter` to `['text','json-summary','lcov']`, `reportsOnly`/`all` so untested domain files count against the threshold, and `coverage.thresholds` using the glob-pattern form with key `'packages/domain/**'` set to `statements: 95` and `branches: 95` (QA-02). Do not set a global threshold for other packages (STACK/TDD skill: report only, no hard gate in v0.1).

    `vitest.integration.config.ts`: separate config with `include: ['tests/integration/**/*.test.ts']`, `testTimeout: 120000`, `hookTimeout: 120000`, `pool: 'forks'`, `fileParallelism: false` (Testcontainers containers are heavy and must not race), no coverage. Do not add any watch flag anywhere.

    `tests/unit/harness.test.ts`: two assertions proving the harness works — one that passes, and one that is written so that inverting its expectation makes the run fail (this is the RED-loop smoke check the noodara-tdd skill requires before any real TDD starts). Add a third assertion that reads `vitest.config.ts` from disk and asserts the string `packages/domain` appears inside a `thresholds` block, so the QA-02 gate cannot be silently deleted later.
  </action>
  <verify>
    <automated>pnpm test && pnpm exec vitest run --coverage 2>&1 | grep -q "packages/domain"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 and reports at least 3 passing assertions
    - `grep -v '^//' vitest.config.ts | grep -c "projects"` returns at least 1 and `grep -c "workspace" vitest.config.ts` returns 0
    - `node -e "const s=require('fs').readFileSync('vitest.config.ts','utf8');process.exit(s.includes('95')&&s.includes('packages/domain')?0:1)"` exits 0
    - `grep -c "watch" vitest.config.ts vitest.integration.config.ts` returns 0 for both files
    - `node -e "const s=require('fs').readFileSync('vitest.integration.config.ts','utf8');process.exit(s.includes('tests/integration')?0:1)"` exits 0
    - `pnpm test:integration` exits 0 (zero integration tests exist yet; configure `passWithNoTests: true` in the integration config only, never in the unit config)
  </acceptance_criteria>
  <done>`pnpm test` runs unit tests with a v8 coverage report and a 95%/95% threshold scoped to `packages/domain`; `pnpm test:integration` runs a separate, Docker-aware project.</done>
</task>

<task type="auto">
  <name>Task 3: packages/domain skeleton — stable module contracts for the whole phase</name>
  <files>packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/index.ts, packages/domain/src/server/index.ts, packages/domain/src/server/server-state.ts, packages/domain/src/server/connection-result.ts, packages/domain/src/security/index.ts, packages/domain/src/security/secret-value.ts, packages/domain/src/security/redactor.ts, packages/domain/src/security/envelope.ts, packages/domain/src/security/setup-token.ts, packages/domain/src/security/login-backoff.ts, packages/domain/src/validators/index.ts, packages/domain/src/validators/network.ts, packages/domain/src/validators/identity.ts, packages/domain/src/validators/password.ts, packages/domain/src/activity/index.ts, packages/domain/src/activity/activity-event.ts, packages/domain/src/purity.test.ts</files>
  <read_first>
    - CLAUDE.md (section 3.1 — `packages/domain` imports nothing with I/O)
    - .claude/skills/noodara-domain-model/SKILL.md (sections 1, 2, 7, 10)
    - .planning/phases/01-dominio-persistencia-y-autenticaci-n/01-RESEARCH.md (section "### Recommended Project Structure" — the exact file layout this task must materialize, and "Pitfall 4")
    - .planning/phases/01-dominio-persistencia-y-autenticaci-n/01-CONTEXT.md (section "Integration Points")
    - packages/config/tsconfig.base.json (created in Task 1)
  </read_first>
  <action>
    Create `packages/domain` as `@noodara/domain`: `"type": "module"`, `"private": true`, a `turbo.tags` array containing `pure-domain` (consumed by the boundaries rule from Task 1), a single runtime dependency on `zod@4.6.1`, and an `exports` map with four subpath entries plus the root: `"."`, `"./server"`, `"./security"`, `"./validators"`, `"./activity"`. Package scripts: `lint`, `typecheck` (`tsc --noEmit`), `test`.

    `tsconfig.json` extends `@noodara/config/tsconfig.base.json`.

    Create every module file this phase will fill, as an explicitly empty module (`export {};`) with a one-line comment naming the plan that implements it. This is the interface-contract step: the barrels below are written ONCE, here, and no later plan in this phase edits them.
    - `src/server/index.ts` re-exports `./server-state.js` and `./connection-result.js` (implemented by Plan 04).
    - `src/security/index.ts` re-exports `./secret-value.js`, `./redactor.js`, `./envelope.js` (Plan 05), `./setup-token.js` (Plan 12) and `./login-backoff.js` (Plan 13).
    - `src/validators/index.ts` re-exports `./network.js`, `./identity.js`, `./password.js` (Plan 06).
    - `src/activity/index.ts` re-exports `./activity-event.js` (Plan 06).
    - `src/index.ts` re-exports all four area barrels.

    `src/purity.test.ts` is the machine-enforced purity guard (RESEARCH Pitfall 4). It must: (a) read `packages/domain/package.json` and assert `dependencies` contains only `zod`; (b) walk every `.ts` file under `packages/domain/src` and assert none contains an import from `node:fs`, `node:net`, `node:http`, `node:child_process`, `node:dns`, `pg`, `drizzle-orm`, `better-auth`, `fastify`, `ioredis`, or any `@noodara/` package other than itself. Explicitly allow `node:crypto` — per RESEARCH Pitfall 4 it is pure computation and `envelope.ts` needs it.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm exec turbo boundaries</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./packages/domain/package.json');process.exit(Object.keys(p.dependencies||{}).join()==='zod'?0:1)"` exits 0
    - `node --input-type=module -e "await import('./packages/domain/src/index.ts')"` is not required, but `pnpm typecheck` exits 0 with all barrels resolving
    - All 12 stub module files exist: `test $(ls packages/domain/src/server/*.ts packages/domain/src/security/*.ts packages/domain/src/validators/*.ts packages/domain/src/activity/*.ts | wc -l) -eq 14` (11 stubs + 4 barrels minus none — verify the exact count against the file list in `<files>` and adjust the assertion to the real number before committing)
    - `packages/domain/src/purity.test.ts` passes, and temporarily adding `import { readFileSync } from 'node:fs';` to `packages/domain/src/security/envelope.ts` makes `pnpm test` exit non-zero; the temporary import is removed before the task completes
    - `node -e "const p=require('./packages/domain/package.json');process.exit((p.turbo?.tags||[]).includes('pure-domain')?0:1)"` exits 0
    - `grep -c "export \*" packages/domain/src/index.ts` returns 4
  </acceptance_criteria>
  <done>`packages/domain` compiles, exports four stable subpaths, has zero I/O dependencies enforced by a passing test, and every module later plans implement already has its import path fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build/runtime | Transitive dependency code executes with full process privileges |
| Source code → process environment | Any module can read `process.env`; a literal fallback silently weakens a required secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Elevation of Privilege | Env var reads anywhere in the monorepo | mitigate | `no-restricted-syntax` ESLint rule in `packages/config/eslint.config.js` rejects `process.env.X ?? 'literal'` / `|| 'literal'`; verified by a deliberate lint failure in Task 1's acceptance criteria (PITFALLS.md #1, Dokploy CVE-2026-45631) |
| T-1-03 | Tampering | `packages/domain` import surface | mitigate | Turborepo `boundaries` rule plus `purity.test.ts` prevent domain code from acquiring I/O reach; a violation fails both `turbo boundaries` and `pnpm test` |
| T-1-SC | Tampering | pnpm installs in this plan (`typescript`, `eslint`, `typescript-eslint`, `prettier`, `turbo`, `vitest`, `@vitest/coverage-v8`, `zod`) | mitigate | Package Legitimacy Audit in 01-RESEARCH.md approved all of these; `vitest` was [SUS] and is cleared by the automated provenance check (`node scripts/check-package-provenance.mjs`) delivered in Plan 01-01, which is a hard `depends_on` of this plan and must exit 0 before this plan installs anything |
</threat_model>

<verification>
- `pnpm install && pnpm lint && pnpm typecheck && pnpm test && pnpm test:integration && pnpm exec turbo boundaries` all exit 0
- Coverage output names `packages/domain` and a 95% threshold
- An intentional env-fallback literal fails lint; an intentional `node:fs` import in domain fails tests
</verification>

<success_criteria>
Every command in CLAUDE.md section 3.2 except `db:migrate` and `test:e2e` runs green on the fresh monorepo, the QA-02 coverage gate is armed before any domain code exists, and the four wave-3 domain plans can run in parallel against fixed import paths.
</success_criteria>

<output>
Create `.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-02-SUMMARY.md` when done
</output>
