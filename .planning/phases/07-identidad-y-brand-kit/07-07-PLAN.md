---
phase: 07-identidad-y-brand-kit
plan: 07
type: execute
wave: 5
depends_on: ["07-05", "07-03"]
files_modified:
  - apps/web/src/components/Sidebar.tsx
  - apps/web/src/components/Sidebar.test.tsx
  - apps/web/src/components/AuthCard.tsx
  - apps/web/src/components/AuthCard.test.tsx
  - tests/e2e/brand.spec.ts
  - docs/ui-review-07-brand.md
  - docs/brand/approved/sidebar-expanded-light.png
  - docs/brand/approved/sidebar-expanded-dark.png
  - docs/brand/approved/sidebar-rail-light.png
  - docs/brand/approved/sidebar-rail-dark.png
  - docs/brand/approved/login-light.png
  - docs/brand/approved/login-dark.png
  - docs/brand/approved/setup-light.png
  - docs/brand/approved/setup-dark.png
autonomous: true
requirements: [BRAND-02]
model_hint: opus
must_haves:
  truths:
    - "The monogram appears in the 64 px rail (900–1279 px) and the horizontal lockup in the expanded sidebar (≥1280 px), painted with currentColor in both themes (D-04, D-09)"
    - "The horizontal lockup appears above the title on /login and /setup in both themes (D-04)"
    - "Existing data-testids (shell-sidebar, shell-sidebar-scrim, shell-theme-toggle, login-submit, setup-banner, login-banner) are untouched; brand-monogram / brand-lockup are supplied at the call sites (PITFALLS §UI redesign)"
    - "A ux-review audit of the four surfaces returns no BLOCK"
  artifacts:
    - path: "apps/web/src/components/Sidebar.tsx"
      provides: "Brand slot above the nav list: Logo (rail) + Lockup (expanded), CSS-switched"
      contains: "data-testid=\"brand-monogram\""
    - path: "apps/web/src/components/AuthCard.tsx"
      provides: "Lockup above the h1"
      contains: "data-testid=\"brand-lockup\""
    - path: "apps/web/src/components/Sidebar.test.tsx"
      provides: "jsdom tests for the brand slot and preserved testids"
    - path: "apps/web/src/components/AuthCard.test.tsx"
      provides: "jsdom tests for the lockup slot"
    - path: "tests/e2e/brand.spec.ts"
      provides: "Breakpoint visibility of brand-monogram/brand-lockup; /login and /setup lockup; both themes"
    - path: "docs/ui-review-07-brand.md"
      provides: "noodara-ux-review report for the brand surfaces (PASS/FLAG only)"
  key_links:
    - from: "apps/web/src/components/Sidebar.tsx"
      to: "@noodara/ui Logo, Lockup"
      via: "import { Logo, Lockup, ThemeToggle } from '@noodara/ui'"
      pattern: "Lockup"
    - from: "apps/web/src/components/AuthCard.tsx"
      to: "@noodara/ui Lockup"
      via: "named import"
      pattern: "Lockup"
---

<objective>
Mount the approved mark in the product's four review surfaces exactly as D-04 specifies — monogram alone in the 64 px rail, horizontal lockup in the expanded sidebar and on `/login` and `/setup` — with component tests first, an E2E spec that proves the breakpoint switch in both themes, and a ux-review report. This plan may only start after `docs/brand/APPROVAL.md` exists (07-05).

Purpose: BRAND-02's in-app surfaces, without touching any existing testid or breaking the 93 E2E.

Output: modified `Sidebar.tsx`/`AuthCard.tsx`, new `Sidebar.test.tsx`/`AuthCard.test.tsx`, `tests/e2e/brand.spec.ts`, `docs/ui-review-07-brand.md`, refreshed `docs/brand/approved/` in-app captures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-PATTERNS.md
@.planning/phases/07-identidad-y-brand-kit/07-05-SUMMARY.md
@.planning/research/PITFALLS.md
@docs/brand/APPROVAL.md
@apps/web/src/components/Sidebar.tsx
@apps/web/src/components/AuthCard.tsx
@apps/web/src/components/ServerList.test.tsx
@packages/ui/src/brand/Logo.tsx
@packages/ui/src/brand/Lockup.tsx
@tests/e2e/shell.spec.ts
@tests/e2e/auth.spec.ts
@tests/e2e/setup.spec.ts
@scripts/check-ui-safety.mjs
@.claude/skills/noodara-ux-apple/SKILL.md
@.claude/skills/noodara-ux-review/SKILL.md
@.claude/skills/noodara-tdd/SKILL.md

<interfaces>
From @noodara/ui (07-03):
```typescript
export function Logo(props: { concept?: ConceptId; size?: number; title?: string; 'data-testid'?: string }): JSX.Element;   // never pass `color` in app code
export function Lockup(props: { concept?: ConceptId; height?: number; title?: string; 'data-testid'?: string }): JSX.Element;
```
Existing Sidebar breakpoints (Sidebar.tsx): `<900` bottom sheet, `min-[900px]` 64 px rail (`w-16`, `p-3`), `min-[1280px]` expanded (`w-60`), `LABEL_CLASSES = 'hidden min-[1280px]:inline'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED Sidebar/AuthCard component tests, then mount Logo/Lockup</name>
  <files>apps/web/src/components/Sidebar.test.tsx, apps/web/src/components/AuthCard.test.tsx, apps/web/src/components/Sidebar.tsx, apps/web/src/components/AuthCard.tsx</files>
  <read_first>
    - apps/web/src/components/Sidebar.tsx (full — the `<nav data-testid="shell-sidebar">`, `ITEM_CLASSES`, `LABEL_CLASSES`, `ICON_PROPS`, imports from `@noodara/ui`, `usePathname`)
    - apps/web/src/components/AuthCard.tsx (full)
    - apps/web/src/components/ServerList.test.tsx (how an apps/web `.test.tsx` imports the harness from `@noodara/ui/testing` and mocks `next/navigation` if it does; mirror it)
    - apps/web/tsconfig.json (relative imports WITHOUT `.js` suffix in apps/web)
    - packages/ui/src/brand/Logo.tsx and Lockup.tsx (props; `title` gives role=img; no `className` prop — use wrapper elements for layout)
    - .planning/research/PITFALLS.md §UI redesign (stable testids) and .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-04, D-09
    - .claude/skills/noodara-ux-apple/SKILL.md §3 Layout (sidebar spec: 64 px rail, item height, `--accent-soft` active) and §8 Don't (no second brand colour, no filled icons)
  </read_first>
  <behavior>
    - Sidebar renders `[data-testid="brand-monogram"]` (an `svg` with `fill="currentColor"`) inside a wrapper whose className contains `min-[1280px]:hidden` and `hidden` and `min-[900px]:flex` (hidden in the <900 bottom sheet, shown in the rail, hidden when expanded)
    - Sidebar renders `[data-testid="brand-lockup"]` (an `svg` with `fill="currentColor"`) inside a wrapper whose className contains `hidden` and `min-[1280px]:flex`
    - Both brand elements precede the `<ul>` nav list in DOM order and live inside `[data-testid="shell-sidebar"]`
    - The brand `svg`s have `role="img"` and an accessible name "Noodara" (via `title`) — the rail has no visible text, so the mark must be named
    - `shell-sidebar`, `shell-theme-toggle` testids still render; when `open` is true `shell-sidebar-scrim` renders; the three nav links keep `aria-label` Servers/Activity/Settings
    - Neither brand `svg` has a `fill` other than `currentColor` (assert `getAttribute('fill') === 'currentColor'`)
    - AuthCard renders `[data-testid="brand-lockup"]` BEFORE the `h1`, inside the card `div`, with `role="img"` name "Noodara"; the `h1` text is still the `title` prop; children still render
  </behavior>
  <action>
    RED first: create `Sidebar.test.tsx` (mock `next/navigation`'s `usePathname` to return `'/servers'`; `next/link` renders an anchor in jsdom without mocking) and `AuthCard.test.tsx` using the harness the existing apps/web tests use. Run `pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard` → fail. Commit `test(07-07): add failing brand mount-point tests`.

    GREEN — Sidebar.tsx: extend the existing `import { ThemeToggle } from '@noodara/ui'` line to also import `Logo, Lockup`. Insert, as the first child of `<nav>` (before `<ul>`), a brand block: a rail wrapper `div` with classes `mb-3 hidden h-11 items-center px-3 text-ink min-[900px]:flex min-[1280px]:hidden` containing `<Logo title="Noodara" size={24} data-testid="brand-monogram" />`, and an expanded wrapper `div` with classes `mb-3 hidden h-11 items-center px-3 text-ink min-[1280px]:flex` containing `<Lockup title="Noodara" height={20} data-testid="brand-lockup" />`. Name the class strings as module constants (`BRAND_RAIL_CLASSES`, `BRAND_EXPANDED_CLASSES`) next to `LABEL_CLASSES`, with a comment tying them to D-04 (rail = monogram, expanded = lockup, <900 sheet = none) and to the existing breakpoints. Colour is inherited from `text-ink` (token) — never `text-accent` (D-09). No Link wrapper (the mark is not a control in v0.2; Phase 8 decides). Do not alter any other line.

    AuthCard.tsx: import `{ Lockup }` from `@noodara/ui`; insert a `div` with classes `flex items-center text-ink` containing `<Lockup title="Noodara" height={22} data-testid="brand-lockup" />` as the first child of the card `div`, before `<h1>`; the card's existing `gap-5` supplies spacing. Update the component's header comment to mention the brand slot (D-04) in one sentence.

    Run tests, `pnpm check:ui-safety`, `pnpm --filter @noodara/web typecheck && pnpm --filter @noodara/web lint`. Commit `feat(07-07): mount the brand mark in the sidebar rail/expanded and the auth card`.
  </action>
  <verify>
    <automated>pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard && pnpm check:ui-safety && pnpm --filter @noodara/web typecheck && pnpm --filter @noodara/web lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="brand-monogram"' apps/web/src/components/Sidebar.tsx` prints 1; `grep -c 'data-testid="brand-lockup"' apps/web/src/components/Sidebar.tsx` prints 1; `grep -c 'data-testid="brand-lockup"' apps/web/src/components/AuthCard.tsx` prints 1
    - `grep -c 'data-testid="shell-sidebar"' apps/web/src/components/Sidebar.tsx` prints 1 and `grep -c 'shell-theme-toggle\|shell-sidebar-scrim' apps/web/src/components/Sidebar.tsx` prints 2 (unchanged)
    - `grep -cE "color=|text-accent|fill=" apps/web/src/components/Sidebar.tsx apps/web/src/components/AuthCard.tsx` prints 0 for both
    - `git diff --stat -- apps/web/src/components/Sidebar.tsx` shows additions only in the import line, the constants block and the brand block (no removed lines other than the import being extended)
    - `pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard` exits 0; `pnpm check:ui-safety` exits 0
    - RED commit `test(07-07)` precedes the `feat(07-07)` commit
  </acceptance_criteria>
  <done>Mark mounted per D-04 with caller-supplied testids; every pre-existing testid intact; token gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E brand.spec.ts — breakpoint switch and auth screens in both themes</name>
  <files>tests/e2e/brand.spec.ts</files>
  <read_first>
    - tests/e2e/shell.spec.ts (login helper, viewport sizes 1440×900 / 1024×800 / 800×800, theme toggling, `test.describe` structure, tag conventions)
    - tests/e2e/auth.spec.ts and tests/e2e/setup.spec.ts (how /login and /setup are visited; `setup-banner`)
    - tests/e2e/fixtures/stack.ts (`E2E_ADMIN_EMAIL`/`PASSWORD`)
    - playwright.config.ts (single chromium project, `workers: 1`; no `waitForTimeout` per the tdd skill §6)
  </read_first>
  <behavior>
    - `/login` at 1440×900: `getByTestId('brand-lockup')` is visible; `getByRole('img', { name: 'Noodara' })` count is 1; switching `data-theme` to dark keeps it visible and its computed `color` differs from the light value (proves currentColor follows the theme)
    - `/setup` (unauthenticated, no token): `brand-lockup` visible
    - After login at 1440×900 on `/servers`: `brand-lockup` visible, `brand-monogram` hidden (`toBeHidden`)
    - At 1024×800: `brand-monogram` visible, `brand-lockup` hidden; the sidebar `nav` bounding box width is 64
    - At 800×800 (sheet closed): both hidden; opening the menu (`shell-menu-button`) still shows the three nav links and both brand elements remain hidden
    - In dark theme at 1024×800 the monogram remains visible
  </behavior>
  <action>
    Write `tests/e2e/brand.spec.ts` (new file — do not edit the existing 14 specs) following shell.spec.ts's helpers verbatim (copy the `login` helper locally; do not import from another spec). Set theme via `page.evaluate(() => document.documentElement.setAttribute('data-theme', 'dark'))` for the theme assertions (deterministic). RED evidence for E2E is Task 1's component tests (running the spec against the pre-mount build would require a second checkout; state this in the SUMMARY). Run `pnpm build && pnpm test:e2e -- brand` (Docker required) and confirm 6 passing tests. Commit `test(07-07): add brand E2E for breakpoints, auth screens and themes`.
  </action>
  <verify>
    <automated>pnpm build && pnpm test:e2e -- brand</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/brand.spec.ts` exists with ≥ 6 `test(` blocks and zero `waitForTimeout`
    - `pnpm test:e2e -- brand` exits 0
    - `git diff --stat -- tests/e2e/shell.spec.ts tests/e2e/auth.spec.ts tests/e2e/setup.spec.ts` is empty
  </acceptance_criteria>
  <done>Breakpoint and theme behaviour of the mark proven in the real stack without touching existing specs.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the mounted surfaces, refresh docs/brand/approved, write the ux-review report</name>
  <files>docs/ui-review-07-brand.md, docs/brand/approved/sidebar-expanded-light.png, docs/brand/approved/sidebar-expanded-dark.png, docs/brand/approved/sidebar-rail-light.png, docs/brand/approved/sidebar-rail-dark.png, docs/brand/approved/login-light.png, docs/brand/approved/login-dark.png, docs/brand/approved/setup-light.png, docs/brand/approved/setup-dark.png</files>
  <read_first>
    - .claude/skills/noodara-ux-review/SKILL.md (procedure, nine dimensions, report format, verdict rules)
    - .claude/skills/noodara-ux-apple/SKILL.md (tokens/rules to audit against)
    - docs/ui-review-05.md (existing report as a format precedent)
    - scripts/brand/capture-brand-review.ts (`--mounted` flag: captures without injection)
    - docs/ui-build-prompt.md §9 and §10
    - docs/brand/APPROVAL.md (the approved concept id)
  </read_first>
  <action>
    Run `pnpm brand:review --mounted --concept <approved id>` (Docker or attach mode) to refresh `docs/brand/review/<id>/` with the REAL mounted mark, then copy the eight in-app captures over the same filenames in `docs/brand/approved/` (keep `board-*.png`; these now show production code and supersede the injected ones). Open at least four captures with the Read tool (rail light, rail dark, login light, sidebar-expanded dark). Write `docs/ui-review-07-brand.md` in the skill's exact format: date, commit sha, mode "screenshots + code", global verdict, one row per dimension with evidence `file:line` or capture path. Check specifically: only `--ink` used for the mark (Tokens), no shadow/gradient (Surface), rail item height alignment on the 8 px grid (Layout), accessible name on the img (Accessibility), both themes verified (Themes). Any FLAG must carry a concrete correction; a BLOCK must be fixed in `Sidebar.tsx`/`AuthCard.tsx` in this task before closing (re-run Task 1's tests). English; no AI attribution. Commit `docs(07-07): ux review of the brand surfaces and refreshed approved captures`.
  </action>
  <verify>
    <automated>test -f docs/ui-review-07-brand.md && grep -qiE "(Veredicto global|Global verdict): (PASS|FLAG)" docs/ui-review-07-brand.md && ! grep -qE "\| *BLOCK *\|" docs/ui-review-07-brand.md && test "$(ls docs/brand/approved/*.png | wc -l | tr -d ' ')" = "10" && pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ui-review-07-brand.md` exists, contains a table with nine dimension rows, a global verdict of PASS or FLAG, and zero rows with verdict BLOCK
    - `grep -ci "claude\|anthropic" docs/ui-review-07-brand.md` prints 0
    - `ls docs/brand/approved/*.png | wc -l` prints 10 and the eight in-app PNGs have mtimes newer than `docs/brand/APPROVAL.md`
    - `pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard` exits 0
  </acceptance_criteria>
  <done>The four surfaces are audited against the design system with evidence; the approved folder shows the real mounted mark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `@noodara/ui` components → apps/web DOM | Inline SVG rendered by React inside authenticated and unauthenticated pages |
| capture script → screenshots committed to `docs/brand/approved/` | Real app content enters git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering / EoP | inline SVG in Sidebar/AuthCard | mitigate | React JSX rendering only; 07-03 tests prove markup has no script/handlers; `check:ui-safety` keeps `dangerouslySetInnerHTML` at exactly one |
| T-07-21 | Information Disclosure | committed captures | mitigate | E2E fixture stack data only, or empty auth forms; executor reviews each image before commit; no browser chrome captured |
| T-07-22 | Denial of Service | regression of the 93 E2E | mitigate | No existing selector or testid changed (acceptance criteria grep); new spec is a separate file; full suite in 07-10 |
| T-07-SC | Tampering | npm installs | accept (n/a) | None |
</threat_model>

<verification>
- `pnpm test -- apps/web/src/components/Sidebar apps/web/src/components/AuthCard` green; `pnpm test:e2e -- brand` green; `pnpm check:ui-safety` green.
- `docs/ui-review-07-brand.md` has no BLOCK.
- Nyquist: `07-W0-05`, `07-W0-06` → Task 1 and Task 2.
</verification>

<success_criteria>
- Monogram in the rail, lockup in the expanded sidebar and on /login and /setup, both themes, currentColor only (D-04, D-09, BRAND-02).
- Existing testids preserved; new `brand-monogram`/`brand-lockup` supplied by call sites.
- ux-review PASS/FLAG with evidence.
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-07-SUMMARY.md` when done.
</output>
