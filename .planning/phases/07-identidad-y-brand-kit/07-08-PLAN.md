---
phase: 07-identidad-y-brand-kit
plan: 08
type: execute
wave: 6
depends_on: ["07-06", "07-05"]
files_modified:
  - docs/brand/BRAND.md
  - docs/brand/construction.svg
  - scripts/brand/construction-sheet.ts
  - tests/unit/brand/construction-sheet.test.ts
  - tests/unit/docs/brand-kit-structure.test.ts
  - package.json
autonomous: true
requirements: [BRAND-01]
model_hint: opus
must_haves:
  truths:
    - "docs/brand/BRAND.md lets a person reproduce the logo correctly without asking: meaning of an invented name that encodes seeing clearly (D-01, D-02), construction on the 24 grid with the real constants, lockups, clear space and minimum size, colour rules, typography, misuse, assets and how Phase 10 consumes them (BRAND-01, success criterion 1)"
    - "The construction sheet SVG is generated from the geometry module, never drawn by hand (D-15)"
    - "The brand kit is in English and carries no AI attribution"
  artifacts:
    - path: "docs/brand/BRAND.md"
      provides: "The brand kit"
      contains: "## Construction"
    - path: "docs/brand/construction.svg"
      provides: "Generated construction sheet (monogram on the grid + labelled constants)"
    - path: "tests/unit/docs/brand-kit-structure.test.ts"
      provides: "Heading presence, constant accuracy, asset-link existence, no-attribution assertions"
  key_links:
    - from: "tests/unit/docs/brand-kit-structure.test.ts"
      to: "packages/ui/src/brand/geometry.ts"
      via: "asserts BRAND.md states the real GRID/STROKE/APERTURE_RADIUS values"
      pattern: "APERTURE_RADIUS"
    - from: "docs/brand/BRAND.md"
      to: "packages/ui/brand/*"
      via: "relative links to every ASSET_FILES entry"
      pattern: "packages/ui/brand/"
---

<objective>
Write the brand kit (`docs/brand/BRAND.md`) for the approved concept and generate its construction sheet from the geometry, with a structure/accuracy test that keeps the document honest against the code — the same discipline `docs/install.md` already has.

Purpose: BRAND-01 and ROADMAP success criterion 1 ("una hoja de brand kit … con la que una persona reproduce el logo correctamente sin preguntar").

Output: `docs/brand/BRAND.md`, `docs/brand/construction.svg` (generated), `scripts/brand/construction-sheet.ts` + test, `tests/unit/docs/brand-kit-structure.test.ts`, root script `brand:construction`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-PATTERNS.md
@.planning/phases/07-identidad-y-brand-kit/07-05-SUMMARY.md
@.planning/phases/07-identidad-y-brand-kit/07-06-SUMMARY.md
@docs/brand/APPROVAL.md
@docs/install.md
@packages/ui/src/brand/geometry.ts
@packages/ui/src/brand/glyphs.ts
@packages/ui/src/brand/concepts/a.ts
@packages/ui/src/brand/concepts/b.ts
@packages/ui/src/brand/concepts/c.ts
@scripts/brand/board-html.ts
@scripts/brand/asset-manifest.ts
@tests/unit/docs/install-docs-accuracy.test.ts
@docs/ui-build-prompt.md
@.claude/skills/noodara-ux-apple/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED construction-sheet test, then the generated construction.svg</name>
  <files>tests/unit/brand/construction-sheet.test.ts, scripts/brand/construction-sheet.ts, docs/brand/construction.svg, package.json</files>
  <read_first>
    - scripts/brand/board-html.ts (the construction section already draws the grid + labelled constants for the board — extract/reuse that builder rather than writing a second one; if it is HTML-embedded, factor a pure `constructionSvg({concept, ink, hairline, annotation})` function into `scripts/brand/construction-sheet.ts` and make board-html.ts call it)
    - packages/ui/src/brand/geometry.ts (constants, `monogramParts`, `bar`, `ring`)
    - scripts/brand/asset-manifest.ts (`readBrandTokens` for ink/hairline colours)
    - scripts/brand/write-if-changed.ts
  </read_first>
  <behavior>
    - `constructionSvg(...)` returns an SVG with `viewBox="0 0 24 24"`-scaled content on a 480×480 canvas, containing `data-grid="24"`, 25 vertical + 25 horizontal grid `<line>`s, the monogram parts with their `data-part` names, a dimension label per constant (`GRID = 24`, `STROKE = <geometry value>`, `APERTURE_RADIUS = <value>`, `MARGIN = <value>`) rendered from the imports, and the concept name from `CONCEPT_META`
    - It contains no `<script`, `href=`, `<foreignObject`, `on*=`; colours are the passed-in arguments only
    - `docs/brand/construction.svg` on disk equals `constructionSvg(...)` for `DEFAULT_CONCEPT` with light-theme ink/hairline tokens (exactness, like the assets test)
  </behavior>
  <action>
    RED first: write the test; run; fail; commit `test(07-08): add failing construction-sheet tests`. GREEN: implement `constructionSvg` (pure) and a CLI entry in the same file guarded by an `isMainModule` check (pattern from `check-package-provenance.mjs`) that writes `docs/brand/construction.svg` via `writeIfChanged`; add root script `"brand:construction": "tsx scripts/brand/construction-sheet.ts"`; run it; commit the SVG. If `board-html.ts` was refactored to reuse the function, re-run `pnpm test -- tests/unit/brand/board-html` to prove no regression.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/brand/construction-sheet tests/unit/brand/board-html && pnpm typecheck && pnpm lint && test -s docs/brand/construction.svg</automated>
  </verify>
  <acceptance_criteria>
    - `docs/brand/construction.svg` exists, > 2 KB, contains `data-grid="24"` and `data-part="aperture"`
    - `package.json` contains `"brand:construction"`
    - `pnpm brand:construction` run twice prints a "no files changed" line the second time
    - `pnpm test -- tests/unit/brand/construction-sheet` exits 0
  </acceptance_criteria>
  <done>The construction sheet is generated from the code and locked by a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED brand-kit structure test, then write BRAND.md</name>
  <files>tests/unit/docs/brand-kit-structure.test.ts, docs/brand/BRAND.md</files>
  <read_first>
    - tests/unit/docs/install-docs-accuracy.test.ts (section-presence idiom, "no internal planning ids" regex at lines 109-116 — the brand kit must also avoid `D-\d{2}`, `0[1-7]-\d{2}`, `T-0\d-\d+` strings; refer to decisions in prose, not ids)
    - docs/install.md (flat `## Topic` structure, tone)
    - packages/ui/src/brand/geometry.ts, glyphs.ts, concepts/<approved>.ts (the header comments written in 07-01 are the source for the Construction section — paraphrase them; the constant values must match)
    - scripts/brand/asset-manifest.ts (`ASSET_FILES` — every file must be linked from the Assets section)
    - docs/brand/APPROVAL.md (link it from the kit)
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md (D-01..D-12 are the content: meaning, lockups, wordmark, colour, OG; Claude's discretion: minimum size and clear space derived from the grid)
    - docs/ui-build-prompt.md §9 (the misuse list must include: no second colour, no accent-painted mark in UI, no gradients, no glow/shadow, no emoji/glyph substitution, no stretching, no rotation, no outlines, no photo backgrounds without the tile)
    - CLAUDE.md §7.1 (English) and the no-attribution rule
  </read_first>
  <behavior>
    - `docs/brand/BRAND.md` contains, in this order, the headings: `# Noodara brand kit`, `## Meaning`, `## Construction`, `## Lockups`, `## Clear space and minimum size`, `## Color`, `## Typography`, `## Misuse`, `## Assets and exports`, `## Approval`
    - The Construction section states the literal strings `GRID = 24`, `STROKE = <geometry.STROKE>`, `APERTURE_RADIUS = <geometry.APERTURE_RADIUS>`, `MARGIN = <geometry.MARGIN>`, `LOCKUP_GAP = <geometry.LOCKUP_GAP>` with values read from the module in the test (not typed in the test), and embeds `![Construction](construction.svg)`
    - The Lockups section names all three: "monogram", "horizontal lockup", "wordmark" and maps each to its surfaces (rail/favicon/apple-touch-icon; expanded sidebar//login//setup/README/site; text and site title)
    - The Clear space section gives a minimum size in px for each lockup (monogram ≥ 16 px, lockup ≥ 80 px wide or similar — a number, derived from the grid: state the rule "clear space = STROKE grid units on every side") and the test asserts a `px` value appears in that section
    - The Color section contains `currentColor`, `--ink`, `--accent-fill`, `--on-accent`, `--canvas` and the sentence fragment "only surface" (the tile is the only blue surface); it contains NO `#rrggbb` literal (reference tokens by name)
    - The Typography section states the wordmark is drawn (not a font) and that the UI keeps the system font stack
    - The Misuse section has ≥ 8 list items and contains the words "gradient", "glow", "shadow", "accent", "emoji", "stretch", "rotate", "outline"
    - The Assets section links every entry of `ASSET_FILES` as `packages/ui/brand/<name>` and contains `@noodara/ui/brand/` and the string `<picture>`; it links `docs/brand/APPROVAL.md` from the Approval section
    - The whole file contains no `Claude`, `Anthropic`, `Co-Authored-By` (case-insensitive) and matches none of `/\bD-\d{2}\b/`, `/\b0[1-7]-\d{2}\b/`, `/\bT-0\d-\d+\b/`
  </behavior>
  <action>
    RED first: write `tests/unit/docs/brand-kit-structure.test.ts` importing the geometry constants and `ASSET_FILES`; run; fail; commit `test(07-08): add failing brand-kit structure tests`.

    GREEN: write `docs/brand/BRAND.md` in English. Content guide — Meaning: the invented name has no etymology; the mark encodes seeing clearly ("Your infrastructure, understood."), the differentiator versus tools that only administer; name the approved concept and its construction idea in two sentences. Construction: the 24-unit grid, filled outlines (why: crisp at 16 px), each stroke's derivation (stems, diagonal, aperture) using the constants, the wordmark glyph rules (x-height, ascender, the "oo" reuse of the aperture radius as the typographic signature, tight tracking), and the embedded construction.svg. Lockups: table of the three with surfaces. Clear space: rule + minimum sizes. Color: in-app and README = ink via currentColor; the tile = accent-fill background with on-accent mark, the only blue surface; OG = ink on canvas dark; never paint the mark in accent inside the UI. Typography: drawn wordmark, system font for UI, tagline in the display role. Misuse: the list. Assets and exports: the table of files with purpose and size, the `@noodara/ui/brand/*` export, how the app syncs its icons (brief, pointing at `apps/web/scripts/sync-brand-assets.mjs`), the README `<picture>` snippet, regeneration commands (`pnpm brand:generate`, `pnpm brand:check`, `pnpm brand:construction`, `pnpm brand:boards`, `pnpm brand:review`). Approval: link to APPROVAL.md with the date and concept. Keep paragraphs short; sentence case; no exclamation marks.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/docs/brand-kit-structure && pnpm test -- tests/unit/docs/install-docs-accuracy</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " docs/brand/BRAND.md` prints 9 and `grep -n "^## " docs/brand/BRAND.md` lists them in the required order
    - `grep -c "packages/ui/brand/" docs/brand/BRAND.md` ≥ 13
    - `grep -cE "#[0-9a-fA-F]{6}\b" docs/brand/BRAND.md` prints 0
    - `grep -ci "claude\|anthropic" docs/brand/BRAND.md` prints 0
    - `pnpm test -- tests/unit/docs/brand-kit-structure` exits 0; `pnpm test -- tests/unit/docs/install-docs-accuracy` still exits 0
    - RED commit precedes GREEN
  </acceptance_criteria>
  <done>The brand kit exists, is complete, accurate to the code, in English, without attribution, and test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → readers (public repo) | Documentation is public; must not leak internal planning identifiers or credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Information Disclosure | BRAND.md / construction.svg | mitigate | Structure test forbids planning-id patterns (same regexes as install-docs-accuracy); no env values or URLs beyond repo-relative paths |
| T-07-24 | Tampering | construction.svg | mitigate | Generated from geometry; exactness test; JSX/string builder without script/href |
| T-07-SC | Tampering | npm installs | accept (n/a) | None |
</threat_model>

<verification>
- `pnpm test -- tests/unit/docs/brand-kit-structure tests/unit/brand/construction-sheet` green.
- Nyquist: `07-W0-03` → `brand-kit-structure.test.ts`.
</verification>

<success_criteria>
- BRAND.md covers meaning, construction, lockups, clear space/minimum size, colour, typography, misuse, assets/exports, approval (BRAND-01, success criterion 1).
- Construction sheet generated, not drawn (D-15).
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-08-SUMMARY.md` when done.
</output>
