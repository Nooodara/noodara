---
phase: 07-identidad-y-brand-kit
plan: 10
type: execute
wave: 7
depends_on: ["07-08", "07-09", "07-07"]
files_modified:
  - README.md
  - tests/unit/docs/install-docs-accuracy.test.ts
  - .github/workflows/ci.yml
  - tests/unit/scripts/vitest-gate-config.test.ts
autonomous: true
requirements: [BRAND-02, BRAND-01]
model_hint: sonnet
must_haves:
  truths:
    - "The README shows the horizontal lockup in light and dark via <picture> + prefers-color-scheme with repo-relative sources, and the install-docs accuracy test still passes (D-10, BRAND-02)"
    - "CI fails if packages/ui/brand/* drifts from the geometry (pnpm brand:check) so the single source of truth cannot silently break"
    - "The full gate set is green after all brand application: pnpm test, pnpm test:e2e (93 existing + brand), pnpm check:ui-safety, pnpm lint, pnpm typecheck, pnpm boundaries, provenance"
  artifacts:
    - path: "README.md"
      provides: "<picture> lockup header"
      contains: "<source media=\"(prefers-color-scheme: dark)\" srcset=\"packages/ui/brand/lockup-dark.svg\">"
    - path: ".github/workflows/ci.yml"
      provides: "brand:check step in the lint job"
      contains: "pnpm brand:check"
  key_links:
    - from: "README.md"
      to: "packages/ui/brand/lockup-{light,dark}.svg"
      via: "<picture> srcset relative paths"
      pattern: "packages/ui/brand/lockup-(light|dark)\\.svg"
    - from: ".github/workflows/ci.yml lint job"
      to: "scripts/brand/generate-brand-assets.ts --check"
      via: "pnpm brand:check"
      pattern: "brand:check"
---

<objective>
Finish BRAND-02's last surface (the README), make the generated assets non-driftable in CI, and run the complete regression that ROADMAP success criterion 3 demands (token gate green, 93 E2E green) now that every surface carries the mark.

Purpose: close the phase with proof, not assumption.

Output: README `<picture>` header, `brand:check` in CI, extended docs-accuracy test, a SUMMARY with the full command log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md
@.planning/phases/07-identidad-y-brand-kit/07-06-SUMMARY.md
@.planning/phases/07-identidad-y-brand-kit/07-09-SUMMARY.md
@README.md
@tests/unit/docs/install-docs-accuracy.test.ts
@tests/unit/scripts/vitest-gate-config.test.ts
@tests/unit/scripts/check-workflow-pins.test.ts
@.github/workflows/ci.yml
@docs/brand/BRAND.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED README assertions, then the <picture> lockup header</name>
  <files>tests/unit/docs/install-docs-accuracy.test.ts, README.md</files>
  <read_first>
    - README.md (current `# Noodara` + tagline header; `## Install`, `## Status`, `## Development` sections that the accuracy test pins)
    - tests/unit/docs/install-docs-accuracy.test.ts (lines 104-116 forbid `:latest` and planning-id patterns in README; lines 202-227 pin sections and `pnpm` commands — the new markup must not violate them; add the brand assertions in a new `describe('README.md brand header')`)
    - .planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md §Architecture Patterns §2 (the `<picture>` snippet; repo-relative paths; `<img>` fallback)
    - docs/brand/BRAND.md Assets section (the snippet documented there must match what the README uses)
    - CONTEXT.md Claude's discretion: README composition (position and size of the lockup)
  </read_first>
  <behavior>
    - README contains a `<picture>` whose sources are exactly `packages/ui/brand/lockup-dark.svg` for `(prefers-color-scheme: dark)` and `packages/ui/brand/lockup-light.svg` for `(prefers-color-scheme: light)`, and an `<img alt="Noodara" src="packages/ui/brand/lockup-light.svg" width="240">` fallback — no `http://`/`https://` inside the `<picture>` block (repo-relative only)
    - Both referenced files exist on disk (test `existsSync`)
    - The `<picture>` block appears before the first `## ` heading; the `# Noodara` H1 remains (GitHub renders the picture above it) and the tagline blockquote remains
    - Existing README assertions in the file still pass (Install links docs/install.md, Status names v0.1, Development commands exist, no `:latest`, no planning ids)
  </behavior>
  <action>
    RED first: add the `describe` to `install-docs-accuracy.test.ts`; run `pnpm test -- tests/unit/docs/install-docs-accuracy`; see the new cases fail; commit `test(07-10): add failing README brand header assertions`. GREEN: insert the `<picture>` block as the very first lines of README.md (before `# Noodara`), wrapped in `<p align="left">` … `</p>` is NOT needed — keep plain `<picture>`; keep the H1 and the `> Your infrastructure, understood.` line as they are. Run the docs tests; commit `docs(07-10): add the lockup to the README header`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/docs/install-docs-accuracy</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<source media="(prefers-color-scheme: dark)" srcset="packages/ui/brand/lockup-dark.svg">' README.md` prints 1
    - `grep -c '<img alt="Noodara" src="packages/ui/brand/lockup-light.svg"' README.md` prints 1
    - `awk '/<picture>/,/<\/picture>/' README.md | grep -c "http" ` prints 0
    - `head -1 README.md` is `<picture>`; `grep -c "^# Noodara$" README.md` prints 1
    - `pnpm test -- tests/unit/docs/install-docs-accuracy` exits 0; RED commit precedes GREEN
  </acceptance_criteria>
  <done>README shows the lockup in both colour schemes with repo-relative sources; docs accuracy test extended and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CI drift gate — pnpm brand:check in the lint job</name>
  <files>.github/workflows/ci.yml, tests/unit/scripts/vitest-gate-config.test.ts</files>
  <read_first>
    - .github/workflows/ci.yml lines 49-78 (the `lint` job: `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm check:ui-safety`, `pnpm check:posix-sh`)
    - tests/unit/scripts/check-workflow-pins.test.ts and tests/unit/scripts/vitest-gate-config.test.ts (existing tests that read ci.yml — extend the one that asserts required steps, or add a small `describe` to vitest-gate-config.test.ts asserting the lint job runs `pnpm brand:check`)
    - scripts/brand/generate-brand-assets.ts (`--check` semantics: read-only, exits 1 on text drift or raster shape mismatch; needs `sharp` installed — the lint job already runs `pnpm install --frozen-lockfile`, and `sharp` ships prebuilt linux-x64 binaries per RESEARCH)
  </read_first>
  <behavior>
    - A unit test reads `.github/workflows/ci.yml` and asserts the `lint` job contains a step `run: pnpm brand:check` after `run: pnpm check:ui-safety`
    - `pnpm brand:check` exits 0 on the current tree; after `printf ' ' >> packages/ui/brand/monogram-light.svg` it exits 1 naming that file (restore with `git checkout`)
  </behavior>
  <action>
    RED first: add the CI-step assertion; run; fail; commit `test(07-10): assert CI runs the brand drift gate`. GREEN: add `- run: pnpm brand:check` to the `lint` job right after the `check:ui-safety` step (no new actions, no new pins — `check-workflow-pins.test.ts` must stay green). Verify the drift behaviour locally as described. Commit `ci(07-10): fail CI when generated brand assets drift from the geometry`.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/scripts && pnpm brand:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run: pnpm brand:check" .github/workflows/ci.yml` prints 1 and it appears within the `lint` job block (between `name: lint` and `name: typecheck`)
    - `pnpm test -- tests/unit/scripts` exits 0 (workflow pins + new assertion)
    - The deliberate-drift experiment is described in the SUMMARY with the exact failure line printed by `pnpm brand:check`
  </acceptance_criteria>
  <done>CI enforces the single source of truth for generated assets.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression across every gate</name>
  <files>none</files>
  <read_first>
    - .planning/phases/07-identidad-y-brand-kit/07-VALIDATION.md (the phase-gate command list and sampling contract)
    - .planning/research/PITFALLS.md §UI redesign (93 E2E must stay green)
    - CLAUDE.md §2.2 Definition of Done (zero skipped tests, zero TS/lint errors)
  </read_first>
  <action>
    Run, in this order, capturing exit codes and the summary line of each: `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm boundaries`, `pnpm check:ui-safety`, `pnpm brand:check`, `node scripts/check-package-provenance.mjs`, `pnpm test` (note total passed and that skipped = 0), `pnpm build`, `pnpm test:e2e` (Docker; note total passed — must be ≥ 93 + the brand spec's tests, 0 failed, 0 skipped, 0 flaky). If anything fails, fix the cause within this phase's files (never by loosening a gate, deleting a test, or editing an existing E2E selector), re-run the failing gate and then the full list again. Update `.planning/phases/07-identidad-y-brand-kit/07-VALIDATION.md`: rewrite the provisional `07-W0-NN` ids to the real plan/task ids used in this phase (`07-W0-01`→07-01, `07-W0-02`→07-06 T3, `07-W0-03`→07-08 T2, `07-W0-04`→07-03/07-09, `07-W0-05`/`06`→07-07, `07-W0-07`→07-09 T2, `07-W0-08`→07-10 T3, `07-W0-09`→07-06 T3, `07-W0-10`→07-05, `07-W0-11`→07-02 T2), set every status to ✅, `wave_0_complete: true`, `nyquist_compliant: true`, `status: complete`. Commit `docs(07-10): close phase 7 validation contract`.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm boundaries && pnpm check:ui-safety && pnpm brand:check && node scripts/check-package-provenance.mjs && pnpm test && pnpm build && pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Every command in `<verify>` exits 0
    - `pnpm test` output reports 0 skipped; `pnpm test:e2e` output reports 0 failed, 0 skipped, 0 flaky and a passed count ≥ 103 (93 existing + ≥ 10 brand)
    - `.planning/phases/07-identidad-y-brand-kit/07-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`, and contains no `07-W0-` id in the Per-Task Verification Map
    - The SUMMARY contains a table of each command with its exit code and the passed/failed/skipped counts
  </acceptance_criteria>
  <done>Phase 7 is green across all gates; ROADMAP success criterion 3 proven; validation contract closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → GitHub renderer | HTML in Markdown is rendered by GitHub; only `<picture>`/`<source>`/`<img>` with repo-relative paths are used |
| CI workflow | Adds a step that executes a repo script with the installed `sharp` binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-29 | Tampering | README `<picture>` sources | mitigate | Test asserts sources are the two repo-relative SVG paths and that no `http` appears inside the block (no remote image can be swapped in) |
| T-07-30 | Tampering | generated assets edited by hand | mitigate | `pnpm brand:check` in CI lint job fails on any byte/shape drift; unit exactness test too |
| T-07-31 | Denial of Service | CI step needs native `sharp` | accept | Prebuilt `@img/sharp-linux-x64` via optionalDependencies (RESEARCH verified); `--frozen-lockfile` install already in the job |
| T-07-SC | Tampering | npm installs | accept (n/a) | None |
</threat_model>

<verification>
- All gates green (see Task 3); 07-VALIDATION.md closed.
- Nyquist: `07-W0-08` (full E2E) → Task 3.
</verification>

<success_criteria>
- README lockup in both schemes (BRAND-02).
- CI drift gate active (single source of truth, criterion 4).
- `pnpm test`, `pnpm test:e2e` (≥ 93 existing green), `check:ui-safety`, `lint`, `typecheck`, `boundaries`, provenance — all green (criterion 3).
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-10-SUMMARY.md` when done (with the full command/exit-code table).
</output>
