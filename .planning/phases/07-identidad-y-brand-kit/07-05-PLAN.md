---
phase: 07-identidad-y-brand-kit
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - packages/ui/src/brand/concepts/a.ts
  - packages/ui/src/brand/concepts/b.ts
  - packages/ui/src/brand/concepts/c.ts
  - packages/ui/src/brand/geometry.ts
  - packages/ui/src/brand/geometry.test.ts
  - docs/brand/APPROVAL.md
  - docs/brand/approved/README.md
  - tests/unit/brand/approval-record.test.ts
autonomous: false
requirements: [BRAND-03]
model_hint: opus
must_haves:
  truths:
    - "The user has seen all three concepts mounted in the real app (rail, expanded sidebar, /login, /setup) in both themes plus a brand board per concept, and picked one (D-13, D-14)"
    - "At most two concrete adjustment rounds were applied to the chosen concept, each re-captured in the app before the next decision (D-16)"
    - "Explicit approval is recorded in docs/brand/APPROVAL.md with date, concept, rounds used and approver, and DEFAULT_CONCEPT equals the approved concept — no BRAND-02 surface is touched before this record exists (D-17)"
  artifacts:
    - path: "docs/brand/APPROVAL.md"
      provides: "Approval record: Date, Concept, Adjustment rounds used, Approver, Evidence"
      contains: "| Concept |"
    - path: "docs/brand/approved/"
      provides: "Committed captures of the approved concept only (board + 4 surfaces × 2 themes)"
    - path: "tests/unit/brand/approval-record.test.ts"
      provides: "Asserts APPROVAL.md fields and that DEFAULT_CONCEPT matches the recorded concept"
    - path: "packages/ui/src/brand/geometry.ts"
      provides: "DEFAULT_CONCEPT set to the approved ConceptId"
  key_links:
    - from: "tests/unit/brand/approval-record.test.ts"
      to: "docs/brand/APPROVAL.md and geometry.ts DEFAULT_CONCEPT"
      via: "parse the Concept row, compare to the export"
      pattern: "DEFAULT_CONCEPT"
---

<objective>
Run the human approval gate that BRAND-03 and D-13..D-17 require, and record its outcome so every later plan can depend on it structurally. The user reviews the three concepts in real context (captures from 07-04), picks one, gets up to two adjustment rounds (each applied to the geometry constants/concept file and re-captured), then approves explicitly. The plan ends by writing `docs/brand/APPROVAL.md`, committing the approved concept's captures under `docs/brand/approved/`, setting `DEFAULT_CONCEPT`, and adding a test that keeps the code and the record in agreement.

Purpose: this is the structural gate — plans 07-06 through 07-10 `depends_on` this plan and cannot start before it. If the user discards all three concepts, this plan STOPS and returns `## PHASE BLOCKED — concepts rejected` to the orchestrator instead of proceeding.

Output: `docs/brand/APPROVAL.md`, `docs/brand/approved/*.png`, `DEFAULT_CONCEPT` updated, `approval-record.test.ts` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-04-SUMMARY.md
@packages/ui/src/brand/geometry.ts
@packages/ui/src/brand/concepts/a.ts
@packages/ui/src/brand/concepts/b.ts
@packages/ui/src/brand/concepts/c.ts
@docs/brand/review/README.md
@scripts/brand/review-paths.ts
@.claude/skills/noodara-ux-apple/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Review the three concepts in context and pick one (round 0)</name>
  <files>none</files>
  <read_first>
    - .planning/phases/07-identidad-y-brand-kit/07-04-SUMMARY.md (where the captures are and which stack mode produced them)
    - docs/brand/review/README.md
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-02, D-03, D-08, D-09, D-13, D-14 (the criteria the user judges against — restate them in the prompt to the user)
  </read_first>
  <action>
    Pause. Present the review package to the user (paths below, criteria from CONTEXT.md) and wait for the resume signal. Do not modify any file in this task. If the reply is "reject all", stop the plan and return `## PHASE BLOCKED — concepts rejected`.
  </action>
  <what-built>Three monogram + wordmark concepts (a "Aperture", b "Focus", c "Viewfinder"), each rendered as a brand board and mounted in the real app by the 07-04 capture script. Nothing has been applied to the product yet.</what-built>
  <how-to-verify>
    1. Open the boards: `docs/brand/review/a/board-light.png`, `board-dark.png`, then the same for `b` and `c`. Each shows the three lockups, the construction sheet on the 24-unit grid with the constants, the 16/32/64/256 scales, the favicon tile on simulated light/dark tab strips, and the OG preview.
    2. Open the in-app captures per concept: `sidebar-expanded-{light,dark}.png` (1440 px), `sidebar-rail-{light,dark}.png` (64 px rail at 1024 px), `login-{light,dark}.png`, `setup-{light,dark}.png` (the `-crop` files are close-ups of the sidebar).
    3. Judge against D-02/D-03: does the N read as N at 16 px (scales row) and does the construction say "seeing clearly"? Do the "oo" read as the aperture without becoming eyes (D-08)? Does the mark stay quiet next to the single blue action colour (D-09)?
    4. Optional (Pitfall 2 — cannot be scripted): with the dev stack running, open http://localhost:3000/login in Chrome and Safari in light and dark OS appearance for page context. The favicon is not applied yet; the tile at 16 px is judged from the board's tab strips now and for real in 07-09.
  </how-to-verify>
  <verify>
    <human-check>User replied with "pick a|b|c" (optionally with round-1 adjustments), "approved a|b|c", or "reject all"</human-check>
  </verify>
  <acceptance_criteria>
    - The user's reply is quoted verbatim in the SUMMARY under "Round 0"
    - No file changed in this task (`git status --porcelain` empty apart from untracked review PNGs, which are gitignored)
  </acceptance_criteria>
  <resume-signal>Reply with one of: "pick a", "pick b", "pick c" — optionally followed by concrete round-1 adjustments (allowed: STROKE value, APERTURE_RADIUS value, MARGIN, LOCKUP_GAP, LETTER_GAP, X_HEIGHT, a specific part's proportion). Or "approved a|b|c" to approve immediately with zero rounds. Or "reject all" to stop the phase.</resume-signal>
  <done>A concept is chosen (or the phase is blocked).</done>
</task>

<task type="auto">
  <name>Task 2: Apply round-1 adjustments (if requested) and re-capture the chosen concept</name>
  <files>packages/ui/src/brand/geometry.ts, packages/ui/src/brand/concepts/a.ts, packages/ui/src/brand/concepts/b.ts, packages/ui/src/brand/concepts/c.ts, packages/ui/src/brand/geometry.test.ts</files>
  <read_first>
    - The user's Task 1 reply (the exact list of adjustments — apply ONLY those; D-16 forbids open-ended polishing)
    - packages/ui/src/brand/geometry.ts and the chosen `concepts/<id>.ts`
    - packages/ui/src/brand/geometry.test.ts (which assertions pin the values you change — update the pinned expectations in the same commit; invariants such as GRID=24, command set, bounds, aperture part must NOT change)
    - docs/brand/review/README.md (regeneration commands)
  </read_first>
  <action>
    If the user typed "approved <id>" in Task 1, skip Tasks 2–5 and go to Task 6 with rounds = 0. Otherwise: apply the requested constant/part changes in `geometry.ts`/the chosen concept file only (never touch the other two concepts unless the change is a shared constant — then note it). Keep every change expressible as a named constant or a documented ratio. Run `pnpm test -- packages/ui/src/brand` and fix pinned expectations that legitimately moved. Re-run `pnpm brand:boards --concept <id>` and `pnpm brand:review --concept <id>` (or attach mode). Commit `feat(07-05): brand round 1 — <one-line summary of the concrete changes>`. If no adjustments were requested with the pick, change nothing and record "round 1: none".
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/brand && pnpm check:ui-safety && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- packages/ui/src/brand` exits 0 after the change
    - `packages/ui/src/brand/geometry.ts` still contains `export const GRID = 24;`
    - If adjustments were applied: a commit whose subject starts with `feat(07-05): brand round 1` exists and the chosen concept's `docs/brand/review/<id>/*.png` have mtimes newer than that commit's parent
    - The SUMMARY lists each applied adjustment as `constant: old → new` (or "round 1: none")
  </acceptance_criteria>
  <done>Round 1 applied exactly as requested (or recorded as none) and re-captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review round 1 in context</name>
  <files>none</files>
  <read_first>
    - The Task 2 SUMMARY notes (which constants changed)
    - docs/brand/review/README.md
  </read_first>
  <action>
    Pause (skip if the user already approved in Task 1). Point the user at the regenerated captures for the chosen concept and wait for the resume signal. No file changes in this task.
  </action>
  <what-built>The chosen concept with round-1 adjustments, re-rendered as a board and re-captured in the app (rail, expanded sidebar, /login, /setup, both themes).</what-built>
  <how-to-verify>Open `docs/brand/review/<id>/board-{light,dark}.png` and the eight in-app captures again. Compare against your round-1 request; check 16 px legibility on the scales row and rail crop.</how-to-verify>
  <verify>
    <human-check>User replied "approved", "round 2: …", or "reject"</human-check>
  </verify>
  <acceptance_criteria>
    - The user's reply is quoted verbatim in the SUMMARY under "Round 1 review"
  </acceptance_criteria>
  <resume-signal>"approved" to approve as is, or "round 2: <concrete adjustments>" for the final adjustment round, or "reject" to stop the phase.</resume-signal>
  <done>Round-1 verdict captured.</done>
</task>

<task type="auto">
  <name>Task 4: Apply round-2 adjustments (if requested) and re-capture</name>
  <files>packages/ui/src/brand/geometry.ts, packages/ui/src/brand/concepts/a.ts, packages/ui/src/brand/concepts/b.ts, packages/ui/src/brand/concepts/c.ts, packages/ui/src/brand/geometry.test.ts</files>
  <read_first>
    - The user's Task 3 reply
    - Same files as Task 2
  </read_first>
  <action>Same procedure as Task 2 for round 2. Commit `feat(07-05): brand round 2 — <summary>`. If the user approved at Task 3, skip to Task 6 with rounds = 1 (or 0 if round 1 was "none"). There is no round 3: if the user is still not satisfied after round 2, Task 5 must end in "approved" or "reject" (D-16).</action>
  <verify>
    <automated>pnpm test -- packages/ui/src/brand && pnpm check:ui-safety && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- packages/ui/src/brand` exits 0
    - If applied: commit subject starts with `feat(07-05): brand round 2` and captures regenerated; the SUMMARY lists each adjustment as `constant: old → new`
  </acceptance_criteria>
  <done>Round 2 applied exactly as requested (or skipped) and re-captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final approval</name>
  <files>none</files>
  <read_first>
    - The Task 4 SUMMARY notes
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-16, D-17
  </read_first>
  <action>
    Pause (skip if already approved at Task 1 or Task 3). Ask for the explicit approval act. No file changes in this task. On "reject", stop and return `## PHASE BLOCKED — concept rejected after two rounds`.
  </action>
  <what-built>The chosen concept after at most two adjustment rounds, captured in context in both themes.</what-built>
  <how-to-verify>Open the final `docs/brand/review/<id>/` set one more time. This is the explicit approval act BRAND-03 requires; after it, the mark will be applied to the sidebar/rail, /login, /setup, favicon, apple-touch-icon, README and exported for the public site.</how-to-verify>
  <verify>
    <human-check>User replied "approved" (optionally with an approver name) or "reject"</human-check>
  </verify>
  <acceptance_criteria>
    - The user's reply is quoted verbatim in the SUMMARY under "Final approval"
  </acceptance_criteria>
  <resume-signal>"approved" (optionally followed by your name as approver — otherwise the git author name is recorded), or "reject" to stop the phase.</resume-signal>
  <done>Explicit approval captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Record the approval, commit the approved captures, set DEFAULT_CONCEPT, add the record test</name>
  <files>docs/brand/APPROVAL.md, docs/brand/approved/README.md, packages/ui/src/brand/geometry.ts, tests/unit/brand/approval-record.test.ts</files>
  <read_first>
    - packages/ui/src/brand/geometry.ts (`DEFAULT_CONCEPT` line and its placeholder comment)
    - tests/unit/docs/install-docs-accuracy.test.ts (analog: read real doc file, assert exact strings)
    - scripts/brand/review-paths.ts (`reviewPngPath` to locate the files to copy)
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-17 (what the record must contain)
    - CLAUDE.md §7 and §7.1 (English for docs/; no Claude attribution anywhere)
  </read_first>
  <behavior>
    - `docs/brand/APPROVAL.md` contains a Markdown table with rows `| Date |`, `| Concept |`, `| Adjustment rounds used |`, `| Approver |`, `| Evidence |`; the Concept cell is one of `a`, `b`, `c` followed by ` — <name>`; the rounds cell matches `/^[0-2] of 2$/`; Date matches `/^\d{4}-\d{2}-\d{2}$/`
    - `DEFAULT_CONCEPT` exported from `packages/ui/src/brand/geometry.ts` equals the id parsed from the Concept row
    - `docs/brand/approved/` contains exactly: `board-light.png`, `board-dark.png`, and `{sidebar-expanded,sidebar-rail,login,setup}-{light,dark}.png` (10 files) plus `README.md`; each PNG is > 1 KB
    - No file under `docs/brand/` contains the strings "Claude", "Anthropic", "Co-Authored-By" (case-insensitive)
  </behavior>
  <action>
    RED first: write `tests/unit/brand/approval-record.test.ts` with the behaviors above; run → fails (file missing). Commit `test(07-05): add failing approval-record test`.

    GREEN: write `docs/brand/APPROVAL.md` (English): title `# Brand approval record`, one paragraph stating that per BRAND-03 the user reviewed the three concepts rendered in the app (rail, expanded sidebar, /login, /setup) and on the brand boards in both themes before any application, then the table (Date = today ISO, Concept = `<id> — <CONCEPT_META name>`, Adjustment rounds used = `<n> of 2`, Approver = the name given or `git config user.name`, Evidence = `docs/brand/approved/`), then a `## Adjustment log` list with one bullet per round (`Round 1: STROKE 4 → 3.5` style, or `none`). Copy the final captures of the chosen concept from `docs/brand/review/<id>/` to `docs/brand/approved/` (the 10 files listed; not the `-crop` files) and add `docs/brand/approved/README.md` (two lines: what these are; regenerate via `pnpm brand:boards --concept <id> && pnpm brand:review --mounted --concept <id>` once 07-07 lands). Set `DEFAULT_CONCEPT = '<id>'` in `geometry.ts` and replace the placeholder comment with "approved 2026-…, see docs/brand/APPROVAL.md". Run `pnpm test -- brand` (all brand unit tests), then commit `docs(07-05): record brand approval — concept <id>`. Record the same facts in the SUMMARY (D-17 requires it in the SUMMARY too).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/brand/approval-record packages/ui/src/brand && git ls-files --error-unmatch docs/brand/APPROVAL.md docs/brand/approved/board-light.png</automated>
  </verify>
  <acceptance_criteria>
    - `docs/brand/APPROVAL.md` exists, tracked by git, contains `| Concept |` and `| Adjustment rounds used |`
    - `grep -E "^export const DEFAULT_CONCEPT: ConceptId = '(a|b|c)';" packages/ui/src/brand/geometry.ts` matches, and the id equals the Concept row's id (the test proves it)
    - `ls docs/brand/approved/*.png | wc -l` prints 10
    - `grep -ril "claude\|anthropic\|co-authored-by" docs/brand/ | wc -l` prints 0
    - `pnpm test -- tests/unit/brand/approval-record` exits 0
    - The SUMMARY contains the lines `Approved concept: <id>`, `Rounds used: <n>`, `Approval date: <date>`, `Approver: <name>`
  </acceptance_criteria>
  <done>Approval recorded in the brand kit and the SUMMARY; approved captures committed; code default equals the record; test enforces it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review PNGs → git history | Screenshots of the running app become permanent repo content |
| human decision → code default | `DEFAULT_CONCEPT` is set from a human reply; a test binds it to the record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Information Disclosure | `docs/brand/approved/*.png` | mitigate | Only the 10 listed files are copied; /login and /setup captures have empty fields (07-04 order of operations); shell captures come from the E2E fixture stack with fixture data; executor visually confirms no real hostnames/tokens appear before committing |
| T-07-14 | Repudiation | approval act | mitigate | Date, concept, rounds, approver recorded in APPROVAL.md and the plan SUMMARY; test enforces presence and consistency with `DEFAULT_CONCEPT` |
| T-07-15 | Tampering | scope creep during rounds | mitigate | Only user-named constants/parts change; max two rounds; each round is a separate commit with the changes in the subject |
| T-07-SC | Tampering | npm installs | accept (n/a) | No installs |
</threat_model>

<verification>
- Human checkpoints answered (Task 1, 3, 5) with an explicit "approved".
- `docs/brand/APPROVAL.md` + `docs/brand/approved/` committed; `approval-record.test.ts` green.
- Nyquist: 07-VALIDATION.md `07-W0-10` → this plan (manual by design) plus the automated record test.
</verification>

<success_criteria>
- One concept chosen from three, ≤ 2 rounds, explicit approval, record in brand kit and SUMMARY (D-13, D-16, D-17).
- Nothing under apps/web changed in this plan (BRAND-02 untouched until the record exists).
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-05-SUMMARY.md` when done — it MUST include `Approved concept`, `Rounds used`, `Approval date`, `Approver`.
</output>
