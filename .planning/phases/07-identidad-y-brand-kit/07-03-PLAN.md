---
phase: 07-identidad-y-brand-kit
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/ui/src/brand/Logo.tsx
  - packages/ui/src/brand/Logo.test.tsx
  - packages/ui/src/brand/Wordmark.tsx
  - packages/ui/src/brand/Wordmark.test.tsx
  - packages/ui/src/brand/Lockup.tsx
  - packages/ui/src/brand/Lockup.test.tsx
  - packages/ui/src/brand/static-svg.ts
  - packages/ui/src/brand/static-svg.test.ts
  - packages/ui/src/index.ts
autonomous: true
requirements: [BRAND-01, BRAND-02]
model_hint: opus
must_haves:
  truths:
    - "Logo, Wordmark and Lockup React components render currentColor SVG from the geometry module — one SVG for the app, theme via the CSS cascade (D-09, D-10)"
    - "Static SVG strings (light ink, dark ink) and the accent-fill favicon tile are produced from the SAME components/paths by passing colours in — never redrawn (D-10, D-11)"
    - "Every SVG part carries a stable data-part attribute (stems, diagonal, aperture, glyphs) so Phase 8 can animate without touching geometry"
    - "Brand components never hardcode a data-testid; the mount site supplies brand-monogram / brand-lockup (PATTERNS.md convention)"
  artifacts:
    - path: "packages/ui/src/brand/Logo.tsx"
      provides: "Logo component (monogram only), props: concept?, size?, title?, color?, aria-hidden?, data-testid?"
      contains: "fill={color ?? 'currentColor'}"
    - path: "packages/ui/src/brand/Wordmark.tsx"
      provides: "Wordmark component ('noodara' lowercase)"
    - path: "packages/ui/src/brand/Lockup.tsx"
      provides: "Lockup component (monogram + wordmark, horizontal)"
    - path: "packages/ui/src/brand/static-svg.ts"
      provides: "renderStaticSvg({kind, concept, color}) and tileSvg({concept, background, ink, radiusRatio})"
      exports: ["renderStaticSvg", "tileSvg", "STATIC_KINDS"]
    - path: "packages/ui/src/index.ts"
      provides: "barrel exports for Logo, Wordmark, Lockup, CONCEPT_IDS, DEFAULT_CONCEPT, ConceptId"
      contains: "export { Logo }"
  key_links:
    - from: "packages/ui/src/brand/Logo.tsx"
      to: "packages/ui/src/brand/geometry.ts"
      via: "monogramParts(concept)"
      pattern: "monogramParts\\("
    - from: "packages/ui/src/brand/static-svg.ts"
      to: "react-dom/server renderToStaticMarkup"
      via: "createElement(Logo|Wordmark|Lockup, { color })"
      pattern: "renderToStaticMarkup"
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/brand/*"
      via: "named re-exports"
      pattern: "from './brand/"
---

<objective>
Turn the geometry into the three brand components (`Logo`, `Wordmark`, `Lockup`) that the app, the README exports, the brand boards and the favicon pipeline all consume, plus a `static-svg.ts` helper that renders those same components to standalone SVG strings with a fixed ink colour (for README/site) or as the accent-fill tile (favicon). Component tests first; no `dangerouslySetInnerHTML`, no colour literals, testids supplied by callers.

Purpose: D-10 ("un solo SVG con currentColor en la app; dos SVG estáticos generados desde la misma fuente") is only true if the static export and the live component share one render path. This plan is that shared path.

Output: six component/test files, `static-svg.ts` + test, barrel exports in `packages/ui/src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-PATTERNS.md
@.planning/phases/07-identidad-y-brand-kit/07-01-SUMMARY.md
@packages/ui/src/brand/geometry.ts
@packages/ui/src/ThemeToggle.tsx
@packages/ui/src/ThemeToggle.test.tsx
@packages/ui/src/Button.tsx
@packages/ui/src/testing/render.tsx
@packages/ui/src/index.ts
@scripts/check-ui-safety.mjs
@.claude/skills/noodara-tdd/SKILL.md
@.claude/skills/noodara-ux-apple/SKILL.md

<interfaces>
<!-- Contract this plan CREATES. 07-04, 07-06, 07-07 code against these. -->

From packages/ui/src/brand/Logo.tsx:
```typescript
export interface LogoProps {
  readonly concept?: ConceptId;            // defaults to DEFAULT_CONCEPT
  readonly size?: number;                  // CSS px for width and height; default 24
  readonly title?: string;                 // when set: role="img" + <title>; when unset: aria-hidden="true"
  readonly color?: string;                 // STATIC EXPORT ONLY — never pass in app code; default 'currentColor'
  readonly 'data-testid'?: string | undefined;
}
export function Logo(props: LogoProps): JSX.Element;
// renders <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width height fill fill-rule="evenodd" ...>
//   <g data-part="monogram"><path data-part="stem-left" d=.../> ... <path data-part="aperture" d=.../></g>
```

From packages/ui/src/brand/Wordmark.tsx:
```typescript
export interface WordmarkProps { readonly concept?: ConceptId; readonly height?: number; readonly title?: string; readonly color?: string; readonly 'data-testid'?: string | undefined; }
export function Wordmark(props: WordmarkProps): JSX.Element;  // viewBox="0 0 {wordmarkWidth} 24", width derived from height * ratio
```

From packages/ui/src/brand/Lockup.tsx:
```typescript
export interface LockupProps { readonly concept?: ConceptId; readonly height?: number; readonly title?: string; readonly color?: string; readonly 'data-testid'?: string | undefined; }
export function Lockup(props: LockupProps): JSX.Element;      // <g data-part="monogram"> + <g data-part="wordmark" transform="translate(x y)">
```

From packages/ui/src/brand/static-svg.ts:
```typescript
export const STATIC_KINDS = ['monogram', 'wordmark', 'lockup'] as const;
export type StaticKind = (typeof STATIC_KINDS)[number];
export interface StaticSvgOptions { readonly kind: StaticKind; readonly concept: ConceptId; readonly color: string; }
export function renderStaticSvg(options: StaticSvgOptions): string;   // renderToStaticMarkup output + trailing "\n"
export interface TileSvgOptions { readonly concept: ConceptId; readonly background: string; readonly ink: string; readonly size: number; readonly radiusRatio: number; }
export function tileSvg(options: TileSvgOptions): string;             // rounded rect background + monogram at 62% of size, centred
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED component tests, then Logo / Wordmark / Lockup</name>
  <files>packages/ui/src/brand/Logo.test.tsx, packages/ui/src/brand/Wordmark.test.tsx, packages/ui/src/brand/Lockup.test.tsx, packages/ui/src/brand/Logo.tsx, packages/ui/src/brand/Wordmark.tsx, packages/ui/src/brand/Lockup.tsx</files>
  <read_first>
    - packages/ui/src/ThemeToggle.tsx (props interface with `readonly 'data-testid'?: string`, doc-comment convention naming spec sections and invariants)
    - packages/ui/src/ThemeToggle.test.tsx (renderUi/screen harness usage; `renderToString` determinism test at lines 135-181)
    - packages/ui/src/Button.tsx (`'data-testid'?: string | undefined` under exactOptionalPropertyTypes)
    - packages/ui/src/testing/render.tsx (import `renderUi`, `screen` from `./testing/render.js` — never from `@noodara/ui/testing` in these files)
    - packages/ui/src/brand/geometry.ts (monogramParts, wordmarkParts, wordmarkWidth, lockupLayout, GRID, DEFAULT_CONCEPT)
    - scripts/check-ui-safety.mjs (gates: exactly one dangerouslySetInnerHTML repo-wide, zero hex/rgb, zero `outline: none`)
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-04, D-09, D-10 and Deferred Ideas (animation is Phase 8 — leave hooks, add no motion)
  </read_first>
  <behavior>
    - `renderUi(<Logo data-testid="brand-monogram" />)`: `screen.getByTestId('brand-monogram')` is an `svg` with `fill="currentColor"`, `viewBox="0 0 24 24"`, `width="24"`, `height="24"`, `fill-rule="evenodd"`, `aria-hidden="true"`, no `role`
    - `<Logo title="Noodara" />`: `role="img"`, contains a `<title>` with text "Noodara", no `aria-hidden`
    - `<Logo size={64} />`: `width="64"` and `height="64"`
    - `<Logo concept="c" />` renders `path[data-part="frame"]`; `<Logo concept="a" />` renders `path[data-part="aperture"]` and `path[data-part="stem-left"]`; the DOM order of `data-part` attributes equals the order of `monogramParts(concept)`
    - Every `<Logo>` render contains a `g[data-part="monogram"]` wrapping all paths
    - `<Logo color="red" />` sets `fill="red"` (static-export escape hatch) — test uses a colour NAME, never a hex, so the test file stays gate-neutral even though tests are excluded
    - `<Wordmark />`: `svg` with `viewBox="0 0 {wordmarkWidth(DEFAULT_CONCEPT)} 24"`, seven `path[data-part^="glyph-"]`, height default 24 and width = `Math.round(24 * wordmarkWidth / 24)`
    - `<Lockup />`: contains `g[data-part="monogram"]` AND `g[data-part="wordmark"]`; the wordmark group has a `transform` attribute starting with `translate(`; `viewBox` width equals `lockupLayout(concept).width`
    - `renderToStaticMarkup(<Lockup />)` called twice yields identical strings (determinism, feeds the exactness test)
    - The markup string of each component contains none of: `<script`, `href=`, `<foreignObject`, `on[a-z]+=` (regex `/\son[a-z]+=/i`), `style=`, `#` (no hex, no fragment refs)
    - No component file contains `dangerouslySetInnerHTML` (assert by reading the source file in the test with `readFileSync(new URL('./Logo.tsx', import.meta.url))`)
  </behavior>
  <action>
    RED first: write the three `*.test.tsx` files (dom project, jsdom) importing from `./Logo.js` etc. and `./testing/render.js`. Run `pnpm test -- packages/ui/src/brand` → three failing suites. Commit `test(07-03): add failing brand component tests`.

    GREEN: implement the three components as plain function components with JSX `<svg>`/`<g>`/`<path>` (never raw strings). Rules:
    - `fill={color ?? 'currentColor'}` on the root `<svg>`; `fillRule="evenodd"` on the root; `xmlns="http://www.w3.org/2000/svg"` on the root so `renderToStaticMarkup` output is a valid standalone SVG.
    - `aria-hidden` is `true` when no `title`; when `title` is set render `role="img"` and `<title>{title}</title>` as the first child (matches RESEARCH Pattern 1 example).
    - Parts render as `<path data-part={part} d={d} />` inside `<g data-part="monogram">` / `<g data-part="wordmark">`. Use `data-part`, NOT `id` — the Sidebar will mount Logo and Lockup simultaneously (CSS-hidden per breakpoint) and duplicate `id`s would be invalid HTML; Phase 8 animates via `[data-part="aperture"]`. Say this in the header comment.
    - `Lockup` composes `lockupParts(concept)` and `lockupLayout(concept)`: monogram group untransformed, wordmark group `transform={\`translate(${fmt(layout.wordmarkX)} ${fmt(layout.wordmarkY)})\`}` (use `fmt` from geometry for byte-stable output); `viewBox="0 0 {width} {height}"`; `height` prop default 24, `width` computed = `Math.round(height * layout.width / layout.height)`.
    - Header doc-comments (English) per PATTERNS.md: decisions satisfied (D-03/D-04/D-05/D-08/D-09/D-10), `geometry.ts` as the single source, the `color` prop is for the static-export path only (app code must never pass it — 07-07's Sidebar/AuthCard tests assert `fill="currentColor"`), and the no-`dangerouslySetInnerHTML` invariant.
    - Do not add `className` props (matches `Button` omitting className); sizing is via `size`/`height` only; wrappers at the mount site handle layout classes.
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/brand && pnpm check:ui-safety</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/brand/Logo.tsx`, `Wordmark.tsx`, `Lockup.tsx` exist; each contains `fill={color ?? 'currentColor'}` and `xmlns="http://www.w3.org/2000/svg"`
    - `grep -c "dangerouslySetInnerHTML" packages/ui/src/brand/*.tsx` prints 0 for each file (`grep -L` lists all three)
    - `grep -c 'data-part="monogram"' packages/ui/src/brand/Logo.tsx packages/ui/src/brand/Lockup.tsx` prints 1 for each
    - `grep -cE "\bid=" packages/ui/src/brand/*.tsx` prints 0 for each file
    - `pnpm test -- packages/ui/src/brand` exits 0 with at least 20 passing component tests across the three files
    - `pnpm check:ui-safety` exits 0 (count for dangerouslySetInnerHTML remains exactly 1, hex literals 0)
    - RED commit `test(07-03)` precedes the GREEN commit
  </acceptance_criteria>
  <done>Three components render currentColor SVG with stable data-part hooks; tests cover a11y props, sizing, part order, determinism and markup safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED static-svg tests, then renderStaticSvg + tileSvg</name>
  <files>packages/ui/src/brand/static-svg.test.ts, packages/ui/src/brand/static-svg.ts</files>
  <read_first>
    - packages/ui/src/brand/Logo.tsx, Wordmark.tsx, Lockup.tsx (Task 1 output)
    - packages/ui/src/contrast.ts (`parseTokensCss` signature — the CALLERS of `tileSvg` will read `--accent-fill`, `--on-accent`, `--ink`, `--canvas` from tokens.css with it; `static-svg.ts` itself must take colours as arguments and contain no literals)
    - packages/ui/tokens.css (radii `--r-sm: 6px`, `--r-md: 10px`, `--r-lg: 16px` — the tile corner radius is expressed as a RATIO of the tile size so the favicon's corner matches the system radius family: use `radiusRatio` = 10/44 ≈ 0.227, i.e. `--r-md` on a 44 px control, passed in by the caller)
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-10, D-11
    - .planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md §Code Examples (exactness test sketch — `renderStaticSvg` is what that test will call)
  </read_first>
  <behavior>
    - `renderStaticSvg({ kind: 'monogram', concept: 'a', color: 'black' })` starts with `<svg xmlns="http://www.w3.org/2000/svg"` and ends with `</svg>\n`; contains `fill="black"`; contains no `currentColor`
    - `renderStaticSvg({ kind: 'lockup', … })` contains both `data-part="monogram"` and `data-part="wordmark"`; `kind: 'wordmark'` contains seven `data-part="glyph-`
    - Output is deterministic (two calls equal) and contains no `<script`, `href=`, `<foreignObject`, `/\son[a-z]+=/i`, `style=`
    - `tileSvg({ concept: 'a', background: 'blue', ink: 'white', size: 512, radiusRatio: 0.227 })`: root `<svg viewBox="0 0 512 512" width="512" height="512">`; first child is `<rect width="512" height="512" rx="116" ry="116" fill="blue"/>` (rx = round(size * radiusRatio)); the monogram group is wrapped in `<g transform="translate(… …) scale(…)">` sized to 62% of the tile and centred; monogram fill is `ink`; contains `data-part="monogram"`
    - `tileSvg` never contains `currentColor`
    - `STATIC_KINDS` equals `['monogram','wordmark','lockup']`
    - Source-level: `static-svg.ts` contains no `#` character outside comments and no `rgb(` (assert by reading the file)
  </behavior>
  <action>
    RED first: write `static-svg.test.ts` (packages project, node env — `.test.ts`, use `createElement` if you need elements; you do not, the API takes plain options). Run → fails. Commit `test(07-03): add failing static-svg tests`.

    GREEN: `static-svg.ts` imports `renderToStaticMarkup` from `react-dom/server` and `createElement` from `react`; `renderStaticSvg` maps `kind` to the component, renders with `{ concept, color, title: 'Noodara' }`? — NO title: static files are decorative images consumed via `<img alt>`; render with no `title` so `aria-hidden="true"` is present (README `<img alt="Noodara">` carries the accessible name). Append `"\n"`. `tileSvg` builds the rounded-rect + a `<g transform>` that wraps `renderToStaticMarkup(createElement(Logo, { concept, color: ink }))`'s inner `<g data-part="monogram">…</g>` — extract it with a single regex on the rendered string (`/<g data-part="monogram">[\s\S]*?<\/g>/`) and assert (throw) if not found; do not hand-write any path. Scale = `size * 0.62 / GRID`, translate = `(size - size*0.62) / 2`. Header comment (English) names D-11 (the only blue surface) and that colours are ALWAYS injected by the caller from `tokens.css` via `parseTokensCss` — this file must stay free of literals so `check:ui-safety` (which scans `packages/ui/src/**/*.ts`) stays green.
  </action>
  <verify>
    <automated>pnpm test -- packages/ui/src/brand/static-svg && pnpm check:ui-safety && pnpm --filter @noodara/ui typecheck && pnpm --filter @noodara/ui lint</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/brand/static-svg.ts` exports `renderStaticSvg`, `tileSvg`, `STATIC_KINDS` (grep each `export`)
    - `grep -v '^\s*//' packages/ui/src/brand/static-svg.ts | grep -v '^\s*\*' | grep -c '#'` prints 0
    - `grep -c "renderToStaticMarkup" packages/ui/src/brand/static-svg.ts` ≥ 1 and `grep -c "<path" packages/ui/src/brand/static-svg.ts` prints 0 (no hand-written paths)
    - `pnpm test -- packages/ui/src/brand/static-svg` exits 0; `pnpm check:ui-safety` exits 0
    - RED commit `test(07-03)` precedes GREEN
  </acceptance_criteria>
  <done>Static SVG strings and the favicon tile derive from the same components; colours are injected; no literals in source.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel exports and package typecheck/lint</name>
  <files>packages/ui/src/index.ts</files>
  <read_first>
    - packages/ui/src/index.ts (existing export style: named exports with `.js` suffix, no wildcard exports of testing)
    - packages/ui/src/brand/geometry.ts (what to re-export: `CONCEPT_IDS`, `DEFAULT_CONCEPT`, `CONCEPT_META`, `type ConceptId`)
    - vitest.shared.ts (`@noodara/ui` alias → `packages/ui/src/index.ts`; apps/web tests resolve the barrel)
  </read_first>
  <action>
    Append to `packages/ui/src/index.ts`, under a `// Brand (07-03-PLAN.md): …` comment: `export { Logo, type LogoProps } from './brand/Logo.js';`, same for `Wordmark`/`WordmarkProps` and `Lockup`/`LockupProps`, and `export { CONCEPT_IDS, CONCEPT_META, DEFAULT_CONCEPT, type ConceptId } from './brand/geometry.js';`. Do NOT export `static-svg.ts`, `glyphs.ts`, or the concept files from the barrel — they are consumed by scripts via relative paths, and the barrel is what `apps/web` sees. Run the package build to ensure `dist/` emits `.d.ts` for the new files (`pnpm --filter @noodara/ui build`), then `pnpm --filter @noodara/ui typecheck && pnpm --filter @noodara/ui lint`.
  </action>
  <verify>
    <automated>pnpm --filter @noodara/ui build && pnpm --filter @noodara/ui typecheck && pnpm --filter @noodara/ui lint && pnpm test -- packages/ui/src/brand && pnpm check:ui-safety</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from './brand/Logo.js'" packages/ui/src/index.ts` prints 1; likewise for `Wordmark.js`, `Lockup.js`, `geometry.js`
    - `grep -c "static-svg\|glyphs\|concepts/" packages/ui/src/index.ts` prints 0
    - `ls packages/ui/dist/brand/Logo.js packages/ui/dist/brand/Logo.d.ts` succeeds after build
    - All four commands in `<verify>` exit 0
  </acceptance_criteria>
  <done>`import { Logo, Lockup, Wordmark, DEFAULT_CONCEPT } from '@noodara/ui'` resolves for apps/web and for scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| geometry strings → SVG attributes | `d` strings from 07-01 are interpolated into JSX attributes (React escapes) and into `renderToStaticMarkup` output committed to the repo and later served/embedded |
| `color` prop → attribute | A string from a script caller becomes a `fill` attribute in a committed SVG; only tokens.css values ever flow here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering / EoP | committed static SVGs | mitigate | Components render via JSX only (React attribute escaping); tests assert output contains no `<script`, `href=`, `<foreignObject`, `on*=`, `style=`; `check:ui-safety` keeps `dangerouslySetInnerHTML` count at exactly 1 (layout.tsx) |
| T-07-07 | Tampering | `color` prop misuse in app | mitigate | Header comments forbid passing `color` in app code; 07-07 mount-point tests assert `fill="currentColor"` on both surfaces |
| T-07-08 | Information Disclosure | none | accept | Components are pure; no env, no network, no user data |
| T-07-SC | Tampering | npm installs | accept (n/a) | No new packages in this plan |
</threat_model>

<verification>
- `pnpm test -- packages/ui/src/brand` green (geometry + 3 component suites + static-svg).
- `pnpm check:ui-safety`, `pnpm --filter @noodara/ui typecheck|lint|build` green.
- Nyquist: 07-VALIDATION.md `07-W0-04` (static gate) exercised here; `07-W0-02` prerequisite (`renderStaticSvg`) delivered for 07-06.
</verification>

<success_criteria>
- Three components consume `geometry.ts` directly; no duplicated coordinates; `currentColor` default; caller-supplied testids.
- Static export path and favicon tile share the component render (D-10, D-11).
- `data-part` hooks present on every part for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-03-SUMMARY.md` when done.
</output>
