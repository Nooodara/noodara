---
phase: 07-identidad-y-brand-kit
plan: 06
type: execute
wave: 5
depends_on: ["07-05", "07-02", "07-03"]
files_modified:
  - scripts/brand/generate-brand-assets.ts
  - scripts/brand/asset-manifest.ts
  - tests/unit/brand/asset-manifest.test.ts
  - tests/unit/brand/brand-assets-accuracy.test.ts
  - tests/unit/brand/package-exports-resolvable.test.ts
  - packages/ui/package.json
  - packages/ui/brand/monogram-light.svg
  - packages/ui/brand/monogram-dark.svg
  - packages/ui/brand/wordmark-light.svg
  - packages/ui/brand/wordmark-dark.svg
  - packages/ui/brand/lockup-light.svg
  - packages/ui/brand/lockup-dark.svg
  - packages/ui/brand/favicon.svg
  - packages/ui/brand/favicon.ico
  - packages/ui/brand/apple-touch-icon.png
  - packages/ui/brand/icon-192.png
  - packages/ui/brand/icon-512.png
  - packages/ui/brand/og-image.png
  - packages/ui/brand/brand-colors.json
  - package.json
autonomous: true
requirements: [BRAND-01, BRAND-02]
model_hint: sonnet
must_haves:
  truths:
    - "Every static brand asset (light/dark SVGs, favicon SVG+ICO, apple-touch-icon 180, PWA 192/512, OG 1200×630, manifest colours) is generated by one script from the approved geometry and committed under packages/ui/brand/ (D-10, D-11, D-12)"
    - "A unit test regenerates the SVGs and JSON in memory and fails on any byte drift; PNG/ICO are checked for dimensions, alpha and container header (single source of truth, success criterion 4)"
    - "packages/ui exports ./brand/* so Phase 10 resolves @noodara/ui/brand/<file> without copying (success criterion 4)"
    - "The only place #0071e3 exists is inside generated .svg/.png/.ico/.json files — never in .ts/.tsx/.css"
  artifacts:
    - path: "scripts/brand/asset-manifest.ts"
      provides: "ASSET_FILES list, buildTextAssets(tokens) → Map<filename, string>, rasterSpecs(tokens) → list of {filename, kind, size, background?}"
      exports: ["ASSET_FILES", "buildTextAssets", "rasterSpecs", "BRAND_DIR"]
    - path: "scripts/brand/generate-brand-assets.ts"
      provides: "CLI: writes packages/ui/brand/*; --check exits 1 on drift"
    - path: "packages/ui/brand/lockup-dark.svg"
      provides: "README/site dark variant"
    - path: "packages/ui/brand/brand-colors.json"
      provides: "{ themeColor, backgroundColor } from tokens.css for manifest.ts"
      contains: "themeColor"
    - path: "tests/unit/brand/brand-assets-accuracy.test.ts"
      provides: "Exactness + safety tests for committed assets"
      min_lines: 60
    - path: "packages/ui/package.json"
      provides: "exports './brand/*'"
      contains: "\"./brand/*\": \"./brand/*\""
  key_links:
    - from: "scripts/brand/asset-manifest.ts"
      to: "packages/ui/src/brand/static-svg.ts"
      via: "renderStaticSvg / tileSvg with colours from parseTokensCss(tokens.css)"
      pattern: "renderStaticSvg\\("
    - from: "scripts/brand/generate-brand-assets.ts"
      to: "scripts/brand/raster.ts"
      via: "svgToPng / pngsToIco"
      pattern: "svgToPng"
    - from: "tests/unit/brand/brand-assets-accuracy.test.ts"
      to: "packages/ui/brand/*.svg"
      via: "readFileSync + buildTextAssets equality"
      pattern: "buildTextAssets"
---

<objective>
Generate and commit the complete static asset set for the approved concept from the geometry module, expose it through `packages/ui`'s `exports` map, and lock it with an exactness test so it can never drift from the components. Mechanical work: the design is already approved (07-05).

Purpose: D-10 (two static SVGs per lockup, generated never drawn), D-11 (blue tile favicon family), D-12 (OG image), ROADMAP success criterion 4 (Phase 10 consumes `@noodara/ui/brand/*`).

Output: `packages/ui/brand/` (13 files), `scripts/brand/{asset-manifest,generate-brand-assets}.ts`, three unit tests, `"./brand/*"` export, root scripts `brand:generate` / `brand:check`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md
@.planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md
@.planning/phases/07-identidad-y-brand-kit/07-PATTERNS.md
@.planning/phases/07-identidad-y-brand-kit/07-05-SUMMARY.md
@.planning/phases/07-identidad-y-brand-kit/07-02-SUMMARY.md
@packages/ui/src/brand/static-svg.ts
@packages/ui/src/brand/geometry.ts
@packages/ui/src/contrast.ts
@packages/ui/tokens.css
@packages/ui/package.json
@scripts/brand/raster.ts
@scripts/brand/write-if-changed.ts
@tests/unit/docs/install-docs-accuracy.test.ts
@docs/brand/APPROVAL.md

<interfaces>
From scripts/brand/asset-manifest.ts (to be created):
```typescript
export const BRAND_DIR: string; // <repo>/packages/ui/brand
export const ASSET_FILES = ['monogram-light.svg','monogram-dark.svg','wordmark-light.svg','wordmark-dark.svg','lockup-light.svg','lockup-dark.svg','favicon.svg','favicon.ico','apple-touch-icon.png','icon-192.png','icon-512.png','og-image.png','brand-colors.json'] as const;
export interface BrandTokens { readonly inkLight: string; readonly inkDark: string; readonly canvasDark: string; readonly accentFill: string; readonly onAccent: string; readonly fontSans: string; }
export function readBrandTokens(tokensCss: string): BrandTokens;         // via parseTokensCss; throws if any token missing
export function buildTextAssets(tokens: BrandTokens, concept: ConceptId): ReadonlyMap<string, string>;   // the 7 svgs + brand-colors.json (pretty JSON + "\n")
export interface RasterSpec { readonly filename: string; readonly svg: string; readonly width: number; readonly height: number; readonly background?: string; }
export function rasterSpecs(tokens: BrandTokens, concept: ConceptId): readonly RasterSpec[];  // apple-touch-icon 180 (opaque), icon-192, icon-512, og-image 1200×630 (opaque) + the 16/32/48 tile PNGs used only to pack favicon.ico
export function ogSvg(tokens: BrandTokens, concept: ConceptId): string;  // 1200×630 SVG: canvasDark rect, lockup in inkDark, tagline text in fontSans
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED asset-manifest tests, then the pure asset manifest (text assets, raster specs, OG SVG)</name>
  <files>tests/unit/brand/asset-manifest.test.ts, scripts/brand/asset-manifest.ts</files>
  <read_first>
    - packages/ui/src/brand/static-svg.ts (`renderStaticSvg`, `tileSvg`, `STATIC_KINDS`)
    - packages/ui/src/contrast.ts (`parseTokensCss` exact return shape and key format — are keys `--ink` or `ink`? read and match)
    - packages/ui/tokens.css (`--ink` light `#1d1d1f` / dark `#f5f5f7`; `--canvas` dark `#161618`; `--accent-fill` `#0071e3`; `--on-accent` `#ffffff`; `--font-sans` stack; `--text-title-*` for the tagline size)
    - .planning/phases/07-identidad-y-brand-kit/07-CONTEXT.md D-10, D-11, D-12
    - .planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md §Common Pitfalls #5 (apple-touch-icon must be opaque)
    - packages/ui/src/brand/geometry.ts (`DEFAULT_CONCEPT`, `lockupLayout` for OG centring maths)
  </read_first>
  <behavior>
    - `readBrandTokens(readFileSync('packages/ui/tokens.css'))` returns `inkLight`, `inkDark`, `canvasDark`, `accentFill`, `onAccent` each matching `/^#[0-9a-f]{6}$/i` and `fontSans` containing `Inter`
    - `buildTextAssets(tokens, DEFAULT_CONCEPT)` has exactly 8 keys: the 7 `.svg` names and `brand-colors.json`
    - `monogram-light.svg` equals `renderStaticSvg({kind:'monogram', concept: DEFAULT_CONCEPT, color: tokens.inkLight})`; `lockup-dark.svg` equals the dark-ink lockup; all six ink SVGs contain their exact ink hex and no `currentColor`
    - `favicon.svg` equals `tileSvg({ concept, background: tokens.accentFill, ink: tokens.onAccent, size: 512, radiusRatio: 10/44 })` — contains `#0071e3` (the ONLY blue surface, D-11)
    - `brand-colors.json` parses to `{ themeColor: tokens.accentFill, backgroundColor: tokens.canvasDark }` and ends with `\n`
    - `rasterSpecs(tokens, concept)` contains entries `apple-touch-icon.png` 180×180 with `background === tokens.accentFill`, `icon-192.png` 192×192, `icon-512.png` 512×512 (transparent — PWA maskable is out of scope; plain icons), `og-image.png` 1200×630 with `background === tokens.canvasDark`, plus `favicon-16.png`, `favicon-32.png`, `favicon-48.png` (tile, marked `packInto: 'favicon.ico'`, not written to disk)
    - `ogSvg(tokens, concept)`: `viewBox="0 0 1200 630"`, first child rect fill `tokens.canvasDark`, contains `data-part="monogram"` and `data-part="wordmark"` in `tokens.inkDark`, contains a `<text>` with exactly `Your infrastructure, understood.` using `font-family="<tokens.fontSans>"` and `fill="<tokens.inkDark>"`, contains no `<script`, no `href`, no `filter`, no `<linearGradient` (D-12: no gradients/glow/shadow)
    - Every SVG string across `buildTextAssets` and `ogSvg` passes the safety regexes (no `<script`, `href=`, `<foreignObject`, `/\son[a-z]+=/i`)
  </behavior>
  <action>
    RED first: write `tests/unit/brand/asset-manifest.test.ts` (root project, `.test.ts`, relative imports to `scripts/brand/asset-manifest.ts` and `packages/ui/src/brand/*.ts`). Run → fails. Commit `test(07-06): add failing asset-manifest tests`.

    GREEN: implement `asset-manifest.ts` with NO colour literals (all from `readBrandTokens`), NO hand-written paths (compose via `renderStaticSvg`/`tileSvg` and, for the OG, wrap the lockup's rendered inner groups — extract `<g data-part="monogram">…</g>` and `<g data-part="wordmark" …>…</g>` from `renderStaticSvg({kind:'lockup', color: inkDark})` with one regex each, throwing if absent — inside a `<g transform="translate(x y) scale(s)">` sized so the lockup is 560 px wide and vertically placed at y≈230, with the tagline `<text>` at y≈420, `font-size="40"`, `letter-spacing="-0.8"` (the display role's −0.02em), `text-anchor="start"` aligned with the lockup's left edge). The tile radius ratio `10/44` is a named constant `TILE_RADIUS_RATIO` with a comment: `--r-md` (10 px) on a 44 px control, so the favicon corner belongs to the system radius family. Header comment (English) citing D-10/D-11/D-12 and stating this module is pure (no I/O) so the exactness test can call it.
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/brand/asset-manifest && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/brand/asset-manifest.ts` exports `ASSET_FILES`, `BRAND_DIR`, `readBrandTokens`, `buildTextAssets`, `rasterSpecs`, `ogSvg`
    - `grep -v '^\s*//' scripts/brand/asset-manifest.ts | grep -v '^\s*\*' | grep -cE "#[0-9a-fA-F]{3,8}\b"` prints 0
    - `grep -c "<path" scripts/brand/asset-manifest.ts` prints 0
    - `grep -c "Your infrastructure, understood." scripts/brand/asset-manifest.ts` prints 1
    - `pnpm test -- tests/unit/brand/asset-manifest` exits 0; `pnpm typecheck`, `pnpm lint` exit 0
    - RED commit precedes GREEN
  </acceptance_criteria>
  <done>A pure manifest describes every asset; colours flow from tokens.css; OG composition satisfies D-12.</done>
</task>

<task type="auto">
  <name>Task 2: generate-brand-assets.ts (write + --check), run it, commit packages/ui/brand/*, add exports and scripts</name>
  <files>scripts/brand/generate-brand-assets.ts, packages/ui/brand/monogram-light.svg, packages/ui/brand/monogram-dark.svg, packages/ui/brand/wordmark-light.svg, packages/ui/brand/wordmark-dark.svg, packages/ui/brand/lockup-light.svg, packages/ui/brand/lockup-dark.svg, packages/ui/brand/favicon.svg, packages/ui/brand/favicon.ico, packages/ui/brand/apple-touch-icon.png, packages/ui/brand/icon-192.png, packages/ui/brand/icon-512.png, packages/ui/brand/og-image.png, packages/ui/brand/brand-colors.json, packages/ui/package.json, package.json</files>
  <read_first>
    - scripts/brand/asset-manifest.ts (Task 1), scripts/brand/raster.ts and write-if-changed.ts (07-02; note `RASTER_BACKEND`)
    - scripts/capture-discovery-fixtures.mjs (header comment style, idempotent ledger, `main().catch`)
    - packages/ui/package.json (exports map; add `"./brand/*": "./brand/*"` after `"./theme.css"`; also add `"files"`? — not needed, package is private)
    - package.json (root scripts block; add `brand:generate` and `brand:check`)
    - .planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md §Anti-Patterns (never regenerate on `next build`; regenerate only via this script)
  </read_first>
  <action>
    `generate-brand-assets.ts`: header comment (English): D-10 makes this the only legitimate way `packages/ui/brand/*` is written; re-run after any geometry change; never hand-edit; PNG/ICO bytes may differ across sharp/libvips versions so tests check their dimensions, not bytes. Flow: read tokens.css → `readBrandTokens` → `buildTextAssets(tokens, DEFAULT_CONCEPT)` → for each: `writeIfChanged(join(BRAND_DIR, name), content)`. Then `rasterSpecs` → `svgToPng` per spec (with `background` when set); the three tile PNGs are packed via `pngsToIco` into `favicon.ico`; the rest written. Mode `--check`: instead of writing, compare text assets byte-for-byte with disk (report each differing filename) and for each PNG/ICO compare `pngMetadata` width/height/hasAlpha (ICO: header entry count = 3) against the spec; exit 1 with a list if anything differs, exit 0 otherwise — this is what CI runs (07-10). Only files named in `ASSET_FILES` are ever written; the script never deletes. Add root scripts `"brand:generate": "tsx scripts/brand/generate-brand-assets.ts"` and `"brand:check": "tsx scripts/brand/generate-brand-assets.ts --check"`. Add `"./brand/*": "./brand/*"` to `packages/ui/package.json` exports.

    Run `pnpm brand:generate`; inspect: `ls -la packages/ui/brand` (13 files); open `packages/ui/brand/og-image.png` and `apple-touch-icon.png` with the Read tool and confirm the lockup/tile rendered and the tagline text is present in the OG (if the text is missing or a fallback box font rendered, `sharp`/librsvg lacks fonts on this machine: switch ONLY the OG spec to the Playwright rasterizer path from 07-02 by adding `backend: 'playwright'` to that spec and handling it in the generator — record this in the SUMMARY). Run `pnpm brand:check` → exit 0. Run `pnpm check:ui-safety` (must still be 0 hex outside tokens.css — the gate never scans `packages/ui/brand/`). Commit assets + script + package.json changes: `feat(07-06): generate brand assets from the approved concept`.
  </action>
  <verify>
    <automated>pnpm brand:check && pnpm check:ui-safety && pnpm typecheck && pnpm lint && test "$(ls packages/ui/brand | wc -l | tr -d ' ')" = "13"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/brand/` contains exactly the 13 `ASSET_FILES`; every file > 100 bytes
    - `grep -c "#0071e3" packages/ui/brand/favicon.svg` ≥ 1; `grep -c "#0071e3" packages/ui/brand/lockup-light.svg` prints 0
    - `head -c 8 packages/ui/brand/icon-512.png | xxd -p` prints `89504e470d0a1a0a`; `head -c 4 packages/ui/brand/favicon.ico | xxd -p` prints `00000100`
    - `packages/ui/package.json` contains `"./brand/*": "./brand/*"`
    - `package.json` contains `"brand:generate"` and `"brand:check"` scripts
    - `pnpm brand:check` exits 0 right after `pnpm brand:generate` (idempotent); running `pnpm brand:generate` a second time prints a "no files changed" line
    - `pnpm check:ui-safety` exits 0
    - The SUMMARY records the OG rasterization backend actually used
  </acceptance_criteria>
  <done>All 13 assets generated, committed and exported; `--check` mode proves idempotence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exactness + safety test for committed assets, and export-resolvability test</name>
  <files>tests/unit/brand/brand-assets-accuracy.test.ts, tests/unit/brand/package-exports-resolvable.test.ts</files>
  <read_first>
    - tests/unit/docs/install-docs-accuracy.test.ts (analog: one `it` per invariant, read real files with cwd-relative paths)
    - scripts/brand/asset-manifest.ts and scripts/brand/raster.ts (`pngMetadata`)
    - packages/ui/package.json (exports) and vitest.shared.ts (the `@noodara/ui` alias only covers the bare specifier and `/testing` — `@noodara/ui/brand/*` must resolve through the REAL package exports via the workspace symlink, which is exactly what this test proves)
    - .planning/phases/07-identidad-y-brand-kit/07-RESEARCH.md §Validation Architecture and §Security Domain (V5 defense-in-depth assertion)
  </read_first>
  <behavior>
    - For each of the 7 SVGs and `brand-colors.json`: `readFileSync('packages/ui/brand/<name>','utf8') === buildTextAssets(readBrandTokens(tokens.css), DEFAULT_CONCEPT).get(name)` — one `it` per file naming the file
    - `apple-touch-icon.png` is 180×180 and `hasAlpha === false`; `icon-192.png` 192×192; `icon-512.png` 512×512; `og-image.png` 1200×630 and `hasAlpha === false`
    - `favicon.ico` starts with `00 00 01 00` and has entry count 3; its entries' width bytes are 16, 32, 48
    - Every `.svg` under `packages/ui/brand/` contains none of `<script`, `href=`, `<foreignObject`, `/\son[a-z]+=/i`, `<linearGradient`, `<filter`
    - `#0071e3` (read from tokens as `accentFill`, not typed) appears in `favicon.svg` and in no other `.svg`
    - `import.meta.resolve('@noodara/ui/brand/lockup-dark.svg')` (or `createRequire(import.meta.url).resolve` from `apps/web/package.json`'s location) returns a `file://` URL ending with `packages/ui/brand/lockup-dark.svg`, and the same for `favicon.ico` and `brand-colors.json`; resolving `@noodara/ui/brand/../src/index.ts` throws (no traversal outside `brand/`)
  </behavior>
  <action>
    RED first: write both test files; the exactness test should fail only if you deliberately corrupt a file — so to prove RED, run the suite once with a temporarily modified `packages/ui/brand/monogram-light.svg` (append a space), see it fail, restore, commit tests as `test(07-06): add brand asset exactness and export-resolution tests`. For the export test, resolve from a location OUTSIDE `packages/ui`: use `createRequire(pathToFileURL(path.resolve('apps/web/package.json')))` and `require.resolve('@noodara/ui/brand/lockup-dark.svg')` — this exercises the real `exports` map through `apps/web`'s `node_modules/@noodara/ui` symlink. GREEN needs no production change if Task 2 was done right; if the resolve test fails, fix `packages/ui/package.json` (not the test).
  </action>
  <verify>
    <automated>pnpm test -- tests/unit/brand && pnpm brand:check</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/brand/brand-assets-accuracy.test.ts` has ≥ 8 `it(` blocks for text assets (one per file) plus raster metadata, ICO header, SVG safety and accent-only-in-favicon assertions
    - `tests/unit/brand/package-exports-resolvable.test.ts` resolves three brand files from `apps/web`'s package location and asserts the traversal case throws
    - `pnpm test -- tests/unit/brand` exits 0 (asset-manifest, brand-assets-accuracy, package-exports-resolvable, raster, board-html, review-paths, approval-record)
    - A deliberate one-byte change to `packages/ui/brand/lockup-light.svg` makes `pnpm test -- tests/unit/brand/brand-assets-accuracy` fail naming that file (verify, then `git checkout -- packages/ui/brand/lockup-light.svg`)
  </acceptance_criteria>
  <done>Committed assets are locked to the geometry; exports proven resolvable from outside the package; SVGs proven script-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → `packages/ui/brand/` | Script writes committed files that are later served (favicon, OG) and embedded (README) |
| `@noodara/ui/brand/*` export → consumers (apps/web sync, Phase 10 site) | A wildcard subpath export exposes exactly one directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Tampering | generated SVGs served to browsers | mitigate | Built from JSX render only; `brand-assets-accuracy.test.ts` asserts no `<script`/`href`/`foreignObject`/`on*=`/`<filter`/`<linearGradient`; drift test binds bytes to source |
| T-07-17 | Tampering | generator file writes | mitigate | Writes only `ASSET_FILES` names inside `BRAND_DIR` computed from `import.meta.dirname`; never deletes; `--check` mode is read-only and is what CI runs |
| T-07-18 | Information Disclosure | `exports: "./brand/*"` | mitigate | Node's `exports` wildcard cannot escape the `./brand/` prefix (test asserts traversal throws); directory holds only generated public assets |
| T-07-19 | Denial of Service | fonts missing on CI for OG text | accept | OG is generated locally and committed; CI only checks dimensions; documented in the script header |
| T-07-SC | Tampering | npm installs | accept (n/a) | No new packages (sharp/png-to-ico installed and gated in 07-02) |
</threat_model>

<verification>
- `pnpm brand:generate` then `pnpm brand:check` → 0; `pnpm test -- tests/unit/brand` → green; `pnpm check:ui-safety` → green.
- Nyquist: `07-W0-02` → `brand-assets-accuracy.test.ts`; `07-W0-09` → `package-exports-resolvable.test.ts`.
</verification>

<success_criteria>
- 13 generated assets committed, byte-locked (SVG/JSON) or shape-locked (PNG/ICO) to the approved geometry.
- `@noodara/ui/brand/*` resolvable from another workspace package (Phase 10 ready).
- Blue exists only inside generated files (D-11); ui-safety gate green.
</success_criteria>

<output>
Create `.planning/phases/07-identidad-y-brand-kit/07-06-SUMMARY.md` when done.
</output>
