---
phase: 05-ui-web
plan: 29
type: execute
wave: 2
gap_closure: true
depends_on: [05-26, 05-28]
files_modified:
  - apps/web/src/app/(shell)/servers/[id]/page.tsx
  - apps/web/src/lib/detail-sync.ts
  - apps/web/src/lib/detail-sync.test.ts
  - apps/web/src/lib/discovery-progress.ts
  - apps/web/src/lib/discovery-progress.test.ts
  - tests/e2e/server-detail.spec.ts
  - tests/e2e/discovery.spec.ts
  - tests/e2e/dod-hardening.spec.ts
autonomous: true
requirements: [DETL-01, DETL-02, DISC-02, UI-02]

must_haves:
  truths:
    - "The server detail screen shows facts reliably under ordinary snapshot/event interleavings: a GET that races a live server.updated or server.deleted cannot leave the page stale, stuck on Connecting…, or showing a deleted server"
    - "Discovery progress is shown check by check and never invented: a page that mounts mid-run renders checks it never received as pending/unknown, never as running or pass"
    - "liveChecks is cleared on every transition into and out of CONNECTING, whether that transition is learned from an SSE event or from a GET snapshot — a finished run's checks can never render as the next run's live progress"
    - "The detail screen does not hard-crash when localStorage is unavailable"
  artifacts:
    - path: "apps/web/src/lib/detail-sync.ts"
      provides: "the pure snapshot-vs-event reconciliation decision"
      exports: ["reconcileDetailSnapshot"]
    - path: "apps/web/src/lib/discovery-progress.ts"
      provides: "check-by-check progress that never renders unreceived checks as resolved"
      contains: "lastReceivedIndex"
    - path: "apps/web/src/app/(shell)/servers/[id]/page.tsx"
      provides: "applyServer(next, source) with latestRequestRef and deletedRef, and safeLocalStorage at the first-trust call site"
      contains: "applyServer"
  key_links:
    - from: "apps/web/src/app/(shell)/servers/[id]/page.tsx"
      to: "GET /api/servers/:id and the SSE server.updated/server.deleted stream"
      via: "a single applyServer choke point instead of two independent setState calls"
      pattern: "applyServer"
    - from: "apps/web/src/app/(shell)/servers/[id]/page.tsx"
      to: "safeLocalStorage"
      via: "the shouldShowFirstTrustNotice / dismissFirstTrustNotice call sites"
      pattern: "safeLocalStorage"
---

<objective>
Close VERIFICATION gap 1 (SC3/DISC-02, invented discovery progress) and the frontend half of gap 2 (SC2/DETL-01/DETL-02, no snapshot-vs-event ordering guard). Both live in the same two files, so they ship together.

Re-verified in the current tree on 2026-09-20 — these line references were checked, not copied from a report:
- `apps/web/src/app/(shell)/servers/[id]/page.tsx`: `fetchServer` (lines 107-132) sets `setState({ kind: 'ready', server: result.data })` from EVERY successful GET with no comparison against the currently held server, no request-sequence ref and no deleted flag. The sibling `(shell)/servers/page.tsx` does have this protection (`reconcileSnapshot`) — read it and follow its shape rather than inventing a second idiom.
- Same file, the subscribe effect (lines 145-165): `setLiveChecks([])` fires only when `previousStatusRef.current !== 'CONNECTING' && event.server.status === 'CONNECTING'` inside the SSE branch. The fetch branch (line 129) assigns `previousStatusRef.current = result.data.status` without ever clearing, and nothing clears on a transition OUT of `CONNECTING`.
- `apps/web/src/lib/discovery-progress.ts` (lines 154-184): in the `CONNECTING` branch, `firstUnresolvedIndex = DISCOVERY_CHECK_IDS.findIndex((id) => !resolvedIds.has(id))`. For a page that joined mid-run, the earliest unreceived id is one the run already finished; it is rendered `running`, and later received checks feed `aggregateLiveStepState`, which can report a step as `pass`. `connectionState` is also set to `'pass'` purely from `receivedChecks.length > 0`.

Purpose: D-05's hard rule, restated by 05-18-PLAN.md as "la regla de no inventar progreso", is that the UI never shows progress it has not received. And DETL-02 names exactly two empty states; a page stuck on a stale "Connecting…" is neither.
Output: a pure reconciliation module, a corrected progress reducer, and a detail page with one write path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-tdd/SKILL.md
@.claude/skills/noodara-ux-apple/SKILL.md
@.planning/phases/05-ui-web/05-UI-SPEC.md
@.planning/phases/05-ui-web/05-CONTEXT.md
@.planning/phases/05-ui-web/05-VERIFICATION.md

<interfaces>
- `apps/web/src/lib/api-client.ts` exports `ServerView` with `updatedAt: string` (ISO wire string, never a `Date`) and `status: 'PENDING' | 'CONNECTING' | 'CONNECTED' | 'DISCONNECTED' | 'UNREACHABLE' | 'ERROR'`. Plan 05-28 added `API_REQUEST_TIMEOUT_MS` and an `AbortSignal` to `performRequest` — read the current file; do NOT modify it here.
- Plan 05-28 created `apps/web/src/lib/safe-storage.ts` exporting `safeLocalStorage`. Read its actual signature before using it.
- `apps/web/src/lib/first-trust.ts` exports `shouldShowFirstTrustNotice(storage, serverId, capturedAt)` and `dismissFirstTrustNotice(storage, serverId)` — both already take storage as a parameter.
- `apps/web/src/lib/discovery-progress.ts` exports `CHECK_STATES`, `CheckState`, `severityFor`, `DiscoveryCheckView`, `DiscoveryStepView`, `DiscoveryChecklist`, `DiscoverySettledSnapshot`, `BuildChecklistInput`, `buildChecklist`, `summarize`. Its module contract is PURE: no `fetch`, no `EventSource`, no clock read. `DISCOVERY_CHECK_IDS` (the fixed order), `CHECK_TO_STEP`, `DISCOVERY_GROUP_STEPS` and `CONNECTION_STEP_NAMES` come from `./discovery-steps`.
- `apps/web/src/app/(shell)/servers/page.tsx`'s `reconcileSnapshot` — the existing, working precedent for this exact class of guard. Read it first and mirror it.
- `apps/web/src/lib/detail-state.ts` exports `deriveDetailState` and `derivePrimaryAction`; its five states are `never-discovered` / `failed-no-history` / `failed-with-history` / `discovered` / `host-key-changed`. Do not change it.
</interfaces>
</context>

<executor_constraints>
- Sequential execution on the main working tree. FORBIDDEN: `git stash`, `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, and any path outside `noodara/code`.
- Conventional Commits in English, subject ≤ 72 chars. NO `Co-Authored-By` and no Claude attribution trailer of any kind.
- GNU `timeout` does not exist on this macOS — never put it in a command.
- `discovery-progress.ts` and the new `detail-sync.ts` stay PURE: no `fetch`, no `EventSource`, no `Date.now()`, no bare `new Date()`, no React. Verify by grep after editing.
- Do NOT modify `apps/web/src/lib/api-client.ts`, `apps/web/src/lib/error-copy.ts`, `apps/web/src/components/TrustFingerprintDialog.tsx` or anything under `apps/control-plane` — other plans own them.
- `buildChecklist`'s existing D-05 rule stands and must not be "optimised away": while `serverStatus === 'CONNECTING'`, the `settled` argument is never read. Keep that and keep its comment.
- All UI copy in English, sourced from the existing vocabulary.
</executor_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stop inventing discovery progress</name>
  <files>apps/web/src/lib/discovery-progress.ts, apps/web/src/lib/discovery-progress.test.ts</files>
  <read_first>
    - apps/web/src/lib/discovery-progress.ts (the whole file)
    - apps/web/src/lib/discovery-progress.test.ts
    - apps/web/src/lib/discovery-steps.ts (DISCOVERY_CHECK_IDS' fixed order, CHECK_TO_STEP, DISCOVERY_GROUP_STEPS, CONNECTION_STEP_NAMES)
    - .planning/phases/05-ui-web/05-CONTEXT.md (D-05, D-06, D-08)
    - .planning/phases/05-ui-web/05-UI-SPEC.md §4.2/§4.3
  </read_first>
  <behavior>
    - Mid-run mount: given `serverStatus === 'CONNECTING'` and `receivedChecks` containing only ids that appear LATE in `DISCOVERY_CHECK_IDS` (e.g. the Docker-group ids), every earlier id that was never received renders `pending` — never `running`, never `pass`, never `skipped`.
    - The unreceived earlier ids are EXCLUDED from step aggregation: a step whose only received check passed, but which also contains an unreceived earlier check, must not report `pass`. Assert the exact expected state for that step and justify the choice against §4.2's seven-word vocabulary in the test name.
    - Exactly one id may render `running`: the id immediately after the LAST received id in `DISCOVERY_CHECK_IDS` order (`lastReceivedIndex + 1`), and only when at least one check has been received. Before any check is received, no discovery id is `running`.
    - The connection steps (`ssh_reachable`, `authenticated`) keep their existing D-05 derivation — a received check still implies a successful connect. Do not change that; add a test pinning it so the change above cannot drift it.
    - Contiguous-from-the-start runs (the common case) are byte-identical to today's output. Assert this with the existing test cases, which must all still pass unmodified.
  </behavior>
  <action>
    Write the mid-run-mount cases into `discovery-progress.test.ts` first and observe RED, recording the literal output (today the earlier ids come back `running`/`pass`).

    Then replace `firstUnresolvedIndex` with a `lastReceivedIndex` computation: the maximum `DISCOVERY_CHECK_IDS` index among ids actually present in `receivedChecks` (`-1` when none). An id whose index is below `lastReceivedIndex` and which was not received is `pending` AND must be excluded from the step's aggregation input — introduce an explicit "unknown/not-observed" notion for aggregation rather than letting a `pending` entry silently satisfy `aggregateLiveStepState`'s `every(c => c.state === 'pending')` branch. `CHECK_STATES`' seven words are the rendering vocabulary: if you need a distinct internal marker, keep it internal to this module and do not add an eighth public state without saying so explicitly in the SUMMARY.

    Rewrite the doc comments on `firstUnresolvedIndex`'s replacement and on `aggregateLiveStepState` to describe the mid-run-mount case they now handle, citing the verification finding. The existing comment asserting the current behaviour is correct is the false premise — remove it.
  </action>
  <verify>
    <automated>pnpm exec vitest run apps/web/src/lib/discovery-progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run apps/web/src/lib/discovery-progress.test.ts` exits 0, with named cases for: mid-run mount with only late ids received, step aggregation excluding unreceived earlier ids, exactly-one-running placement at `lastReceivedIndex + 1`, and no-checks-received.
    - Every pre-existing test in that file still passes with its assertions unmodified (if any had to change, the SUMMARY must justify each change individually).
    - `grep -c "firstUnresolvedIndex" apps/web/src/lib/discovery-progress.ts` is 0.
    - `grep -c "lastReceivedIndex" apps/web/src/lib/discovery-progress.ts` ≥ 2.
    - `grep -cE "Date\.now\(\)|new Date\(\)|fetch\(|EventSource" apps/web/src/lib/discovery-progress.ts` is 0 (purity preserved — this is the file's own documented invariant).
    - The SUMMARY quotes the RED output.
  </acceptance_criteria>
  <done>A page that joins mid-run shows only what it received.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A pure snapshot-vs-event reconciliation, then one write path on the page</name>
  <files>apps/web/src/lib/detail-sync.ts, apps/web/src/lib/detail-sync.test.ts, apps/web/src/app/(shell)/servers/[id]/page.tsx</files>
  <read_first>
    - apps/web/src/app/(shell)/servers/[id]/page.tsx (the whole file)
    - apps/web/src/app/(shell)/servers/page.tsx (`reconcileSnapshot` — the working precedent)
    - apps/web/src/lib/api-client.ts (READ ONLY — `ServerView.updatedAt`'s type)
    - apps/web/src/lib/safe-storage.ts (created by plan 05-28)
    - apps/web/src/lib/first-trust.ts
  </read_first>
  <behavior>
    Pure module `reconcileDetailSnapshot`:
    - A snapshot whose `updatedAt` is strictly older than the currently held server's is REJECTED.
    - A snapshot equal or newer is ACCEPTED.
    - Any snapshot for a server already known deleted is REJECTED, regardless of `updatedAt`.
    - A snapshot from a superseded request (a lower request sequence than the latest issued) is REJECTED.
    - An event is always accepted over a held snapshot of the same or older `updatedAt` — events are the live source; the guard exists to stop a stale GET overwriting a fresh event, not the reverse.
    - The function is pure: it takes the held state, the incoming value, its source and its sequence, and returns a decision. No refs, no setState, no clock.

    Page behaviour:
    - All four writers (`fetchServer`'s success, the resync path, `server.updated`, `server.deleted`) go through ONE `applyServer(next, source)`; `setState({ kind: 'ready', … })` appears in exactly one place.
    - `latestRequestRef` increments per issued GET; a resolved GET whose sequence is not the latest is dropped.
    - `deletedRef` is set by `server.deleted` and never unset for that mount; a subsequent GET cannot resurrect the server.
    - `liveChecks` is cleared on EVERY transition into `CONNECTING` and on every transition OUT of `CONNECTING`, whether learned from an event or from a GET snapshot — both branches route through one shared status-transition handler.
    - `shouldShowFirstTrustNotice` / `dismissFirstTrustNotice` receive `safeLocalStorage()` instead of `window.localStorage`.
  </behavior>
  <action>
    Create `apps/web/src/lib/detail-sync.ts` exporting `reconcileDetailSnapshot` plus whatever small result type it needs, and `apps/web/src/lib/detail-sync.test.ts` covering every bullet above. Write the tests first; this module is new so they start GREEN once implemented — that is acceptable for a new pure unit, but the PAGE-level reproduction in Task 3 must be observed RED.

    Then refactor `servers/[id]/page.tsx`: introduce `applyServer(next: ServerView, source: 'snapshot' | 'event')` as the single state writer, consulting `reconcileDetailSnapshot` with `latestRequestRef`/`deletedRef`/the held server. Route the `CONNECTING`-transition detection (both directions) through one helper used by both the fetch branch and the SSE branch — today only the SSE branch has it. Swap the two `window.localStorage` arguments for `safeLocalStorage()`.

    Preserve every existing behaviour that is not the bug: `hasLoadedRef`'s first-load-only skeleton rule, the `NOT_FOUND` → `not-found` mapping, `requireSession()` on 401 (the page never navigates itself), `registerResync(fetchServer)`, and the dedup in the `discovery_progress` accumulator.
  </action>
  <verify>
    <automated>pnpm exec vitest run apps/web/src/lib/detail-sync.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run apps/web/src/lib/detail-sync.test.ts` exits 0 with a named case per bullet (older snapshot, newer snapshot, deleted, superseded request, event precedence).
    - `grep -c "setState({ kind: 'ready'" apps/web/src/app/\(shell\)/servers/\[id\]/page.tsx` is exactly 1.
    - `grep -c "window.localStorage" apps/web/src/app/\(shell\)/servers/\[id\]/page.tsx` is 0 and `grep -c "safeLocalStorage" …` is ≥ 1.
    - `grep -c "latestRequestRef" …` ≥ 2 and `grep -c "deletedRef" …` ≥ 2.
    - `grep -cE "Date\.now\(\)|new Date\(\)|fetch\(|useState|useEffect" apps/web/src/lib/detail-sync.ts` is 0.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>The detail page has one guarded write path and a safe storage call site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: E2E reproductions for both gaps, and un-fixme the storage case</name>
  <files>tests/e2e/server-detail.spec.ts, tests/e2e/discovery.spec.ts, tests/e2e/dod-hardening.spec.ts</files>
  <read_first>
    - tests/e2e/server-detail.spec.ts
    - tests/e2e/discovery.spec.ts
    - tests/e2e/dod-hardening.spec.ts (plan 05-28 left a `test.fixme` case naming 05-29)
    - apps/web/src/app/(shell)/servers/[id]/page.tsx (as edited)
  </read_first>
  <behavior>
    - E2E (gap 2): a GET response delayed so it resolves AFTER a live `server.updated` has already arrived does not roll the screen back to the older state; and a `server.deleted` followed by a late-resolving GET leaves the "This server no longer exists." state, not a resurrected server.
    - E2E (gap 3): navigating to a detail page while a run is already in progress shows unreceived earlier checks as pending and never shows a step as passed on the strength of checks the page did not receive.
    - E2E (gap 3): after a run finishes and a NEW run starts, the previous run's checks are gone from the live progress — no leftovers.
    - E2E (gap 4 handover): with `localStorage` access forced to throw, the detail screen renders its facts and the page reports no uncaught error.
  </behavior>
  <action>
    Use Playwright route interception to control GET timing and the existing SSE test helpers to inject events; reproduce each case RED before relying on Tasks 1-2 having fixed it. Where the fix is already in the working tree, temporarily revert the specific guard to observe RED, then restore — and say so in the SUMMARY rather than claiming an unobserved RED.

    Remove the `test.fixme` marker on `tests/e2e/dod-hardening.spec.ts`'s localStorage case and make it pass. Do not delete the test.

    Then run the full web-side gate. `pnpm test:integration` is not required by this plan (no `apps/control-plane` file is touched); the cross-suite gate runs in 05-37.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm exec playwright test tests/e2e/server-detail.spec.ts tests/e2e/discovery.spec.ts tests/e2e/dod-hardening.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.fixme" tests/e2e/dod-hardening.spec.ts` is 0 and the file passes.
    - `tests/e2e/server-detail.spec.ts` contains named cases for the late-GET-after-event and the deleted-then-late-GET scenarios; both pass.
    - `tests/e2e/discovery.spec.ts` contains named cases for mid-run mount and for run-to-run liveChecks clearing; both pass.
    - `pnpm lint`, `pnpm typecheck`, `pnpm test` all exit 0 and all three spec files pass.
    - No Playwright timeout value was increased anywhere in this plan; the SUMMARY states this explicitly.
  </acceptance_criteria>
  <done>Both gaps have browser-level proof and the 05-28 handover is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control plane (GET snapshot + SSE events) → rendered server facts | Two unordered sources writing the same state |
| route param `[id]` → API paths | Untrusted input already handled via `encodeURIComponent`; must stay that way |
| browser storage → first-trust notice gating | Storage is not guaranteed available |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-29-01 | Spoofing | a resurrected deleted server, or a stale fingerprint shown as current | mitigate | `deletedRef` + `updatedAt` ordering in `reconcileDetailSnapshot`; the HOST_KEY_CHANGED banner and the trust dialog read from this same state, so a stale snapshot here would show the admin a fingerprint that is no longer current |
| T-5G-29-02 | Denial of Service | a permanently stale "Connecting…" blocking every action as SERVER_BUSY | mitigate | One guarded write path plus plan 05-26's backend wedge fix; E2E covers the late-GET interleaving |
| T-5G-29-03 | Tampering | invented discovery progress misleading the admin about what was actually verified on the host | mitigate | `lastReceivedIndex` + exclusion of unreceived ids from aggregation; D-05's "never render unreceived progress" rule enforced by unit tests |
| T-5G-29-04 | Information Disclosure | route param echoed into a rendered error | mitigate | Unchanged: an unknown/malformed id renders the same `NOT_FOUND` copy as a deleted one, never a raw dump (05-UI-SPEC.md §10). Assert it still holds. |
| T-5G-29-05 | Denial of Service | blocked storage crashing the screen | mitigate | `safeLocalStorage()` at both first-trust call sites + plan 05-28's `(shell)/error.tsx` boundary; E2E un-fixme'd |
| T-5-29-SC | Tampering | npm/pnpm installs | mitigate | NO new dependency. If one seems necessary, stop and report. |
</threat_model>

<verification>
1. `pnpm lint && pnpm typecheck && pnpm test` exit 0.
2. The three E2E spec files pass.
3. Purity greps on `discovery-progress.ts` and `detail-sync.ts` return 0.
4. Exactly one `setState({ kind: 'ready'` remains in the detail page.
5. The SUMMARY records a RED observation for each of the two gaps (or states precisely how RED was demonstrated when the guard had to be temporarily reverted).

## Nyquist note
`pnpm test:integration` is not required here (no `apps/control-plane` file changed). The full cross-suite gate runs in plan 05-37.
</verification>

<success_criteria>
- No invented discovery progress under any mount timing.
- No stale, stuck or resurrected server state under ordinary GET/event interleavings.
- The detail screen survives blocked storage.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-29-SUMMARY.md` when done, quoting the RED evidence for both gaps and listing every pre-existing `discovery-progress.test.ts` assertion that had to change, with a justification each.
</output>
</content>
</invoke>
