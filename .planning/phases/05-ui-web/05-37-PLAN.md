---
phase: 05-ui-web
plan: 37
type: execute
wave: 4
gap_closure: true
depends_on: [05-26, 05-27, 05-28, 05-29, 05-30, 05-31, 05-32, 05-33, 05-34, 05-35, 05-36]
files_modified:
  - .planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md
autonomous: false
requirements: [SERV-04, DETL-01, DETL-02, ACT-02, SET-01, UI-01, UI-02, DISC-02, QA-04, QA-05]

must_haves:
  truths:
    - "The full cross-suite gate passes in one run on the final tree: lint, typecheck, unit, integration, boot, E2E, boundaries and the secrets canary"
    - "Each of the eight verification gaps has a re-derived, first-hand verdict — closed, partially closed, or still open — with a command or source assertion as evidence, not a citation of a SUMMARY"
    - "Everything that genuinely cannot be verified without a human (visual quality on a real display, a real CI run on a real remote, a live SSE walkthrough, a screen-reader pass) is presented to the user as an explicit open item rather than being marked done"
  artifacts:
    - path: ".planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md"
      provides: "the per-gap re-derived verdict table and the full-gate run record"
  key_links:
    - from: ".planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md"
      to: "05-VERIFICATION.md's eight gaps"
      via: "one row per gap with first-hand evidence"
      pattern: "gap"
---

<objective>
The closing gate for this gap-closure wave: run every suite together on the final tree, re-derive a first-hand verdict for each of the eight verification gaps, and hand the genuinely-human items to the user.

Why a separate plan: per-wave gates that only ran unit tests let an integration regression survive roughly twenty plans in this phase. Eleven plans landed changes across `packages/domain`, `packages/ui`, `apps/web`, `apps/control-plane`, the CI workflows and the design tokens. Cross-suite interaction between them has never been executed in one run.

**This plan trusts nothing.** Eleven SUMMARY files will exist by the time it runs, and this phase already produced at least six confident-but-wrong subagent claims (a SUMMARY asserted `api-client.ts` had timeouts; it did not). Every verdict in the audit must be re-derived by running a command or reading the current source, with the command and its output recorded. A verdict whose only evidence is "05-NN-SUMMARY.md says so" is not acceptable and must be written down as `UNVERIFIED`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-release-gate/SKILL.md
@.claude/skills/noodara-security/SKILL.md
@.planning/phases/05-ui-web/05-VERIFICATION.md
@docs/ui-review-05.md
@.planning/REQUIREMENTS.md

<interfaces>
The full command set from `package.json`:
`pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration` (Docker required, ~31 min for a full run), `pnpm test:boot`, `pnpm test:e2e`, `pnpm boundaries`, `pnpm security:scan-leaks`, `pnpm check:ui-safety`, `node scripts/check-package-provenance.mjs`.

Known baselines to compare against (from 05-VERIFICATION.md, stated by the orchestrator, NOT re-run by that verification): unit 1381/1381, integration 487 passed / 0 failed / 1 skipped, E2E 73/73. Treat these as the prior baseline to diff against, not as a target to match — the eleven plans above added tests, so the counts must go UP, and any newly skipped test must be named and justified.
</interfaces>
</context>

<executor_constraints>
- Sequential execution on the main working tree. FORBIDDEN: `git stash`, `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, and any path outside `noodara/code`.
- Conventional Commits in English, subject ≤ 72 chars. NO `Co-Authored-By` and no Claude attribution trailer of any kind.
- FORBIDDEN: `git push`, `git remote add`, or anything that publishes the repository.
- GNU `timeout` does not exist on this macOS — never put it in a command.
- Do NOT edit `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`. The orchestrator owns them. If QA-04/QA-05 ought to change status, SAY SO in the audit; do not do it.
- Do NOT fix anything you find. This plan's job is to measure and report. If the gate goes red, record the exact failure and report it — a fix belongs in its own plan with its own failing test first.
- Do not raise a timeout, do not re-run a flaky test until it passes, do not mark a test skipped. A bimodal duration (fast pass / exact-timeout fail) is a hang: capture a trace and record it.
</executor_constraints>

<tasks>

<task type="auto">
  <name>Task 1: The full cross-suite gate, in one run, on the final tree</name>
  <files>.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md</files>
  <read_first>
    - package.json (the authoritative script definitions)
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the prior baselines)
    - .planning/phases/05-ui-web/deferred-items.md (the known flakes — `servers-list.spec.ts`'s order-dependent menu case and the 60s hover timeout are documented and still partly open; you must distinguish a known flake from a new regression, not wave either away)
  </read_first>
  <action>
    Run, in this order, recording each command's exit code, wall-clock duration and pass/fail/skip counts:
    `pnpm lint`, `pnpm typecheck`, `pnpm boundaries`, `pnpm check:ui-safety`, `node scripts/check-package-provenance.mjs`, `pnpm test`, `pnpm build`, `pnpm test:boot`, `pnpm test:integration`, `pnpm test:e2e`, `pnpm security:scan-leaks`.

    Run each one separately rather than chaining with `&&`, so a failure does not hide the results of the suites behind it — you need the complete picture, not the first red.

    For every failure: record the literal output, classify it as (a) a new regression introduced by this gap-closure wave, (b) a previously-documented flake from `deferred-items.md`, or (c) an environment problem, and state the evidence for the classification. A flake classification requires citing the specific `deferred-items.md` entry; "it passed on the second run" is not a classification.

    Diff the counts against the baselines and state what changed and why.
  </action>
  <verify>
    <automated>pnpm lint; pnpm typecheck; pnpm boundaries; pnpm check:ui-safety; node scripts/check-package-provenance.mjs; pnpm test; pnpm build; pnpm test:boot; pnpm test:integration; pnpm test:e2e; pnpm security:scan-leaks</automated>
  </verify>
  <acceptance_criteria>
    - All eleven commands were run and each one's exit code, duration and counts are recorded in `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` section 1.
    - Unit, integration and E2E counts are compared against the 1381 / 487+1-skipped / 73 baselines with the delta explained.
    - Every skipped test in the final run is named with its justification.
    - Every failure is classified with evidence; no failure is dismissed as "flaky" without a `deferred-items.md` citation.
    - No timeout value was changed and no test was newly skipped to make the gate green — assert this explicitly.
  </acceptance_criteria>
  <done>The complete gate result is recorded, red or green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive a first-hand verdict for each of the eight gaps</name>
  <files>.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md</files>
  <read_first>
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the frontmatter `gaps:` list — all eight truths and their `missing:` items)
    - The CURRENT source of every file named in each gap's `artifacts:` list (re-read them; do not rely on any SUMMARY)
    - .planning/phases/05-ui-web/05-REVIEW.md (only for the WR-x-xx identifiers the gaps reference)
  </read_first>
  <action>
    Build section 2 of `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md`: one row per gap, with columns `gap`, `truth`, `verdict` (CLOSED / PARTIAL / OPEN / UNVERIFIED), `first-hand evidence`, `residual`.

    For each of the eight, re-derive the evidence yourself. Concretely, at minimum:
    1. SC3/DISC-02 — `grep -c "firstUnresolvedIndex" apps/web/src/lib/discovery-progress.ts` (expect 0) and the named mid-run-mount tests actually passing; read the `CONNECTING` branch and confirm `settled` is still never read there.
    2. SC2/DETL-01/02 — `grep -c "setState({ kind: 'ready'" ` in the detail page (expect 1); `grep -c "failInFlightConnection" apps/control-plane/src/services/connect-and-discover.ts` (expect ≥1); `grep -c "worker_job_failed" apps/control-plane/src/queue/connect-server-worker.ts` (expect ≥1); the named E2E and integration cases passing.
    3. SC5/QA-04/QA-05 — `awk`/`grep` that the CI security job has a `playwright install` step; `git remote -v` still empty; `docs/ci-readiness.md` exists and does not claim completion. **Verdict must remain OPEN** until a human observes a real run — say so.
    4. DoD bar — `grep -v '^\s*[/*]' apps/web/src/lib/api-client.ts | grep -cE "AbortSignal|AbortController"` (expect ≥1); `grep -c "navigator\.clipboard\.writeText" packages/ui/src/CopyButton.tsx` (expect 0); `apps/web/src/app/(shell)/error.tsx` exists; the contrast gate passes at 4.5:1 in both themes.
    5. SC1/UI-02 — the named E2E proving a server-rejected field is highlighted, passing; `grep -c "normalizeFieldPath" apps/web/src/lib/error-copy.ts` ≥2; the setup page's invalid-link banner no longer unconditional.
    6. Host-key trust — `grep -c "TrustFingerprintBodySchema" ` in routes and schemas; `grep -c "and(eq(servers.id" apps/control-plane/src/services/trust-fingerprint.ts` ≥1; `grep -c "apiGet" apps/web/src/components/TrustFingerprintDialog.tsx` (expect 0); the mid-review-swap E2E passing; `grep -c "row.status === 'ERROR' && row.pendingFingerprint !== null" apps/control-plane/src/services/edit-server.ts` (expect 0).
    7. SC4/ACT-02 — read plan 05-32's reproduction verdicts and CONFIRM each independently: for any finding it marked non-reproducible, re-read the code yourself and state whether you agree; `grep -n "groupByDay(" apps/web/src/components/ActivityList.tsx` shows three arguments.
    8. Triage batch — one sub-row per item actually planned (WR-B-15, WR-C-01, WR-A-03, WR-A-04, WR-B-10, setup-token + Referrer-Policy, UF-02, WR-C-14), each with its own grep/test evidence.

    Then reconcile requirements: for each of SERV-04, DETL-01, DETL-02, ACT-02, SET-01, UI-01, UI-02, DISC-02, QA-04, QA-05, state whether the evidence now supports the status `.planning/REQUIREMENTS.md` currently records, and where it does not, say what the status SHOULD be and why. Do not edit that file.

    Also reconcile the todos: state for `.planning/todos/pending/2026-09-19-trust-fingerprint-toctou.md` and `.planning/todos/pending/2026-09-19-setup-token-url-hardening.md` whether each is now closable, based on your own re-derivation.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md && grep -c "UNVERIFIED\|CLOSED\|PARTIAL\|OPEN" .planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 has exactly eight gap rows (gap 8 with its per-item sub-rows), each with a verdict and first-hand evidence.
    - Every evidence cell contains a command that was actually run with its result, or a source assertion with a file:line that was actually read — never a SUMMARY citation.
    - Gap 3 (SC5/QA-04/QA-05) is recorded as OPEN, with the exact human observation still required.
    - The requirements reconciliation covers all ten IDs.
    - Both pending todos have a stated disposition.
    - `git status` shows no change to `.planning/REQUIREMENTS.md`, `.planning/STATE.md` or `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>Eight independent verdicts exist, each standing on its own evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Hand the human-only items to the user</name>
  <files>.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md</files>
  <read_first>
    - .planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md (sections 1 and 2, as written by Tasks 1-2)
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the `human_verification:` frontmatter block — all eight items)
    - docs/ui-review-05.md (the "Needs human review" section)
    - docs/ci-readiness.md (written by plan 05-36)
    - docs/contrast-decision-05.md (written by plan 05-33 — the user's own colour decision)
  </read_first>
  <action>
    Present the six items listed under `how-to-verify` below, verbatim and in that order, then STOP and wait. Do not summarise them away, do not merge them, and do not mark any of them satisfied on the strength of an automated result — each one is on this list precisely because no command in this repository can settle it. Record the user's literal response, item by item, as section 3 ("Human verification") of `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` and in the SUMMARY. If the user defers an item, write "deferred" against it with their reason; never write "passed" for an item they did not actually check.
  </action>
  <what-built>
    Eleven gap-closure plans (05-26 … 05-36) landed across the domain package, the UI package, the web app, the control plane, the CI workflows and the design tokens. Section 1 of `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` records the full cross-suite gate; section 2 records a re-derived verdict for each of the eight verification gaps and for each triaged finding.
  </what-built>
  <how-to-verify>
    Read `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` first — especially any row that is not CLOSED.

    Then the items that genuinely cannot be checked from this sandbox. Each is listed in `05-VERIFICATION.md`'s `human_verification:` block and in `docs/ui-review-05.md`'s "Needs human review" section; none has been closed by this wave:

    1. **The contrast change on a real display, both themes.** The new token values measure ≥4.5:1 (plan 05-33, your own decision), but a ratio is not an aesthetic judgement. Start the app, switch light/dark, and say whether the primary buttons, the segmented control and the four status pills read as calm and Apple-consistent or as muddy. This is also where your earlier "No me gusta la UI" stands: that broader dissatisfaction is explicitly NOT in this gap closure's scope, and if it still stands it needs its own phase.
    2. **A real CI run.** QA-04 and QA-05 remain `Pending`. This repository still has no remote, by your policy. To close them, a human must push and observe `nightly.yml`'s e2e-repeat job pass 20/20 with its canary job passing in the same run, plus `ci.yml`'s `security` job passing. `docs/ci-readiness.md` records exactly this. No agent did or will do this step.
    3. **A live walkthrough with SSE actually visible** — add server → connect → watch discovery fill in check by check → detail → activity → settings, NOT through a buffering tunnel (the 2026-09-20 walkthrough used a Cloudflare Quick Tunnel that buffers SSE, so live updates have still never been seen by a human). This is the one that exercises plan 05-29's mid-run-mount and liveChecks-clearing fixes in the way they were actually designed for.
    4. **Sheet/Dialog/RowMenu elevation** — flat + hairline + backdrop-blur versus the skill's specified floating shadow. `docs/ui-review-05.md` FLAG 2 defers this to a human on a real display. Unchanged by this wave.
    5. **RowMenu with a real screen reader** (VoiceOver/NVDA) — open/close state and item selection announcements. Static ARIA audits already produced one false flag in this phase.
    6. **Responsive behaviour below 1280px on real touch hardware**, and **`prefers-reduced-motion`'s felt effect** — Playwright proves computed breakpoints and CSS, not device feel.
  </how-to-verify>
  <acceptance_criteria>
    - The audit document is complete and every non-CLOSED row has a named residual.
    - The user has been shown all six human items explicitly, with QA-04/QA-05 presented as still `Pending` and not quietly rounded up.
    - The user's response is recorded verbatim in the SUMMARY, including which items they checked and which they deferred.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Human verification" .planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md</automated>
    <human-check>The user has been shown all six items and has answered each one.</human-check>
  </verify>
  <resume-signal>Type "approved" to close the gap-closure wave, or describe what still needs work (and which of the six items you actually checked).</resume-signal>
  <done>Section 3 of the audit records the user's verbatim response per item, with QA-04/QA-05 still Pending unless a real CI run was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eleven plans' SUMMARY claims → this phase's completion record | The documented failure mode of this phase; the audit is the control |
| the local sandbox → real CI / a real display / real assistive tech | Whole classes of evidence are unobtainable here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-37-01 | Repudiation | a gap recorded closed on an unverified claim | mitigate | Every audit verdict re-derived by a run command or a read of current source; a SUMMARY-only verdict must be written `UNVERIFIED` |
| T-5G-37-02 | Tampering | making the gate green by raising a timeout, skipping a test or re-running a flake | mitigate | Explicitly forbidden; each failure must be classified with evidence and a `deferred-items.md` citation for any flake claim; the executor may not fix anything in this plan |
| T-5G-37-03 | Repudiation | QA-04/QA-05 marked complete without a real CI run | mitigate | Verdict forced to OPEN; the exact required human observation is restated in the checkpoint and in `docs/ci-readiness.md`; REQUIREMENTS.md is not edited by this plan |
| T-5G-37-04 | Information Disclosure | the secrets canary regressing across eleven plans of log/SSE/error changes | mitigate | `pnpm security:scan-leaks` is part of the single-run gate and its result is recorded |
| T-5G-37-05 | Tampering | supply chain drift introduced across the wave | mitigate | `node scripts/check-package-provenance.mjs` and `pnpm audit --audit-level=high` equivalents run in the gate; `git diff package.json pnpm-lock.yaml` across the wave is reported |
| T-5-37-SC | Tampering | npm/pnpm installs | mitigate | This plan installs NOTHING. It runs existing scripts and writes one markdown file. |
</threat_model>

<verification>
1. All eleven gate commands were run individually and recorded with exit code, duration and counts.
2. `.planning/phases/05-ui-web/05-GAP-CLOSURE-AUDIT.md` exists with sections 1 and 2 complete.
3. No `.planning/REQUIREMENTS.md`, `.planning/STATE.md` or `.planning/ROADMAP.md` edit.
4. `git remote -v` still empty; no push attempted.
5. The human checkpoint was reached and the user's verbatim answer recorded.
</verification>

<success_criteria>
- One run of every suite on the final tree, fully recorded.
- Eight first-hand gap verdicts and a ten-requirement reconciliation.
- The six human-only items presented honestly, with QA-04/QA-05 still Pending.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-37-SUMMARY.md` when done, containing the gate result table, the per-gap verdict table, the requirements reconciliation, the todo dispositions, and the user's verbatim checkpoint response.
</output>
</content>
</invoke>
