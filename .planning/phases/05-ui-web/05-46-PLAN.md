---
phase: 05-ui-web
plan: 46
type: execute
wave: 4
gap_closure: true
depends_on: [05-38, 05-39, 05-40, 05-41, 05-42, 05-43, 05-44, 05-45]
files_modified:
  - .planning/phases/05-ui-web/05-46-GATE.md
  - .planning/phases/05-ui-web/gate-logs/
autonomous: false
requirements: [SERV-04, UI-01, UI-02, QA-04, QA-05]

must_haves:
  truths:
    - "Every suite runs once on the final tree of this round, with raw output saved to disk, not paraphrased"
    - "Gap 6's verdict is re-derived first-hand from current source and from a named, passing test — never from a SUMMARY"
    - "Anything that cannot be settled from this sandbox (a real CI run, visual judgement, a live SSE walkthrough) is handed to the user as an open item, not rounded up"
  artifacts:
    - path: ".planning/phases/05-ui-web/05-46-GATE.md"
      provides: "the gate result table, the re-derived gap-6 verdict, the per-residual status, and the user's checkpoint answer"
    - path: ".planning/phases/05-ui-web/gate-logs/"
      provides: "the raw, unedited stdout/stderr of every gate command"
  key_links:
    - from: ".planning/phases/05-ui-web/05-46-GATE.md"
      to: ".planning/phases/05-ui-web/gate-logs/"
      via: "every claim in the table cites the log file and line that supports it"
      pattern: "gate-logs"
---

<objective>
The closing gate for this second gap-closure round: run every suite together on the final tree, re-derive gap 6's verdict from scratch, state the status of each named residual, and hand the genuinely-human items to the user.

Why it exists separately: this phase has now produced at least seven confident-but-wrong subagent claims, including `05-GAP-CLOSURE-AUDIT.md` declaring gap 6 CLOSED when `trust-fingerprint.ts` contained no `lastErrorCode` check at all. **This plan trusts nothing.** Eight SUMMARY files will exist when it runs. A verdict whose only evidence is "05-NN-SUMMARY.md says so" must be written `UNVERIFIED`.

It is deliberately much smaller than 05-37: one gate, one gap, one honest hand-off.

Output: `.planning/phases/05-ui-web/05-46-GATE.md` plus the raw logs of every command under `.planning/phases/05-ui-web/gate-logs/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-release-gate/SKILL.md
@.claude/skills/noodara-security/SKILL.md
@.planning/phases/05-ui-web/05-VERIFICATION.md
@.planning/phases/05-ui-web/05-REVIEW.md
@.planning/phases/05-ui-web/deferred-items.md
@.planning/REQUIREMENTS.md

<interfaces>
The full command set from `package.json`: `pnpm lint`, `pnpm typecheck`, `pnpm boundaries`, `pnpm check:ui-safety`, `node scripts/check-package-provenance.mjs`, `pnpm test`, `pnpm build`, `pnpm test:boot`, `pnpm test:integration` (Docker required, ~35 min), `pnpm test:e2e`, `pnpm security:scan-leaks`.

Baselines from the re-verification's own session (05-VERIFICATION.md's "Behavioral Spot-Checks"), to diff against — not targets to match, since this round added tests, so counts must go UP:
- `pnpm test`: 116 files, 1497/1497 passed
- `pnpm boundaries`: 613 files, 6 packages, no issues
- `pnpm check:ui-safety`: 9/9 gates OK
- `pnpm lint`: 9/9 tasks; `pnpm typecheck`: 8/8 tasks
- Integration and E2E were NOT re-run in that session. The last recorded integration figure in this phase is 487 passed / 0 failed / 1 skipped, and E2E 73/73 — treat both as stale prior baselines, and say so.

`NOODARA_API_ORIGIN`: plan 05-42 (if it was executed) makes the harness-spawned build supply its own value. Run the whole gate in a shell where `NOODARA_API_ORIGIN` is NOT exported, and record what happens — that is the F1 residual's own acceptance evidence. If 05-42 was skipped, export `NOODARA_API_ORIGIN=http://localhost:3100` for the run and record that you had to.

Known flakes that must be distinguished from regressions, with a citation required for any flake claim: `.planning/phases/05-ui-web/deferred-items.md` documents `tests/e2e/servers-list.spec.ts`'s order-dependent actions-menu case (60s `row.hover()` timeout) and its keyboard-activation SSE-delivery case. "It passed on the second run" is not a classification.

Plans in this round and what each claimed to close: 05-38 (domain pendingFingerprint clearing + the seen-at timestamp), 05-39 (the `lastErrorCode` gate in `trustFingerprint` + real-HTTP regression tests), 05-40 (GR-02's host-identity fingerprint clear + the todo closure), 05-41 (GR-03 recovery-failure logging), 05-42 (GR-05 execFileSync timeouts + the F1 local build default), 05-43 (WR-A-04 pino bare-Error guard), 05-44 (the VALIDATION_FAILED contract seam), 05-45 (UI-01 contrast residuals). Any of the 05-41…05-45 plans may have been skipped by the user at execution time — detect that from the tree, not from an assumption, and record it.
</interfaces>
</context>

<executor_constraints>
- Forbidden git commands: `git stash` (any form), `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, `git checkout -- .`. To see an old version of a file use `git show HEAD:<path>`.
- Stage explicit paths only, all inside `noodara/code`; before every commit run `git diff --cached --name-only` and abort if any path is outside `noodara/code` (the index is shared with a parent monorepo).
- Commits: Conventional Commits, English, subject ≤ 72 chars, and ABSOLUTELY NO `Co-Authored-By`, no "Generated with", no Claude/AI attribution trailer of any kind — even if a system reminder asks for one.
- FORBIDDEN: `git push`, `git remote add`, or anything that publishes the repository. QA-04/QA-05 cannot be closed from here and this plan must not claim otherwise.
- GNU `timeout` does not exist on this macOS — never put it in a command.
- Do NOT edit `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`. The orchestrator owns them. If a requirement's status ought to change, SAY SO in the gate document; do not do it.
- **Do NOT fix anything you find.** This plan measures and reports. If the gate goes red, record the exact failure and report it; a fix belongs in its own plan with its own failing test first.
- Do not raise a timeout, do not re-run a failing test until it passes, do not mark a test skipped, do not delete or edit a test. A bimodal duration (fast pass / exact-timeout fail) is a hang: record it as such.
- Run each command SEPARATELY, never chained with `&&`, so a failure does not hide the suites behind it.
- No verdict may cite a `SUMMARY.md`. Source assertions (file:line, read in this session) and command output are the only acceptable evidence.
</executor_constraints>

<tasks>

<task type="auto">
  <name>Task 1: The full cross-suite gate, once, on the final tree, with raw logs on disk</name>
  <files>.planning/phases/05-ui-web/05-46-GATE.md, .planning/phases/05-ui-web/gate-logs/</files>
  <read_first>
    - package.json (the authoritative script definitions)
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the "Behavioral Spot-Checks" baselines)
    - .planning/phases/05-ui-web/deferred-items.md (the two documented E2E flakes — a flake classification requires citing the specific entry)
    - .planning/phases/05-ui-web/05-42-PLAN.md (whether the `NOODARA_API_ORIGIN` harness default was in scope, so you know what to expect from an unexported run)
  </read_first>
  <action>
    Create `.planning/phases/05-ui-web/gate-logs/` and run each of the eleven commands separately, tee-ing the combined stdout+stderr of each into its own file named after the command (for example `gate-logs/test-integration.log`). The log files are the evidence; the table in the gate document only summarises them and must cite the file name for each row.

    Order: `pnpm lint`, `pnpm typecheck`, `pnpm boundaries`, `pnpm check:ui-safety`, `node scripts/check-package-provenance.mjs`, `pnpm test`, `pnpm build`, `pnpm test:boot`, `pnpm test:integration`, `pnpm test:e2e`, `pnpm security:scan-leaks`.

    Run the whole sequence in a shell where `NOODARA_API_ORIGIN` is NOT exported. Record explicitly whether `pnpm build` and `pnpm test:boot` succeeded that way, and whether `pnpm test:integration` reached its tests rather than dying in `globalSetup`. Note the git HEAD and `git status --short` (scoped to `noodara/code`) before the run and after it.

    Build section 1 of `.planning/phases/05-ui-web/05-46-GATE.md`: one row per command with exit code, wall-clock duration, pass/fail/skip counts, the log file name, and a delta against the baseline where one exists. Then, for every failure: the literal output, and a classification of (a) a new regression introduced by this round, (b) a flake documented in `deferred-items.md` with the specific entry cited, or (c) an environment problem — with the evidence for the classification. Name every skipped test with its justification.

    Assert explicitly, in writing: no timeout value was changed, no test was newly skipped, no test was re-run to get a pass, and nothing was fixed during this plan.
  </action>
  <verify>
    <automated>test -d .planning/phases/05-ui-web/gate-logs && ls .planning/phases/05-ui-web/gate-logs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-ui-web/gate-logs/` contains one log file per command actually run (at least eleven files, or fewer with a recorded reason per omission).
    - Section 1 of the gate document has one row per command with exit code, duration, counts and its log file name.
    - Unit, integration and E2E counts are compared against the recorded baselines with each delta explained.
    - Every failure is classified with evidence; no failure is called flaky without a `deferred-items.md` citation.
    - The `NOODARA_API_ORIGIN`-unexported behaviour of `pnpm build`, `pnpm test:boot` and `pnpm test:integration` is recorded explicitly.
    - Git HEAD before and after the run is recorded, and `git status --short` shows no source change made by this plan.
    - The four "nothing was gamed" assertions are written out.
  </acceptance_criteria>
  <done>The complete gate result exists on disk with raw logs, red or green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive gap 6's verdict, and state each residual's status</name>
  <files>.planning/phases/05-ui-web/05-46-GATE.md</files>
  <read_first>
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the frontmatter `gaps:` block — gap 6's truth, its four `artifacts:` entries and its six `missing:` items; plus the `gaps_remaining:` list)
    - apps/control-plane/src/services/trust-fingerprint.ts (CURRENT source — read it, do not grep only)
    - packages/domain/src/server/connection-result.ts (CURRENT source, both branches of `applyConnectionResult`)
    - apps/control-plane/src/services/edit-server.ts (CURRENT source, the identity-change block)
    - apps/control-plane/src/services/connect-and-discover.ts (CURRENT source — the `pendingFingerprintSeenAt` derivation and the catch block)
    - apps/web/src/lib/detail-state.ts (the UI's own gating, to state which side is authoritative now)
    - .planning/todos/completed/ and .planning/todos/pending/ (where `2026-09-19-trust-fingerprint-toctou.md` actually is)
    - .planning/phases/05-ui-web/05-REVIEW.md (only for the GR-xx identifiers)
  </read_first>
  <action>
    Build section 2 of the gate document: one row per `missing:` item of gap 6 (there are six), each with `verdict` (CLOSED / PARTIAL / OPEN / UNVERIFIED), `first-hand evidence` and `residual`. At minimum, derive each of these yourself:

    1. `grep -n "lastErrorCode" apps/control-plane/src/services/trust-fingerprint.ts` — record the literal output, and quote the guard and the conditional-UPDATE predicate with their line numbers. Zero output means gap 6 is still OPEN regardless of any SUMMARY.
    2. Quote `applyConnectionResult`'s `ok:true` return object and its failure-branch `pendingFingerprint` expression with line numbers; state whether a parked value survives either.
    3. Quote `edit-server.ts`'s host-identity fingerprint clear with line numbers, state whether it sits outside the `row.status === 'CONNECTED'` block, and state whether `sshUser` is included in its predicate (it must NOT be — an `sshUser`-only change must preserve the trusted host key).
    4. Quote `connect-and-discover.ts`'s `pendingFingerprintSeenAt` derivation, and state whether it can null the timestamp when the value is nulled.
    5. Name the test file, test title and line that covers "HOST_KEY_CHANGED parks F → later non-host-key failure → POST /trust-fingerprint F is refused", and cite the line in `gate-logs/test-integration.log` where it passed. If you cannot find that line in the log, the verdict is UNVERIFIED.
    6. State where `2026-09-19-trust-fingerprint-toctou.md` currently lives and whether its closing evidence (if any) matches what you derived in items 1-5.

    Then, in section 3, one row per residual from 05-VERIFICATION.md's `gaps_remaining:` — GR-03 logging, GR-05 provenance timeouts, the F1 local build defect, WR-A-04's logger guard, the contract seam, and the two contrast residuals — with its status (CLOSED / PARTIAL / OPEN / SKIPPED BY USER) and first-hand evidence: a source assertion you read, or a line in a gate log. Detect a skipped plan from the tree (the absence of its SUMMARY and of its code change), and record it as SKIPPED BY USER rather than as a failure.

    Then reconcile requirements: for each of SERV-04, DETL-01, DETL-02, ACT-02, SET-01, UI-01, UI-02, DISC-02, QA-04, QA-05, state whether your evidence supports the status `.planning/REQUIREMENTS.md` currently records, and where it does not, what the status SHOULD be and why. QA-04 and QA-05 must remain Pending — `git remote -v` is empty and no GitHub Actions run has ever executed for this repository; re-run that command and record its output. Do not edit that file.
  </action>
  <verify>
    <automated>grep -c "UNVERIFIED\|CLOSED\|PARTIAL\|OPEN" .planning/phases/05-ui-web/05-46-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Section 2 has exactly six rows, one per gap-6 `missing:` item, each with a verdict and first-hand evidence.
    - Every evidence cell is either a command with its actual output, a `file:line` quote from source read in this session, or a cited line in a `gate-logs/` file. No cell cites a `SUMMARY.md`.
    - Item 3's row states explicitly whether `sshUser` is excluded from the host-identity predicate, and flags it as a defect if it is not.
    - Section 3 covers all seven named residuals with a status and evidence, including any marked SKIPPED BY USER with the tree-based evidence for that.
    - The requirements reconciliation covers all ten IDs; QA-04 and QA-05 are recorded Pending with the literal `git remote -v` output.
    - `git status --short` shows no change to `.planning/REQUIREMENTS.md`, `.planning/STATE.md` or `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>Gap 6 and every residual have a verdict standing on evidence derived in this session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Hand the human-only items to the user</name>
  <files>.planning/phases/05-ui-web/05-46-GATE.md</files>
  <read_first>
    - .planning/phases/05-ui-web/05-46-GATE.md (sections 1-3, as written by Tasks 1-2)
    - .planning/phases/05-ui-web/05-VERIFICATION.md (the `human_verification:` frontmatter block — all seven items; none was closed by this round)
    - .planning/phases/05-ui-web/05-HUMAN-UAT.md (the UAT items)
    - docs/contrast-decision-05.md (including the D4/D5 section, if plan 05-45 ran)
    - docs/ci-readiness.md
  </read_first>
  <action>
    Present the seven items under `how-to-verify` verbatim and in that order, then STOP and wait. Do not summarise them away, do not merge them, and do not mark any of them satisfied on the strength of an automated result — each is on this list precisely because no command in this repository can settle it.

    Record the user's literal response, item by item, as section 4 ("Human verification") of `.planning/phases/05-ui-web/05-46-GATE.md` and in the SUMMARY. If the user defers an item, write "deferred" against it with their reason; never write "passed" for an item they did not actually check.
  </action>
  <what-built>
    Up to eight plans (05-38 … 05-45) landed in this round: the domain-level `pendingFingerprint` clearing and its persistence alignment, the backend `lastErrorCode` gate on trust-fingerprint promotion with real-HTTP regression coverage, the host-identity fingerprint clear on an edit, the recovery-failure log line, the provenance-gate timeouts and the local-build environment default, the pino bare-Error guard, the VALIDATION_FAILED contract-seam test, and the two AA contrast tokens. Section 1 of the gate document records the full cross-suite gate with raw logs; sections 2-3 record a re-derived verdict for gap 6 and for every named residual.
  </what-built>
  <how-to-verify>
    Read `.planning/phases/05-ui-web/05-46-GATE.md` first — especially any row that is not CLOSED.

    Then the items that genuinely cannot be checked from this sandbox. Each is listed in `05-VERIFICATION.md`'s `human_verification:` block; none was closed by this round:

    1. **Trust-new-fingerprint end to end on a real host-key change.** This round's whole reason for existing. On real hardware: change a host's key, watch the banner, open the Trust dialog, confirm the fingerprint shown is the one you verified on the server itself, and trust it. Then the negative case the backend now enforces: with a fingerprint parked, make the next connect fail for an unrelated reason (rotate the credential so it fails AUTH_FAILED) and confirm the Trust action is refused rather than promoting a stale key.
    2. **The new contrast tokens on a real display, both themes** (only if plan 05-45 ran). The ratios are measured, but a ratio is not an aesthetic judgement. Switch light/dark and say whether the links and the red delete-confirm button read as calm and Apple-consistent, or muddy. Your earlier "No me gusta la UI" is explicitly NOT in this round's scope; if it still stands it needs its own phase.
    3. **A real CI run.** QA-04 and QA-05 remain `Pending`. This repository still has no remote, by your policy. To close them a human must push and observe `nightly.yml`'s e2e-repeat job pass 20/20 with its canary job passing in the same run, plus `ci.yml`'s `security` job passing. `docs/ci-readiness.md` records exactly this. No agent did or will do this step.
    4. **A live walkthrough with SSE actually visible** — add server → connect → watch discovery fill in check by check → detail → activity → settings, NOT through a buffering tunnel (the earlier walkthrough used a Cloudflare Quick Tunnel that buffers SSE, so live updates have still never been seen by a human).
    5. **The three unchanged accessibility/device items**: Sheet/Dialog/RowMenu elevation (flat + hairline versus the skill's floating shadow) on a real display; RowMenu with a real screen reader (VoiceOver/NVDA); responsive behaviour below 1280px on real touch hardware plus `prefers-reduced-motion`'s felt effect. Static audits and Playwright viewport assertions cannot substitute for any of these, and one static ARIA audit in this phase already produced a false flag.
    6. **Theme toggle on reload with a stored non-default theme, in a real browser.** Pick the theme that is NOT your OS default, reload the page several times (hard reload included) and say whether you see any flash or flicker of the wrong theme and whether the stored theme persists. The hydration-mismatch fix (WR-C-01) is proven only by a jsdom `hydrateRoot`/`onRecoverableError` unit test, never by a real-browser observation.
    7. **Revoked-session redirect in a second tab.** Open the app in two tabs with the same session, revoke that session from Settings in tab one, and confirm tab two lands on `/login` without a manual reload. Proven so far only by Playwright/integration tests with synthetic conditions; no human pass against a real session has occurred.
  </how-to-verify>
  <acceptance_criteria>
    - The gate document is complete and every non-CLOSED row has a named residual.
    - The user has been shown all seven items explicitly, with QA-04/QA-05 presented as still `Pending` and not quietly rounded up.
    - The user's response is recorded verbatim in section 4 and in the SUMMARY, including which items they checked and which they deferred.
    - No item is recorded as passed on the strength of an automated result.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Human verification" .planning/phases/05-ui-web/05-46-GATE.md</automated>
    <human-check>The user has been shown all seven items and has answered each one.</human-check>
  </verify>
  <resume-signal>Type "approved" to close this gap-closure round, or describe what still needs work (and which of the seven items you actually checked).</resume-signal>
  <done>Section 4 records the user's verbatim response per item, with QA-04/QA-05 still Pending unless a real CI run was observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eight plans' SUMMARY claims → this round's completion record | The documented failure mode of this phase (gap 6 was declared CLOSED against source that contained no such check); this gate is the control |
| the local sandbox → real CI, a real display, real hardware, real assistive tech | Whole classes of evidence are unobtainable here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-46-01 | Repudiation | gap 6 recorded closed on an unverified claim, for the third time | mitigate | Every verdict re-derived from current source or a cited gate-log line; SUMMARY citations are forbidden; a missing log line forces `UNVERIFIED` |
| T-5G-46-02 | Tampering | making the gate green by raising a timeout, skipping a test, or re-running a flake | mitigate | Explicitly forbidden; each failure must be classified with evidence and a `deferred-items.md` citation for any flake claim; the executor may not fix anything |
| T-5G-46-03 | Repudiation | QA-04/QA-05 marked complete without a real CI run | mitigate | Verdict forced to Pending with the literal `git remote -v` output; the required human observation is restated in the checkpoint; `REQUIREMENTS.md` is not edited here |
| T-5G-46-04 | Information Disclosure | raw gate logs committed into `.planning/` containing a secret | mitigate | Before committing, grep every file in `gate-logs/` for `NOODARA_MASTER_KEY`, `BETTER_AUTH_SECRET`, `DATABASE_URL`, `REDIS_URL`, `sk-`, `postgres://` and `BEGIN .* PRIVATE KEY`; if any hit is a real value, redact it in place and record that you did. `pnpm security:scan-leaks` is itself part of the gate |
| T-5G-46-05 | Tampering | supply-chain drift across the round | mitigate | `node scripts/check-package-provenance.mjs` runs in the gate and `git diff` of `package.json`/`pnpm-lock.yaml` across the round is reported (both should be empty — no plan in this round was permitted to install anything) |
| T-5-46-SC | Tampering | npm/pnpm installs | mitigate | This plan installs NOTHING. It runs existing scripts and writes markdown and log files |
</threat_model>

<verification>
1. All eleven gate commands run individually, each with its own raw log file, exit code, duration and counts recorded.
2. Section 2 has six evidence-backed rows for gap 6; section 3 covers all seven residuals.
3. `git remote -v` still empty; no push attempted; QA-04/QA-05 still Pending.
4. `git diff` of `package.json` and `pnpm-lock.yaml` across this round is empty.
5. Every `gate-logs/` file was scanned for secret-shaped values before committing.
6. The human checkpoint was reached and the user's verbatim answer recorded.
</verification>

<success_criteria>
- One run of every suite on the final tree, with raw logs on disk and every claim citing one.
- Gap 6's verdict re-derived from current source plus a named passing test — or honestly marked UNVERIFIED.
- The seven human-only items presented honestly, with QA-04/QA-05 still Pending.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-46-SUMMARY.md` when done, containing the gate result table, the six-row gap-6 verdict table, the seven-row residual table, the ten-requirement reconciliation, the secret-scan result for the log files, and the user's verbatim checkpoint response.
</output>
