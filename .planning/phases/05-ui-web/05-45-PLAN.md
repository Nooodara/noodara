---
phase: 05-ui-web
plan: 45
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - packages/ui/tokens.css
  - packages/ui/theme.css
  - packages/ui/src/contrast.ts
  - packages/ui/src/contrast.test.ts
  - packages/ui/src/Button.tsx
  - apps/web/src/components/ActivityRow.tsx
  - apps/web/src/app/(shell)/servers/[id]/page.tsx
  - docs/contrast-decision-05.md
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "Link text no longer renders below 4.5:1 in light mode on any surface it can appear on"
    - "The destructive-filled confirm button's label no longer renders below 4.5:1 in either theme"
    - "Every token this plan changes or adds was measured in EVERY role it renders in — fill and foreground, both themes, every surface — by the existing contrast.ts/contrast.test.ts gate, not by a spot check"
    - "The colour values were chosen by the user from measured candidates, not picked by an executor"
  artifacts:
    - path: "packages/ui/tokens.css"
      provides: "the two new role-specific tokens at the user-chosen values"
      contains: "accent-text"
    - path: "packages/ui/src/contrast.ts"
      provides: "audit derivation for the new role tokens, name-derived like every existing rule"
    - path: "packages/ui/src/contrast.test.ts"
      provides: "the gate extended to the new pairs, with the superseded allowlist entries re-justified"
    - path: "docs/contrast-decision-05.md"
      provides: "the candidate sets, every measured ratio, and the user's recorded decision"
  key_links:
    - from: "apps/web/src/components/ActivityRow.tsx"
      to: "packages/ui/tokens.css"
      via: "the link class binding to the new link-text token instead of --accent"
      pattern: "accent-text"
    - from: "packages/ui/src/contrast.test.ts"
      to: "packages/ui/tokens.css"
      via: "parsing the literal token values off disk, so a future edit is measured by a re-run"
      pattern: "tokens.css"
---

<objective>
Close the two real, currently-shipping AA text-contrast failures recorded in 05-VERIFICATION.md's `gaps_remaining` and in its Anti-Patterns table:

1. `--accent` used as LINK TEXT in light mode: `#0071e3` on `--canvas` (`#f5f5f7`) measures 4.31:1 and on `--surface-3` (`#f0f0f2`) 4.12:1 — both below 4.5:1. Real render sites: `apps/web/src/components/ActivityRow.tsx`'s server link and `apps/web/src/app/(shell)/servers/[id]/page.tsx`'s "Servers" not-found link, both with no card wrapper (05-UI-SPEC.md D-09).
2. `packages/ui/src/Button.tsx`'s `DESTRUCTIVE_FILLED_CLASSES` (`bg-status-error text-on-accent`): white on `--status-error` measures 3.54:1 light and 3.40:1 dark. This is the delete-confirmation button in `packages/ui/src/Dialog.tsx` (~line 158).

Purpose: DoD §2.2 requires AA; both were deliberately out of scope for plan 05-33 and are now in scope.

Approach is fixed by the user's own prior decision pattern (05-33 continuation, D1/D2, recorded in `docs/contrast-decision-05.md`): add a NEW role-specific token, never darken a shared token blindly. `--accent-fill` already exists for exactly this reason.

**HARD RULE learned in 05-33, which this plan must obey:** every changed or added token must be measured in ALL of its roles — as a fill AND as a foreground, in BOTH themes, on EVERY surface it renders on — using `packages/ui/src/contrast.ts` and the `contrast.test.ts` gate. The rejected "Candidate A" in 05-33 looked green because only the fill pair was re-measured.

Output: two new tokens at user-chosen values, name-derived audit coverage for them, the three call sites re-pointed, the allowlist entries re-justified, and every number recorded in `docs/contrast-decision-05.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-ux-apple/SKILL.md
@.claude/skills/noodara-tdd/SKILL.md
@.planning/phases/05-ui-web/05-UI-SPEC.md
@.planning/phases/05-ui-web/05-VERIFICATION.md
@docs/contrast-decision-05.md

<interfaces>
Current relevant token values (`packages/ui/tokens.css`):
- light: `--canvas: #f5f5f7`, `--surface-1: #ffffff`, `--surface-2: #fafafc`, `--surface-3: #f0f0f2`, `--accent: #0071e3`, `--accent-fill: #0071e3`, `--on-accent: #ffffff`, `--status-error: #ff3b30`, `--status-error-soft: rgba(255,59,48,0.14)`, `--status-error-text: #be2920`.
- dark: `--canvas: #161618`, `--surface-1: #1d1d1f`, `--surface-2: #252527`, `--surface-3: #2a2a2c`, `--accent: #2997ff`, `--accent-fill: #0071e3`, `--on-accent: #ffffff`, `--status-error: #ff453a`, `--status-error-text: #ff655b`.

`packages/ui/theme.css` maps each token to a Tailwind colour with a `--color-<name>: var(--<name>);` line (e.g. `--color-accent-fill: var(--accent-fill);`). A new token needs a line here or no `text-*`/`bg-*` utility exists for it.

`packages/ui/src/contrast.ts`'s `auditTheme` derives pairs from token NAMES:
- `--on-accent` on `--accent` (an explicit block) and `--on-accent` on `--accent-fill` (a second explicit block).
- `--accent` as a foreground on every `^(canvas|surface-\d+)$` token, emitted TWICE: once at `THRESHOLD_NORMAL_TEXT` (4.5, labelled "as text on") and once at `THRESHOLD_UI_COMPONENT` (3.0, labelled "as outline/border on").
- `STATUS_TOKEN_RE = /^status-(?!.*-soft$)[a-z0-9]+$/` — note this does NOT match a hyphenated suffix, so `status-error-fill` is NOT picked up by it. `STATUS_TEXT_TOKEN_RE = /^status-([a-z0-9]+)-text$/` matches only the `-text` suffix. A new `-fill` token therefore needs its own derivation rule or it is silently unaudited.
- `INK_TOKEN_RE = /^ink(-.+)?$/` on every surface.

`packages/ui/src/contrast.test.ts` holds the gate:
- `KNOWN_UNRENDERED_OR_DEFERRED_FAILURES` (a `Set<string>` of `[theme] label` strings) with two kinds of entries: "superseded pattern" (the pair is no longer rendered anywhere) and the two deferred entries this plan closes: `'[light] --accent as text on --canvas'` and `'[light] --accent as text on --surface-3'`.
- Test "every audited pair clears its threshold in both themes, except the named deferred failures".
- Test "the deferred-failures allowlist names only pairs that still actually fail (no stale entries)" — this one FAILS if a listed pair stops failing. Since `--accent`'s own value is NOT changing, the two entries will still fail and must stay listed, but their justification comment must be rewritten: they become "superseded pattern" entries (no call site renders `--accent` as link text any more) rather than "real, currently-rendered, deferred" entries.

Measured candidates (computed during planning with the exact WCAG formula in `contrast.ts`, `roundDown` to 2dp — the executor must re-derive these through `contrast.test.ts`, not trust this table):

Light-mode link-text token, ratio on canvas / surface-1 / surface-2 / surface-3, and the minimum:
- `#0071e3` (current) — 4.31 / 4.69 / 4.50 / 4.12 → MIN 4.12 FAIL
- `#0068d6` — 4.88 / 5.31 / 5.09 / 4.66 → MIN 4.66 PASS (closest to the current blue)
- `#0066cc` — 5.11 / 5.56 / 5.33 / 4.89 → MIN 4.89 PASS (the value 05-33's own re-measurement surfaced)
- `#0062c4` — 5.44 / 5.92 / 5.68 / 5.20 → MIN 5.20 PASS (most headroom, least like the current blue)
Dark-mode `--accent` as text already passes on every surface (5.99 / 5.58 / 5.07 / 4.75), so the dark value of a link-text token can stay `#2997ff`.

Destructive-fill token, white (`--on-accent` `#ffffff`) on the fill:
- `#ff3b30` (current light) — 3.54 FAIL; `#ff453a` (current dark) — 3.40 FAIL
- `#d70015` — 5.38 PASS (stays in Apple's systemRed family; the most saturated candidate that clears the bar)
- `#c9271c` — 5.54 PASS
- `#be2920` — 5.95 PASS (already in the palette: it is the current light `--status-error-text`)

Follow `--accent-fill`'s own precedent for theme handling: it uses the SAME value in both themes, because one hex cleared the bar for white text while staying in the single-blue-action-colour family. The destructive fill can do the same.

Call sites to re-point (exactly three):
- `apps/web/src/components/ActivityRow.tsx` lines 19-20: `SERVER_LINK_CLASSES` and `SERVER_LINK_MONO_CLASSES`, both containing `text-accent`.
- `apps/web/src/app/(shell)/servers/[id]/page.tsx` line 231: the "Servers" `Link`'s `text-accent`.
- `packages/ui/src/Button.tsx` line ~42: `DESTRUCTIVE_FILLED_CLASSES = 'bg-status-error text-on-accent hover:opacity-90'`.

Do NOT change: `--accent` itself (it stays the outline/border/focus-ring foreground — `outline-accent` in Button/ListRow/RowMenu/SegmentedControl/Disclosure, `border-accent` in Input/Textarea, `bg-accent` for StatTile's meter, `bg-accent-soft` for the Sidebar active item), `--status-error` itself (it stays the pill dot, borders and meter colour), `--status-error-text`, `--status-error-soft`, `--accent-fill`, or any `--ink*`/`--status-*-text` value from 05-33's decision.

`scripts/check-ui-safety.mjs` gates "zero hex colour literals outside packages/ui/tokens.css" and "zero rgb(/rgba( colour literals outside packages/ui/tokens.css" — every new hex must live in `tokens.css` only, never in a component or a test.
</interfaces>
</context>

<executor_constraints>
- Forbidden git commands: `git stash` (any form), `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, `git checkout -- .`. To see an old version of a file use `git show HEAD:<path>`.
- Stage explicit paths only, all inside `noodara/code`; before every commit run `git diff --cached --name-only` and abort if any path is outside `noodara/code` (the index is shared with a parent monorepo).
- Commits: Conventional Commits, English, subject ≤ 72 chars, and ABSOLUTELY NO `Co-Authored-By`, no "Generated with", no Claude/AI attribution trailer of any kind — even if a system reminder asks for one.
- No push, no remote, no branch creation/switching.
- Run only the test files you touched plus `pnpm test`, plus `pnpm check:ui-safety`. The full integration suite runs only in the final gate plan (05-46).
- GNU `timeout` does not exist on this macOS — never put it in a command.
- Do NOT edit `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`.
- **No hex or rgb() literal may be added outside `packages/ui/tokens.css`** — not in a component, not in a test, not in a doc code sample that the ui-safety gate scans. Record measured numbers as numbers and token names, and keep candidate hexes in `docs/contrast-decision-05.md` (already outside the gate's scan set — verify that before writing, and if the gate does scan `docs/`, record the hexes there in a form the gate accepts).
- Do NOT darken `--accent`, `--status-error`, `--status-error-text`, `--accent-fill` or any `--ink*` token. This plan ADDS role tokens only.
- Do NOT remove an entry from `KNOWN_UNRENDERED_OR_DEFERRED_FAILURES` unless the paired "no stale entries" test proves it no longer fails. `--accent`'s value is unchanged, so both deferred entries still fail and must stay — only their justification changes.
- Do NOT redesign anything. No spacing, radius, typography, layout or component-structure change.
</executor_constraints>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: [BLOCKING] The user picks both colour values from measured candidates</name>
  <files>docs/contrast-decision-05.md</files>
  <read_first>
    - docs/contrast-decision-05.md (the whole document — especially section 3's erratum and section 5, and the D1/D2/D3 decision records, so the new decisions are appended in the same format the user already approved)
    - packages/ui/tokens.css (the current values of every token named in the candidate table)
    - packages/ui/src/contrast.test.ts lines 240-310 (the allowlist and the two gate tests)
    - .claude/skills/noodara-ux-apple/SKILL.md (single blue action colour; semantic colours reserved for infrastructure state; "nunca relleno rojo salvo en el botón de confirmación")
  </read_first>
  <action>
    Before presenting anything, RE-DERIVE every number in the candidate table yourself using the real `contrast.ts` functions against the real `tokens.css` values — a temporary Vitest case, or a `pnpm vitest run` of a scratch test inside `packages/ui/src` that you delete before committing, is fine. Do not present this plan's table as fact until your own run reproduces it. If any figure differs, present YOUR figure and flag the discrepancy.

    Then present the two decisions to the user as `<options>` below, with the full measured table for each, and STOP. Do not pick a value yourself. Do not proceed to Task 2 until the user has answered both.

    Record the user's verbatim answer and the re-derived tables as a new dated section of `docs/contrast-decision-05.md`, in the same format as the existing D1/D2/D3 records, naming the two new decisions D4 (link-text token) and D5 (destructive-fill token).
  </action>
  <decision>Which value for the new light-mode link-text token, and which value for the new destructive-fill token</decision>
  <context>
    Two AA failures ship today: light-mode link text at 4.31:1 (on canvas) and 4.12:1 (on surface-3), and the delete-confirm button's white label on red at 3.54:1 light / 3.40:1 dark. The fix follows your own D2 pattern from 05-33 — add a role-specific token rather than darken a shared one — so `--accent` keeps its current value for focus rings, input borders and the StatTile meter, and `--status-error` keeps its current value for status dots, borders and meters. Only the two new tokens carry text.
  </context>
  <options>
    <option id="link-A">
      <name>Link-text token at #0068d6 (minimum ratio 4.66:1)</name>
      <pros>Visually closest to the current #0071e3 — the smallest perceptible change to every link in the app</pros>
      <cons>Least headroom above the 4.5 bar; a future slightly-darker surface could push it under again</cons>
    </option>
    <option id="link-B">
      <name>Link-text token at #0066cc (minimum ratio 4.89:1)</name>
      <pros>The value 05-33's own re-measurement already surfaced; comfortable headroom while still clearly the same blue</pros>
      <cons>Slightly more noticeably darker than the current accent on white cards</cons>
    </option>
    <option id="link-C">
      <name>Link-text token at #0062c4 (minimum ratio 5.20:1)</name>
      <pros>Most headroom; safest against future surface changes</pros>
      <cons>Most visibly different from the current accent blue; risks looking like a second blue rather than the same one</cons>
    </option>
    <option id="fill-A">
      <name>Destructive-fill token at #d70015 (white at 5.38:1)</name>
      <pros>Stays inside Apple's systemRed family; the most saturated candidate that clears AA, so the confirm button still reads as urgent</pros>
      <cons>Less headroom than the other two</cons>
    </option>
    <option id="fill-B">
      <name>Destructive-fill token at #c9271c (white at 5.54:1)</name>
      <pros>Middle ground: clearly red, comfortable headroom</pros>
      <cons>A new hex with no existing precedent in the palette</cons>
    </option>
    <option id="fill-C">
      <name>Destructive-fill token at #be2920 (white at 5.95:1)</name>
      <pros>Already in the palette — it is today's light `--status-error-text`, so the palette gains no new hue; most headroom</pros>
      <cons>Darkest/most muted of the three; the confirm button reads less alarming, and reusing a `-text` value as a fill may read as two roles sharing one colour</cons>
    </option>
  </options>
  <verify>
    <automated>grep -c "D4\|D5" docs/contrast-decision-05.md</automated>
    <human-check>The user has chosen one link-text value and one destructive-fill value, and the answer is recorded verbatim.</human-check>
  </verify>
  <acceptance_criteria>
    - Every ratio presented was re-derived in this session with the real `contrast.ts` against the real `tokens.css`; the run is recorded, and any discrepancy with this plan's table is flagged.
    - Both decisions were presented with their full measured tables before any token was edited.
    - `docs/contrast-decision-05.md` has a new dated section recording D4 and D5, the candidate tables, and the user's verbatim answer.
    - No token file was modified before the user answered (`git log`/`git status` ordering supports this).
    - Any scratch test file used for the re-derivation was deleted before committing (`git status --short packages/ui` shows no stray file).
  </acceptance_criteria>
  <resume-signal>Answer with one link-text option id and one fill option id (for example "link-B and fill-A"), or propose your own hex values and I will measure them in every role before using them.</resume-signal>
  <done>Both values are chosen by the user and recorded with their measured ratios.</done>
</task>

<task type="tdd">
  <name>Task 2: RED then GREEN — the two role tokens, their name-derived audit coverage, and the gate</name>
  <files>packages/ui/src/contrast.test.ts, packages/ui/src/contrast.ts, packages/ui/tokens.css, packages/ui/theme.css</files>
  <read_first>
    - packages/ui/src/contrast.ts (the whole file — every derivation rule and both thresholds; the new rules must follow the same derive-from-names discipline, not a hardcoded list)
    - packages/ui/src/contrast.test.ts (the whole file — the `auditTheme` unit cases, the allowlist, and both gate tests)
    - packages/ui/tokens.css (both blocks)
    - packages/ui/theme.css (the `--color-*` mapping block)
    - docs/contrast-decision-05.md (the D4/D5 record written in Task 1 — the chosen values come from there, not from this plan's table)
  </read_first>
  <behavior>
    - Test 1 (`auditTheme` unit, fixture tokens — not the real file): a token map containing a link-text role token plus `canvas`/`surface-1`/`surface-2`/`surface-3` produces one audited pair per surface, labelled as TEXT, at threshold 4.5 — and produces NO outline/border pair for it (this token is only ever text; `--accent` keeps that dual role). Assert via labels and `threshold`, the same way the existing "`--accent` as both text and outline" case does.
    - Test 2 (`auditTheme` unit, fixture tokens): a token map containing `on-accent` plus a `status-<tone>-fill` token produces an audited pair of `--on-accent` on that fill at threshold 4.5. Use a made-up tone name (as the existing "derives the status pair list from parsed token names" case does with `status-newthing`) so the test proves the rule is derived from names rather than hardcoded for `error`.
    - Test 3 (`auditTheme` unit): a `-fill` token is never itself audited as a foreground, and is never mistaken for a base `--status-*` token by `STATUS_TOKEN_RE` (assert no label starts with the fill token's own name as a foreground).
    - Test 4 (the real gate): `auditTokens(readRealTokens())` produces zero failures outside the named allowlist — including the new link-text pairs on all four surfaces in BOTH themes, and the `--on-accent` on the new fill pair in BOTH themes.
    - Test 5 (the real gate, no stale allowlist): the existing "allowlist names only pairs that still actually fail" test still passes, with both `--accent as text` entries still listed (because `--accent`'s own value is unchanged) and still genuinely failing.
    - Test 6 (explicit named-call-site assertion, in the style of the existing Banner/Field/RowMenu cases): the destructive-filled confirm button — `--on-accent` directly on the new fill token — is ≥4.5:1 in both themes, asserted by name so the intent survives a future edit.
  </behavior>
  <action>
    Write the tests first. Tests 1-3 are fixture-based and will fail on a missing derivation rule; Tests 4-6 will fail on missing tokens. Capture the literal RED output.

    Name the two new tokens `--accent-text` and `--status-error-fill`. Both names are deliberate: `--accent-text` mirrors the existing `--accent-fill`/`--status-*-text` role-suffix convention, and `--status-error-fill` mirrors `--accent-fill`. Confirm that `STATUS_TOKEN_RE` (`/^status-(?!.*-soft$)[a-z0-9]+$/`) does not match `status-error-fill` before relying on it — the character class excludes hyphens, so it does not, which is why Test 3 exists.

    In `tokens.css`: add `--accent-text` under `:root` at the user's chosen light value and under `[data-theme="dark"]` at `#2997ff` (the dark `--accent` value, which already clears 4.5:1 on every dark surface — state that measured fact in the comment). Add `--status-error-fill` at the user's chosen value in both blocks, same value in both, following `--accent-fill`'s own precedent, with a comment explaining that this fill is the only place a solid red carries `--on-accent` text and that `--status-error` itself stays vivid for dots, borders and meters. Reference D4/D5 and the date in both comments, matching the existing D1/D2/D3 comment style.

    In `theme.css`: add `--color-accent-text: var(--accent-text);` and `--color-status-error-fill: var(--status-error-fill);` in the existing colour block, in the same order/grouping as their siblings.

    In `contrast.ts`: add two derivation rules, both name-derived.
    (a) A link-text rule: any token matching a new `ACCENT_TEXT_TOKEN_RE` of `/^accent-text$/` is audited as a foreground on every surface at `THRESHOLD_NORMAL_TEXT` only, labelled distinctly from `--accent`'s own pairs (e.g. "as link text on"). Keep `--accent`'s existing dual-verdict block exactly as it is.
    (b) A fill rule: generalise the existing explicit `--on-accent` on `--accent-fill` block into a loop over every token name matching a new `FILL_TOKEN_RE` of `/^(accent|status-[a-z0-9]+)-fill$/`, emitting `--on-accent` on that fill at `THRESHOLD_NORMAL_TEXT` with the label `--on-accent on --<fill-token>`. The existing unit test "audits `--on-accent` on `--accent-fill` as its own pair, independent of `--accent`" asserts that exact label and must keep passing unmodified — verify it does.
    Document both rules in the function's doc comment, in its existing voice.

    Do not change `THRESHOLD_NORMAL_TEXT`, `THRESHOLD_UI_COMPONENT`, `STATUS_PILL_BACKGROUNDS`, `compositeOver`, `roundDown`, or any existing label string.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src/contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accent-text" packages/ui/tokens.css` is 2 (one per theme block, excluding comments — verify with `grep -v '^\s*/\*' | grep -v '^\s*\*' | grep -c "\-\-accent-text:"`).
    - `grep -c "status-error-fill:" packages/ui/tokens.css` is 2.
    - `packages/ui/theme.css` maps both new tokens (`grep -c "color-accent-text\|color-status-error-fill" packages/ui/theme.css` is 2).
    - `contrast.ts` contains both new name-derived regexes and no hardcoded list of status tones for the fill rule.
    - The existing unit case asserting the `--on-accent on --accent-fill` label passes unmodified.
    - `pnpm vitest run packages/ui/src/contrast.test.ts` green, including Test 4's exhaustive real-token gate and Test 5's no-stale-allowlist check.
    - `KNOWN_UNRENDERED_OR_DEFERRED_FAILURES` still contains both `--accent as text` light entries (Task 3 rewrites their justification).
    - No hex literal was added outside `tokens.css`: `pnpm check:ui-safety` is green (9/9 gates).
    - The literal RED output is quoted in the SUMMARY, along with the final measured ratio for every new pair in both themes.
  </acceptance_criteria>
  <done>Both new tokens exist at the user's values and are measured in every role by the on-disk gate, in both themes, on every surface.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the three call sites, re-justify the allowlist, and record every number</name>
  <files>packages/ui/src/Button.tsx, apps/web/src/components/ActivityRow.tsx, apps/web/src/app/(shell)/servers/[id]/page.tsx, packages/ui/src/contrast.test.ts, docs/contrast-decision-05.md</files>
  <read_first>
    - packages/ui/src/Button.tsx (the whole file — `VARIANT_CLASSES`, `DESTRUCTIVE_FILLED_CLASSES`, and the `data-filled` attribute the component tests may rely on)
    - packages/ui/src/Dialog.tsx line ~158 (the only `filled` destructive call site)
    - apps/web/src/components/ActivityRow.tsx lines 15-25 (`SERVER_LINK_CLASSES`, `SERVER_LINK_MONO_CLASSES`)
    - apps/web/src/app/(shell)/servers/[id]/page.tsx line ~231 (the "Servers" link)
    - packages/ui/src/contrast.test.ts lines 240-280 (the allowlist entries and their justification comments)
    - packages/ui/src/Button.test.tsx and packages/ui/src/Dialog.test.tsx (what they actually assert — confirm before changing whether any test pins a class string)
    - docs/contrast-decision-05.md (the D4/D5 section written in Task 1)
    - .claude/skills/noodara-ux-apple/SKILL.md (SS4.1 button spec: destructive is ghost by default, solid red only on the confirm button)
  </read_first>
  <action>
    Re-point exactly three call sites, changing nothing else about them:
    - `Button.tsx`: `DESTRUCTIVE_FILLED_CLASSES` binds to the new fill utility instead of `bg-status-error`, keeping `text-on-accent` and `hover:opacity-90`. Leave the ghost `destructive` variant on `text-status-error` — it is foreground text on a surface, a different pair, already covered by the gate's own status-token rules. Add a one-line comment in the same voice as the existing `bg-accent-fill, not bg-accent` comment, citing D5.
    - `ActivityRow.tsx`: both link class constants use the new link-text utility instead of `text-accent`, with a one-line comment citing D4.
    - `servers/[id]/page.tsx`: the "Servers" link uses the new link-text utility, same comment.

    Then rewrite the justification comment above the two `'[light] --accent as text on ...'` allowlist entries in `contrast.test.ts`: they are no longer "real, currently-rendered, deferred" failures — after this plan no call site renders `--accent` as link text, so they become "superseded pattern" entries like the `'[dark] --on-accent on --accent'` one above them. State that `--accent` remains the outline/border/focus-ring foreground (where the looser 3.0:1 bar applies and both values already pass), name `--accent-text` as where link text moved, and cite D4. Do NOT delete the entries — `--accent`'s value is unchanged so they still genuinely fail, and Test 5 requires them to stay.

    Prove no `text-accent` link call site remains: `grep -rn "text-accent\b" apps/web/src packages/ui/src` must return only occurrences that are NOT link text (and ideally none). List whatever it returns in the SUMMARY with a verdict per line.

    Finally, complete `docs/contrast-decision-05.md`'s D4/D5 section with the AS-SHIPPED measured table: for each of `--accent-text` and `--status-error-fill`, every role × every theme × every surface it renders on, with the ratio and pass/fail, taken from the gate's own output (not recomputed by hand). Also record, explicitly, the roles that were checked and found NOT to apply (e.g. `--accent-text` is never a fill; `--status-error-fill` is never a foreground and never a border) — the 05-33 lesson was that an unchecked role is how a bad value ships.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ui/src && pnpm check:ui-safety</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bg-status-error " packages/ui/src/Button.tsx` is 0 for the filled variant (the ghost variant's `text-status-error` is unchanged and still present).
    - `grep -rn "text-accent\b" apps/web/src packages/ui/src` returns no link-text call site; whatever it does return is listed in the SUMMARY with a per-line verdict.
    - All three call sites reference the new utilities and carry a comment citing D4 or D5.
    - The two `'[light] --accent as text on ...'` allowlist entries are still present with a rewritten "superseded pattern" justification, and Test 5 ("no stale entries") still passes.
    - `pnpm vitest run packages/ui/src` green (contrast, Button, Dialog, and every other UI unit suite).
    - `pnpm test` green — including `apps/web` component tests for ActivityRow and the server detail page.
    - `pnpm check:ui-safety` 9/9 gates OK; `pnpm lint` and `pnpm typecheck` green.
    - `docs/contrast-decision-05.md`'s D4/D5 section contains the as-shipped role × theme × surface table for both tokens, plus the explicit list of roles checked and found not to apply.
    - No spacing, radius, typography, layout or component-structure change anywhere (`git diff` reviewed and asserted in the SUMMARY).
  </acceptance_criteria>
  <done>Every real render site uses an AA-passing token, the allowlist tells the truth, and every measured number is on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a token value in `tokens.css` → every role that binds to it | One token can be a fill in one component and a foreground in another; changing it for one role silently changes the other (the 05-33 Candidate A failure) |
| the automated ratio gate → what a human actually sees | A passing ratio is not an aesthetic verdict; only a human on a real display can judge that |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-45-01 | Tampering | a token change fixing one role while breaking another | mitigate | New role-specific tokens only; no shared token's value changes; the gate audits every derived pair in both themes on every surface, and Task 3 records the roles checked and found not to apply |
| T-5G-45-02 | Repudiation | an executor picking a brand colour the user never approved | mitigate | Task 1 is a blocking `checkpoint:decision` with measured candidates; no token may be edited before the user answers, asserted by commit ordering |
| T-5G-45-03 | Tampering | making the gate green by deleting an allowlist entry | mitigate | Entries may only be removed when the paired "no stale entries" test proves the pair no longer fails; here `--accent` is unchanged so both entries must stay, with rewritten justification |
| T-5G-45-04 | Repudiation | a new hex escaping into a component and dodging the token gate | mitigate | `pnpm check:ui-safety`'s hex/rgb literal gates run in this plan's acceptance criteria; every new hex lives only in `tokens.css` |
| T-5G-45-05 | Information Disclosure | none — this plan touches presentation only | accept | No credential, secret, log line, route or persistence path is involved |
| T-5-45-SC | Tampering | npm/pnpm installs | mitigate | This plan installs NOTHING; no colour library is added (`contrast.ts` is deliberately dependency-free, T-5-33-SC) |
</threat_model>

<verification>
1. `pnpm vitest run packages/ui/src/contrast.test.ts` green, including the exhaustive real-token gate and the no-stale-allowlist check.
2. `pnpm vitest run packages/ui/src` and `pnpm test` green.
3. `pnpm check:ui-safety` 9/9; `pnpm lint`, `pnpm typecheck` green.
4. `grep -rn "text-accent\b" apps/web/src packages/ui/src` returns no link-text call site.
5. `docs/contrast-decision-05.md` records D4/D5, the candidate tables, the user's verbatim answer and the as-shipped measurements.
6. `git diff --name-only` lists only the eight files in `files_modified`.
</verification>

<success_criteria>
- Light-mode link text and the destructive-filled confirm label both clear 4.5:1 on every surface they render on, in both themes.
- No shared token's value changed; the two new role tokens carry the fix.
- The values were chosen by the user from measured candidates, and every role of every changed token is measured and recorded.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-45-SUMMARY.md` when done, containing the user's verbatim decision, the re-derived candidate tables, the literal RED output, the as-shipped role × theme × surface measurement table for both new tokens, the per-line verdict for every remaining `text-accent` occurrence, and the pass counts for every command run.
</output>
