---
phase: 05-ui-web
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - scripts/check-package-provenance.mjs
  - docs/adr/0000-package-legitimacy-approvals.md
  - .planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md
  - .github/workflows/ci.yml
autonomous: false
requirements: [UI-01, QA-05]

must_haves:
  truths:
    - "Every net-new frontend and component-test dependency this phase installs is listed in the provenance gate with its expected owner/repo, and the gate passes (ADR-0000)"
    - "The eight packages not covered by 05-RESEARCH.md's audit table are human-verified on their registry pages before any install"
    - "04-SECURITY.md records the UF-01/T-4-02/T-4-10/T-4-32/T-4-38 remediations with evidence and reads threats_open: 0 (D-17)"
    - "CI runs the provenance gate as its own named step so a future dependency cannot be added without passing it"
  artifacts:
    - path: "scripts/check-package-provenance.mjs"
      provides: "EXPECTED_PACKAGES rows for next, react, react-dom, tailwindcss, @tailwindcss/postcss, @types/react, @types/react-dom, lucide-react, playwright, @playwright/test, the seven @radix-ui/react-* packages, and the five component-test DOM packages"
      contains: "vercel/next.js"
    - path: "docs/adr/0000-package-legitimacy-approvals.md"
      provides: "Phase 5 additions section recording every verdict, including the component-test DOM stack"
      contains: "Phase 5 additions"
    - path: ".planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md"
      provides: "remediation audit trail and closed sign-off"
      contains: "threats_open: 0"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-package-provenance.mjs"
      via: "a provenance step in the security job"
      pattern: "check-package-provenance"
---

<objective>
Open the gates this phase's frontend build depends on: the package legitimacy gate for the whole Next.js/Radix/Tailwind/Playwright stack **plus the Vitest DOM component-test stack**, and the Phase 4 security sign-off D-17 requires.

Purpose: ADR-0000 makes the provenance script the non-bypassable gate for every new dependency, and 05-RESEARCH.md's audit table covers 14 of the 22 packages this phase needs — the eight it does not cover must be human-verified, not silently installed. Nothing in `packages/ui` or `apps/web` may be installed before this plan completes.
Output: an extended provenance table that passes, an ADR record, a CI step, and a closed Phase 4 security file.

**Why five test packages appear here:** CLAUDE.md §2.1 makes RED→GREEN test-first non-negotiable, so `packages/ui` and `apps/web` components are verified by colocated Vitest component tests (ADR-0005, written in Plan 05-06), not deferred wholesale to Playwright. That requires a DOM environment and a React testing library. Those packages enter through **this existing gate and this existing checkpoint** — no second gate is created anywhere in the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ui-web/05-CONTEXT.md
@.planning/phases/05-ui-web/05-RESEARCH.md
@.planning/phases/05-ui-web/05-PATTERNS.md
@.planning/phases/05-ui-web/05-01-SUMMARY.md
@.planning/phases/05-ui-web/05-02-SUMMARY.md
@docs/adr/0000-package-legitimacy-approvals.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the eight packages missing from the research audit table</name>
  <what-built>
    05-RESEARCH.md's Package Legitimacy Audit ran `slopcheck` and `npm view` against 14 packages and approved all of them: `next`, `react`, `react-dom`, `tailwindcss`, `lucide-react`, `@playwright/test`, `playwright` and seven `@radix-ui/react-*` packages.

    Eight packages this phase genuinely needs are NOT in that table and therefore carry no verdict.

    **Group A — build/type packages (3):**
    - `@tailwindcss/postcss` — required to run Tailwind v4 inside Next.js (expected repository: `tailwindlabs/tailwindcss`)
    - `@types/react` — required for TSX type-checking under this repo's strict tsconfig (expected repository: `DefinitelyTyped/DefinitelyTyped`)
    - `@types/react-dom` — same (expected repository: `DefinitelyTyped/DefinitelyTyped`)

    **Group B — component-test DOM stack (5), all `devDependencies`, none ever shipped to the browser bundle:**
    - `jsdom` — the DOM environment Vitest runs component tests in (expected repository: `jsdom/jsdom`; resolved 30.1.0)
    - `@testing-library/dom` — required peerDependency of the two packages below, not an optional extra (expected repository: `testing-library/dom-testing-library`; resolved 10.4.2)
    - `@testing-library/react` — renders a component into jsdom and queries it by role/text (expected repository: `testing-library/react-testing-library`; resolved 16.3.3)
    - `@testing-library/jest-dom` — the `toBeDisabled` / `toHaveAttribute` / `toBeInTheDocument` matchers, registered via its `/vitest` entry point (expected repository: `testing-library/jest-dom`; resolved 7.0.1)
    - `@testing-library/user-event` — real keystroke and click sequences, needed to drive the type-the-name confirm gate and the Radix segmented control (expected repository: `testing-library/user-event`; resolved 14.6.7)

    Group B exists because CLAUDE.md §2.1's TDD rule is non-negotiable: every component task in this phase writes a failing component test before the component. `@vitejs/plugin-react` is deliberately NOT in this list — Vite's esbuild transform already honours `jsx: "react-jsx"` from `packages/ui/tsconfig.json`, and Fast Refresh is irrelevant in a test run.

    Per ADR-0000 and this project's `[ASSUMED]`-package rule, all eight are treated as unverified until a human confirms them. No install has happened yet, in any package.
  </what-built>
  <action>
    Present all eight unaudited packages (grouped as A and B above) to the user with their expected repositories and resolved versions, wait for an explicit verdict, and record that verdict in the SUMMARY. Install nothing until the verdict is given; if a package is rejected, stop and report rather than substituting an alternative.
  </action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@tailwindcss/postcss and confirm the repository link points at github.com/tailwindlabs/tailwindcss, the weekly download count is in the millions, and the publisher is the Tailwind Labs org.
    2. Open https://www.npmjs.com/package/@types/react and https://www.npmjs.com/package/@types/react-dom and confirm both point at github.com/DefinitelyTyped/DefinitelyTyped and are published by the `types` org.
    3. Open https://www.npmjs.com/package/jsdom and confirm it points at github.com/jsdom/jsdom, downloads are in the tens of millions weekly, and the publisher is a jsdom maintainer.
    4. Open https://www.npmjs.com/package/@testing-library/dom, https://www.npmjs.com/package/@testing-library/react, https://www.npmjs.com/package/@testing-library/jest-dom and https://www.npmjs.com/package/@testing-library/user-event. Confirm each points at the corresponding `github.com/testing-library/*` repository named above and that the publisher is the Testing Library org — a `@testing-library/*` package pointing anywhere other than that org is a typosquat and must be rejected.
    5. If `slopcheck` is available locally, optionally run `slopcheck scan --pkg npm <name> --json` for each of the eight and confirm `"status": "OK"`.
    6. Confirm you accept all eight for install. If any is rejected, say so and name the consequence:
       - `@tailwindcss/postcss` rejected → the only alternative is dropping Tailwind v4 for hand-written CSS, which changes 05-UI-SPEC.md's token strategy and must come back to planning.
       - any Group B package rejected → component-level test-first verification is impossible, which contradicts CLAUDE.md §2.1; that is a planning decision for you, not something the executor may work around by falling back to grep-only acceptance criteria. Stop and return to planning.
  </how-to-verify>
  <resume-signal>Type "approved" to accept all eight, or name the package(s) you reject and why.</resume-signal>
  <done>
    The user has explicitly approved or rejected each of the eight packages, and the verdict is recorded in the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend the provenance gate, record it in ADR-0000, and run it in CI</name>
  <files>scripts/check-package-provenance.mjs, docs/adr/0000-package-legitimacy-approvals.md, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/check-package-provenance.mjs (whole file — `EXPECTED_PACKAGES` is a flat array of `{ name, expectedOwnerRepo }`; `normaliseRepoUrl` uses exact equality, never `.includes()`; the script imports only `node:` builtins by design)
    - docs/adr/0000-package-legitimacy-approvals.md (the existing "Phase 4 additions" section — copy its exact table columns: Package, Expected repository, Observed repository, Resolved version, slopcheck verdict, Automated verdict, Date)
    - .planning/phases/05-ui-web/05-RESEARCH.md (the Package Legitimacy Audit table for the 14 approved packages, and its "Component-test DOM stack" subsection for the five pending ones — the source of every resolved version and repository value; do not invent values)
    - .github/workflows/ci.yml (the `security` job — the new step belongs there, after `pnpm audit`, before `pnpm security:scan-leaks`; note the repo-root assumption comment at the top of the file)
  </read_first>
  <action>
    Append one `{ name, expectedOwnerRepo }` row to `EXPECTED_PACKAGES` for each of these 22 packages, using exactly these expected repositories: `next` → `vercel/next.js`; `react` and `react-dom` → `react/react`; `tailwindcss` and `@tailwindcss/postcss` → `tailwindlabs/tailwindcss`; `@types/react` and `@types/react-dom` → `DefinitelyTyped/DefinitelyTyped`; `lucide-react` → `lucide-icons/lucide`; `playwright` and `@playwright/test` → `microsoft/playwright`; `radix-ui/primitives` for each of `@radix-ui/react-dialog`, `@radix-ui/react-tooltip`, `@radix-ui/react-collapsible`, `@radix-ui/react-radio-group`, `@radix-ui/react-scroll-area`, `@radix-ui/react-visually-hidden`, `@radix-ui/react-checkbox`; and for the component-test stack `jsdom` → `jsdom/jsdom`, `@testing-library/dom` → `testing-library/dom-testing-library`, `@testing-library/react` → `testing-library/react-testing-library`, `@testing-library/jest-dom` → `testing-library/jest-dom`, `@testing-library/user-event` → `testing-library/user-event`. Change nothing else in the script — in particular do not relax `normaliseRepoUrl`'s exact-equality comparison, and keep the zero-third-party-import rule. Note in a comment above the new rows that `react`/`react-dom` legitimately resolve to `react/react` because GitHub redirects the renamed `facebook/react` org (evidence in 05-RESEARCH.md), and that the four `@testing-library/*` packages deliberately do NOT share one repository — each has its own, so a single shared expectation would weaken the gate.

    Run the script and reconcile: if a package's live `repository.url` does not normalise to the expected value, stop and report it rather than editing the expectation to match reality — that inversion is exactly what the gate exists to prevent.

    Append a `### Phase 5 additions` section to `docs/adr/0000-package-legitimacy-approvals.md` using the existing Phase 4 table's column set, one row per package above, with the resolved versions from 05-RESEARCH.md, the slopcheck verdict (`OK` for the 14 audited; `human-verified, registry page` for the eight from Task 1), the automated verdict from the script run, and today's date. Add one sentence recording that the five component-test packages are `devDependencies` of the workspace root only and never reach a browser bundle, and that `@vitejs/plugin-react` was considered and declined (esbuild's `react-jsx` transform suffices).

    Add a step to `ci.yml`'s `security` job running `node scripts/check-package-provenance.mjs`, with a comment stating that it makes the ADR-0000 gate part of the merge gate rather than a manual habit.
  </action>
  <verify>
    <automated>node scripts/check-package-provenance.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-package-provenance.mjs` exits 0 and its output names all 22 new packages
    - `grep -c "radix-ui/primitives" scripts/check-package-provenance.mjs` is 7
    - `scripts/check-package-provenance.mjs` contains `vercel/next.js`, `DefinitelyTyped/DefinitelyTyped`, `tailwindlabs/tailwindcss`, `jsdom/jsdom`, `testing-library/dom-testing-library`, `testing-library/react-testing-library`, `testing-library/jest-dom` and `testing-library/user-event`
    - `grep -c "includes(" scripts/check-package-provenance.mjs` is 0
    - `docs/adr/0000-package-legitimacy-approvals.md` contains `### Phase 5 additions` and one table row per package (22 rows)
    - `.github/workflows/ci.yml` contains `check-package-provenance.mjs`
  </acceptance_criteria>
  <done>Every package this phase will install — build-time, runtime and test-time — is covered by a passing, CI-enforced provenance check and recorded in ADR-0000.</done>
</task>

<task type="auto">
  <name>Task 3: Close the Phase 4 security sign-off</name>
  <files>.planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md</files>
  <read_first>
    - .planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md (whole file — frontmatter `status`/`threats_open`, the threat register rows for T-4-02, T-4-10, T-4-32, T-4-38, the UF-01 finding, and the unchecked Sign-Off section)
    - .planning/phases/05-ui-web/05-01-SUMMARY.md and .planning/phases/05-ui-web/05-02-SUMMARY.md (the actual evidence: files changed, test names, commands run)
  </read_first>
  <action>
    Update the register rows for T-4-02, T-4-10, T-4-32, T-4-38 and the UF-01 finding to a remediated state, each citing the concrete evidence from the two SUMMARY files: the file and symbol that changed (`services/edit-server.ts`'s ERROR-status branch, `auth/session-lookup.ts`'s `withSessionLookupTimeout` applied in `auth/require-session.ts` and `routes/events.ts`, `logger.ts`'s `serializers.err`, `queue/worker-shutdown.ts`'s `runWorkerShutdown`) and the test that proves it. Set the frontmatter to `status: verified` and `threats_open: 0`, and tick the Sign-Off checkboxes. Add a dated audit-trail entry naming Phase 5 Plans 05-01 and 05-02 as the remediation and today's date. Do not restate the vulnerability details as if still open, and do not delete the original findings — the register keeps its history.

    If any of the five items is not actually closed by the two SUMMARY files' evidence, do not flip the frontmatter: leave `status: open`, record what remains, and report it as a blocker instead.
  </action>
  <verify>
    <automated>grep -c "threats_open: 0" .planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-http-routes-worker-bullmq-y-sse/04-SECURITY.md` frontmatter contains `status: verified` and `threats_open: 0`
    - The file contains `session-lookup`, `serializers`, `worker-shutdown` and `edit-server` as remediation evidence references
    - The file's Sign-Off section has no unchecked boxes (`grep -c "^- \[ \]"` against the Sign-Off section is 0)
    - The file contains a dated entry referencing `05-01` and `05-02`
  </acceptance_criteria>
  <done>D-17's precondition is documented as satisfied with traceable evidence, unblocking the frontend waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer/CI machine | 22 net-new packages execute install scripts; 17 of them also ship code into the browser bundle |
| npm registry → test runner | The five component-test packages run with full Node privileges inside `pnpm test`, so a compromised one is as dangerous as a runtime dependency even though it never ships |
| planning record → future reviewer | A prematurely closed security file would let a real open threat be treated as fixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-SC | Tampering | npm installs for the whole frontend stack and the component-test DOM stack | mitigate | `EXPECTED_PACKAGES` extended with exact owner/repo for all 22 packages; script run required to pass; the eight packages absent from the research audit go through this plan's blocking human checkpoint first |
| T-5-09 | Spoofing | typosquatted `@radix-ui/*`, `@types/*` or `@testing-library/*` package | mitigate | Exact-equality repository comparison (never substring) plus a per-package expected owner/repo; each `@testing-library/*` package carries its own distinct repository expectation, so one shared org string cannot satisfy four gates |
| T-5-10 | Repudiation | undocumented dependency approval | mitigate | ADR-0000 "Phase 5 additions" table records expected/observed repo, version, verdict and date per package, plus the devDependency-only note for the test stack |
| T-5-11 | Tampering | gate bypassed by a future dependency addition | mitigate | New CI step in the `security` job runs the script on every PR |
| T-5-12 | Repudiation | security file closed without evidence | mitigate | Task 3 requires per-threat evidence from the two SUMMARY files and forbids flipping the frontmatter otherwise |
</threat_model>

<verification>
- `node scripts/check-package-provenance.mjs` exits 0
- `04-SECURITY.md` reads `status: verified`, `threats_open: 0`
- ADR-0000 has a Phase 5 additions table with 22 rows
- `pnpm lint` green (the script is linted)
</verification>

<success_criteria>
- No package is installed anywhere in this phase without a row in the provenance gate — including the five test-only packages
- The human checkpoint approved (or explicitly rejected, with a recorded consequence) the eight unaudited packages
- Phase 4's security file is closed with traceable evidence
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-03-SUMMARY.md` when done. Commit with a Conventional Commit subject in English, ≤72 chars, and no attribution trailers.
</output>
