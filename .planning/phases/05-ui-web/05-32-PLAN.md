---
phase: 05-ui-web
plan: 32
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - apps/web/src/app/(shell)/activity/page.tsx
  - apps/web/src/lib/activity-groups.ts
  - apps/web/src/lib/activity-groups.test.ts
  - apps/web/src/components/ActivityList.tsx
  - tests/e2e/activity.spec.ts
autonomous: true
requirements: [ACT-02]

must_haves:
  truths:
    - "A failed background refresh keeps the already-loaded activity list on screen instead of replacing it (and every older page already loaded) with an error banner"
    - "A page-1 refresh that returns a full page detects that it may not have reached the previously-newest row and closes the gap rather than silently dropping rows"
    - "Day headers are grouped in the viewer's own time zone, injected explicitly, and deterministic in tests"
  artifacts:
    - path: "apps/web/src/lib/activity-groups.ts"
      provides: "pure grouping and page merging, with a gap-aware refresh merge"
      exports: ["groupByDay", "mergePage"]
    - path: "apps/web/src/app/(shell)/activity/page.tsx"
      provides: "refresh that never discards loaded state on failure"
    - path: "apps/web/src/components/ActivityList.tsx"
      provides: "the viewer time zone threaded into groupByDay"
      contains: "timeZone"
  key_links:
    - from: "apps/web/src/components/ActivityList.tsx"
      to: "groupByDay"
      via: "an explicitly passed viewer time zone, not the UTC default"
      pattern: "groupByDay\\([^)]*timeZone|groupByDay\\(.*,.*,"
---

<objective>
Close VERIFICATION gap 7 (SC4/ACT-02: the activity log stays correct while it refreshes) — 05-REVIEW.md WR-B-04, WR-B-05, WR-B-06.

**These three were reported from reading, never reproduced.** This plan's rule is therefore strict: every task begins with a test that reproduces the defect against the current code. If a defect cannot be reproduced, the task RECORDS that finding and does NOT apply a fix. A "fix" for a bug that was never demonstrated is how this phase accumulated six false subagent claims; do not add a seventh.

Re-verified by reading on 2026-09-20 (starting hypotheses, not conclusions):
- `activity/page.tsx`'s `fetchPage1` failure branch calls `setState({ kind: 'error', … })` unconditionally, including when the previous state was `ready`. `scheduleRefresh` routes tab-focus regain and every `server.updated`/`server.deleted` SSE event into that same `fetchPage1`.
- `mergePage(prev.items, result.data.items, 'refresh')` prepends only genuinely-new items and never compares page boundaries; `PAGE_LIMIT` is 50. A refresh returning a full 50 that share no id with `prev.items` implies an unknown number of unseen rows in between, with no marker.
- `ActivityList.tsx:68` calls `groupByDay(state.items, now)` with two arguments; `groupByDay`'s third parameter is `timeZone: string = DEFAULT_TIME_ZONE` and `DEFAULT_TIME_ZONE` is the literal `'UTC'`.

Purpose: ACT-02 says the admin sees the activity log as a reverse-chronological list. A list that is wiped by a background network blip, silently gapped during a burst, or bucketed into the wrong calendar day is not that list.
Output: three reproductions, and a fix for each one that actually reproduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-tdd/SKILL.md
@.claude/skills/noodara-ux-apple/SKILL.md
@.planning/phases/05-ui-web/05-UI-SPEC.md
@.planning/phases/05-ui-web/05-VERIFICATION.md

<interfaces>
- `apps/web/src/lib/activity-groups.ts` exports `DayGroup<T>`, `groupByDay<T extends HasOccurredAt>(items, now, timeZone = DEFAULT_TIME_ZONE)` and `mergePage<T extends HasId>(existing, incoming, mode: PageMergeMode)` where `PageMergeMode` is `'append' | 'refresh'`. It is pure by contract: no React, no fetch, no platform clock, every calendar computation through `Intl.DateTimeFormat` with an explicit `timeZone`. Preserve that contract.
- `apps/web/src/components/ActivityList.tsx` exports `ActivityListState` (`loading` | `error` | `ready` with `items`, `nextCursor`, `loadingMore`, `onLoadOlder`) and takes an explicit `now: Date` plus `lookupServer`.
- `activity/page.tsx`: `PAGE_LIMIT = 50`, `REFRESH_DEBOUNCE_MS = 500`, `stateRef` mirrors state for stale-closure-free callbacks, `loadOlder`'s failure branch ALREADY does the right thing (keeps rows, clears `loadingMore`) — use it as the in-file precedent for `fetchPage1`'s failure branch.
- `Intl.DateTimeFormat().resolvedOptions().timeZone` is the platform read for the viewer's zone. It is a clock/environment read and therefore belongs in the component layer, NOT in `activity-groups.ts`.
</interfaces>
</context>

<executor_constraints>
- Sequential execution on the main working tree. FORBIDDEN: `git stash`, `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, and any path outside `noodara/code`.
- Conventional Commits in English, subject ≤ 72 chars. NO `Co-Authored-By` and no Claude attribution trailer of any kind.
- GNU `timeout` does not exist on this macOS — never put it in a command.
- `activity-groups.ts` stays pure: no `Date.now()`, no `new Date()` without an argument, no `Intl.DateTimeFormat()` without an explicit `timeZone`, no React, no fetch. Verify with a grep after editing.
- Tests must be deterministic regardless of the runner's own `TZ`. Inject the zone; never assert on the host default.
- Do NOT modify `apps/web/src/lib/api-client.ts` or `apps/web/src/lib/error-copy.ts` (plans 05-28/05-30 own them this wave).
- Metadata rendering rules are unchanged: the UI renders only keys it knows (D-14). Do not widen that.
</executor_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce and fix WR-B-04 — a failed background refresh must not wipe the list</name>
  <files>apps/web/src/app/(shell)/activity/page.tsx, tests/e2e/activity.spec.ts</files>
  <read_first>
    - apps/web/src/app/(shell)/activity/page.tsx (the whole file; compare `loadOlder`'s failure branch with `fetchPage1`'s)
    - apps/web/src/components/ActivityList.tsx
    - tests/e2e/activity.spec.ts
  </read_first>
  <behavior>
    - Given a `ready` list with page 1 and one older page loaded, when a background refresh (triggered by tab-focus regain or an SSE event) fails with any non-401 code, then the list still shows every previously-loaded row, the scroll position is retained, and no full-screen error banner replaces it.
    - A 401 still routes to `requireSession()` exactly as today — that path is unchanged.
    - A FIRST load (previous state is `loading`, not `ready`) that fails still shows the full error banner with Retry — that is the correct behaviour for an empty screen and must not regress.
  </behavior>
  <action>
    Reproduce first. Add an E2E case to `tests/e2e/activity.spec.ts`: load the page, load an older page, then stub `GET /api/activity` to fail and trigger a refresh (dispatch a visibility change, or drive a real server mutation that emits an SSE event — prefer whichever the existing spec already has a helper for). Assert the previously-loaded rows are still present. Observe RED and record the literal output. If it does NOT reproduce, stop, record exactly what you observed and why the reading-based hypothesis was wrong, and skip the fix.

    If it reproduces: in `fetchPage1`'s failure branch, mirror `loadOlder`'s shape — use the functional `setState((prev) => …)` form and, when `prev.kind === 'ready'`, return `prev` unchanged (optionally with a non-destructive inline indication if the UI-SPEC already defines one; if it does not, keep it silent and say so in the SUMMARY rather than inventing an affordance). Only transition to `{ kind: 'error' }` when `prev.kind !== 'ready'`.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/activity.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/activity.spec.ts` contains a named case for a failed background refresh preserving loaded rows, and it passes.
    - `tests/e2e/activity.spec.ts`'s existing first-load-error case still passes (no regression).
    - `grep -n "setState({$\|setState({ *$\|kind: 'error'" apps/web/src/app/\(shell\)/activity/page.tsx` shows the error transition inside a functional updater guarded on `prev.kind`, not as an unconditional `setState` object literal.
    - The SUMMARY quotes the RED output, or — if it did not reproduce — states that plainly and confirms no fix was applied.
  </acceptance_criteria>
  <done>Either the defect is reproduced and fixed, or it is documented as non-reproducible with evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reproduce and address WR-B-05 — the silent gap when a refresh returns a full page</name>
  <files>apps/web/src/lib/activity-groups.ts, apps/web/src/lib/activity-groups.test.ts, apps/web/src/app/(shell)/activity/page.tsx</files>
  <read_first>
    - apps/web/src/lib/activity-groups.ts (`mergePage`)
    - apps/web/src/lib/activity-groups.test.ts
    - apps/web/src/app/(shell)/activity/page.tsx (the `'refresh'` merge branch and `PAGE_LIMIT`)
    - .planning/phases/05-ui-web/05-UI-SPEC.md §2.6 (the Refresh section — whether it already specifies a gap affordance)
  </read_first>
  <behavior>
    - Unit: given `existing` whose newest id is X, and an `incoming` refresh page of exactly `PAGE_LIMIT` items none of which is X, `mergePage` must signal that the boundary was not reached rather than silently concatenating two non-contiguous runs.
    - Unit: an `incoming` refresh page shorter than `PAGE_LIMIT`, or one that overlaps `existing` by at least one id, is contiguous and merges exactly as today.
    - Page behaviour: a detected gap results in the loaded list being made contiguous again (re-fetch to close it, or reset to the fresh page-1 result with the older pages dropped and the cursor reset) — the admin must never be shown two adjacent rows with an unmarked hole between them.
  </behavior>
  <action>
    Reproduce first, at the unit level, in `activity-groups.test.ts`: construct the exact-`PAGE_LIMIT`, zero-overlap case and assert the non-contiguity is detectable. Observe RED. Then, at the page level, prove the user-visible consequence with an E2E or a component test before changing `page.tsx`. If the user-visible consequence cannot be demonstrated, record that and limit the change to the pure layer (or to nothing) — do not restructure the page on a hypothesis.

    Design constraint for the fix: `mergePage` must stay pure and keep its current signature usable by `loadOlder`. Prefer returning a richer result for the `'refresh'` mode (items plus a contiguity flag) over adding a second exported function that call sites can forget to use, and over having the page re-derive contiguity itself. Decide between "re-fetch with a cursor to close the gap" and "reset to the fresh page and drop the older pages" by which one 05-UI-SPEC.md §2.6 actually supports — read it; if it supports neither explicitly, take the simpler reset, document the choice and its UX cost in the SUMMARY, and do not invent a new "N events hidden" affordance that the spec never defined.
  </action>
  <verify>
    <automated>pnpm exec vitest run apps/web/src/lib/activity-groups.test.ts && pnpm exec playwright test tests/e2e/activity.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `activity-groups.test.ts` contains a named case for the exact-`PAGE_LIMIT` zero-overlap refresh and a named case for the contiguous refresh; both pass.
    - `grep -cE "Date\.now\(\)|new Date\(\)|fetch\(|useState|useEffect" apps/web/src/lib/activity-groups.ts` is 0 (purity preserved).
    - `pnpm exec playwright test tests/e2e/activity.spec.ts` exits 0.
    - The SUMMARY quotes the RED output and names the chosen gap-closing strategy with its 05-UI-SPEC.md justification — or documents non-reproduction and no change.
  </acceptance_criteria>
  <done>The silent-gap case is demonstrated and closed, or documented as not demonstrable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-B-06 — group day headers in the viewer's time zone, deterministically</name>
  <files>apps/web/src/components/ActivityList.tsx, apps/web/src/lib/activity-groups.test.ts</files>
  <read_first>
    - apps/web/src/lib/activity-groups.ts (`groupByDay`, `dayKeyFor`, `shiftDayKey`, `labelForDayKey`, `DEFAULT_TIME_ZONE`)
    - apps/web/src/components/ActivityList.tsx (line ~68, the two-argument `groupByDay` call)
    - packages/ui/src/format.ts (`formatRelativeTime` — the existing explicit-clock precedent)
  </read_first>
  <behavior>
    - An event at `2026-03-15T02:30:00Z` renders under the `2026-03-14` day header for a viewer in `America/Mexico_City`, and under `2026-03-15` for a viewer in `UTC`. Both assertions run in the same test file with the zone injected, so the result does not depend on the runner's `TZ`.
    - "TODAY"/"YESTERDAY" labels are computed in the same injected zone as the grouping — no mixing of a UTC `todayKey` with a local `dayKey`.
    - `groupByDay` itself still never reads the platform zone; the viewer's zone is resolved in `ActivityList.tsx` and passed in.
  </behavior>
  <action>
    Add the two-zone unit cases to `activity-groups.test.ts` first and observe whether they already pass — `groupByDay` already accepts a `timeZone` parameter, so the pure layer is likely already correct and the defect is confined to the caller. State plainly in the SUMMARY which layer actually had the bug; do not "fix" the pure module if its tests were green.

    In `ActivityList.tsx`, resolve the viewer zone once (`Intl.DateTimeFormat().resolvedOptions().timeZone`) behind an optional `timeZone` prop so tests can inject a fixed zone and the component defaults to the platform value at runtime. Pass it as `groupByDay`'s third argument. Add a component test (or an E2E with `Playwright`'s `timezoneId` context option, whichever the existing harness supports) asserting the header is the viewer's day, not UTC's.

    Then run the full web gate.
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && pnpm test && pnpm exec playwright test tests/e2e/activity.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `activity-groups.test.ts` contains the two-zone case for the same instant and it passes under at least two different `TZ` values for the runner (run the command twice with different `TZ=` prefixes and record both in the SUMMARY).
    - `grep -n "groupByDay(" apps/web/src/components/ActivityList.tsx` shows a three-argument call.
    - `grep -c "resolvedOptions" apps/web/src/lib/activity-groups.ts` is 0 (the platform read stayed out of the pure module).
    - `pnpm lint`, `pnpm typecheck`, `pnpm test` exit 0 and the activity E2E passes.
  </acceptance_criteria>
  <done>Day boundaries follow the viewer, proven deterministically in two zones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control plane `GET /api/activity` → rendered rows | Server-controlled strings and an opaque cursor crossing into the DOM |
| SSE event burst → refresh scheduling | An attacker-influencable event rate driving client requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-32-01 | Repudiation | activity rows silently dropped in a gap | mitigate | Contiguity detection on the `'refresh'` merge; the list is never shown with an unmarked hole. The audit trail itself is server-side and unaffected — this is a display-integrity fix. |
| T-5G-32-02 | Denial of Service | refresh storm from an SSE burst | accept | The existing 500ms `REFRESH_DEBOUNCE_MS` coalescing is unchanged and adequate; do not remove it while adding gap-closing re-fetches, and assert the debounce still holds |
| T-5G-32-03 | Information Disclosure | activity metadata rendering | accept | D-14's known-keys-only rule is unchanged by this plan; no widening of the rendered key set |
| T-5G-32-04 | Tampering | opaque cursor | mitigate | The cursor stays opaque — passed back exactly as received, `encodeURIComponent`-wrapped, never parsed or reconstructed client-side. Any gap-closing re-fetch must obey the same rule. |
| T-5-32-SC | Tampering | npm/pnpm installs | mitigate | NO new dependency. `Intl` is a platform API. If one seems necessary, stop and report. |
</threat_model>

<verification>
1. `pnpm lint && pnpm typecheck && pnpm test` exit 0.
2. `pnpm exec playwright test tests/e2e/activity.spec.ts` exits 0.
3. `activity-groups.ts` purity grep (`Date.now()`, bare `new Date()`, `fetch(`, `useState`, `useEffect`, `resolvedOptions`) returns 0 matches.
4. The SUMMARY states, for each of WR-B-04/05/06, whether it reproduced — with the literal evidence — and what was (or deliberately was not) changed.

## Nyquist note
`pnpm test:integration` is NOT required by this plan: every file it touches is under `apps/web`/`packages` with no `apps/control-plane` change. The full cross-suite gate runs in plan 05-37.
</verification>

<success_criteria>
- Three defects each either reproduced-and-fixed or documented-as-not-reproducible, with evidence in both cases.
- No fix applied to a defect that was never demonstrated.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-32-SUMMARY.md` when done, with an explicit per-finding reproduction verdict table (WR-B-04 / WR-B-05 / WR-B-06: reproduced yes/no, evidence, action taken).
</output>
</content>
</invoke>
