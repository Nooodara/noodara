---
phase: 05-ui-web
plan: 33
type: execute
wave: 3
gap_closure: true
depends_on: [05-29, 05-31, 05-32, 05-35]
files_modified:
  - packages/ui/tokens.css
  - packages/ui/src/contrast.ts
  - packages/ui/src/contrast.test.ts
  - packages/ui/src/StatusPill.tsx
  - docs/contrast-decision-05.md
autonomous: false
requirements: [UI-01, SERV-04, SET-01]

must_haves:
  truths:
    - "Primary-action text meets WCAG AA (≥4.5:1) in BOTH themes, including dark, which is the theme a fresh install boots into"
    - "Status-pill text meets WCAG AA (≥4.5:1) against its own composited pill background in BOTH themes"
    - "The replacement colours were chosen by the user from measured candidates, not picked by an executor"
    - "An automated test fails the build if any token pair the design system relies on drops below 4.5:1, so this cannot silently regress"
  artifacts:
    - path: "packages/ui/src/contrast.ts"
      provides: "WCAG 2.x relative-luminance and contrast-ratio computation, including alpha compositing over a named surface"
      exports: ["relativeLuminance", "contrastRatio", "compositeOver"]
    - path: "packages/ui/src/contrast.test.ts"
      provides: "the gate that fails below 4.5:1 for every audited pair in both themes"
    - path: "packages/ui/tokens.css"
      provides: "the user-chosen token values"
    - path: "docs/contrast-decision-05.md"
      provides: "the candidate sets, their measured ratios, and the user's recorded decision"
  key_links:
    - from: "packages/ui/src/contrast.test.ts"
      to: "packages/ui/tokens.css"
      via: "parsing the literal token values out of the stylesheet, not a duplicated copy of them"
      pattern: "tokens.css"
---

<objective>
Close the contrast half of VERIFICATION gap 4 (item 3) together with the already-escalated status-pill contrast item (`docs/ui-review-05.md` Accessibility FLAG 1, routed to gap closure by the user on 2026-09-20) and WR-C-08 (`--ink-tertiary` content text).

Measured facts, re-derived from the literal hex values in `packages/ui/tokens.css` on 2026-09-20:
- Dark (the DEFAULT theme a fresh install boots into): `--on-accent #ffffff` on `--accent #2997ff` = **3.02:1** — fails AA's 4.5:1. This is every primary button, every selected segmented-control option, and the skip link.
- Light: `--on-accent #ffffff` on `--accent #0071e3` measures around 4.7:1 — recompute it in Task 1 and treat the recomputed number as authoritative, not this sentence.
- Status pills (full-saturation text on its own 14%-alpha `-soft` background composited over `--surface-1`), from `docs/ui-review-05.md`: light — ok 1.98:1, warn 1.96:1, error 2.95:1, idle 2.84:1 (all fail); dark — ok 6.42:1, warn 6.30:1, error 4.21:1, idle 4.22:1 (error and idle fail narrowly). Recompute all eight; the review's own first draft was wrong once already.
- WR-C-08: `--ink-tertiary` used as content text measures down to 1.83:1.

**The user chooses the replacement colours. An executor does not.** This plan computes candidates, stops at a blocking decision checkpoint, applies exactly what the user picked, and then locks it behind an automated gate.

Purpose: CLAUDE.md §2.2 requires UX consistent with the design system; the `noodara-ux-apple` skill fixes a single blue action colour and reserves semantic colours for infrastructure state. An AA failure on the default theme's primary action is a Definition-of-Done violation.
Output: a measurement module, measured candidate sets, a recorded user decision, the token change, and a regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/skills/noodara-ux-apple/SKILL.md
@.claude/skills/noodara-ux-review/SKILL.md
@.claude/skills/noodara-tdd/SKILL.md
@docs/ui-review-05.md
@.planning/phases/05-ui-web/05-UI-SPEC.md
@.planning/phases/05-ui-web/05-VERIFICATION.md

<interfaces>
- `packages/ui/tokens.css`: `:root` holds the LIGHT theme (`--canvas #f5f5f7`, `--surface-1 #ffffff`, `--surface-2 #fafafc`, `--surface-3 #f0f0f2`, `--ink #1d1d1f`, `--ink-secondary #6e6e73`, `--ink-tertiary #aeaeb2`, `--accent #0071e3`, `--accent-soft rgba(0,113,227,0.1)`, `--on-accent #ffffff`, `--status-ok #34c759`, `--status-warn #ff9500`, `--status-error #ff3b30`, `--status-idle #8e8e93`, each with a `-soft` at 14% alpha). `[data-theme="dark"]` holds the dark theme (`--canvas #161618`, `--surface-1 #1d1d1f`, `--surface-2 #252527`, `--surface-3 #2a2a2c`, `--ink #f5f5f7`, `--ink-secondary #a1a1a6`, `--ink-tertiary #6e6e73`, `--accent #2997ff`, `--on-accent #ffffff`, `--status-ok #30d158`, `--status-warn #ff9f0a`, `--status-error #ff453a`, `--status-idle #8e8e93`).
- `tokens.css` already carries an `OPEN QUESTION` comment block about the status-pill ratios, explicitly saying it was not silently patched. That comment must be replaced by the resolution, not left dangling.
- `packages/ui/src/StatusPill.tsx` maps tone → `bg-status-*-soft text-status-*`; the dot uses `bg-current`. The status word is always in the DOM (never colour-only) — that stays.
- `pnpm check:ui-safety` (`scripts/check-ui-safety.mjs`) enforces zero hex/rgb literals OUTSIDE `tokens.css`. Candidate colours therefore all live in `tokens.css`; the test module must PARSE them from there rather than hardcoding a second copy.
- There is currently no contrast computation anywhere in the repo (`grep -rln "contrast\|luminance" packages/ui/src scripts tests` returns nothing) — `contrast.ts` is new.
</interfaces>
</context>

<executor_constraints>
- Sequential execution on the main working tree. FORBIDDEN: `git stash`, `git add .`, `git add -A`, `git commit -a`, `git reset --hard`, `git clean`, and any path outside `noodara/code`.
- Conventional Commits in English, subject ≤ 72 chars. NO `Co-Authored-By` and no Claude attribution trailer of any kind.
- GNU `timeout` does not exist on this macOS — never put it in a command.
- **This plan is NOT autonomous.** Task 2 is a blocking user decision. Do NOT choose colours, do NOT proceed past it, and do NOT apply a "reasonable default" while waiting. `workflow.auto_advance` does not apply.
- Design-system constraints from `noodara-ux-apple`, non-negotiable: exactly ONE action colour (blue) — do not introduce a second accent; semantic `--status-*` colours are reserved for infrastructure state and never decoration; no shadows on cards or buttons; no decorative gradients; dark mode is first-class and light mode is mandatory.
- Scope boundary: this plan changes token VALUES and, if strictly required by the chosen option, the one class pair in `StatusPill.tsx`. It is NOT a visual redesign. The user's "No me gusta la UI… la vamos a ir mejorando" is future work, explicitly out of scope here.
- No new dependency. The WCAG formula is ~20 lines; do not install a colour library.
</executor_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A measurement module, and the measured audit of every current pair</name>
  <files>packages/ui/src/contrast.ts, packages/ui/src/contrast.test.ts, docs/contrast-decision-05.md</files>
  <read_first>
    - packages/ui/tokens.css (the whole file, both theme blocks)
    - packages/ui/src/StatusPill.tsx
    - packages/ui/src/Button.tsx and the segmented-control component (which token pairs primary actions actually render)
    - scripts/check-ui-safety.mjs (what it forbids and where)
    - docs/ui-review-05.md (Accessibility FLAG 1's table — the figures to reproduce or refute)
  </read_first>
  <behavior>
    - `relativeLuminance(hex)` implements WCAG 2.x exactly: sRGB channel / 255, then `c <= 0.03928 ? c/12.92 : ((c+0.055)/1.055) ** 2.4`, weighted 0.2126 / 0.7152 / 0.0722. Pin it with the standard reference values: white = 1, black = 0, and `#767676` on white ≈ 4.54:1.
    - `contrastRatio(a, b)` = `(Lmax + 0.05) / (Lmin + 0.05)`.
    - `compositeOver(rgbaForeground, opaqueBackground)` performs alpha compositing, so a 14%-alpha `-soft` pill background over `--surface-1` yields the real rendered colour. Pin it with a hand-computed case.
    - The module parses token values out of `packages/ui/tokens.css` (both the `:root` and the `[data-theme="dark"]` block) rather than duplicating them, so a token edit cannot pass a stale test.
    - The audit covers, per theme: `--on-accent` on `--accent`; each of the four `--status-*` on its own composited `-soft` over `--surface-1`; `--ink`, `--ink-secondary` and `--ink-tertiary` on `--canvas`, `--surface-1`, `--surface-2` and `--surface-3`.
  </behavior>
  <action>
    Create `packages/ui/src/contrast.ts` and `packages/ui/src/contrast.test.ts`. Write the reference-value tests first (white/black/`#767676`, plus the hand-computed compositing case) and get them green — this is a new pure module, so a green-from-the-start unit is acceptable here; the RED that matters is the audit assertion in Task 3.

    Then produce the full measured audit as a table and write it into `docs/contrast-decision-05.md` as section 1 ("Measured today"). Report each ratio to two decimals with its pair, theme and PASS/FAIL against 4.5:1. Where your figure disagrees with `docs/ui-review-05.md`'s, say so explicitly and show the inputs — that review's own first draft was wrong once, and the verification independently reproduced only the 3.02:1 figure.

    Do NOT change any token value in this task.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/contrast.test.ts && test -f docs/contrast-decision-05.md</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/ui/src/contrast.test.ts` exits 0 with named reference-value cases (white=1, black=0, `#767676`-on-white ≈ 4.54) and a compositing case.
    - `docs/contrast-decision-05.md` section 1 lists every audited pair with a measured ratio and a PASS/FAIL verdict, for both themes.
    - `git diff packages/ui/tokens.css` is empty.
    - The SUMMARY states every figure that disagrees with `docs/ui-review-05.md` and why.
  </acceptance_criteria>
  <done>Every relevant pair is measured with a reproducible, tested method.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: [BLOCKING] The user chooses the replacement token set</name>
  <files>docs/contrast-decision-05.md</files>
  <read_first>
    - docs/contrast-decision-05.md (section 1, from Task 1)
    - .claude/skills/noodara-ux-apple/SKILL.md (the single-action-colour and semantic-colour rules)
    - packages/ui/tokens.css
  </read_first>
  <decision>Which replacement token set should Noodara adopt so that primary-action text and status-pill text meet WCAG AA (≥4.5:1) in both light and dark?</decision>
  <context>
    Before presenting, compute 2-3 complete, internally consistent candidate sets and write them into `docs/contrast-decision-05.md` as section 2. For EACH candidate, give: the exact hex/rgba value of every token it changes, the resulting measured ratio for every audited pair in BOTH themes (recomputed with `contrast.ts`, not estimated), and a one-paragraph note on what it costs visually against the `noodara-ux-apple` skill.

    Candidates must each respect: exactly one blue action colour; `--status-*` reserved for infrastructure state; no new token invented without saying so; dark mode first-class and light mode mandatory; no shadows, no gradients.

    Suggested (not mandatory) axes to differentiate the candidates — pick genuinely different trade-offs, not three variations of one idea:
    - A: darken/desaturate `--accent` in dark mode so white text clears 4.5:1, and darken each `--status-*` text token while keeping the `-soft` backgrounds; visual cost = a less vivid accent in dark.
    - B: keep the accent hues and instead change what sits on them — e.g. a non-white `--on-accent`, and raise the `-soft` background alpha (or swap the pill to a solid, higher-contrast treatment) so the full-saturation text clears; visual cost = heavier-looking pills.
    - C: keep the current hues for large/non-text uses (dots, borders, meters) and introduce a paired `--status-*-text` token used only for the pill's word, tuned per theme; visual cost = one more token per status and a slightly duller word than the dot beside it.

    Also state, for each candidate, what it does about WR-C-08 (`--ink-tertiary` measured as low as 1.83:1 on content text) — whether the token is darkened, or whether `--ink-tertiary` is restricted to non-text/decorative use with the affected call sites moved to `--ink-secondary`.

    Present the table and the three short visual-cost notes directly in the checkpoint message. Do not pre-select a favourite in a way that forecloses the choice; if you have a recommendation, label it clearly as a recommendation and give the reason in one sentence.
  </context>
  <options>
    <option id="candidate-a"><name>Candidate A</name><pros>[fill from the measured table]</pros><cons>[fill]</cons></option>
    <option id="candidate-b"><name>Candidate B</name><pros>[fill]</pros><cons>[fill]</cons></option>
    <option id="candidate-c"><name>Candidate C</name><pros>[fill]</pros><cons>[fill]</cons></option>
  </options>
  <action>
    Write section 2 of `docs/contrast-decision-05.md`, present the candidates, and STOP. Do not edit `tokens.css`. Do not proceed to Task 3 until the user has named a candidate (or supplied their own values). Record the user's literal answer as section 3 of `docs/contrast-decision-05.md`, with the date.
  </action>
  <acceptance_criteria>
    - `docs/contrast-decision-05.md` section 2 contains 2-3 candidates, each with every changed token's exact value and every audited pair's recomputed ratio in both themes.
    - Execution paused and the user's decision is recorded verbatim in section 3 with a date.
    - `git diff packages/ui/tokens.css` is still empty at the moment of pausing.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## 3\." docs/contrast-decision-05.md</automated>
    <human-check>The user named a candidate (or supplied their own hex values) and it is recorded verbatim in section 3 with a date.</human-check>
  </verify>
  <resume-signal>Reply with `candidate-a`, `candidate-b`, `candidate-c`, or your own token values.</resume-signal>
  <done>The user has chosen; the choice is written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply the chosen set and lock it behind an automated gate</name>
  <files>packages/ui/tokens.css, packages/ui/src/contrast.test.ts, packages/ui/src/StatusPill.tsx</files>
  <read_first>
    - docs/contrast-decision-05.md (section 3 — the user's recorded decision)
    - packages/ui/tokens.css
    - packages/ui/src/contrast.ts and contrast.test.ts
    - packages/ui/src/StatusPill.tsx
    - tests/e2e/shell.spec.ts (the theme-toggle case that must keep passing)
  </read_first>
  <behavior>
    - Every audited pair measures ≥4.5:1 in both themes after the change.
    - The gate test parses `tokens.css` and asserts the threshold for every pair in an exhaustive, data-driven table — adding a new `--status-*` token without adding it to the audit must fail the test, not silently pass. Assert that property explicitly (e.g. the test derives its pair list from the parsed token names, not a hand-written list).
    - `pnpm check:ui-safety` still passes: no hex/rgb literal outside `tokens.css`.
    - The existing theme-toggle E2E, the StatusPill component tests, and every screen's visual-state E2E still pass.
    - `StatusPill.tsx`'s "status word always in the DOM, never colour-only" property is unchanged.
  </behavior>
  <action>
    Write the gate assertion FIRST, at the 4.5:1 threshold over the CURRENT token values, and observe it RED — that failing run is the regression proof that the gate actually bites. Record the literal output.

    Then apply exactly the values the user chose in section 3 — no substitutions, no "slightly adjusted for taste". If a chosen value turns out not to clear 4.5:1 when applied (a transcription error, a compositing subtlety), STOP and report back to the user rather than nudging the value yourself.

    Replace `tokens.css`'s `OPEN QUESTION` comment block with a short resolution note citing `docs/contrast-decision-05.md` and the decision date. Touch `StatusPill.tsx` only if the chosen candidate requires a class-pair change; if it does not, leave the file alone and remove it from the SUMMARY's changed-files list.

    Then run the full web/UI gate. Note for the SUMMARY: a real human still has to look at this on a real display — `docs/ui-review-05.md`'s own human-review items 1 and 3 stay open and are re-surfaced by plan 05-37's checkpoint. Measuring 4.5:1 is not the same as looking good.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/ui/src/contrast.test.ts && pnpm check:ui-safety && pnpm lint && pnpm typecheck && pnpm test && pnpm exec playwright test tests/e2e/shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run packages/ui/src/contrast.test.ts` exits 0, and its pair list is derived from the parsed token names (verify by adding a throwaway `--status-test` token locally, observing the test fail, then removing it — record this in the SUMMARY).
    - Every audited pair reports ≥4.5:1 in both themes; the SUMMARY reproduces the final table.
    - `grep -c "OPEN QUESTION" packages/ui/tokens.css` is 0.
    - `pnpm check:ui-safety`, `pnpm lint`, `pnpm typecheck`, `pnpm test` all exit 0 and the shell E2E passes.
    - The applied hex values byte-match section 3 of `docs/contrast-decision-05.md`.
    - The SUMMARY quotes the RED gate output from before the token change.
  </acceptance_criteria>
  <done>AA is met in both themes and cannot silently regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design tokens → every rendered surface | A token value change is global; there is no per-screen review of it |
| status colour → infrastructure state meaning | Semantic colour carries operational meaning; changing it can change what an admin infers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5G-33-01 | Information Disclosure (comprehension failure) | status pills and primary actions below AA | mitigate | Measured audit + user-chosen replacements + a gate at 4.5:1 in both themes. An operator who cannot read a FAILED status pill is an availability/safety problem, not a cosmetic one. |
| T-5G-33-02 | Tampering | a future token edit silently regressing contrast | mitigate | `contrast.test.ts` parses `tokens.css` and derives its pair list from the parsed token names, so a new token cannot dodge the audit |
| T-5G-33-03 | Spoofing (semantic) | a second action colour, or `--status-*` used decoratively | mitigate | `noodara-ux-apple`'s single-action-colour and semantic-reservation rules are stated as hard constraints on every candidate; `pnpm check:ui-safety` keeps literals out of components |
| T-5G-33-04 | Repudiation | an executor silently picking the product's colours | mitigate | Blocking `checkpoint:decision`; the user's literal answer is recorded with a date in `docs/contrast-decision-05.md`; applied values must byte-match it |
| T-5-33-SC | Tampering | npm/pnpm installs | mitigate | NO new dependency — the WCAG formula is implemented locally. Do not install a colour library. |
</threat_model>

<verification>
1. `pnpm exec vitest run packages/ui/src/contrast.test.ts` exits 0 with every pair ≥4.5:1 in both themes.
2. `pnpm check:ui-safety && pnpm lint && pnpm typecheck && pnpm test` all exit 0.
3. `tests/e2e/shell.spec.ts` passes.
4. `docs/contrast-decision-05.md` has all three sections and the applied values byte-match section 3.
5. The human-display review (`docs/ui-review-05.md` items 1 and 3) stays OPEN and is handed to plan 05-37.

## Nyquist note
`pnpm test:integration` is not required here (no `apps/control-plane` file changed). The full cross-suite gate runs in plan 05-37.
</verification>

<success_criteria>
- AA met on primary actions and status pills in both themes, with measured evidence.
- The colours were chosen by the user and recorded.
- A regression gate exists that a new token cannot bypass.
</success_criteria>

<output>
Create `.planning/phases/05-ui-web/05-33-SUMMARY.md` when done, reproducing the before/after contrast table, quoting the user's decision, and restating that human visual review on a real display remains open.
</output>
</content>
</invoke>
