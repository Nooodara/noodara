---
phase: 02-adaptador-ssh-aislado-y-probado-con-testcontainers
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - docs/adr/0004-ssh-adapter-empirical-contracts.md
  - tests/integration/ssh/contracts.test.ts
  - scripts/capture-discovery-fixtures.mjs
  - packages/domain/src/discovery/fixtures/README.md
autonomous: true
requirements: [SEC-03, DISC-01, QA-03]
user_setup: []

must_haves:
  truths:
    - "02-RESEARCH.md's three open questions are answered by measurement against a real Ubuntu sshd container, not by assumption, and the answers are recorded in a committed ADR that the implementation plans cite"
    - "The exact object ssh2 hands to hostVerifier is known, and the recorded procedure produces a fingerprint byte-identical to ssh-keygen -lf on the same host plus the host key's SSH algorithm name (D-04, D-05)"
    - "The observable shape of docker version --format json with a CLI present and no daemon is captured from the real image, distinguished from command-not-found, so D-12's parser is written against measured output"
    - "Every discovery command's output for Ubuntu 22.04 and 24.04 is captured from the real fixture images into committed parser fixtures — no parser in this phase is tested against fabricated sample text (02-RESEARCH.md assumption A3)"
    - "The CONNECT_TIMEOUT simulation strategy is chosen by measurement on this machine, with the GitHub ubuntu-latest behaviour of the rejected alternatives documented, and the choice is a deterministic scenario with no arbitrary sleep"
    - "The real ssh2 error shape for each failure mode (bad credentials, unresolved host, refused port, silent peer, and a transport that dies while a command is mid-flight) is captured as a table, so the classifier prefers structured fields over message-string matching where a structured field exists (02-RESEARCH.md assumption A5)"
    - "The mid-exec failure shape is measured, not assumed: with a command blocked inside the remote shell and its channel still open, the container is stopped and whatever ssh2 emits (a stream error with ECONNRESET, a bare stream close with no exit code, or a client-level error) is recorded verbatim in ADR 0004 as its own row, distinct from the stop-then-exec row"
    - "The spike leaves permanent assertions, not throwaway scripts: contracts.test.ts fails if any recorded empirical contract stops holding"
  artifacts:
    - path: "docs/adr/0004-ssh-adapter-empirical-contracts.md"
      provides: "The measured answers to open questions 1-3 plus the observed ssh2 error-shape table"
      contains: "Status"
    - path: "tests/integration/ssh/contracts.test.ts"
      provides: "Standing integration assertions for every recorded empirical contract"
    - path: "scripts/capture-discovery-fixtures.mjs"
      provides: "Reproducible capture of real command output from both fixture images"
  key_links:
    - from: "tests/integration/ssh/contracts.test.ts"
      to: "tests/integration/helpers/ssh.ts"
      via: "startSshd / hostKeyFingerprint / startBlackholeListener"
      pattern: "helpers/ssh"
    - from: "scripts/capture-discovery-fixtures.mjs"
      to: "packages/domain/src/discovery/fixtures"
      via: "writes captured stdout per command per Ubuntu version"
      pattern: "discovery/fixtures"
---

<objective>
Answer 02-RESEARCH.md's three open questions and capture the real command output every parser in this phase will be tested against, before any of that code is written.

Purpose: 02-RESEARCH.md flags five assumptions, three of which are load-bearing for code that does not exist yet. A1 (hostVerifier's argument shape) decides how TOFU is implemented; A3 (real command output) decides whether the parsers are tested against reality or against this project's own guesses; A4 (unroutable-IP behaviour) decides whether a QA-03 scenario is deterministic or flaky in CI. Guessing any of the three produces code that passes its own tests and fails against a real server.
Output: `docs/adr/0004-ssh-adapter-empirical-contracts.md`, a standing `contracts.test.ts`, a reproducible capture script, and committed fixtures for both Ubuntu versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-RESEARCH.md
@.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-02-SUMMARY.md
@docs/adr/0003-runtime-entrypoints-and-module-resolution.md
</context>

<interfaces>
<!-- From plan 02-02's tests/integration/helpers/ssh.ts -->
```typescript
export async function startSshd(options: { ubuntu: '22.04' | '24.04'; dockerCli?: boolean; hostPort?: number }): Promise<SshdFixture>;
export async function readTestKey(fixture: SshdFixture, name: 'ed25519' | 'rsa3072' | 'ecdsa' | 'ed25519_unauthorized' | 'ed25519_locked'): Promise<string>;
export async function hostKeyFingerprint(fixture: SshdFixture, keyType: 'ed25519' | 'rsa' | 'ecdsa'): Promise<string>;
export async function startBlackholeListener(): Promise<{ host: string; port: number; stop: () => Promise<void> }>;
export async function assertNoStrayTestContainers(): Promise<void>;
```
`SshdFixture` carries `{ container, host, port, password, keyPassphrase, stop }`.

<!-- From plan 02-01's packages/ssh: the command strings to capture output for -->
`COMMAND_TEMPLATES` maps 11 names to fixed strings: `hostname`, `cat /etc/os-release`, `uname -m`, `nproc`, `cat /proc/meminfo`, `df -P -k /`, `cat /proc/uptime`, `docker version --format '{{json .}}'`, `docker compose version --short`, `sudo -n true`, `id -nG`.

<!-- ssh2 1.17.0 surface this plan measures (documented, not yet verified in combination) -->
`client.connect({ hostVerifier, hostHash, readyTimeout, algorithms: { serverHostKey: [...] } })`; `client.on('error', (err) => ...)`; `utils.parseKey(input)` returning `ParsedKey | Error` with a `.type` property.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure ssh2's hostVerifier contract and prove the fingerprint matches ssh-keygen (open question 1)</name>
  <files>tests/integration/ssh/contracts.test.ts, docs/adr/0004-ssh-adapter-empirical-contracts.md</files>
  <read_first>
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-RESEARCH.md (Open Questions item 1 in full, Assumptions Log rows A1 and A2, and Architecture Patterns "Pattern 1")
    - tests/integration/helpers/ssh.ts (startSshd, hostKeyFingerprint, readTestKey)
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (D-04 exact fingerprint format, D-05 accepted host key types and ed25519-first negotiation, D-07 no insecure mode)
    - docs/adr/0003-runtime-entrypoints-and-module-resolution.md (the ADR format and tone to match: Status / Context / Decision / Rejected alternatives / Consequences)
    - node_modules/ssh2/lib/client.js and node_modules/ssh2/lib/protocol/keyParser.js (read the actual installed source for the hostVerifier call site and the parse entrypoints — this is faster and more reliable than inference)
  </read_first>
  <action>
    Start `docs/adr/0004-ssh-adapter-empirical-contracts.md` with the same section structure ADR 0003 uses, status `Accepted`, and one `##` section per open question. This task fills the first section; Tasks 2 and 3 fill the others.

    Write `tests/integration/ssh/contracts.test.ts` as a permanent test file, not a scratch script. Against a 24.04 fixture, connect once with `hostVerifier` supplied and `hostHash` omitted, and record inside the verifier: `typeof` the argument, whether it is a `Buffer`, its byte length, and the first length-prefixed field decoded as an RFC 4253 algorithm name (a 4-byte big-endian length followed by that many ASCII bytes). Assert what you observe, then write the observation into the ADR.

    Determine which of these yields the host key's algorithm name and assert the winner in the test:
    (a) `utils.parseKey(rawArgument).type`,
    (b) manual RFC 4253 decode of the leading length-prefixed algorithm-name field,
    (c) neither, in which case document what does.
    02-RESEARCH.md's own recommendation is that (b) always works because an SSH host key blob is required to begin with its algorithm name; if (a) also works, prefer it and record that `parseKey` accepts the wire-format blob. If `parseKey` returns an `Error`, record the error message verbatim in the ADR so a future reader does not repeat the experiment.

    Assert the fingerprint equivalence with no tolerance: `createHash('sha256').update(rawArgument).digest('base64')` with trailing `=` stripped, prefixed with `SHA256:`, must equal the `SHA256:` field of `hostKeyFingerprint(fixture, 'ed25519')`'s `ssh-keygen -lf` output on the same container. This is the whole of D-04 and it either matches exactly or the implementation approach is wrong.

    Repeat the equivalence assertion for an RSA and an ECDSA host key by forcing `algorithms.serverHostKey` to a single non-ed25519 algorithm on two further connections (D-05 accepts all three types), and assert the ed25519-first default negotiation actually selects ed25519 when the server offers all three.

    Also settle A2 while a container is up, because it costs one extra assertion: call `utils.parseKey` on `ed25519_locked` with the correct passphrase (expect a `ParsedKey`), with a wrong passphrase, and on a deliberately truncated key, and record in the ADR whether the two failures are distinguishable (message, `name`, or any own property). Record the verbatim messages — plan 02-06's key loader must map both to a safe message, and knowing whether they can be told apart decides whether it can say "wrong passphrase" or only "unable to parse".

    Close every connection and stop every container in `afterEach`, then call `assertNoStrayTestContainers()`.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts` exits 0
    - A test asserts the computed `SHA256:<base64-no-padding>` value equals the `SHA256:` field of the container's own `ssh-keygen -lf` output, for ed25519, RSA and ECDSA host keys
    - A test asserts the resolved host key algorithm name for each of the three types and names the technique that produced it
    - `grep -c 'hostVerifier' docs/adr/0004-ssh-adapter-empirical-contracts.md` returns at least 1
    - `grep -c 'ssh-keygen -lf' docs/adr/0004-ssh-adapter-empirical-contracts.md` returns at least 1
    - The ADR states, in prose, whether `utils.parseKey` accepts the raw `hostVerifier` argument, and which technique plan 02-06 must use
    - The ADR records whether a wrong passphrase is distinguishable from a malformed key, with the verbatim error message(s)
    - `docker ps -aq --filter label=noodara.test=true | wc -l` prints 0 after the run
  </acceptance_criteria>
  <done>How TOFU must be implemented is a measured fact recorded in a committed ADR, with a standing test that fails if ssh2 ever changes it.</done>
</task>

<task type="auto">
  <name>Task 2: Capture real discovery output for both Ubuntu versions, including the docker-no-daemon shape (open question 2, assumption A3)</name>
  <files>scripts/capture-discovery-fixtures.mjs, packages/domain/src/discovery/fixtures/README.md, docs/adr/0004-ssh-adapter-empirical-contracts.md, tests/integration/ssh/contracts.test.ts</files>
  <read_first>
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-RESEARCH.md (Open Questions item 2, Assumptions Log row A3, Common Pitfalls "Pitfall 2")
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (D-12 — docker absent is a warning, detection via docker version --format json only, plus docker compose version --short)
    - packages/ssh/src/commands/allowlist.ts (plan 02-01 — the 11 template strings whose output is captured; import them, never retype them)
    - tests/integration/helpers/ssh.ts (startSshd with dockerCli true/false, container.exec)
    - scripts/check-package-provenance.mjs (the existing repo convention for a standalone `.mjs` script: shebang, argv handling, exit codes)
    - packages/domain/src/purity.test.ts (fixtures live under src but are read only from *.test.ts files, which are exempt from the node:fs ban)
  </read_first>
  <action>
    `scripts/capture-discovery-fixtures.mjs` starts each fixture image variant through `tests/integration/helpers/ssh.ts`' `startSshd` (import it; do not re-implement container startup) and writes one file per command per version into `packages/domain/src/discovery/fixtures/ubuntu-<version>/<check-id>.txt`, preserving stdout byte-for-byte including the trailing newline. Import the command strings from `packages/ssh` so the captured output can never drift from the command that will actually run. Run each command as `deployer` (`container.exec(['su','-','deployer','-c',<command>])` or the equivalent) for `sudo`/`docker_group`, and capture the `restricted` user's output for those two into `<check-id>.restricted.txt` — plan 02-05's parsers and plan 02-09's check logic both need the failing shapes.

    Capture, per version, from three variants:
    - plain image: all 11 commands. `docker version` and `docker compose version` here produce the command-not-found shape — capture stdout, stderr and the exit code, writing the exit code and stderr into a sibling `<check-id>.meta.json` so nothing about the failure is lost.
    - `dockerCli: true` image: `docker version --format '{{json .}}'` and `docker compose version --short`. This is the CLI-present-daemon-unreachable case. Capture stdout, stderr and exit code the same way.
    - `dockerCli: true` image with a fabricated-but-real successful `docker version` JSON is NOT capturable without a daemon; instead record in the ADR that the daemon-present shape is not reproducible in this fixture, and have plan 02-05 test the `.Server`-present branch against the `.Client`-only capture plus a hand-written variant clearly labelled as derived from Docker's documented JSON schema rather than captured. Do not pretend a captured file is real when it is not.

    Write `packages/domain/src/discovery/fixtures/README.md` stating: which image and which Ubuntu version each file came from, the exact command, the date, the capture script's name, and an explicit statement of which files are captured versus derived. Any future parser change must re-run the script, not hand-edit a fixture.

    Add assertions to `tests/integration/ssh/contracts.test.ts`: for both versions, `docker version --format '{{json .}}'` on the `dockerCli: true` image produces stdout that is valid JSON with a `Client` key, and its exit code is the value recorded in the ADR; on the plain image the same command produces exit code 127 with no JSON on stdout. These two facts are what let D-12 distinguish "not installed" from "installed, daemon unreachable" without ever collapsing a JSON-parse failure into `docker_installed = false` (Pitfall 2).

    Write the measured exit codes, the stdout/stderr split, and whether stderr contaminates stdout into the ADR's second section.
  </action>
  <verify>
    <automated>node scripts/capture-discovery-fixtures.mjs && pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/capture-discovery-fixtures.mjs` exits 0 and is idempotent: a second run produces a `git diff` limited to nothing or to genuinely variable fields (uptime, meminfo counters) — the script prints which files changed
    - `ls packages/domain/src/discovery/fixtures/ubuntu-22.04 | wc -l` and the same for `ubuntu-24.04` each report at least 11 files
    - `grep -c 'VERSION_ID="22.04"' packages/domain/src/discovery/fixtures/ubuntu-22.04/os_release.txt` returns 1 and `grep -c 'VERSION_ID="24.04"' packages/domain/src/discovery/fixtures/ubuntu-24.04/os_release.txt` returns 1
    - `node -e "JSON.parse(require('fs').readFileSync('packages/domain/src/discovery/fixtures/ubuntu-24.04/docker_version.txt','utf8'))"` exits 0 for the docker-CLI capture
    - `node -e "const m=require('./packages/domain/src/discovery/fixtures/ubuntu-24.04/docker_version.meta.json'); if (typeof m.exitCode !== 'number') process.exit(1)"` exits 0
    - `grep -c 'MemTotal' packages/domain/src/discovery/fixtures/ubuntu-24.04/memory.txt` returns 1
    - `grep -c 'docker' packages/domain/src/discovery/fixtures/ubuntu-24.04/docker_group.txt` returns 1 and the `.restricted.txt` counterpart returns 0
    - `grep -c 'captured\|derived' packages/domain/src/discovery/fixtures/README.md` returns at least 2
    - `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts` exits 0 with the docker exit-code assertions present
    - `grep -c '127' docs/adr/0004-ssh-adapter-empirical-contracts.md` returns at least 1
  </acceptance_criteria>
  <done>Every parser in this phase will be written against real Ubuntu 22.04 and 24.04 output, and the docker-detection branches are grounded in measured exit codes rather than in a guess.</done>
</task>

<task type="auto">
  <name>Task 3: Choose the CONNECT_TIMEOUT strategy and record the real ssh2 error shapes (open question 3, assumption A5)</name>
  <files>tests/integration/ssh/contracts.test.ts, docs/adr/0004-ssh-adapter-empirical-contracts.md</files>
  <read_first>
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-RESEARCH.md (Open Questions item 3, Assumptions Log rows A4 and A5, and Claude's Discretion's error-classification sketch)
    - tests/integration/helpers/ssh.ts (startBlackholeListener — the candidate strategy already built in plan 02-02)
    - .planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-CONTEXT.md (Deferred Ideas — toxiproxy is the documented fallback only if the simple scenarios prove unstable; D-08 timeout semantics; D-10 which codes are retried)
    - tests/integration/helpers/ssh.ts (plan 02-02 — `startSshd({ slowDf: true })` and `waitForSlowCommandStart`, the mid-exec primitive this task measures through)
    - packages/domain/src/server/connection-result.ts (the seven codes; the classifier must land on exactly one of them)
    - .github/workflows/ci.yml (the `integration` job — the environment the chosen strategy must also work in)
  </read_first>
  <action>
    Measure, do not reason. For each candidate `CONNECT_TIMEOUT` simulation, attempt a connection with `readyTimeout` set to a small value and record the elapsed time, the event that fired (`error` vs `timeout` vs `ready`), and the error's `code`, `level`, `errno`, `syscall` and `message`:
    - (a) the blackhole listener from plan 02-02 (accepts TCP, never sends a banner),
    - (b) an address on a Docker `--internal` user-defined network with no route from the host,
    - (c) `10.255.255.1` as 02-CONTEXT.md originally sketched.
    Choose the candidate that produces a genuine `readyTimeout`-shaped failure deterministically, and record the loser's observed behaviour so nobody re-tries it. If (a) wins, as expected, note explicitly that it is host-network-independent and therefore behaves identically on macOS Docker Desktop and on GitHub `ubuntu-latest`, which is precisely why it was preferred over (c); state that (c)'s behaviour on `ubuntu-latest` was not measured because the chosen strategy makes it irrelevant. Do not claim a CI measurement that was not performed.

    In the same test file, capture the real error shape for each failure mode against a live fixture, and write the results into the ADR as a table with one row per mode and columns for `err.level`, `err.code`, `err.errno`/`err.syscall` and the verbatim `err.message`:
    - wrong password against `pwuser`,
    - a valid-but-unauthorised key (`ed25519_unauthorized`) against `deployer`,
    - a correct key with a wrong username,
    - a hostname under the RFC 2606 `.invalid` TLD (guaranteed NXDOMAIN),
    - the host and port of a stopped container (connection refused),
    - the blackhole listener (silent peer),
    - a pinned fingerprint that does not match, by returning `false` from `hostVerifier`,
    - **a transport that dies while a command is mid-flight** (see the paragraph below) — measured separately from the refused-port row, because it is the only mode where a channel already existed and had produced no output.
    Assert each captured shape in the test, so plan 02-07's classifier can key on a structured field wherever one exists and fall back to a message match only where none does. 02-RESEARCH.md's A5 specifically warns that `err.level` may be present and more reliable than the message string — settle it here.

    Measure the mid-exec shape with the primitive plan 02-02 built for it, using a raw `ssh2.Client` in this spike (the adapter does not exist yet): start `startSshd({ ubuntu, slowDf: true })`, connect, open an exec channel for the literal `df -P -k /` string — the same text the frozen `discovery.disk` template holds, so nothing is added to the production allowlist and nothing in `packages/ssh` is bypassed — then `await waitForSlowCommandStart(fixture)` and only then call `fixture.stop()`. Awaiting the marker is what makes this deterministic: the remote command is provably blocked with output pending before the transport is cut, so there is no timer to race and roughly nineteen seconds of margin. Record, for both Ubuntu versions, which listener fires first (the channel's `error` vs `close` vs `end`, or the client's `error`), whether an `exitCode`/signal arrives at all, and the verbatim `code`/`errno`/`syscall`/`level`/`message`. If the observation differs between 22.04 and 24.04, record both rows rather than picking one. Assert the measured shape in `contracts.test.ts` for both versions so a future ssh2 upgrade that changes it fails here rather than silently in plan 02-07's classifier.

    Record in the ADR the classifier decision that follows from the measurements, including the one the seven-code union forces: a refused port and an unreachable network are not distinct codes in `SERVER_ERROR_CODES`, so they map to `CONNECT_TIMEOUT` (the roadmap's state table puts "port closed" under `UNREACHABLE`, and `CONNECT_TIMEOUT` is the `UNREACHABLE`-landing code whose D-10 retry is harmless for a refused port). State the alternative (`CONNECTION_LOST`) and why it was rejected: a refused connection never established a session to lose.

    Every scenario in this task must be deterministic with no arbitrary sleep: the timeout is asserted by the elapsed time falling within a window around the configured `readyTimeout`, not by waiting a fixed duration and hoping.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/contracts.test.ts` exits 0
    - The ADR contains a table with one row for each of the eight failure modes above and a non-empty verbatim message column for each
    - The ADR's mid-exec row states which event fired first, whether an exit code arrived, and the verbatim error fields, separately for 22.04 and 24.04 if they differ, and names `CONNECTION_LOST` as the code plan 02-07 must map it to
    - A test asserts the mid-exec shape for both Ubuntu versions, sequences the kill after `await waitForSlowCommandStart(...)`, and `grep -c 'waitForSlowCommandStart' tests/integration/ssh/contracts.test.ts` returns at least 1
    - The ADR names the chosen `CONNECT_TIMEOUT` strategy in a single sentence and records the measured behaviour of the two rejected candidates
    - The ADR states the refused-port mapping decision and the rejected alternative with its reason
    - A test asserts the blackhole-listener connection fails within a window around the configured `readyTimeout` (for example, at least the timeout and no more than the timeout plus a few seconds) and not by elapsed-time-agnostic assertion
    - `grep -c 'setTimeout\|sleep(' tests/integration/ssh/contracts.test.ts` returns 0, or every occurrence is inside the elapsed-time measurement helper and not a fixed wait before an assertion
    - `grep -c '\.invalid' tests/integration/ssh/contracts.test.ts` returns at least 1
    - `grep -c 'err.level\|level' docs/adr/0004-ssh-adapter-empirical-contracts.md` returns at least 1
    - `docker ps -aq --filter label=noodara.test=true | wc -l` prints 0 after the run
    - `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/` exits 0 (this file plus images.test.ts)
  </acceptance_criteria>
  <done>The CONNECT_TIMEOUT scenario is a measured, deterministic, host-network-independent choice, the mid-exec channel death has a measured shape instead of an assumed ECONNRESET, and the classifier's input table is observed fact rather than a research guess.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Research assumption → shipped security control | TOFU is a security control; implementing it against an unverified assumption about `hostVerifier`'s argument produces a fingerprint that looks right and verifies nothing |
| Fabricated test fixture → parser correctness | A parser tested against invented sample text passes its own suite and misreads a real server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-13 | Spoofing | TOFU fingerprint derivation | mitigate | The fingerprint is asserted byte-identical to the container's own `ssh-keygen -lf` output for all three key types, so a fingerprint the operator compares in their terminal cannot silently be a different digest of different bytes |
| T-2-14 | Tampering (of trust in reported state) | Docker detection branches | mitigate | Exit codes and stdout/stderr split measured from the real CLI-without-daemon image and from the command-not-found case; ADR records both, so a JSON-parse failure is never collapsed into `docker_installed = false` |
| T-2-15 | Denial of Service | Flaky CONNECT_TIMEOUT scenario masking a real regression | mitigate | Strategy chosen by measurement; the winner is host-network-independent, so the same scenario is deterministic locally and in CI; toxiproxy stays deferred per 02-CONTEXT.md |
| T-2-16b | Denial of Service | Mid-exec transport death assumed rather than measured | mitigate | The `error-classifier.ts` branch for a channel dying mid-command is written against a shape observed on both Ubuntu versions and asserted in `contracts.test.ts`; without this row the branch would be reachable only by a hostile network and unproven by any test (the gap this revision closes) |
| T-2-16 | Information Disclosure | Spike output containing key material | mitigate | The captured fixtures are command output, never key bytes; `readTestKey` values are used in-process only and never written to a file; an acceptance criterion in plan 02-02 already asserts no key material enters the tree |
</threat_model>

<verification>
- `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/ssh/` exits 0
- `node scripts/capture-discovery-fixtures.mjs` exits 0 and reports what it wrote
- `docs/adr/0004-ssh-adapter-empirical-contracts.md` has a section per open question, each ending in a decision an implementer can act on without repeating the experiment
- Zero containers labelled `noodara.test=true` survive
</verification>

<success_criteria>
No implementation plan in this phase has to guess: TOFU's mechanics, the docker detection branches, the timeout scenario and the classifier's input shapes are all measured facts with standing tests behind them.
</success_criteria>

<output>
Create `.planning/phases/02-adaptador-ssh-aislado-y-probado-con-testcontainers/02-04-SUMMARY.md` when done
</output>
</content>
