---
phase: 03-servicios-de-aplicaci-n-activity-log-y-redacci-n
plan: 10
type: execute
wave: 6
depends_on: [03-06, 03-07, 03-08, 03-09]
files_modified:
  - tests/integration/activity/canary-full-flow.test.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [SEC-02, DISC-03]

must_haves:
  truths:
    - "SEC-02 / D-18: a full register -> connect+discover -> edit(replace credential) -> trust fingerprint -> delete flow, run against a real sshd container with per-run canary secrets, leaves those secrets absent from application logs, every service result, every activity_events.metadata row, every discovery_snapshots.payload row and a simulated service error"
    - "SEC-02: the canary walks the same path a real admin takes (encrypted credential -> decrypted -> ssh2 -> outputs), not a double"
    - "DISC-03: against a real server, one discovery snapshot per run is persisted and the denormalized `servers` columns match the collected facts"
    - "`pnpm security:scan-leaks` runs both the fast phase-1 canary and this full-flow canary, and CI's security job fails on a leak"
  artifacts:
    - path: "tests/integration/activity/canary-full-flow.test.ts"
      provides: "D-18 full-flow leak scan"
      contains: "BEGIN OPENSSH PRIVATE KEY"
    - path: "package.json"
      provides: "widened security:scan-leaks script"
      contains: "canary-full-flow.test.ts"
  key_links:
    - from: "package.json security:scan-leaks"
      to: "tests/integration/activity/canary-full-flow.test.ts"
      via: "vitest integration config file list"
      pattern: "canary-full-flow"
    - from: ".github/workflows/ci.yml security job"
      to: "pnpm security:scan-leaks"
      via: "CI step"
      pattern: "security:scan-leaks"
---

<objective>
SEC-02's evidence: the flow-driven leak scan D-18 specifies, plus the script and CI wiring that make
it a merge gate and leave it ready for QA-05's nightly in phase 5.

Resolution of RESEARCH Open Question 2: the full-flow canary lives in a NEW file,
`tests/integration/activity/canary-full-flow.test.ts`, and `security:scan-leaks` names both files.
The existing `canary.test.ts` stays fast and container-free (its own docstring already anticipated
this split) and is not rewritten.

Output: the canary suite, a widened script, and a CI security job that guards its own containers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-CONTEXT.md
@.planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-RESEARCH.md
@.planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-PATTERNS.md
@.planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-VALIDATION.md
@CLAUDE.md
@.claude/skills/noodara-security/SKILL.md
@.claude/skills/noodara-tdd/SKILL.md

<interfaces>
Test fixtures already built (phase 2 / plan 03-05):
  tests/integration/helpers/ssh.ts
    startSshd({ ubuntu: '22.04' | '24.04'; dockerCli?; slowDf?; hostPort? }): Promise<SshdFixture>
    SshdFixture = { container, host, port, password, keyPassphrase, stop }   // password/keyPassphrase are per-run random
    readTestKey(fixture, 'ed25519' | 'rsa3072' | 'ecdsa' | 'ed25519_unauthorized' | 'ed25519_locked'): Promise<string>
    assertNoStrayTestContainers(): Promise<void>
  tests/integration/services/helpers/service-fixture.ts
    startServiceFixture() -> { db, deps, setSshPort, stop }
  apps/control-plane/src/logger.ts
    createLogger({ level, destination }), writableForTests() -> { stream, records() }
  apps/control-plane/src/activity/redaction.ts
    appRedactor.register(value, kind) / appRedactor.release(value) / appRedactor.redact(value)
  @noodara/ssh
    createSsh2Adapter()

Root package.json today:
  "security:scan-leaks": "vitest run --config vitest.integration.config.ts tests/integration/activity/canary.test.ts"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the D-18 full-flow canary against a real sshd container</name>
  <files>tests/integration/activity/canary-full-flow.test.ts</files>
  <read_first>
    - tests/integration/activity/canary.test.ts (the full file: per-run `randomBytes` canaries, register/release in try/finally, the `for (const captured of [...]) expect(captured).not.toContain(...)` assertion loop, and the "import only after env is set" discipline)
    - tests/integration/helpers/ssh.ts (startSshd, readTestKey, the per-run password/keyPassphrase fields, assertNoStrayTestContainers)
    - tests/integration/ssh/discovery.test.ts (how a real `createSsh2Adapter()` connect + runDiscovery run is arranged against the container, and which SSH users the fixtures expose)
    - tests/integration/services/helpers/service-fixture.ts (startServiceFixture, setSshPort)
    - apps/control-plane/src/services/server-services.ts (createServerServices — the entry point the canary drives)
    - apps/control-plane/src/logger.ts (createLogger + writableForTests)
    - apps/control-plane/src/activity/redaction.ts (appRedactor, toLogSafe)
    - .planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-CONTEXT.md (D-18's exact required capture list)
  </read_first>
  <behavior>
    One `it` driving the whole flow, plus focused assertions:

    Arrange:
    - `startServiceFixture()` for Postgres + deps; `startSshd({ ubuntu: '24.04' })` for a real server;
      `setSshPort(createSsh2Adapter())` so the real adapter is used end to end.
    - Canary values, all per-run, never literals: the container's `fixture.password` (used as the
      real SSH password), `readTestKey(fixture, 'ed25519_locked')` plus `fixture.keyPassphrase` (the
      real encrypted key and its passphrase), and `env.NOODARA_MASTER_KEY`.

    Act, through `createServerServices(deps)` only (no HTTP — routes do not exist until phase 4):
    1. `registerServer` with the password credential for the container's SSH user.
    2. `connectAndDiscover` -> assert `connection.ok` is true and `discovery` is present.
    3. `editServer` replacing the credential with the encrypted private key + passphrase.
    4. Force a host-key change without a second container: overwrite `servers.host_fingerprint` with a
       syntactically valid but different value via raw SQL, then `connectAndDiscover` again ->
       assert the result's `connection.errorCode` is 'HOST_KEY_CHANGED' and a `pending_fingerprint` was parked.
    5. `trustFingerprint` -> assert it succeeds.
    6. `connectAndDiscover` once more with the key credential -> assert `connection.ok` is true.
    7. `deleteServer` with the exact name -> assert it succeeds.

    Capture, exactly the five sources D-18 names:
    a. application log output: log every service result AND the raw `servers`/`credentials` rows
       (through `toLogSafe`) to a pino logger built with `writableForTests()`, then `JSON.stringify(records())`
    b. `JSON.stringify` of every service result collected during the flow
    c. every `activity_events.metadata` row in the database, stringified together
    d. every `discovery_snapshots.payload` row in the database, stringified together
    e. a simulated service error: throw an Error whose message embeds all canaries, catch it, and
       run `appRedactor.redact(error.message)` — the exact pattern phase 4's error handler will use

    Assert, for each of the five captures:
    - it does not contain the password canary
    - it does not contain the private key text
    - it does not contain the literal 'BEGIN OPENSSH PRIVATE KEY'
    - it does not contain the key passphrase
    - it does not contain the master key value

    Additionally assert (DISC-03 against a real server, not a fake):
    - exactly three `discovery_snapshots` rows exist for the server before deletion (one per
      successful connect run: steps 2 and 6, plus none for the failed step-4 connect) — state the
      expected count explicitly from the flow above rather than a magic number
    - the denormalized `servers.hostname`, `os_distribution`, `arch` and `docker_installed` columns
      are non-null and match the last snapshot's facts
    - after `deleteServer`, zero `discovery_snapshots` rows remain for that server id

    Cleanup: `afterEach` stops both fixtures, releases every registered canary from `appRedactor` in a
    `finally`, and calls `assertNoStrayTestContainers()`.
  </behavior>
  <action>
    Create tests/integration/activity/canary-full-flow.test.ts implementing the behaviour above,
    modelled structurally on tests/integration/activity/canary.test.ts (same register/release
    discipline, same assertion-loop shape, same dynamic-import-after-env ordering). The file header
    must state that this is SEC-02's evidence, that it drives the real credential path
    (encrypt -> store -> decrypt -> ssh2 -> outputs) deliberately rather than a double, and that it is
    part of `pnpm security:scan-leaks`.

    A failure in this suite is a security defect, not a flake: do not add retries, do not soften an
    assertion to `toContain`-with-substring tricks, and do not `.skip` any case.
    Commit as `test(03-10): add full-flow secret leakage canary`.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config vitest.integration.config.ts tests/integration/activity/canary-full-flow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The file contains `startSshd(`, `createSsh2Adapter(`, `createServerServices(` and `appRedactor`
    - It asserts `not.toContain('BEGIN OPENSSH PRIVATE KEY')` against every one of the five captures
    - It captures all five sources named in D-18 (logs, service results, activity metadata, snapshot payloads, simulated error)
    - CLI: `grep -c "\.skip\|\.todo\|retry" tests/integration/activity/canary-full-flow.test.ts` returns 0
    - Behavior: the flow reaches `HOST_KEY_CHANGED`, then `trustFingerprint` succeeds, then a later connect succeeds
    - Behavior: after `deleteServer`, `select count(*) from discovery_snapshots where server_id = ...` is 0
    - The verify command exits 0
  </acceptance_criteria>
  <done>A real admin-path flow against a real Ubuntu sshd container proves no canary secret reaches any of the five outputs, and proves DISC-03's snapshot + denormalization against real facts.</done>
</task>

<task type="auto">
  <name>Task 2: widen security:scan-leaks and guard the CI security job</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    - package.json (the current `security:scan-leaks` script naming exactly one file, and the neighbouring `test:integration` / `test:boot` scripts for style)
    - .github/workflows/ci.yml (the `security` job: pnpm audit, security:scan-leaks, gitleaks; and the `integration` job's "Fail if any noodara.test container survived" step to mirror)
    - tests/integration/activity/canary.test.ts (the fast, container-free canary that must stay in the script and stay fast)
    - .claude/skills/noodara-security/SKILL.md §9 (what the leak-scan script is required to cover)
  </read_first>
  <action>
    1. In package.json, change `security:scan-leaks` to
       `vitest run --config vitest.integration.config.ts tests/integration/activity/canary.test.ts tests/integration/activity/canary-full-flow.test.ts`
       (both files named explicitly, fast one first; `vitest.integration.config.ts` already sets
       `fileParallelism: false`, so ordering is deterministic).
    2. In .github/workflows/ci.yml's `security` job, update the comment above the
       `pnpm security:scan-leaks` step to record that it now also builds and runs a real sshd
       Testcontainer for the phase-3 full-flow canary (so the job's runtime increase is intentional
       and documented), and add an `if: always()` step after it that fails the job when any container
       labelled `noodara.test=true` survived — copy the `integration` job's existing
       "Fail if any noodara.test container survived" step verbatim, including its `docker ps -aq --filter` command.
    3. Do not move the canary into the `integration` job and do not remove it from `security`: a leak
       must fail a dedicated, obviously-named job.
    Commit as `chore(03-10): extend security:scan-leaks to the full-flow canary`.
  </action>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts['security:scan-leaks']; process.exit(s.includes('canary.test.ts') && s.includes('canary-full-flow.test.ts') ? 0 : 1)" && pnpm security:scan-leaks</automated>
  </verify>
  <acceptance_criteria>
    - `package.json`'s `security:scan-leaks` names both `tests/integration/activity/canary.test.ts` and `tests/integration/activity/canary-full-flow.test.ts`
    - `.github/workflows/ci.yml`'s `security` job contains a step whose `run` includes `docker ps -aq --filter "label=noodara.test=true"` with `if: always()`
    - CLI: `grep -c "security:scan-leaks" .github/workflows/ci.yml` returns at least 1 (the step still exists in the security job)
    - The verify command exits 0 (both canary suites pass through the real script)
  </acceptance_criteria>
  <done>`pnpm security:scan-leaks` is the single command that runs both canaries, CI fails on a leak, and the job cleans up after its containers — ready for QA-05's nightly in phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every output surface -> operator / attacker | Logs, API results, audit metadata, persisted snapshots and error messages are all places a secret can escape |
| real SSH session -> persisted data | This is the only test in the repo that exercises the full encrypt -> decrypt -> ssh2 -> persist path with real material |

## STRIDE Threat Register

| Threat ID | Category | Description | Asset | Severity | Disposition | Mitigation (task) |
|---|---|---|---|---|---|---|
| T-3-03 | Information Disclosure | A credential reaches an application log line | logs | high | mitigate | Task 1 capture (a) with `toLogSafe` and pino redaction, asserted against five canary values |
| T-3-01 | Information Disclosure | A credential reaches a service result that phase 4 will serialise into an API response | service results | high | mitigate | Task 1 capture (b) over every result in the flow |
| T-3-02 | Information Disclosure | A credential reaches `activity_events.metadata` | audit log | high | mitigate | Task 1 capture (c) over every row written during the flow |
| T-3-04 | Information Disclosure | A credential echoed by a remote command reaches `discovery_snapshots.payload` | persisted snapshots | high | mitigate | Task 1 capture (d) over every persisted payload, produced by a real discovery run |
| T-3-33 | Information Disclosure | A thrown error's message carries the credential to the client | error responses | high | mitigate | Task 1 capture (e) through `appRedactor.redact` — the exact pattern phase 4's error handler must use |
| T-3-34 | Repudiation | The leak scan silently stops covering the real flow (file renamed, script not updated) | SEC-02 assurance | medium | mitigate | Task 2 names both files in the script and keeps the CI step; the script itself is the verify command |
| T-3-35 | Denial of Service | A leaked sshd/Postgres container survives the CI security job and exhausts the runner | CI capacity | low | mitigate | Task 2's `if: always()` stray-container step plus the suite's own `assertNoStrayTestContainers()` |
</threat_model>

<verification>
- `pnpm security:scan-leaks` green (both canaries).
- `pnpm test`, `pnpm test:integration`, `pnpm typecheck`, `pnpm lint`, `pnpm exec turbo boundaries` green.
- No stray `noodara.test=true` container after a full run.
- Phase gate: with this plan green, every phase-3 success criterion has automated evidence.
</verification>

<success_criteria>
- A real register -> connect+discover -> edit -> host-key-change -> trust -> connect -> delete flow runs against a real Ubuntu sshd container through the application services only.
- None of the five per-run canary secrets appears in logs, service results, activity metadata, snapshot payloads or a simulated error.
- Discovery snapshots and denormalized facts are proven against real collected data, and vanish with the server.
- `pnpm security:scan-leaks` covers both canaries and gates CI.
</success_criteria>

<output>
Create `.planning/phases/03-servicios-de-aplicaci-n-activity-log-y-redacci-n/03-10-SUMMARY.md` when done
</output>
