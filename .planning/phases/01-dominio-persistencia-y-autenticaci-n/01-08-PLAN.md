---
phase: 01-dominio-persistencia-y-autenticacion
plan: 08
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - tests/integration/helpers/migrations.ts
  - tests/integration/fixtures/representative-data.ts
  - tests/integration/db/migrations.test.ts
  - tests/integration/db/migration-hygiene.test.ts
autonomous: true
requirements: [QA-06]

must_haves:
  truths:
    - "Migrations are proven to apply cleanly against an empty database (QA-06 first half)"
    - "Migrations are proven to apply on top of the previous snapshot with representative data already present, and that data survives intact (QA-06 second half, PITFALLS.md #10)"
    - "The from-snapshot test is written generically over the drizzle journal, so migration 3, 4 and 5 are covered automatically without editing the test (CONTEXT.md: en esta fase el snapshot anterior es la migracion inicial)"
    - "A migration file that is not defensive fails CI rather than a production upgrade"
  artifacts:
    - path: "tests/integration/helpers/migrations.ts"
      provides: "Journal-driven partial migration application"
      exports: ["applyMigrationsUpTo", "readJournal"]
    - path: "tests/integration/db/migrations.test.ts"
      provides: "QA-06 from-scratch and from-previous-snapshot coverage"
    - path: "tests/integration/db/migration-hygiene.test.ts"
      provides: "Static guard that every migration SQL file is defensive"
  key_links:
    - from: "tests/integration/db/migrations.test.ts"
      to: "apps/control-plane/src/db/migrations"
      via: "journal enumeration"
      pattern: "_journal"
---

<objective>
Satisfy QA-06 with a test shape that keeps working as the project accumulates migrations.

Purpose: RESEARCH Pitfall 3 is precise about the failure mode — the natural first test is "spin up an empty Postgres and migrate", which never exercises the case that actually breaks users' upgrades. Coolify shipped four separate migration-breaks-upgrade issues (#3848, #2820, #3618, #5776). Writing the from-snapshot harness now, while there is only one migration and it costs almost nothing, is what makes it load-bearing at migration 3.
Output: a journal-driven migration helper, a representative seed fixture, and two integration suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-CONTEXT.md
@.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Journal-driven migration helper and the from-scratch test</name>
  <files>tests/integration/helpers/migrations.ts, tests/integration/db/migrations.test.ts</files>
  <behavior>
    - `readJournal()` parses `apps/control-plane/src/db/migrations/meta/_journal.json` and returns the ordered list of migration tags
    - `applyMigrationsUpTo(db, tag)` applies every migration up to and including `tag` and no migration after it
    - `applyMigrationsUpTo(db, null)` applies nothing
    - Applying all migrations to an empty database creates the eight expected tables and the seven expected enums
    - Applying all migrations twice in a row against the same database is a no-op the second time and exits without error
    - The drizzle bookkeeping table records exactly one row per migration in the journal after a full apply
  </behavior>
  <read_first>
    - tests/integration/helpers/postgres.ts (created in Plan 01-07 — the container fixture; this test must start a container with NO migrations pre-applied, so add or use the documented option for that)
    - apps/control-plane/src/db/migrate.ts (the production migration path these tests must reuse, not re-implement)
    - apps/control-plane/src/db/migrations/meta/_journal.json (the journal shape being parsed)
    - .claude/skills/noodara-tdd/SKILL.md (section 5 — resources destroyed per test, no arbitrary sleeps)
    - .planning/phases/01-dominio-persistencia-y-autenticaci-n/01-RESEARCH.md (section "### Pitfall 3: Migrations only tested against an empty database")
  </read_first>
  <action>
    Write the tests first and watch them fail.

    `tests/integration/helpers/migrations.ts` exports `readJournal()` and `applyMigrationsUpTo(db, tag)`. Implement the partial apply by reading the journal, selecting the SQL files up to the requested tag, and executing each file's statements in order using the same statement-splitting drizzle's own migrator uses (drizzle separates statements with the `--> statement-breakpoint` marker — split on that marker, not on bare semicolons, which would break on a function body or a quoted string).

    If `tests/integration/helpers/postgres.ts` currently always migrates on start, extend it with an explicit `{ migrate: false }` option rather than duplicating the container setup here; keep one container fixture in the repo.

    `tests/integration/db/migrations.test.ts` gets the from-scratch describe block covering the first four bullets. Assert table and enum presence by querying `information_schema.tables` and `pg_type`, not by importing the Drizzle schema, so the test verifies what actually landed in the database rather than what the TypeScript says should have.
  </action>
  <verify>
    <automated>pnpm exec vitest run --config vitest.integration.config.ts tests/integration/db/migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run --config vitest.integration.config.ts tests/integration/db/migrations.test.ts` exits 0
    - A test asserts all eight tables and all seven enums exist after a full apply against an empty database
    - A test applies all migrations twice and asserts the second run adds zero rows to the drizzle bookkeeping table
    - `grep -c "statement-breakpoint" tests/integration/helpers/migrations.ts` returns at least 1
    - `grep -c "sleep\|setTimeout" tests/integration/helpers/migrations.ts` returns 0
    - `docker ps --filter label=noodara.test=true --format '{{.ID}}' | wc -l` returns 0 after the run
  </acceptance_criteria>
  <done>Migrations are proven to apply from scratch, and the harness can stop at any journal entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: From-previous-snapshot test with representative data, plus the defensiveness guard</name>
  <files>tests/integration/fixtures/representative-data.ts, tests/integration/db/migrations.test.ts, tests/integration/db/migration-hygiene.test.ts</files>
  <behavior>
    - `seedRepresentativeData(db)` inserts at least one row into every table that exists at the previous-snapshot point: a user, a session, an account, a setup token, a login attempt, a credential, a server referencing that credential, and an activity event
    - The from-snapshot test applies migrations up to the second-to-last journal entry, seeds representative data, then applies the remaining migrations and asserts every seeded row is still present with unchanged values
    - When the journal has only one entry (the current state of this phase), the test applies that single migration, seeds, re-applies the full folder, and asserts the seeded rows survived and no migration was re-run — the test must skip nothing and must not be marked `.skip`
    - After the upgrade step the test asserts the `servers` row still resolves its `credential_id` foreign key and that `status` is unchanged
    - `migration-hygiene.test.ts` reads every `.sql` file under the migrations folder and asserts: every `CREATE TABLE` is `IF NOT EXISTS`, every `CREATE INDEX` is `IF NOT EXISTS`, every `DROP` is `IF EXISTS`, and every `CREATE TYPE` is guarded against re-run
    - `migration-hygiene.test.ts` fails if a migration file exists that is not listed in the journal, or vice versa
  </behavior>
  <read_first>
    - tests/integration/helpers/migrations.ts (created in Task 1)
    - apps/control-plane/src/db/schema/ (all files — the exact column names the seed fixture must satisfy)
    - packages/domain/src/security/envelope.ts (the seed credential must be a real encrypted blob, not a fake string)
    - .planning/research/PITFALLS.md (Pitfall 10, "How to avoid" — apply N-1, populate representative data, apply N, verify integrity)
    - .planning/phases/01-dominio-persistencia-y-autenticaci-n/01-CONTEXT.md (Claude's Discretion: "Tests de migracion (QA-06)")
  </read_first>
  <action>
    Write both test bodies first and watch them fail.

    `tests/integration/fixtures/representative-data.ts` exports `seedRepresentativeData(db)` returning the inserted ids so assertions can look rows up by id rather than by scanning. Encrypt the seeded credential with `encryptSecret` from `@noodara/domain` under key version 1, so the fixture exercises the real stored format and a later rotation migration would be caught.

    Add the from-snapshot describe block to `tests/integration/db/migrations.test.ts`. Compute the previous snapshot as `journal[journal.length - 2] ?? journal[0]` so the test automatically shifts forward as migrations accumulate and needs no edit at migration 3.

    `migration-hygiene.test.ts` is a pure static guard over the SQL files — no container needed, but keep it in the integration project so it runs in the same CI job as the rest of the migration coverage. Parse each statement, ignore comment lines when counting, and produce a failure message naming the offending file and statement.
  </action>
  <verify>
    <automated>pnpm test:integration</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:integration` exits 0 with both `migrations.test.ts` describe blocks and `migration-hygiene.test.ts` running
    - The from-snapshot test asserts, after the upgrade step, that every id returned by `seedRepresentativeData` is still retrievable and field-identical
    - `grep -c "\.skip\|\.todo" tests/integration/db/migrations.test.ts` returns 0
    - Temporarily adding a migration file containing `CREATE TABLE foo (id uuid);` (no `IF NOT EXISTS`) makes `migration-hygiene.test.ts` exit non-zero; the file is removed before the task completes
    - Temporarily adding an unreferenced `.sql` file to the migrations folder makes the journal-consistency assertion fail; the file is removed before the task completes
    - `docker ps --filter label=noodara.test=true --format '{{.ID}}' | wc -l` returns 0 after the run
  </acceptance_criteria>
  <done>QA-06 is met in both directions, the from-snapshot test needs no maintenance as migrations accumulate, and a non-defensive migration cannot reach main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Previous installed version → new version | An in-place upgrade runs migrations against a database full of real user data and secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-20 | Denial of Service (availability) | In-place upgrade migration path | mitigate | The from-snapshot test applies N-1, seeds representative data including a real encrypted credential, applies N, and asserts data integrity — the exact scenario PITFALLS.md #10 documents as breaking Coolify upgrades four separate times |
| T-1-21 | Tampering | Non-defensive migration SQL reaching main | mitigate | `migration-hygiene.test.ts` fails the build on any unguarded `CREATE`/`DROP` and on journal/file drift; verified by two deliberate failure injections in the acceptance criteria |
</threat_model>

<verification>
- `pnpm test:integration` exits 0 covering from-scratch, from-snapshot and hygiene
- Zero containers labelled `noodara.test=true` survive
- No test in the migration suites is skipped
</verification>

<success_criteria>
QA-06 holds today with one migration and keeps holding without edits once the project has many.
</success_criteria>

<output>
Create `.planning/phases/01-dominio-persistencia-y-autenticaci-n/01-08-SUMMARY.md` when done
</output>
