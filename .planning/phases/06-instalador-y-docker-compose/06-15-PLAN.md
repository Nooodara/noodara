---
phase: 06-instalador-y-docker-compose
plan: 15
type: execute
wave: 12
depends_on: [06-14]
files_modified:
  - .planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md
  - docs/releases/v0.1-gate.md
autonomous: false
requirements: [INST-01, INST-02, INST-03, INST-04, INST-05]
must_haves:
  truths:
    - "Every automated gate in the repository runs once, in one session, and its raw output is recorded — not summarised from memory"
    - "D-18 layer 3: the human prerequisites that no plan may perform (creating the GitHub repository, pushing, cutting the first release, running the real curl | sh on a real VPS) are written down as an explicit, checkable list"
    - "D-02: no plan in this phase created a repository, a remote, a tag or a push; the first real release is a human action"
    - "The v0.1 release gate report exists with a per-criterion verdict and real evidence, produced through the noodara-release-gate skill's own procedure"
    - "QA-04 and QA-05, Pending since phase 5 for lack of a remote, are explicitly re-evaluated against whatever the human prerequisites produced"
  artifacts:
    - path: ".planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md"
      provides: "The human-only verification checklist for this phase, with space for the operator's observed results"
      contains: "curl"
    - path: "docs/releases/v0.1-gate.md"
      provides: "The v0.1 release gate report in the noodara-release-gate skill's required format"
      contains: "Veredicto"
  key_links:
    - from: "docs/releases/v0.1-gate.md"
      to: "docs/roadmap-v0.1-v0.5.md"
      via: "one row per v0.1 acceptance criterion with its evidence"
      pattern: "v0.1"
    - from: ".planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md"
      to: ".planning/phases/05-ui-web/05-HUMAN-UAT.md"
      via: "the carried-forward QA-04/QA-05 debt this phase's prerequisites can finally clear"
      pattern: "QA-04"
---

<objective>
Close the phase and the milestone: run every automated gate once and record the raw output, write
the human-only verification checklist that D-18 layer 3 and D-02 require, and produce the v0.1
release gate report.

Purpose: this is the last phase of v0.1. Phase 5 closed with verification debt (QA-04 and QA-05
Pending, because no remote and no real CI run had ever existed), and this phase's human
prerequisites — creating the repository, pushing, cutting the first release — are exactly what can
clear it. This plan must not pretend any of that happened; it must state precisely what is proven,
what is pending a human, and what remains debt.
Output: `06-HUMAN-UAT.md`, `docs/releases/v0.1-gate.md`, and a checkpoint that stops for the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-instalador-y-docker-compose/06-CONTEXT.md
@.planning/phases/06-instalador-y-docker-compose/06-VALIDATION.md
@.planning/phases/06-instalador-y-docker-compose/06-13-SUMMARY.md
@.planning/phases/06-instalador-y-docker-compose/06-14-SUMMARY.md

This plan is NOT autonomous. Task 3 is a blocking human checkpoint.

D-02 remains absolute: no task here runs `gh repo create`, `git push`, `git remote add`, `git tag`
or anything that contacts a remote. Creating the repository and cutting the first release are the
human's actions, documented for them to perform.

`.claude/skills/noodara-release-gate/SKILL.md` defines the gate procedure and the exact report
format; follow it rather than inventing one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run every gate once and record the raw output</name>
  <files>.planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md</files>
  <read_first>
    - package.json (the authoritative list of gate commands: lint, typecheck, boundaries, test, test:integration, test:boot, test:installer, test:e2e, security:scan-leaks, check:ui-safety, check:posix-sh)
    - .planning/phases/06-instalador-y-docker-compose/06-VALIDATION.md (the sampling contract and the operating notes: read the test COUNT, not just the exit code; a cold `pnpm test:integration` needs NOODARA_API_ORIGIN for the next build; stray containers cascade)
    - .planning/phases/05-ui-web/05-HUMAN-UAT.md (the format this phase's own UAT file should follow, and the QA-04/QA-05 items still Pending there)
    - .claude/skills/noodara-release-gate/SKILL.md (§1 procedure: run what can be run locally, report real output, say plainly what cannot be run here)
  </read_first>
  <action>
    Run each gate command once, in this order, capturing the full raw output of each:
    `pnpm lint`, `pnpm typecheck`, `pnpm boundaries`, `pnpm check:ui-safety`, `pnpm check:posix-sh`,
    `pnpm test`, `pnpm test:boot`, `pnpm test:integration`, `pnpm test:installer`, `pnpm test:e2e`,
    `pnpm security:scan-leaks`.

    For every suite, record the passed/failed/skipped COUNTS, not only the exit code —
    06-VALIDATION.md's first operating note exists because a mis-globbed run reports "No test files
    found" and exits 0. A zero-count run is a red result, not a green one.

    Set `NOODARA_API_ORIGIN` for the runs that need it, as the CI workflows do.

    After each Docker-using suite, run `docker ps -aq --filter "label=noodara.test=true"` and record
    the output; any surviving container is a failure of this task, not a footnote.

    Create `.planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md` and begin it with a
    "Gate run" section holding this evidence: the command, the exit code, the counts, and the
    wall-clock duration for each. Paste the raw tail of any failing output verbatim. Do not
    summarise a failure into prose.

    If any gate is red, stop and report it as a blocker rather than proceeding to Task 2 — a
    release gate report built on a red suite is worthless.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md && grep -c 'pnpm ' .planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` exists and its "Gate run" section lists all eleven commands with exit code, counts and duration for each.
    - Every recorded test-suite count is greater than 0.
    - Every recorded stray-container check printed nothing.
    - Any red gate is reported as a blocker in the plan SUMMARY and the plan stops there.
  </acceptance_criteria>
  <done>Every automated claim this phase makes has raw, first-hand evidence recorded in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Human prerequisites checklist and the v0.1 release gate report</name>
  <files>.planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md, docs/releases/v0.1-gate.md</files>
  <read_first>
    - .claude/skills/noodara-release-gate/SKILL.md (read in full: §1 procedure, §2's v0.1 criterion-to-evidence map, §3's exact report format, §4's rules — notably that a numeric criterion requires the real observed number and that documentation alone never counts as evidence)
    - docs/roadmap-v0.1-v0.5.md §6 (the v0.1 acceptance criteria, the source of truth the gate evaluates)
    - .planning/phases/05-ui-web/05-HUMAN-UAT.md (the QA-04/QA-05 debt and the six unconfirmed human items carried from phase 5)
    - .planning/REQUIREMENTS.md (the traceability table: QA-04 and QA-05 are the only two v1 requirements still Pending besides INST-01..05)
    - .planning/phases/06-instalador-y-docker-compose/06-13-SUMMARY.md (any `TODO(06-15)` action-SHA placeholders left in release.yml — these are blocking items for the human's first release)
    - docs/install.md (the published command and its owner/repo placeholder the human must fill in)
  </read_first>
  <action>
    Extend `06-HUMAN-UAT.md` with a "Human prerequisites" section listing, as an explicit checklist
    with space for the operator's observed result, every action D-02 and D-18 layer 3 reserve for
    the human:

    1. Create the public GitHub repository with `noodara/code` as its root, and push `main`.
    2. Resolve any `TODO(06-15)` action commit SHA left in `.github/workflows/release.yml` and
       confirm no floating tag remains.
    3. Fill in the owner/repo placeholder in `install.sh` and `docs/install.md` with the real values.
    4. Trigger `release.yml` manually (`workflow_dispatch`) as a dry run before tagging, and confirm
       both images publish and the `verify` job reports both architectures.
    5. Push the first real version tag and confirm the release publishes.
    6. Run `docker manifest inspect` on both published images and record that `linux/amd64` and
       `linux/arm64` both appear.
    7. On a clean Ubuntu 22.04 VPS, run the real published `curl … | sh`, record the printed URL and
       token, open the panel, redeem the token, and complete login.
    8. Re-run the installer on that same VPS and confirm nothing changed and no data was lost.
    9. With `ufw` active on that VPS, confirm the advisory appears and that the suggested command is
       correct, and check whether the published port is in fact reachable from outside despite ufw —
       the "typically bypass" claim in the docs must be confirmed or corrected against reality.
    10. Start the stack on an arm64 host and confirm argon2 and ssh2 load (the native-addon risk
        D-16 names).
    11. Confirm the first real CI run on `main` is green, and the first real nightly run completes —
        these are what finally clear QA-04 and QA-05 from phase 5's debt.

    For each item record: not started / done + observed result. Leave the results blank for the
    human to fill; do not pre-fill any of them.

    Then produce `docs/releases/v0.1-gate.md` using the exact format in the release-gate skill's §3:
    the Dashboard block, the per-criterion table with a READY / NOT READY / UNVERIFIED verdict and
    concrete evidence (a test path, a job name, a recorded number), a Bloqueantes section and a
    Notas section. Use the raw numbers from Task 1, not adjectives. Any criterion whose evidence
    depends on an unperformed human prerequisite is UNVERIFIED — never READY. Per the skill's §4,
    the overall verdict is READY only with zero NOT READY and zero UNVERIFIED, so this report will
    almost certainly come out NOT READY pending the human items, and that is the correct and honest
    outcome to write down.
  </action>
  <verify>
    <automated>test -f docs/releases/v0.1-gate.md && grep -q 'Veredicto' docs/releases/v0.1-gate.md && grep -q 'QA-04' .planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md && echo gate-report-ok</automated>
  </verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` contains a "Human prerequisites" section with all eleven numbered items, each with an unfilled result field.
    - `06-HUMAN-UAT.md` names QA-04 and QA-05 and states which prerequisite clears each.
    - `docs/releases/v0.1-gate.md` exists and contains the Dashboard, the per-criterion table, Bloqueantes and Notas sections named in the release-gate skill's §3.
    - Every row in the criterion table carries a concrete evidence reference (a test file path, a workflow job name, or a recorded number) — no row says only "implemented" or "should work".
    - Every numeric criterion (100 consecutive connections, 20/20 E2E, ≥95% coverage) carries the real observed number or is marked UNVERIFIED.
    - The overall verdict is `READY` only if zero rows are NOT READY and zero are UNVERIFIED.
  </acceptance_criteria>
  <done>The human knows exactly what is left to do, and the v0.1 gate states honestly what is proven and what is not.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User review of the v0.1 gate and the human prerequisite list</name>
  <files>.planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md, docs/releases/v0.1-gate.md</files>
  <read_first>
    - .planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md (Tasks 1 and 2 — the gate evidence and the prerequisite checklist)
    - docs/releases/v0.1-gate.md (Task 2 — the report under review)
    - .planning/phases/05-ui-web/05-HUMAN-UAT.md (the precedent: phase 5 closed by explicit user approval with its verification debt tracked rather than hidden)
  </read_first>
  <what-built>
    Every automated gate in the repository has been run once in a single session with its raw output
    recorded in `06-HUMAN-UAT.md`. The eleven human-only prerequisites that D-02 and D-18 layer 3
    reserve for the user are listed as an unfilled checklist. `docs/releases/v0.1-gate.md` states,
    per v0.1 acceptance criterion, what is proven by an executable artifact and what is UNVERIFIED
    pending one of those prerequisites.

    No plan in this phase created a GitHub repository, added a remote, pushed, or cut a tag.
  </what-built>
  <how-to-verify>
    1. Read the "Gate run" section of `.planning/phases/06-instalador-y-docker-compose/06-HUMAN-UAT.md`
       and confirm every suite reports a non-zero passed count and an exit code of 0.
    2. Read `docs/releases/v0.1-gate.md` and confirm you agree with each row's verdict and evidence —
       in particular that nothing is marked READY on the strength of documentation alone.
    3. Read the eleven human prerequisites and confirm the list is complete and that none of them is
       something an agent could have done for you.
    4. Decide how to proceed. The options are: perform the prerequisites now and re-run this gate;
       or accept the phase as complete with the remaining items tracked as verification debt, as
       phase 5 was closed; or send back specific corrections.

    Note: item 9 (the ufw reachability check) can change what `docs/install.md` says. If reality
    contradicts the "typically bypass" wording on your provider, say so — the documentation gets
    corrected, not the reality.
  </how-to-verify>
  <acceptance_criteria>
    - The user has read both documents and stated a decision.
    - If the decision is to accept with debt, the remaining unperformed prerequisites are recorded verbatim in the plan SUMMARY and carried into `.planning/STATE.md` by the orchestrator, exactly as phase 5's debt was.
    - If the decision is to perform the prerequisites first, the gate report is regenerated afterwards with the real results before the phase is closed.
    - No agent action in this plan created a repository, remote, tag or push — confirm by `git remote -v` printing nothing for this project and by the absence of any `git push` in the session history.
  </acceptance_criteria>
  <resume-signal>Reply "accept with debt", "prerequisites done — re-run the gate", or describe the corrections you want.</resume-signal>
  <done>The phase closes on an explicit, informed user decision rather than on an agent's assertion that everything is fine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent claims → release decision | the gate report is what a human will rely on to decide that v0.1 is shippable |
| local evidence → real-world behaviour | nothing in this repository has ever executed as a real GitHub Actions run or on a real VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-62 | Repudiation | a gate report asserting a criterion is met without executable evidence | mitigate | The release-gate skill's §4 rules are binding: no READY without a named test, job or observed number; documentation alone never counts. Task 2's acceptance criteria enforce a concrete evidence reference on every row. |
| T-06-63 | Spoofing | local simulation being presented as a real CI or real-VPS result | mitigate | Every criterion whose evidence depends on an unperformed human prerequisite is marked UNVERIFIED by construction; the eleven prerequisites are listed explicitly and left unfilled for the human. Phase 5's own precedent — QA-04/QA-05 Pending for exactly this reason — is referenced so the debt is continuous, not reset. |
| T-06-58 | Repudiation | a green local YAML file being mistaken for a passing release pipeline | mitigate | Prerequisite 4 requires a manual `workflow_dispatch` dry run before any real tag, and prerequisite 6 requires a real `docker manifest inspect` — neither can be satisfied by reading the file. |
| T-06-64 | Tampering | an agent creating the repository or pushing, violating D-02 | mitigate | D-02 is restated as a binding constraint in this plan's context, and Task 3's acceptance criteria include confirming `git remote -v` prints nothing and no push occurred. |
| T-06-05 | Information Disclosure | the ufw documentation being wrong on the user's actual provider | mitigate | Prerequisite 9 requires confirming the published port's real reachability with ufw active, and the checkpoint explicitly invites a correction to `docs/install.md` if reality differs. The claim is verified against a real host rather than inherited from research. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new package; this plan runs existing scripts and writes Markdown. |
</threat_model>

<verification>
- All eleven gate commands executed once, with raw output, counts and durations recorded.
- `06-HUMAN-UAT.md` and `docs/releases/v0.1-gate.md` both exist and satisfy their acceptance criteria.
- `git remote -v` prints nothing for this project; no tag, branch or push was created.
- The user has given an explicit decision at the checkpoint.
</verification>

<success_criteria>
- Every automated claim in this phase is backed by raw, first-hand output recorded in one file.
- The human-only prerequisites are complete, specific and unfilled.
- The v0.1 gate report follows the release-gate skill's format and marks unproven criteria UNVERIFIED rather than READY.
- The phase closes on an explicit user decision, with any remaining verification debt written down.
</success_criteria>

<output>
Create `.planning/phases/06-instalador-y-docker-compose/06-15-SUMMARY.md` when done.
</output>
